# 小筱工作台 版本历史

> **维护约定（重要）**：本文件是项目的持续版本账本。**每次功能改动 / 发版后，由阿布把对应条目追加到「四、后续变更记录」段**，不要覆盖历史、不要新建重复文件。
>
> 两条独立编号线：
> - **功能版本**：v1–v27（网页本体的业务 / 样式迭代，止于 2026-08-03 的 v27）
> - **APK 打包版本**：v1.0.0–v1.1.16（WebView 壳发布序号，每次发一个包 +1）
>
> 详细开发日志在 `.workbuddy/memory/2026-08-0X.md`，本文件只记里程碑与决策。

---

## 编号对照（先读这个）

- **v1.1.16 的功能基础 = v27**。业务功能在 2026-08-03 定稿于 v27（含 PWA 化、Supabase 云端同步）。2026-08-04 至 08-05 的所有改动都是「WebView 壳显示适配」（顶部对齐 / 状态栏 / overlay / 打包脚本），没有新增业务模块，因此功能版本号仍停在 v27。
- 即：**v1.1.16 = v27 功能代码 + WebView 壳显示适配 + 打包发布**。
- 功能版本线：v1（基础）→ … → v27（定稿）
- APK 版本线：v1.0.0 → … → v1.1.16（共 27 个命名发布包）
- 当前最新（2026-08-05）：功能 **v27** / APK **v1.1.16**（versionCode 27，144444 字节）
- **2026-08-06 起进入功能 v29 迭代**（注：v28 为 WebView 壳 SW 缓存轮次，未涨功能号；本轮业务改动以 v29 标记）。v29 业务代码已在 `workstation/` 工作树，但**尚未打包发布**；下次发版建议 APK 升到 **v1.1.17**（versionCode 28）。

---

## 一、功能版本演进（网页本体）

### 2026-08-01 · 创造起点 + v2
- **v1 基础版**：解析桌面《个人工作台完整设计需求文档.docx》→ 纯 HTML/CSS/JS 单页应用，部署 CloudStudio。4 大模块 / 13 子页：首页·自媒体发布记录（四平台）、开团记录 / 厂家 / 打样 / 价目计算器、灵感 / 接稿排期 / 授权 / 价目表、OC 人物档案 / 关系图谱 / 故事小记 / 约稿。localStorage 持久化、CRUD、图片预览、统计卡片、响应式（电脑 + 手机）。
- **v2 大规模改造（13 项）**：浅蓝主题、select→combobox 可手输、多制品、人物档案全新字段、关系图谱思维导图等。

### 2026-08-02 · v14
- v14 改造：恢复内联「+ 新增记录」按钮、价目计算改实时报价小票风格、接稿排期同模字段下沉到制品级（含数据迁移）。

### 2026-08-03 · v15–v27（密集迭代 + 三件大事）
- v15（9 项）：OC 时间轨迹板块、思维导图圆形节点 + 自由缩放、报价计算器改版、列表两列布局、人物档案手动排序…
- v16（7 项）· v17（4 项，节点自适应）· v18（7 项，拖拽平移 / 甘特图贪心算法重写）· v19（7 项，自定义关系类型）· v20（5 项，冰川蓝主题 / 删毛玻璃）· v21（3 项 + 3 补丁，排序按钮布局）…
- **v22 大更新**：报价计算器柄图分组 + 同模新公式 + 自动 SET 折扣（SET 与折扣互斥、同模单品层可叠加）。
- v23 · v24（柄图控制同模）· v25（3 项 + 补丁，combobox 化）· v26（3 项 + 补丁）· v27（日历格子缩小 / 颜色绑定修正 / 降饱和度）…
- **v27 补丁 11**：PWA 化（离线可装、html2canvas 本地化、manifest + service worker）。
- **v27 补丁 12**：Supabase 云端同步（17 个业务 store 双向、last-write-wins、防抖推送 + 定时拉取）。
- **v27 补丁 13**：PWA 合规化准备打包（manifest.json + PNG 图标）。
- **关键决策（8/3 深夜）**：TWA 路线在 OPPO/ColorOS 16 被判走不通（系统强制第三方链接走浏览器）→ 放弃 TWA，改原生 WebView APK（手动 aapt2 构建）。

---

## 二、APK 打包版本（WebView 壳）

### 2026-08-04 · 打包长征 v1.0.0–v1.1.2（13 个包）
手动 aapt2 构建（包名 `io.github.quella_x.webview`，versionCode 1→13）。连踩 5 大坑：
1. 缺 `<uses-sdk>` → ColorOS 弹「古早 App」兼容性警告（修：minSdk 22 / targetSdk 34）。
2. 点开闪退 → `NoClassDefFoundError`（匿名内部类 `MainActivity$1` 没喂进 d8）。
3. ColorOS 16 拦截 WebView 外网 → `loadUrl` 5 秒超时（改本地 assets）。
4. `file://` 协议层被禁用 → `loadDataWithBaseURL` + `shouldInterceptRequest` 内联方案。
5. ColorOS 16 asset 沙箱路径黑名单 → 资产根级平铺才读得到。

### 2026-08-05 · 发布微调 v1.1.3–v1.1.16（14 个包）
- v1.1.3–v1.1.11：**顶部对齐连调 9 版**才对（核心教训：`align-items:flex-start` + `min-height`，不用 `align-items:center` 让内容下沉）。
- v1.1.12：撤掉「白白让出 32px 空间」（header padding-top 32→8）。
- v1.1.13：撤 main-header 白色横条 + overlay `inset:32px`（阴影不染状态栏）。
- v1.1.14：恢复装饰线条 + 主区浅蓝。
- v1.1.15：建立 git 基线（首次提交 v1.1.15）+ 上线 GitHub Pages；确立「电脑浏览器 = 最终效果」预览流程（除顶部 32dp 状态栏差异外布局一致）。
- **v1.1.16（13:40 发布成功）**：144444 字节，versionCode 27，sw.js 缓存 v38，用 `build_v1.1.16.sh`。OPPO 先卸载旧版再装。

---

## 三、关键坑 / 经验（跨版本通用）

- **OPPO/ColorOS 16 系统性限制**：TWA 走不通、`file://` 被禁用、WebView 外网被拦、asset 沙箱限路径、versionCode 跨版需先卸载再装。
- **顶部对齐黄金规则**：`align-items:flex-start` + `min-height`；header 是占位条就透明，装饰条才留 background/border；header 内部不要再重复让出系统状态栏已让出的 32dp。
- **构建链路**：aapt2 → javac → d8 → `jar uf` → zipalign → apksigner；脚本见 `D:\wbkey\webview-app\build_v1.1.16.sh`；Bash 环境禁用 `rm`，靠独立工作目录避免清理。
- **版本双轨**：功能版本（vN）与打包版本（vX.Y.Z）独立计数，发版只涨打包号，功能无新增时不涨功能号。

---

## 四、后续变更记录（在此持续追加）

<!-- 追加格式示例：
### YYYY-MM-DD · 功能 vN / APK vX.Y.Z
- 改动摘要（一句话说清做了什么、为什么）
- 交付 / 影响（如 APK 路径、versionCode）
-->

### 2026-08-06 · 功能 v29（WebView 壳内未发版，待打包 v1.1.17）

- **报价计算（design-calc）**：手机端「制品列表 / 加价项目」改为卡片式录入（对齐接稿排期同款），解决制品名称显示不全。
- **接稿排期（design-commission）**：字段「客户信息 → 单主」；列表展示块增加 单主 / 进度 / 支付 / 用途 / 制品名称 / 截稿 / 报价；日历内开稿、截稿统一用实心圆点 + 单主名显示。
- **首页日历（home）**：日期格顶部固定平台发布圆点、底部固定开稿 / 接稿记录；小红书 / 抖音 / 视频号 / 公众号按钮高度缩小；日历下方新增「灵感速记」模块（写入 `inspirations`，进入「美工·灵感记录」，首页不展示）。
- **价目表（design-pricelist）**：每个条目新增上移 / 下移排序按钮（同类目内交换 `sort` 字段），保留原有编辑 / 删除。
- **全局弹窗**：新增记录 / 查看记录支持手机端横向右滑手势返回（等效点 ✕ 关闭）；仅移动端启用，输入 / 按钮区域不触发。
- 代码注释统一以 `v29` 标记本轮改动；`sw.js` 缓存轮次此前已到 v30。
- 交付状态：源码改动在 `workstation/`（未提交）；APK 仍为 v1.1.16，需重建 v1.1.17 才能上机。

### 2026-08-06（当晚修正）· 功能 v29 调优

- **报价计算（design-calc）**：手机端制品 / 加价项目从卡片式改回横向字段行（对齐接稿排期制品列表），字段顺序：制品名称 → 柄图标识 → 单价 → 数量 → 同模 → 删除。
- **接稿排期（design-commission）**：列表卡片标题已展示单主，正文展示块去掉重复的单主；「制品名称」标签改为「制品」，多个制品用「、」隔开；日历开稿恢复为仅实心圆点，截稿显示圆点 +「截稿」文字。
- **首页日历（home）**：平台发布圆点保持在日期格顶部原位置，下一行显示开稿 / 接稿 / 截稿记录（实心点 + 文字）；灵感速记输入框改为 combobox 下拉选择器，占位词改为「灵感主题」「制品名称」，文字思路 textarea 增加上间距。
- **价目表（design-pricelist）**：移除条目后的 inline 上移 / 下移按钮，改为工具栏「↕️ 调整排序」按钮，点击弹出独立排序面板，在面板内完成同类目 reorder，完成后刷新价目表。
- 源码已提交 `a008180`；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-06（二次修正）· 功能 v29 继续调优

- **报价计算（design-calc）**：手机端「柄图标识」占位词改为「柄图」，避免横向行布局下显示不全；并微调 mobile 列宽让输入框更宽松。
- **接稿排期（design-commission）**：日历开稿 / 截稿统一恢复为 v27 显示样式——日期数字同行显示实心圆点 +「开稿 / 截稿」文字。
- **首页日历（home）**：平台发布记录圆点保持在日期格顶部原位置；下一行显示发布记录数量；再下一行显示开稿 / 接稿 / 截稿标签（实心点 + 文字）。灵感速记的「灵感主题」从 combobox 改回普通输入框，「制品名称」保持下拉选择器。
- **人物档案（oc-profiles）**：取消每张卡片右上角的 inline 上移 / 下移按钮，改为工具栏「↕️ 调整排序」按钮，点击打开独立排序弹窗，在弹窗内 reorder 后统一保存。
- 源码已提交 `7e54d23`；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-06（三次修正）· 功能 v29 继续调优

- **报价计算（design-calc）**：制品列表 header「制品名称 → 制品」；柄图标识占位恢复「柄图标识」四字并加宽手机端列宽（1fr/76/50/44/44/28）确保显示全；加价项目 header「项目名称 → 加价项目」。
- **接稿排期（design-commission）**：制品列表字段标题「制品名称 → 制品」；加价项目字段标题「项目名称 → 加价项目」；日历开稿橙（`#ff9a3c`）/截稿黄（`#f5c518`）实心圆点 + 文字、大小一致，截稿用 `margin-left:auto` 固定右侧，对齐 v27。
- **首页日历（home）**：平台发布圆点移到日期数字下方独立行；开稿橙 / 接稿蓝 / 截稿黄实心圆点 + 文字，截稿固定右侧，对齐接稿排期日历样式。
- **人物档案（oc-profiles）**：「↕️ 调整排序」按钮改用 `btn btn-outline toolbar-eq`，与 OC 时间线「📥 导入故事小记」按钮显示样式一致。
- **开团记录（groupbuy-records）/ 售后记录**：制品名称去掉动态 options，渲染为纯输入框（不再显示为 combobox 下拉）。
- **打样记录（groupbuy-samples）**：评价「不合格」标签颜色由灰改为红（`.tag-danger`）；打样品类 `text` 改为 `combobox` 下拉框，固定选项（吧唧、明信片、镭射票、大肠发圈、发带、小卡、封口贴、贴纸、手提袋、色纸）+ 手动输入记录（`customSampleCategories`，A-Z 排序），UI 与灵感记录制品名称下拉一致。
- **灵感记录（design-inspiration）**：制品名称标签「制品名称 → 制品」；下拉框固定选项改为（封面、明信片、其他、镭射票、壁纸、海报）+ 手动输入记录，A-Z 排序。
- **价目表（design-pricelist）**：制品名称字段标签「制品名称 → 制品」；展示块「价目表」标题去字间距、字号 18→22px、padding 收紧。
- 源码已提交 `3633e74`；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-06（四次修正）· 功能 v29 继续调优

- **接稿排期日历（design-commission）**：开稿与截稿落在同一天时，合并为单一「开+截」实心圆点 + 文字，色号 `#712258`（此前为开稿橙 / 截稿黄两个独立标签）；底部图例改回 v27 样式——分别用实际色展示「开稿(橙 #ff9a3c) / 截稿(黄 #f5c518) / 开+截同天(#712258)」，再附紧急 / 正常 / 充裕进度色。
- **首页日历（home）**：① 删除「接稿」标签（仅保留开稿 / 截稿）；② 平台发布圆点 `.cal-dot` 由 5px 改为 6px，与开稿 / 截稿圆点（`.cal-day-tag-dot` 6px）等大；③ 日历底部新增「开稿(橙) / 截稿(黄)」示意图例。
- **打样记录（groupbuy-samples）**：评价「不合格」标签在列表卡片 multiselect 数组分支中由灰（`.tag-gray`）改为红（`.tag-danger`），与单行分支一致（此前数组分支漏改，导致仍显示灰）。
- **开团记录（groupbuy-records）**：① 字段「截止时间 → 截团时间」（数据键 `endTime` 不变）；② 售后记录子表新增「单号」列，置于「制品名称」前；③ 列表展示模块仅保留 状态 / 制品数 / 开团时间 / 截团时间 / 购买人数（制品总价 / 邮费 / 成本不再列于列表）；④ 开团营收 / 成本 / 利润柱状图顶部数字去除（仅保留柱形，月份标签与图例保留）。
- **开团三项金额计算说明（告知项，非代码改动）**：制品总价 / 邮费总价 / 总成本均为**手动填写的数字字段**（type:number），系统不会自动汇总；`制品列表` 里的「单价 × 售卖数量」不会自动累加到「制品总价」。统计区「总营收 = Σ(制品总价 + 邮费总价)」「总成本 = Σ(成本)」「总利润 = 总营收 − 总成本」、以及柱状图，均直接读取这三个手填值。如需改为按制品列表自动计算，可后续再加。
- 源码已提交 `4c9cd94`（并已推送到 GitHub origin/main；此前 `d836a25`→`3633e74` 五笔提交亦一并推送成功）；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-06（五次修正）· 功能 v29 继续调优

- **首页空白紧急修复**：`renderCalendar` 中 `START_COLOR` / `END_COLOR` 被错误地定义在 `for` 循环内部（块级作用域），而底部图例在循环外部引用它们，导致运行时 `ReferenceError`，`renderHome` 中断、首页主体空白。已将这两个颜色常量上提到函数顶部，首页恢复正常。
- **接稿排期日历（design-commission）**：① 开稿 / 截稿 / 同天标签所在 `.cal-date-row` 改为 `justify-content:center`，在日期数字后整体居中；② 开稿与截稿同一天时文案由「开+截」改为「同天」（色号仍 `#712258`）；③ 底部图例实心点与文字间距调近（`.cal-legend .status-dot` 去 margin-right，保留 `legend-item` 的 4px gap）。
- **开团记录柱状图**：多 series 柱形最后一根（利润）原先被设为 `border-radius:3px 3px 0 0` 呈半圆顶，现统一改为 `border-radius:0`，与营收 / 成本柱一样平头。
- **记录详情弹窗**：左侧标题 `.detail-label` 宽度由 100px 缩至 80px，`.detail-row` 的 gap 由 12px 缩至 8px，标题与内容更紧凑。
- 源码已提交 `63ee272` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（六次修正）· 功能 v29 继续调优

- **首页日历**：① 日期格拆分为 `.cal-day-top`（平台发布圆点 + 条数）与底部 `home-comm-tags`（开稿 / 截稿 / 开+截），上部永远留出空间给平台发布；② 平台发布实心点与日期数字间距和接稿排期时间条一致；③ 同一天开稿+截稿合并为「开+截」；④ 底部图例改为平台（小红书/抖音/视频号/公众号）+ 开稿 / 截稿 / 开+截，按顺序排在公众号后面。
- **接稿排期日历**：① 日期数字回到左侧（`.commission-cal-grid .cal-date-row` 改回 `justify-content:flex-start`）；② 截稿标签不再 `margin-left:auto` 固定右侧，与开稿顺序排列；③ 同一天开稿+截稿文案由「同天」改回「开+截」。
- **开团记录柱状图**：恢复柱顶数字显示（v29-fix5 误理解为要去掉数字，实际用户希望显示）。
- **价目表**：新增「单位」字段（`priceUnit`），支持选择「元」或「元/p」，列表展示相应显示 `¥price` 或 `¥price/p`。
- **统一列宽与标题对齐**：将所有 dynamic-list 中名称列（制品名称/制品/项目名称/加价项目）统一为 `flex:1.5 1 0; min-width:90px`；规范 `.dynamic-list-header span` 的 `box-sizing:border-box;padding-left:8px` 与透明边框占位，确保标题与下方输入框内容起点一致。
### 五次修正（2026-08-07 01:36，用户反馈 F 轮 5 项）

- **首页日历**：① 移除 `.cal-day-top` 与 `justify-content:space-between` 结构，改为日期数字 → 平台圆点 → 条数 → 开稿/截稿/同天顺序流式排列；② 平台发布圆点到日期数字的上边距由 4px 缩为 1px，与接稿排期时间条到日期数字间距一致；③ 条数不再居中，紧跟平台圆点；④ 开稿/截稿/同天标签整体上移；⑤ 同一天开稿+截稿合并为「开+截」；⑥ 底部图例平台（小红书/抖音/视频号/公众号）+ 开稿 / 截稿 / 同天。
- **接稿排期日历**：① 日期格中继续显示「开+截」；② 底部图例由「开+截」改为「同天」，与「开稿」「截稿」并列。
- **开团记录柱状图**：当记录未填写开团时间（`startTime`）时，原柱图区域空白且无提示；现增加空态提示「请为记录填写开团时间，柱图即可显示」，并告知用户三项金额（制品总价/邮费总价/总成本）为手动填写，柱图按 `startTime` 年月聚合。
- **价目表**：① 制品分类下拉框增加 `noSort: true`，保持固定顺序「纸片类 / 其他材质类 / 线上&应援类 / 加价项目」，不再被按拼音排序；② 「单位」字段由 `<select>` 改为 `multiselect` + `single: true`，复用多选 checkbox 样式（可打勾），但限制单选；保存后按字符串处理，列表展示 `¥price` 或 `¥price/p`。
- **标题栏与内容框对齐**：① 修复 `.dynamic-list-header` 因 `margin:-8px -8px 6px;padding:4px 0` 导致标题文字起点比输入框内容起点少 8px 的问题，改为 `padding:4px 8px 4px 8px`，使标题 span 文字起点与 `.form-input` 内容起点严格对齐；② 给售后记录「单号」列补全 header/input 宽度规则（`flex:0 0 70px`）；③ 开团记录制品列表、售后记录、灵感速记、报价计算的「制品名称」统一改为「制品」。
- 源码已提交 `3125d5f` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（G 轮修正）· 功能 v29 继续调优

- **首页日历（home）**：① 平台发布圆点区域固定高度（`.cal-dots` `min-height:12px`、`margin-top:4px`），无论当天有无发布记录都留出相同区域；② 开稿 / 截稿 / 同天标签通过 `margin-top:auto` 固定于日期格底部，不再随平台点有无上下浮动；③ 同一天开稿+截稿继续显示「开+截」；④ 底部图例「同天」文案改为「开稿+截稿同天」；⑤ 灵感速记「制品」占位词改为「请选择或输入制品」；⑥ 平台发布新增记录的「内容类型」改为单选（`multiselect` + `single: true`），保持 checkbox 样式，无默认勾选。
- **接稿排期日历（design-commission）**：底部图例「同天」文案改为「开稿+截稿同天」；日期格中同一天继续显示「开+截」。
- **开团记录（groupbuy-records）**：① 移除柱图空态提示；② 制品列表与售后记录子表中的「制品」列标签改为「制品名称」；③「进度状态」改为单选（`single: true`），默认勾选「进行中」。
- **厂家记录（groupbuy-factories）**：「合作状态」改为单选（`single: true`），默认勾选「临时合作」。
- **打样记录（groupbuy-samples）**：「样品评价」改为单选（`single: true`），保持 checkbox 样式，无默认勾选。
- **灵感记录（design-inspiration）**：「制品」占位词改为「请选择或输入制品」。
- **接稿排期（design-commission）**：①「稿件用途」改为单选，默认勾选「自用」；②「同柄图」改为单选，无默认勾选；③「修改次数」字段标签改为「修改类型、次数、价格」。
- **授权记录（design-auth）**：「授权类型」改为单选，默认勾选「商用」。
- **价目表（design-pricelist）**：①「制品分类」增加选项「修改类型」；②「单位」增加选项「元/次」。
- **任务档案（oc-profiles）**：「性别」改为单选，保持 checkbox 样式，无默认勾选。
- **人物关系（oc-relations）**：①「关系状态」改为单选，默认勾选「稳定」；②「关系类型」选项按 A-Z（拼音）排序；③ 自定义关系输入框占位词改为「请输入自定义关系类型」。
- **故事小记（oc-stories）**：①「剧情标签」改为单选，默认勾选「灵感」；②「剧情状态」改为单选，默认勾选「连载中」。
- **时间线（oc-timeline）**：「重要性」改为单选，保持 checkbox 样式，无默认勾选。
- **约稿记录（oc-commission）**：①「稿件用途」改为单选，默认勾选「自用」；②「约稿平台」改为单选，无默认勾选；③「交易状态」改为单选，无默认勾选，并新增选项「待接稿」；④ 筛选器同步增加「待接稿」。
- **电脑端标题对齐**：将 `.dynamic-list-header span` 的 `padding-left` 由 `8px` 调整为 `9px`，补偿透明边框缺失的 1px 视觉边界，使标题文字与下方输入框内容在电脑端严格左对齐。

### 2026-08-07（H 轮修正）· 功能 v29 继续调优

- **首页日历（home）**：① 平台发布记录条数（`.cal-day-count`）上边距由 `2px` 缩为 `0`，更贴近平台发布圆点；② 开稿 / 截稿 / 同天标签（`.home-comm-tags`）由 `margin-top:auto` 改为 `margin-top:2px`，不再被推到日期格底部，而是紧跟在平台圆点 / 条数区域下方。
- **开团记录（groupbuy-records）**：年度营收 / 成本 / 利润柱状图顶部堆叠数字去除（保留柱形、月份标签与图例）。
- **接稿排期（design-commission）**：①「稿件用途」「支付状态」「稿件进度」改为单选（`multiselect` + `single: true`），分别默认勾选「自用」「定金」「已接稿」；② 原「修改类型、次数、价格」单字段拆分为三个独立字段：「修改类型」（text）、「修改次数」（number）、「修改价格」（number，提示词「元/次」）。
- **人物关系（oc-relations）**：① 自定义关系类型在渲染时按 A-Z（拼音）排序；② 给自定义关系类型选项增加删除按钮（`✕`），点击可从 DB 与当前 checkbox 组中移除该自定义选项。
- **电脑端标题对齐**：将 `.dynamic-list-header span` 的 `padding-left` 由 `9px` 调回 `8px`（保留透明边框占位），进一步修正标题文字与下方输入框内容的左对齐。
- 源码已提交 `a55a27e` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（I 轮修正）· 功能 v29 继续调优

- **首页日历（home）**：① 平台发布圆点区域固定高度（`.cal-dots` 由 `min-height:12px` 改为 `height:12px`），无论当天有无发布记录都占 12px；② 发布条数 `.cal-day-count` 始终渲染，无记录时以 `&nbsp;` 占位并设 `min-height:12px`，保证高度固定；③ 开稿 / 截稿 / 同天标签（`.home-comm-tags`）通过固定上方的平台区域获得稳定位置，不再因平台记录有无上下浮动。
- **接稿排期（design-commission）**：将「修改类型」「修改次数」「修改价格」三个独立字段合并为 `modifications` 动态列表，一行展示三列（修改类型 / 次数 / 价格（元/次）），支持添加条数，最多保留 2 条（通过 `maxRows: 2` 限制）。
- **人物关系（oc-relations）**：自定义关系类型选项后的删除按钮由红色实心 `✕` 改为更轻量的 `.custom-opt-del` 灰色小圆角按钮，hover 变红，视觉上不再突兀。
- **电脑端标题对齐**：修正 `.dynamic-list-header span` 的 `padding-left` 计算逻辑。因 `.dynamic-list-header` 用 `margin:-8px` 延伸至容器边缘，标题 span 的内容起点比输入框内容起点少 8px，故将 `padding-left` 由 `8px` 调整为 `16px`，使电脑端所有 dynamic-list 标题与下方输入框严格左对齐。
- 源码已提交 `6ff21df` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（J 轮修正）· 功能 v29 继续调优

- **接稿排期（design-commission）**：① 默认视图由「列表」改为「日历」；② 在日历视图下，记录列表与日历位置互换，列表在上、日历在下；③ 「修改项目」动态列表中「价格（元/次）」列宽由 `90px` 加宽到 `110px`，提示词可完整显示。
- **人物关系（oc-relations）**：① 自定义关系类型的删除按钮由常驻 `×` 改为 hover 时才显示，避免视觉杂乱；② 思维导图节点圆形由 `80px` 缩小到 `60px`，内部头像 / 文字圆由 `56px` 缩小到 `44px`，连线端点半径同步从 `35` 调整为 `25`。
- **电脑端标题对齐**：区分普通列与 combobox/datalist 列的标题偏移。`.dynamic-list-header span` 默认 `padding-left:8px` 与普通输入框对齐；给 combobox/datalist 列的标题 span 额外加上 `.combobox-col` 类并设 `padding-left:16px`，解决 I 轮统一 16px 后普通列反而偏右的问题。
- 源码已提交 `9664379` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（K 轮修正）· 功能 v29 继续调优

- **接稿排期（design-commission）**：① 顶部「列表 / 日历」切换按钮位置互换，改为「日历 / 列表」顺序；② 日历视图下内容区域恢复「日历在上、列表在下」；③「修改项目」动态列表增加「备注」列，并将「修改类型」改为下拉选择器（`combobox`），选项从价目表「修改类型」分类自动加载，UI 与灵感记录制品名称下拉框一致。
- **人物关系（oc-relations）**：① 自定义关系类型的删除按钮不再放在表单字段的 checkbox 项内，改为在关系页顶部单独列出所有自定义关系类型，每个类型旁提供删除按钮；② 思维导图节点圆形由 `60px` 进一步缩小到 `48px`，内部文字 / 头像圆由 `44px` 缩小到 `36px`，连线端点半径由 `25` 调整为 `20`。
- **电脑端标题对齐**：去掉 combobox/datalist 列标题的 `.combobox-col` 特殊 `padding-left:16px`，所有 `.dynamic-list-header span` 统一使用 `padding-left:8px` + 透明边框占位，标题文字与输入框内容起点一致，解决接稿加价项目、开团制品名称 / 单价 / 是否流团、售后制品名称 / 补偿方式等列标题偏左偏右问题。
- 源码已提交 `1e9cded`；因本地代理 `127.0.0.1:7897` TLS 握手失败，暂未能推送到 GitHub origin/main，需网络/代理恢复后手动重推；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（L 轮修正）· 功能 v29 对齐治根

- **人物关系（oc-relations）**：① 自定义关系类型管理做成独立卡片，始终显示在人物关系页顶部，空状态时提示“暂无自定义关系类型”；卡片内可直接输入新增，每个自定义类型带常驻 `✕` 删除按钮；② 思维导图节点圆形回退到 `60px`（内部文字 / 头像圆 `44px`，连线端点半径 `25`），保持 J 轮确定的尺寸。
- **电脑端 dynamic-list 标题对齐（治根）**：发现历次反复偏左偏右的根因是 `.dynamic-list-header` 用 `margin:-8px` 伸出容器边缘，导致 header 内容区起点与 row 内容区起点相差 8px。L 轮去掉负 margin，header 与 row 共享 container 内容区，再使用 `::before` 伪元素把标题背景延伸至容器边缘。所有 `.dynamic-list-header span` 统一 `padding-left:8px + 透明边框`，与输入框内容起点严格一致，彻底解决接稿修改项目、开团制品列表、售后记录等列标题偏左偏右问题。
- 源码已提交 `dc0920e` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（M 轮修正）· 功能 v29 对齐补刀

- **电脑端 dynamic-list 标题底色恢复**：L 轮用 `::before` 伪元素承载淡蓝背景，在某些渲染路径下标题底色丢失。M 轮改为直接给 `.dynamic-list-header` 设置 `background:var(--c-primary-bg)`，同时用 `margin:0 -8px` 把背景延伸至容器边缘，标题文字仍靠 `span{padding-left:8px + 透明边框}` 与输入框内容对齐。这样标题既有淡蓝底色，又保持像素级左对齐。
- **人物关系（oc-relations）**：自定义关系类型管理从关系页顶部独立卡片中移除，放回「关系类型」字段内。在新增 / 编辑关系表单中，每个自定义关系类型选项标签右侧显示常驻 `✕` 删除按钮（内置选项无删除按钮），点击即可从 DB 和当前选项中移除该自定义类型，与管理其他 allowCustom 字段体验一致。
- 源码已提交 `2144fc2` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（N 轮修正）· 功能 v29 回退 K 轮 + 关系删除归位

- **电脑端 dynamic-list 标题对齐**：M 轮改坏标题底色与左对齐，用户要求按 K 轮 `1e9cded` 回退。N 轮将 `.dynamic-list-header` 恢复为 K 轮样式：`margin:-8px -8px 6px;padding:4px 8px 4px 8px`，背景色 `var(--c-primary-bg)` 延伸至容器四边；标题 span 统一 `padding-left:8px + 透明边框`，与输入框内容起点一致。不再做 combobox-col 特殊补偿或 `::before` 伪元素，只按 K 轮给出的样式精修。
- **首页日历（home）**：平台发布记录条数 `.cal-day-count` 从独立行挪到平台圆点后面（与圆点同一行）；开稿 / 截稿 / 同天标签整体上调并固定位置，不再被推到日期格底部。
- **人物关系（oc-relations）**：删除按钮不再放在每个自定义关系类型选项后面，改为放在「自定义关系类型」输入框 +「添加」按钮旁边：输入要删除的自定义关系类型后点「删除」即可从 DB 和当前 checkbox 组中移除。
- 源码已提交 `01f4e2d` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（O 轮修正）· 功能 v29 七项精修

- **电脑端标题对齐（只修标出的两处）**：接稿排期「修改项目」列表的「次数 / 价格 / 备注」标题此前没有定义 header 宽度导致挤成一坨，现补上对应 flex 宽度；后将 header 的 `padding-left` 减到 `0px` 以修正视觉偏右。开团记录「制品列表」的「单价 / 对应厂家」标题偏左，将 header span 的 `padding-left` 加大到 `22px`，使其与输入内容视觉左对齐。同时发现接稿排期与开团记录的「制品列表」都使用 `data-key="products"`，导致两者的 header 样式互相影响；现改用 `data-module="schedule"` / `data-module="groupbuy-records"` 分别控制，避免改开团时把接稿排期的「价格」header 也推偏。其余列一律不动。
- **人物关系（oc-relations）关系类型删除改为红色勾选删除**：自定义类型选项标记 `data-custom="true"`，红色「删除」按钮与「添加」按钮并排，无额外提示文案；勾选要删的自定义类型后点红色「删除」即从 DB 与当前表单所有关系类型 checkbox 组中移除。
- **首页平台发布（home）**：24 小时数据（点赞 / 收藏 / 播放量 / 阅读量）与 7 天数据各用 `row` 分组并排成一行四个。
- **开团记录（groupbuy-records）字段重组**：①「制品总价 / 邮费总价」合并一行；② 新增「拼团人气」放在「购买人数」后并排；③「总成本」改名「制品总成本」，新增「邮费总成本」放在其后并排；④ 数据柱状图与盈亏统计的成本统一改为「制品总成本 + 邮费总成本」。
- **打样记录（groupbuy-samples）**：「样品费用 / 样品数量」用 `row` 分组并排。
- **时间线（oc-timeline）重要性按钮**：按参考图改为「一个长椭圆容器 + 内部四段」的分段胶囊样式。未选中时每段显示彩色圆点 + 文字（重要 / 较重要 / 一般 / 次要）；选中段整体填充对应颜色，圆点和文字变白。表单内字段与列表顶部过滤按钮统一此样式，并同步更新选项 label 为不带「红/橙/绿/蓝」前缀的纯文字。后将时间线 toolbar 增加 `timeline-toolbar` 类，把 toolbar 与过滤条间距统一为 `8px`；过滤条宽度改为 `width:100%; max-width:200px` 并水平居中，使移动端与搜索框同宽、桌面端保持 200px。
- **设置页**：①「导航栏图标自定义」改为图标 + 名称都可编辑，图标输入框与名称输入框并排且等宽（新增 `navLabels` 设置项与 `getNavLabel()`，侧栏与设置页同步）；②「备份数据」按钮文案改为「导出数据」。
- 源码已提交 `de3eb50` 并推送到 GitHub origin/main；Service Worker 缓存已升级到 v44。若本地预览仍看不到变化，请 **Ctrl+F5 硬刷新** 或浏览器「设置 → 隐私与安全 → 清除浏览数据」清掉 localhost:8123 / 127.0.0.1:51063 的缓存。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（O 轮收尾微调）· 功能 v29

- **开团记录「制品列表」标题继续右移**：用户截图反馈「单价 / 对应厂家」标题仍偏左，将 `data-module="groupbuy-records"` 下这两个 header span 的 `padding-left` 从 `22px` 进一步加大到 `34px`，使其与输入框内容视觉左对齐。
- **时间线重要性过滤条宽度明确对齐搜索框**：将 `.timeline-filter-bar` 从 `width:100%; max-width:200px` 改为桌面端固定 `width:200px`（与 `.search-box input` 完全一致），并在 `@media (max-width:768px)` 下改为 `width:100%`，避免旧写法在某些容器下被压缩得比搜索框窄。
- 源码已提交 `de3eb50`；Service Worker 缓存已升级到 v44。由于网络 SSL/TLS 握手失败，最后一次推送未成功，待网络恢复后手动重推。

### 2026-08-07（O 轮再微调）· 功能 v29

- **接稿排期「修改项目」价格列缩短**：将 `modifyPrice` 的 label 从「价格（元/次）」改为「价格（元）」，输入框与标题宽度从 `110px` 减到 `80px`，整体更紧凑自然。
- **时间线重要性按钮改为内容自适应**：取消固定 `200px` 和等分 `flex:1`，改为 `.timeline-filter-bar{width:auto}`，每个 `.timeline-color-filter{flex:0 0 auto;padding:6px 14px}`；选中后的胶囊只包裹文字，不会撑开变长。手机端 padding 进一步减到 `6px 10px`，更窄。
- 源码已提交 `33b8a37` 并推送到 GitHub origin/main；Service Worker 缓存已升级到 v45。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（P 轮修正）· 功能 v29

- **接稿排期「修改项目」标题继续左移**：用户反馈「次数 / 价格 / 备注」标题仍偏右，在 `padding-left:0` 基础上再加 `margin-left:-6px`，使标题文字进一步向左对齐输入内容。
- **开团记录「制品列表」标题继续微调**：
  - 「单价 / 对应厂家」标题 `padding-left` 从 `34px` 改为 `30px`；
  - 「售卖数量 / 是否流团」标题在默认 `8px` 基础上减 `2px`，设为 `padding-left:6px`。
- **时间线重要性按钮与搜索框同高、选中段缩窄**：将 `.timeline-color-filter` 垂直内边距从 `6px` 提到 `8px`，使胶囊总高度与 `.search-box input` 一致；仅对 `.timeline-color-filter.active` 减少水平内边距到 `10px`（手机端 `8px`），让选中后有底色的段更窄。保留未选中按钮原样，避免整体过滤条被撑得过宽。
- Service Worker 缓存升级到 v46；源码已提交 `52d8973` 并推送到 GitHub origin/main。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（Q 轮修正）· 功能 v29

- **时间线重要性按钮回退到 v44 并缩短选中段**：整体恢复 v44 的长椭圆容器 + 四段等分样式（`.timeline-filter-bar{width:200px}`，手机端 `width:100%`；`.timeline-color-filter{flex:1;padding:6px 0}`）。仅对 `.timeline-color-filter.active` 改为 `flex:0 0 auto;padding:6px 10px`，让选中后有底色的段缩短、只包裹文字。
- **接稿排期「修改项目」标题回退到 v45**：去掉 P 轮新增的 `margin-left:-6px`，恢复为 `padding-left:0`。
- **开团记录等其余表格保持 P 轮状态不动**：单价/厂家 `padding-left:30px`，售卖数量/是否流团 `padding-left:6px`。
- Service Worker 缓存升级到 v47；源码已提交 `df40651` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（R 轮修正）· 功能 v29

- **时间线重要性按钮完全回到 v44 并加宽**：移除 Q 轮的选中缩短效果，恢复 v44 的 `.timeline-color-filter{flex:1;padding:6px 0}` 与 `.timeline-color-filter.active{border-radius:999px;color:#fff}`；电脑端 `.timeline-filter-bar` 宽度从 `200px` 加大到 `240px`（手机端仍为 `100%`）。
- **接稿排期「修改项目」标题再左移 4px**：在 v45 `padding-left:0` 基础上加 `margin-left:-4px`。
- Service Worker 缓存升级到 v48；源码已提交 `e47a45b` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（S 轮修正）· 功能 v29

- **时间线重要性按钮恢复两侧半圆并加宽**：电脑端 `.timeline-filter-bar` 宽度从 `240px` 加大到 `300px`；选中项 `.timeline-color-filter.active` 改为 `flex:0 0 auto;padding:6px 14px;border-radius:999px`，让有底色段只包裹文字且左右保持半圆；未选中项仍 `flex:1` 等分剩余空间。
- **接稿排期「修改项目」标题继续左移**：「次数 / 价格 / 备注」标题 `margin-left` 从 `-4px` 调到 `-8px`，与输入内容更严格对齐。
- Service Worker 缓存升级到 v49；源码已提交 `6e732f3`，但推送 GitHub origin/main 失败（HTTP 401 Unauthorized，鉴权失败，待恢复后重试）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（T 轮修正）· 功能 v29

- **时间线重要性按钮位置固定、电脑端继续加宽**：回到四段等分布局（`.timeline-color-filter{flex:1;padding:6px 0}`），选中项只改变颜色/背景，不改变宽度或位置；电脑端 `.timeline-filter-bar` 宽度从 `300px` 加大到 `360px`（手机端仍为 `100
### 2026-08-07��T ���������� ���� v29

- **ʱ������Ҫ�԰�ťλ�ù̶������Զ˼����ӿ�**���ص��Ķεȷֲ��֣�.timeline-color-filter{flex:1;padding:6px 0}����ѡ����ֻ�ı���ɫ/���������ı���Ȼ�λ�ã����Զ� .timeline-filter-bar ���ȴ� 300px �Ӵ� 360px���ֻ�����Ϊ 100%����
- **�Ӹ����ڡ��޸���Ŀ���������΢��**�����������������ƣ�margin-left:-12px�������۸񡹱��� margin-left:-8px������ע������ margin-left:-4px���ָ�֮ǰ����Ч�������ټ������ƣ���
- Service Worker ���������� v50��Դ�����ύ 416ea72 �����͵� GitHub origin/main��ˢ��ǰ�� **Ctrl+F5** Ӳˢ�¡�APK ��δ��������ؽ� v1.1.17 �ϻ���

### 2026-08-07��U ���������� ���� v29

- **ʱ������Ҫ�԰�ťλ�ù̶� + ѡ�б�������ֻ��������**��ÿ��ѡ��� lex:1 �ȷ֣����ʱ�������/λ�ò��䣻��ѡ������ڲ� .timeline-color-label ��ʾ��ɫ���ұ��������׵���֣�������ֻ�������֣����ͼ1һ�¡����Զ� .timeline-filter-bar ���ȱ��� 360px���ֻ��� 100%���������򡢵���/������ť�� toolbar ��ʽδ�Ķ���
- **�Ӹ����ڡ��޸���Ŀ���������΢��**����������margin-left �� -12px �Ӵ� -16px�����۸񡹱��� -8px������ע���� -4px ���� �����Ҷ����������ݡ�
- Service Worker ���������� v51��Դ�����ύ 2f2e0c7 �����͵� GitHub origin/main��ˢ��ǰ�� **Ctrl+F5** Ӳˢ�¡�APK ��δ��������ؽ� v1.1.17 �ϻ���

### 2026-08-07（V 轮修正）· 功能 v29

- **时间线重要性按钮选中背景再放大、电脑端容器继续加宽**：电脑端 .timeline-filter-bar 宽度从 360px 加大到 440px；选中项内部 .timeline-color-label 的内边距从 4px 12px 加大到 6px 18px，让底色胶囊更宽。
- **接稿排期「修改项目」标题继续微调**：「次数」margin-left 从 -16px 加大到 -20px；「价格」从 -8px 调到 ；「备注」从  调到 6px，整体更对齐输入内容。
- Service Worker 缓存升级到 v52；源码已提交 5f4fbdd，但 git push origin main 失败（SSL/TLS 握手失败，走本地代理 127.0.0.1:7897），待网络恢复后重试。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（W 轮修正）· 功能 v29

- **时间线重要性按钮选中背景再放大**：外框 .timeline-filter-bar 保持 440px 不变；选中项内部 .timeline-color-label 内边距从 6px 18px 加大到 8px 24px，让底色胶囊更宽。
- **接稿排期「修改项目」标题再微调**：「次数」从 -20px 回调到 -12px（往右）；「价格」从  调到 +4px（往右）；「备注」保持 +6px。
- **搜索框 / 导入 / 新增按钮 toolbar**：HTML/CSS 与 v44 一致，未改动。
- Service Worker 缓存升级到 v53；源码已提交 e2cbf9c，但 git push origin main 失败（HTTP 401 Unauthorized，凭据失效），待恢复后重试。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（X 轮修正）· 功能 v29

- **时间线重要性按钮完全回退到 v1.1.16 独立胶囊样式**：取消长椭圆外框，恢复为 .timeline-filter-bar{display:flex;flex-wrap:wrap;gap:8px} 容器 + .timeline-color-filter 独立胶囊按钮；未选中项使用 .inactive 白底灰字带边框，选中项直接填充原色（#e74c3c/#fa8c16/#27ae60/#3498db）并显示白色圆点。按钮尺寸比 v1.1.16 稍大（padding:8px 18px）。
- **接稿排期「修改项目」标题最终对齐**：「次数」从 -12px 回调到 -6px（往右）；「价格」保持 +4px；「备注」从 +6px 回调到 （往左）。
- Service Worker 缓存升级到 v54；源码已提交 eddd374 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（Y 轮修正）· 功能 v29

- **时间线 toolbar 恢复 v1.1.16 样式**：移除 `timeline-toolbar` 类及其 `margin-bottom:8px` 覆盖，恢复为 `<div class="toolbar">`，使用默认 `margin-bottom:16px`。
- **时间线重要性按钮改回 W 轮样式**：恢复固定长椭圆外框（`.timeline-filter-bar{width:440px}`），内部 `.timeline-color-label` 选中时显示彩色胶囊，外框尺寸点击不变。
- **接稿排期「修改项目」标题继续微调**：「次数」保持 `-6px`；「价格」从 `+4px` 调到 `-2px`（往左）；「备注」从 `0` 调到 `-4px`（往左）。
- Service Worker 缓存升级到 v55；源码已提交 `72d37d4` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（Z 轮修正）· 功能 v29

- **接稿排期「修改项目」标题「次数」继续往左**：margin-left 从 -6px 调到 -10px。
- Service Worker 缓存升级到 v56；源码已提交 21b5c1b 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AA 轮修正）· 功能 v29

- **接稿排期「修改项目」标题「次数」继续往左**：margin-left 从 -10px 调到 -16px。
- **接稿排期「修改项目」「修改类型」下拉框缩短**：列宽由 `flex:1 1 0; min-width:80px` 改为固定 `70px`，使下拉框更紧凑。
- Service Worker 缓存升级到 v57；源码已提交 `9a15bcf` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AB 轮修正）· 功能 v29

- **搜索框 / 筛选 / 新增记录确认 = F 轮样式**：拉取 F 轮（3125d5f）`index.html` 逐行核对 `.toolbar`、`.search-box`、`.filter-combobox`、`.btn-primary` 及 `@media(max-width:480px)` 窄屏布局，与当前完全一致（自 v1.1.16 起未变动），无需改动。
- **接稿排期「修改项目」「修改类型」下拉框还原**：AA 轮缩到的 `70px` 过短，还原回 F 轮原始 `flex:1 1 0; min-width:80px`。
- **时间线重要性按钮确认 = 固定外框**：`.timeline-filter-bar{width:440px}` 固定，点击只改内部 `.timeline-color-label` 胶囊，外框尺寸不变，符合需求。
- Service Worker 缓存升级到 v58；源码已提交 `8410ddb` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AC 轮修正）· 功能 v29

- **时间线重要性按钮外框尺寸严格固定**：给 `.timeline-filter-bar` 加固定 `width:440px;height:46px`，4 个 `.timeline-color-filter` 改为 `flex:0 0 25%;height:40px;min-width:0;overflow:hidden`，并限制内部 `.timeline-color-label` 最大宽度，确保点击选中后只变内部胶囊颜色/白点，外框宽高完全不变。
- **接稿排期「修改项目」「修改类型」下拉框重新缩短到 75px**：AB 轮回退到 80px 后用户仍嫌宽，本次改为固定 `75px`（比 F 轮 80px 略短，又不像 AA 轮 70px 那么挤），并通过更具体的选择器 `.dynamic-list-row .combobox-wrapper[data-subkey="modifyType"]` 覆盖通用 `flex:1.5` 规则，实际生效。
- Service Worker 缓存升级到 v59；源码已提交 `36421c4` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AD 轮修正）· 功能 v29

- **时间线重要性按钮：宽度与搜索框一致、居中、高度自适应**：`.timeline-filter-bar` 宽度从 440px 改为 `200px`（等于搜索框 `.search-box input{width:200px}`），保持 `margin:0 auto` 居中，去掉 AC 轮加的固定 `height:46px` 与 segment 固定高，恢复自适应高度（与 Y 轮/W 轮一致，不加宽加高）；点击选中仍只变内部胶囊，外框尺寸不变。
- **接稿排期「修改项目」「修改类型」下拉框参考上方制品列表「制品名称」列宽**：制品名称列规则为 `flex:1.5 1 0; min-width:90px`，本次将修改类型下拉框定为固定 `90px`（参考其 min-width:90px），比 AC 轮 75px 略宽，与制品名称视觉协调。
- **间距说明**：toolbar 内 `.toolbar{gap:8px}`，但 `spacer` 把「导入故事小记」+「+新增事件」推到最右，故搜索框与导入故事小记按钮实际水平间距并非 8px 而是被撑开；时间线筛选栏居中于页面，与上方 toolbar 的垂直间距为 `.toolbar{margin-bottom:16px}` + 自身 `margin:0 auto 16px`（上16px）。若用户要的是"重要性按钮水平对齐到导入故事小记按钮"，需改为非居中方案，待确认。
- Service Worker 缓存升级到 v60；源码已提交 `e03de11` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AE 轮修正）· 功能 v29

- **时间线重要性按钮选中胶囊改小**：`.timeline-color-filter.active .timeline-color-label` 的 `padding` 从 `0 14px` 改为 `0 8px`，与截图中胶囊大小一致。
- **时间线重要性按钮与上方 toolbar 间距改为 8px**：`.timeline-filter-bar` 的 `margin` 改为 `8px auto 16px`，并用 `.toolbar:has(+ .timeline-filter-bar){margin-bottom:8px}` 让时间线页面 toolbar 下边距也变成 8px，与 toolbar 内部按钮 `gap:8px` 一致。
- **接稿排期「修改项目」「修改类型」下拉框改为 130px**：由 90px 改为固定 130px，header 同步 130px。
- **接稿排期「修改项目」标题左对齐底下内容框**：去掉「次数/价格/备注」标题的负 margin，统一恢复 `padding-left:8px;margin-left:0`，让标题 span 的左边缘与下方 input 内容框左边缘对齐。
- Service Worker 缓存升级到 v61；源码已提交 `3deb31e` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AF 轮修正）· 功能 v29

- **时间线重要性按钮选中胶囊放大**：`.timeline-color-filter.active .timeline-color-label` 的 `padding` 从 `0 8px`（AE 轮）放大回 `0 16px`，让彩色胶囊更宽。
- **所有搜索框到下方按钮间距改为 10px**：全局 `.toolbar{margin-bottom}` 从 `16px` 改为 `10px`；时间线页面 override 同步 `8px → 10px`，并把 `.timeline-filter-bar` 顶部 margin 从 `8px` 归 `0`，使"搜索框工具栏 → 重要性按钮"垂直间距精确为 10px。
- **接稿排期「修改项目」「次数」输入框加宽**：`form-input[data-subkey="modifyCount"]` 与对应 header span 从 `flex:0 0 55px` 改为 `70px`。
- Service Worker 缓存升级到 v62；源码已提交 `f4633ca` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AG 轮修正）· 功能 v29

- **时间线重要性按钮选中胶囊再放大**：`.timeline-color-filter.active .timeline-color-label` 的 `padding` 从 `0 16px`（AF 轮）改为 `0 22px`。⚠️ 注意：时间线栏宽锁定为搜索框宽度 `200px`，每个重要性分段仅约 48px，胶囊在 `padding≈14px` 时已填满整段并被 `overflow:hidden` 裁切，故继续加大水平 padding 在视觉上不会再变宽；若需胶囊明显更大，只能加宽整条时间线栏（如 200→260px）或增加胶囊高度（会撑高外框）。待用户确认方向。
- **时间线间距回退 8px**：`.toolbar:has(+ .timeline-filter-bar){margin-bottom}` 与 `.timeline-filter-bar` 顶部 margin 均由 AF 轮的 10px/0 回退为 `8px`（恢复"搜索框工具栏 → 重要性按钮"垂直间距 16px，与 toolbar 内部按钮 gap 一致）。全局 `.toolbar{margin-bottom:10px}` 保持不变（仅时间线页被 override 回 8px）。
- Service Worker 缓存升级到 v63；源码已提交 `f52372a` 并推送到 GitHub origin/main（首次推送遇 schannel SSL 握手失败，重试成功）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AH 轮修正）· 功能 v29

- **全局 `.toolbar{margin-bottom}` 回退 16px**：AF 轮改的 `10px` 撤销，恢复初始 `16px`（与 v1.1.16 一致）。时间线页 override 仍保持 `8px`（AG 轮定）。
- **时间线选中胶囊高度加高**：`.timeline-color-filter.active .timeline-color-label` 的 `padding` 从 `0 22px` 改为 `5px 22px`（加垂直 5px，胶囊明显变高）；同时为保持外框高度稳定不跳动，未选中态 `.timeline-color-label` 同步加 `padding:5px 4px`，使整条时间线栏高度一致、仅选中段显彩色。
- **外框内边距取消**：`.timeline-filter-bar` 的 `padding:3px` 改为 `padding:0`，让胶囊（分段）紧贴外框边框，不再被 3px 内边距挤在中间。
- Service Worker 缓存升级到 v64；源码已提交 `7bc961a` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AI 轮修正）· 功能 v29

- **故事小记 / 时间线 / 约稿记录 顶部「角色筛选排」改为只显示一排**：`.relation-person-grid` 由 `flex-wrap:wrap` 改为 `flex-wrap:nowrap; overflow-x:auto`（单行横向滚动，角色再多也不换行）；`.relation-person-btn` 加 `flex:0 0 auto` 防止被压缩。三个模块顶部这排按钮（📋全部 + 各角色名）现在始终只占一行。
- Service Worker 缓存升级到 v65；源码已提交 `9647aff` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AJ 轮修正）· 功能 v29

- **角色筛选排改为「折叠」方式（替代横向滚动）**：用户要求只显示一排且用折叠而非滚动。`.relation-person-grid` 恢复 `flex-wrap:wrap`，新增 `.collapsed{max-height:42px;overflow:hidden}` 折叠态 + `.relation-toggle-btn`（展开 ▾ / 收起 ▴）按钮；故事小记、约稿记录（通用 render）与时间线（独立 render）的角色筛选 grid 默认带 `collapsed` 类，并在其后插入「展开」按钮，点击由新函数 `togglePersonGrid(btn)` 切换折叠/展开。人物关系的「快捷查看」角色排未改（用户只指定三个模块）。
- **人物关系底部关系列表分页改为每页 5 条**：`renderRelations` 内 `pageSize` 由 `20` 改为 `5`，同步 `PAGE_SIZES['oc-relations']` 由 `20` 改为 `5`。
- Service Worker 缓存升级到 v66；源码已提交 `dd4c76e`（本地，推送因 GCM 凭据管理器无头环境卡住失败，待恢复后 `git push origin main`）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AK 轮修正）· 功能 v29

- **「展开/收起」折叠按钮并入同一排**：用户要求折叠按钮与角色筛选按钮放同一行（而非单独一行显示在下方）。新增 `.relation-person-row{flex:nowrap}` 容器包裹「角色 grid + 展开按钮」，角色 grid 改 `flex:1; min-width:0`，按钮 `flex:0 0 auto` 固定在右侧同一行；`togglePersonGrid` 仍用 `previousElementSibling` 取 grid（结构未变）。故事小记、约稿记录、时间线三模块统一生效。
- **人物关系底部关系列表分页改为每页 10 个**：`renderRelations` 内 `pageSize` 由 `5` 改为 `10`，同步 `PAGE_SIZES['oc-relations']` 由 `5` 改为 `10`（用户明确「一页最多显示10个」）。
- Service Worker 缓存升级到 v67；源码已提交 `fc373b0`（本地，GitHub 连接被重置/无法连接，待网络恢复后 `git push origin main`）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AL 轮修正）· 功能 v29

- **人物关系「快捷查看」角色排也改为折叠 + 一头一尾固定 + 中间均匀分布**：之前快捷查看排无折叠、无「全部/展开」端点。现与三模块统一结构——新增 `.relation-person-row` 包裹「角色 grid + 展开按钮」；grid 加 `distribute` 类用 `justify-content:space-between`，使「📋 全部」固定头部、「展开 ▾」固定在行外右侧尾端、中间角色名均匀铺开；grid 默认 `collapsed`（只显一排，超出隐藏）。新增 `clearPersonRelations()` 作为「全部」点击处理（取消选中并收起内联详情）。
- **角色筛选排「全部」与「展开」分别固定在行首/行尾，中间角色按钮均匀分布**：之前「全部」在 grid 内部，grid 用 `justify-content:space-between` 导致角色少时中间空白过大。现把「全部」拆到 row 最左作为固定项，grid 仅含角色按钮并改为 `justify-content:space-evenly`，「展开」固定在 row 最右。故事小记、时间线、约稿记录、人物关系快捷查看四模块统一。
- **实时报价 4 项改造（AN 轮）**：
  1. **制品序号**：制品列表新增「序号」列（只读，显示在制品名称左侧），序号 = 行序 01、02、03…；删除行后随渲染自动重排；仅作展示引用编号，不参与价格运算。
  2. **加价项目绑定制品序号**：加价项目行新增「绑定」下拉，选项为制品序号 + 「无（不绑定）」。绑定后该加价项在实时报价中缩进（`.dc-rr.bound`，左侧竖线）嵌套展示在对应制品下方；不绑定则独立「加价项目（不绑定）」块展示；无论是否绑定，金额全部计入加价合计/总价。
  3. **同模行内化**：移除全局「同模阶梯价倍率」卡片；改为每行制品勾选【同模】后行内出现「同模类型」下拉（改色+字×0.6 / 改人+字色×0.8 / 改人×0.5）+ 可手改倍率输入框。柄图标识 >1 自动勾选本行同模；同柄图标识多行自动继承同模类型（仅初始赋值，每行独立可单独取消/改倍率）。
  4. **加急功能**：稿件用途倍率下方新增「加急」卡片（整单复选框 + 倍率输入框，默认 2，可改）。计算顺序严格：制品→SET→加价→用途倍率→（折扣/同担同推）→**最后整体 × 加急倍率 = 最终报价**；不勾选则 ×1。
- Service Worker 缓存升级到 v70；源码已提交 `71fda0e`（本地，GitHub 当前连接失败待重推）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AO 轮修正）· 功能 v29

- **制品列表提示词精简**：提示由「序号自动生成，柄图自动分组，行内勾选同模启用阶梯计价」改为「柄图自动分组，勾选同模启用阶梯计价」（移除"序号自动生成"，因序号本就自动）。
- **序号去框**：制品列表「序号」列去掉背景方框（`.dc-prod-seq` 改为纯数字、字号字重与制品文字统一、无背景无圆角）；收据序号 `.dc-r-no` 保持加粗（序号加粗早已生效）。
- **同模类型下拉框显示格式**：选项由 `改色+字 ×0.6 / 改人+字色 ×0.8 / 改人 ×0.5` 改为 `改色+字*0.6`、`改人+字/色*0.8`、`改人*0.5`（× 改为 *，与用户公式一致）。
- **加价项目表头/行重排**：提示由「不参与同模计价、SET折扣；可绑定制品序号缩进展示」改为「不参与同模计价、SET折扣」；表头与每行重排为【制品绑定】(置最前、列宽扩大，下拉选项"无") →【名称】(占位由"项目名称"缩短为"名称") → 数量 → 单价 → 删除。
- **加急改为制品单独勾选**：移除整单加急复选框，每个制品行新增【加急】复选框；保留「加急倍率」输入（默认 2）。计算规则：加急仅作用于被勾选制品——最终价 = 用途/优惠/同担后的价格 × (1 + (加急倍率-1) × 勾选制品基价占比)；无制品勾选则不生效。导入/保存按制品存储 `urgent` 字段（兼容旧版整单 `urgentEnabled`）。
- **实时报价加价项目树形展示**：绑定的加价项改为缩进树形（`├─` / `└─`），去掉左侧蓝色竖条（移除 `.dc-rr.bound` 的 `border-left`），字体颜色与制品相同（`var(--c-text)`），序号（01/02…）保持加粗；不再显示"（绑定XX）"冗余标注。
- **加急在收据的展示与「倍率计算」同款**：加急板块移到「优惠」之后、「最终报价」之前，显示「加急倍率 ×rate」与「加急后价格」，格式与倍率计算（稿件用途 ×rate / 倍率后价格）一致。
- Service Worker 缓存升级到 v71；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AP 轮修正）· 功能 v29

- **修复同模类型下拉显示为 `undefined*0.6`**：DC_MODEL 中除"无同模"外未设 `label`，AO 轮误用 `m.label` 导致 undefined。改回 `${m.value}*${m.rate}`，显示为 `改色+字*0.6`、`改人+字/色*0.8`、`改人*0.5`。
- **mobile 制品列表改为两行布局**：之前 8 列单行在手机上拥挤。现 768px 以下 `.dc-product-row` 改为 grid-template-areas 两行：第一行「序号/制品/柄图标识/单价/数量/删除」，第二行「同模勾选/加急勾选」，与接稿排期制品列表的 mobile 换行风格一致；同模展开行仍占满整行。
- **加价项目树行与制品首字左对齐**：`.dc-rr.bound` 左内边距从 28px 改为 16px，使 `├─/└─` 后的文字与制品序号后第一个字处于同一 x 坐标（40px）。
- **加急仅精确应用于勾选项目**：移除 AO 轮的"勾选基价占比整体相乘"估算方案。新算法：
  1. 先计算每个制品及其绑定加价在 SET 组折扣后的金额；
  2. 按制品 `urgent` 勾选状态拆分为 `urgentBase` / `nonUrgentBase`；
  3. 用途倍率：勾选部分 = urgentBase × uRate × urgentRate，未勾选部分 = nonUrgentBase × uRate；
  4. 折扣/同担同推优惠再作用于加急后的金额；
  5. 最终报价 = 上述结果 - 同担减免。
  这样只有勾选制品及其绑定加价享受加急倍率，未勾选项目完全不受影响。
- Service Worker 缓存升级到 v72；源码待提交。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AQ 轮修正）· 功能 v29

- **制品行布局精调**：
  - 序号列由 44px 收窄到 28px（mobile 24px），行列间距由 6px 收紧到 4px（mobile 3px），让制品下拉框更贴近序号；
  - 加急/同模位置互换：加急在左、同模在右，并且共占同一行、间距收紧；
  - 同模改为「勾选 + 类型下拉框」inline 形式，勾选后直接在 checkbox 右侧出现类型下拉与倍率输入，去掉原来的独立「同模类型」展开行。
- **新增同模规则示意表**：在「优惠体系」卡片上方新增「同模规则」卡片，列出 `改色+字 ×0.6 / 改人+字/色 ×0.8 / 改人 ×0.5` 示意。
- **同模明细改回显示类型标签**：收据中同模明细由通用「同模 ¥… ×…」改回「改人+字/色 ¥… ×…」等具体类型标签；多件时显示为「改人+字/色 首件 ¥… + 余N件 ×¥…」。
- **加急收据展示优化（明确仅勾选生效）**：加急板块改为显示「加急基数（仅已勾选制品）/ 加急倍率 / 加急后价格」，与倍率计算格式一致，避免视觉上误以为整单加急；计算逻辑保持仅勾选制品及其绑定加价乘以加急倍率，未勾选项目不受影响。
- Service Worker 缓存升级到 v73；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AR 轮修正）· 功能 v29

- **mobile 制品列表对齐与显示精修**：
  - 序号左边缘与「+ 添加制品」按钮文字左边缘对齐（mobile `.dc-prod-seq` 增加 `padding-left:10px`）；
  - 柄图标识列加宽（64px → 72px），placeholder 完整显示；
  - 第二行 grid-template-areas 改为 `. urgent same same same same`，使「加急」checkbox 与上方「制品」输入框左对齐，并解决文字截断；
  - 同模选择区去掉 select/number 原生箭头与小圆（`-webkit-appearance:none`、隐藏 spinner）。
- **同模规则卡片改为 radio 行样式**：移除 AQ 轮的静态表格，改为与「稿件用途倍率」同款的 `calc-rate-row`：左侧 radio + 名称，右侧数字显示倍率；包含「无同模」示例行，标题改为「同模阶梯价倍率（选择同模更改项）」。
- **加价项目缩进树与制品名对齐**：`.dc-rr.bound` 左内边距由 16px 改为 4px，配合 `.dc-r-tree` 22px 宽度，使 `├─/└─` 后文字与制品序号后第一个字严格同 x 坐标。
- Service Worker 缓存升级到 v74；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AS 轮修正）· 功能 v29

- **mobile 制品列表布局精调**：数量从第一行移到第二行；第二行布局改为 `. urgent same same qty`，加急与同模相邻且靠近；制品名跨两列，柄图标识保持 60px 展示完整；序号左侧 padding 保持与「+ 添加制品」按钮左对齐。
- **同模类型下拉改为 combobox 下拉选择器**：与制品名称同款结构（input + ▼ toggle + 下拉选项），选项显示 `改色+字*0.6 / 改人+字/色*0.8 / 改人*0.5`；去掉原生 select 的箭头/小圆。
- **加价项目缩进树与制品明细文字左对齐**：`.dc-rr.bound` 左内边距由 4px 改为 16px，使 `├─/└─` 后文字与制品明细文字左边缘对齐。
- **导入接稿增加修改加价模块**：从接稿记录 `modifications` 字段导入修改类型/次数/价格/备注；实时报价新增「修改加价」编辑区，支持新增/删除/下拉选择修改类型；次数×价格计入修改加价总额。
- **实时报价收据新增修改加价与最终总价**：定金尾款后追加：修改加价、需付尾款（尾款+修改加价）、最终总价（最终报价+修改加价）。
- **修复导入接稿时加急覆盖全部制品的问题**：导入时不再使用旧版整单 `urgentEnabled` 覆盖所有制品的加急状态，改为仅读取 per-product `urgent` 字段。
- Service Worker 缓存升级到 v75；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AT 轮修正）· 功能 v29

- **制品列表列序修正**：header 与 row 的列序统一为「序号 / 制品 / 柄图标识 / 价格 / 数量 / 加急 / 同模 / 删除」，解决 mobile 端 price 区域缺失导致布局错乱的问题。
- **mobile 制品列表两行布局重排**：第一行「序号 / 制品 / 柄图标识 / 价格 / 数量 / 删除」；第二行「加急 / 同模（含类型下拉与倍率）」，让加急与同模相邻，数量放到第一排。
- **移除同模下拉框后的小圆**：同模类型 combobox 去掉 ▼ toggle 按钮，保留 input focus/click 打开下拉，避免 mobile 上显示多余小圆。
- **实时报价加价项目缩进树对齐**：`.dc-rr.bound` 左内边距调整为 18px，`.dc-r-tree` 固定宽度 22px，使 `├─/└─` 后文字与制品字段严格左对齐。
- **修改加价收据间距与标题**：在定金尾款后增加 `margin-top:12px`，新增「修改项目」小标题；最终总价放到需付尾款前。
- **总结性价格数字统一加粗**：`.dc-rr.total > span:last-child` 统一 `font-size:13px;font-weight:700`，确保倍率后价格、折后价格、加急后价格、需付尾款、最终总价等数字与正文同字号且加粗。
- Service Worker 缓存升级到 v76；源码已提交 `946f1b1` 并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AU 轮修正）· 功能 v29

- **删除同模区域的第二个小圆（倍率数字框）**：AT/AU 前同模区域存在两个圆——「同模」勾选框（功能开关，保留）+ 倍率手填数字框（空着时缩成小白圆，多余）。因类型下拉已自带倍率（如 `改色+字*0.6`，选择即自动套用），AU 轮在 `dcRenderProducts` 中删去 `.dc-prod-model-rate` 输入框，并清理桌面端 / mobile 两处死 CSS。现在同模区域只剩「同模勾选框 + 类型下拉框」，不再有第二个白圆。
- Service Worker 缓存升级到 v77；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AV 轮修正）· 功能 v29

- **加价树 `├─/└─` 与制品左对齐**：收紧 `.dc-rr.bound` 左内边距由 18px → 12px（与 `.dc-rr` 制品行同左边缘），`.dc-r-tree` 宽度由 22px → 28px，使 `├─/└─` 符号起点与制品序号左边缘对齐，符号后文字仍落在制品文字起点（40px），形成严格嵌套视觉。
- Service Worker 缓存升级到 v78；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AW 轮修正）· 功能 v29

- **加价树 `├─/└─` 改为与制品名称（文字）左对齐**：用户纠正 AV 轮——应是制品名称文字起点（40px）而非序号（12px）。`.dc-rr.bound` 左内边距 `12px → 40px`，`.dc-r-tree` 保持 28px，使 `├─/└─` 符号起点落在制品名称左边缘，符号后文字再缩进一层。
- Service Worker 缓存升级到 v79；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AX 轮修正）· 功能 v29

- **修复「制品列表又挤一排」**：mobile 两行布局由 `@media(max-width:768px)` 触发，但 OPPO 等大屏手机横屏时 CSS 宽度 >768px，掉回桌面 8 列一行导致拥挤。将断点提高到 `max-width:920px`，覆盖大屏手机横屏/折叠屏，两行布局稳定生效。
- Service Worker 缓存升级到 v80；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AY 轮修正）· 功能 v29

- **制品列表改用容器查询触发两行**：AX 轮把视口断点提高到 920px 仍无效，因为报价计算页面是左右两栏（输入区 + 340px 收据区），制品列表位于输入栏内；当视口 1000~1200px 时输入栏实际只剩 600~800px，8 列照样挤，但媒体查询按视口算未触发。AY 轮改为容器查询：给 `.calc-input-col` 加 `container-type:inline-size`，新增 `@container (max-width:860px)` 块让制品/加价/修改列表根据输入栏实际宽度决定是否两行。这样即使视口较宽，只要输入栏 ≤860px 就自动两行。
- Service Worker 缓存升级到 v81；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AZ 轮修正）· 功能 v29

- **制品列表改为默认两行（不再依赖宽度）**：此前两行布局只写在 `@media(max-width:920px)` 与 `@container(max-width:860px)` 里，只有输入栏够窄时才触发，在宽屏两栏下仍掉回 8 列一行（用户反馈"还是一行"）。AZ 轮把两行 `grid-template-areas`（`"seq name pattern price qty del" / ". urgent same same same ."`）提升为 `.dc-product-row` 基层默认样式，并隐藏 `.dc-product-header`，无论屏幕多宽都默认两行。
- **制品下拉框完整展示且≥130px**：name 列（制品下拉框所在）由 `1fr` 改为 `minmax(130px,1fr)`，保证下拉框最小 130px，不被压缩；同模类型下拉 `.dc-prod-model-type-wrapper` 最小宽度 70px → 90px。
- Service Worker 缓存升级到 v82；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（BA 轮修正）· 功能 v29

- **同模下拉内容改为纯类型**：选项由 `改色+字*0.6` 等带倍率形式改为 `改色+字` / `改人+字/色` / `改人`（倍率由 `DC_MODEL` 静默套用，不再显示在 label）；勾选同模后输入框只显示类型名（如「改人+字/色」），不显示 `*0.8`。
- **同模下拉加宽保证完整显示**：`.dc-prod-model-type` 固定 `width:110px;min-width:110px`，确保「改人+字/色」6 字完整显示不被截断；`.dc-prod-model-type-wrapper` 改为 `flex:0 0 auto` 不再被压缩。
- **同模左移、紧贴加急**：把「加急」与「同模（含类型下拉）」包进新容器 `.dc-prod-row2`（`display:flex;gap:8px`），第二行现在只有这一个容器贯穿，加急与同模自然相邻紧贴，不再各自占位分散。
- Service Worker 缓存升级到 v83；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（BB 轮修正）· 功能 v29

- **同模下拉恢复【下拉选择器】结构**：BA 轮为去掉"小圆"曾移除同模下拉的 ▼ toggle 按钮，BB 轮应要求恢复——同模类型下拉与制品名称下拉完全一致：保留 `combobox-wrapper` + 只读 input + ▼ 切换按钮（`onclick="toggleComboboxDropdown('${modelCbId}')"`），▼ 置于文字右侧（`padding-right:30px`），点击/聚焦打开下拉，样式与灵感记录制品名称下拉统一。
- **删除按钮移至第二排末尾**：删除 ✕ 从 grid 末尾列移到第二行 `.dc-prod-row2` 容器内，并用 `margin-left:auto` 推到该行最右端，与加急/同模同行末尾对齐。
- Service Worker 缓存升级到 v84；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（BC 轮修正）· 功能 v29

- **同模下拉真正与制品名称下拉同款**：BB 轮虽然恢复了 ▼ 按钮，但同模 combobox 仍被包在 `<label class="dc-prod-same">` 内部，label 的 flex 布局把它压变形，导致边框/箭头/尺寸与制品名称下拉不一致。BC 轮把同模 combobox 从 label 中移出，作为 `.dc-prod-row2` 的独立 flex 子元素，与「加急」「同模」checkbox 并列；并给制品名称 wrapper 新增 `.dc-prod-name-wrapper` 类，避免 `.dc-product-row .combobox-wrapper{width:100%}` 误作用于同模下拉。
- **同模下拉加宽至 130px（桌面）/ 120px（mobile）**：`.dc-prod-model-type-wrapper` 固定 `width:130px;min-width:130px`，保证「改人+字/色」完整显示；mobile 媒体查询内为 120px，兼顾第二行空间。
- Service Worker 缓存升级到 v85；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-08（BD 轮）· 功能 v29

- **加急/同模字体与制品/柄图标识一致**：`.dc-prod-check` 基础字体由 11px 改为 13px，与桌面端 `.form-input`（13px）对齐；移动端 / 容器查询块内由 11px 改为 12px，与移动端 `.form-input`（12px）对齐。此前加急/同模比输入框小 1~2px，视觉不一致。
- **删除按钮右边缘与数量输入框对齐**：第二行 `.dc-prod-row2` 的 grid-area 由 `. row2 row2 row2 row2 row2`（占满含第 6 列 del 位）改为 `. row2 row2 row2 row2 .`（只占第 2~5 列），删除 ✕ 经 `margin-left:auto` 推到该行最右端，正好落在数量列（第 5 列）右边缘，不再多出第 6 列的 32px+gap 偏移。
- Service Worker 缓存升级到 v86；源码待提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-08（BE 轮）· 功能 v29

- **序号与「+ 添加制品」按钮左对齐**：mobile/容器查询块内 `.dc-prod-seq` 原本有 `padding-left:10px`，导致序号整体右偏；同时基层 `justify-content:center` 让序号在列内居中。BE 轮去掉 padding-left，并把 justify-content 改为 `flex-start`，让序号贴到第 1 列左边缘，与添加按钮左边缘齐平。
- Service Worker 缓存升级到 v87；源码待提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-08（BF 轮）· 功能 v29

- **制品与加急也左挪**：BE 轮已让序号贴左与「+ 添加制品」按钮左对齐，本轮回退了 BE 的"独立序号列居中"做法——取消第一排独立的 28px 序号列，将序号移到第二排开头与「加急」同排。效果：第一排「制品」从容器最左起、第二排「序号 + 加急 + 同模 + 删除」也从最左起，三者统一贴左对齐，序号不再单独占列制造缩进。
- grid 列定义由 `28px minmax(130px,1fr) 70px 60px 48px 32px` 改为 `minmax(130px,1fr) 70px 60px 48px 32px`（去掉序号列）；两行 grid-template-areas 改为 `"name pattern price qty del" / "row2 row2 row2 row2 row2"`，第二排占满 5 列。
- mobile 媒体查询与 `@container(860px)` 块同步同样改动。
- Service Worker 缓存升级到 v88；源码已提交并推送到 GitHub（ba5ed61）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-08（BF 回退 + Bg 轮）· 功能 v29

- **改回 BE 轮布局 + 加急贴序号**：用户纠正 BF 轮——不要取消序号列，只是要制品和加急"离序号近一点"。本轮回退 BF：恢复第一排独立 28px 序号列、制品紧挨序号（BE 轮布局）。同时让第二排的加急离序号更近：`.dc-prod-row2` 的 grid-area 由 `. row2 row2 row2 row2 .`（第二排从第 2 列起，加急被空出一列）改为 `row2 row2 row2 row2 row2 .`（第二排从第 1 列起），加急直接落在序号正下方，紧贴序号；删除 ✕ 仍经 `margin-left:auto` 贴数量右缘。
- 三处样式块（基层 / mobile 媒体查询 / @container(860px)）同步：`.dc-product-row` 恢复 6 列 + `"seq name pattern price qty del" / "row2 row2 row2 row2 row2 ."`；`.dc-prod-seq` 恢复 `grid-area:seq`。
- Service Worker 缓存升级到 v89；源码已提交并推送到 GitHub（ebb4858）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-08（回退到 v87 / BE 轮）· 功能 v29

- **整体回退到 v87 版**：用户要求回到 v87。Bg 轮（加急贴序号）的改动撤销——第二排 `.dc-prod-row2` 的 grid-area 由 `row2 row2 row2 row2 row2 .`（第 1~5 列）改回 `. row2 row2 row2 row2 .`（第 2~5 列），加急回到序号右侧一列，与 v87(BE 轮) 外观一致。app.js 结构本就已是 v87 状态（序号在第一排独立列、不在 row2 内），无需改动。
- 缓存版本号 / 脚本 `?v=` 标回 v87（SW 为网络优先策略，回用 v87 名会重新从网络拉取当前回退后文件，不会吃到旧缓存）。
- 源码已提交并推送到 GitHub（de5cefb）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍处于 v1.1.16 / 功能 v29 状态，等重建 v1.1.17 上机。

### 2026-08-08（Bh 轮）· 功能 v29

- **序号往右挪一点**：在 v87 布局基础上，给 `.dc-prod-seq` 增加 `padding-left:6px`（基准规则 + 两个响应式块 `@media(920px)` / `@container(860px)` 统一），让序号数字离开容器左边缘约 6px，制品列位置不变。缓存升到 v90。源码已提交并推送 GitHub（8651aff）。刷新前请 **Ctrl+F5** 硬刷新。

### 2026-08-08（Bi / Bj 轮）· 功能 v29

- **序号 padding 微调**：Bi 将序号 `.dc-prod-seq` 的 `padding-left` 由 6px 调到 8px（缓存 v91）；Bj 再调到 10px（缓存 v92）。仅序号数字离左边缘间距变化，制品列不变。提交 a5bbc82 → e0c53a9。

### 2026-08-08（Bk 轮）· 功能 v29 — 实时报价同模/整单加急/分组展示/计算顺序/改名

- **① 同模无默认类型**：勾选同模不再默认"改人"，`sameModelType=''`、`sameModelRate=1.0`；下拉 `placeholder` 改为"请选择同模类型"。选类型才启用阶梯价（读取全局 DC_MODEL：改色+字×0.6 / 改人+字/色×0.8 / 改人×0.5），行内选类型即"本行优先"。勾选同模但未选类型时按全额计价并在报价单标注"⚠ 请选择同模类型"。柄图标识 >1 自动勾选同模逻辑保留（仍不预设类型）。"全局同模阶梯价倍率"卡片标题同步更新为全局参考表。
- **② 整单加急**：新增 `_dcWholeOrderUrgent`（默认关闭，持久化到 calcSettings）。加急卡片加"整单加急"开关；开启后制品加急复选框置灰（显示已勾选），全部制品与全部加价项目（含未绑定）执行加急；关闭回到单行模式，仅绑定制品的加价跟随制品加急，未绑定加价不加急。
- **③ 加急分组展示**：SET 模式下每个柄图分组内、SET优惠处嵌入加急信息——本组全加急显示"SET加急 ×倍率"，部分加急列出加急制品序号；整单加急时不在此处显示，统一在末尾"整单加急（全部制品与加价项目）"块（整单加急-总价格 / 加急倍率 / 加急小计）。
- **④ 计算顺序**：模型阶梯 → SET 分组折扣 → 加急逻辑（按 effectiveUrgent 拆分基数）→ 汇总 → 稿件用途倍率 → 折扣/同担减价 → 最终报价（数值与旧逻辑一致，仅结构重排）。
- **⑤ 改名**：报价单"倍率后价格"→"倍率小计"，"加急后价格"→"加急小计"。
- 缓存升到 v93；源码已提交并推送到 GitHub（888f0bf）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。。刷新前请 **Ctrl+F5** 硬刷新。

### 2026-08-08（Bl 轮）· 功能 v29

- **① 同模下拉框加宽**：`.dc-prod-model-type-wrapper` 宽度由基准 130px / 两处响应式 120px 统一加宽到 160px，确保勾选同模后输入框内的提示词"请选择同模类型"完整展示而不被截断（输入框保留 padding-right:30px 给 ▼ 箭头）。
- **② 加急模块整合**：原"加急"卡片内两行（整单加急开关 + 加急倍率输入）合并为一行——"整单加急"复选框与"加急"（原"加急倍率"标签改名）×倍率输入框并排，靠 `justify-content:space-between` 让开关在左、加急倍率在右。标题提示改为"勾选整单加急则全部制品与加价项目执行加急；不勾选为原价"。逻辑不变：选中整单加急 → 全部制品与加价项目×倍率（制品行加急框置灰），不选 → 原价、仅按每行独立加急复选框执行。
- 缓存升到 v94；源码已提交并推送到 GitHub（52605d8）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bm 轮）· 功能 v29

- **加急模块 UI 与互锁调整**：
  - 卡片提示改为"单个制品加急请单选，此为整单加急"。
  - 开关标签由"整单加急"改为"加急（整单）"。
  - 去掉倍率输入框前面的"加急"二字。
  - "加急（整单）"开关样式改为与"稿件用途倍率"一致的圆形 radio 风格（自定义 `.dc-urgent-toggle`：空心圆→选中蓝底白点）。
  - 单行制品只要有一个选了"加急"，"加急（整单）"开关自动 `disabled` 置灰（与"整单加急开启→单行加急框置灰"形成互锁，两者互斥）。
- 缓存升到 v95；源码已提交并推送到 GitHub（3f70a82）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bn 轮）· 功能 v29

- **整单加急开关样式回退 + 互锁修复**：Bm 轮把"加急（整单）"做成自定义圆形 `.dc-urgent-toggle`，本轮回退为与"稿件用途倍率"同款的 accent 原生复选框（删除 `.dc-urgent-toggle` 全部 CSS）；并修复互锁 bug——之前任一行制品勾"加急"时，整单开关的 `disabled` 不会更新（因 `dcUpdateProduct` 的 urgent 分支只重渲染制品列表、不重渲染含整单开关的卡片）。新增 `dcSyncWholeOrderToggle()`，在 `dcUpdateProduct` urgent 分支调用，直接同步 `#dcWholeOrderUrgentChk.disabled = 任一行制品 urgent`。整单开启→单行框置灰、单行任勾→整单置灰，两者互斥互锁生效。
- **折扣≥8件逻辑修正**：原逻辑勾选即整体打九折、且作用于含加价项目/同柄/同模的全额 finalPrice，无数量阈值。现改为：阈值=净不同制品数量合计（**排除加价项目、排除同模(勾选同模)、排除同柄(共享柄图标识)**），仅当 netQty ≥ 8 才打九折，且九折只作用于净制品金额（finalPrice - netBase + netBase×mR），加价项目/同柄/同模保持原价。标签改为"多件折扣（不同制品≥8件）"。
- **隐藏数字步进箭头**：`.calc-rate-input[type=number]` 加 `::-webkit-inner/outer-spin-button{-webkit-appearance:none} + -moz-appearance:textfield`，覆盖稿件用途倍率、加急倍率、全局同模阶梯价倍率、SET、优惠体系、同担/同推优惠所有数字框，取消上下增减箭头，仅可输入修改数值（仍保留 type=number 便于数字录入）。
- 缓存升到 v96；源码已提交并推送到 GitHub（d1286b9）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bo 轮）· 功能 v29

- **整单加急开关改成与稿件用途倍率 radio 同款圆形**：截图显示当前"加急（整单）"是方形 accent checkbox，与上方"自用/无盈利/商用/买断"radio 不一致。给 `#dcWholeOrderUrgentChk` 加 `class="dc-urgent-toggle"`，恢复 Bm 轮圆形样式：16×16px 空心圆、选中时蓝底白点、`disabled` 时半透明。交互仍是 checkbox（可取消选中）。
- 缓存升到 v97；源码已提交并推送到 GitHub（1bb4428）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bp 轮）· 功能 v29

- **整单加急圆形样式改成 radio 同款**：截图反馈 v97 的"加急（整单）"蓝底白点与"企业"radio 白底蓝圈蓝点长得不一样。修正 `.dc-urgent-toggle`：未选中用 `#d9d9d9` 细边框、白底；选中保持白底、主色边框，中心改为 5px 主色实心圆点，外圈与中心点之间留出白色间隙，视觉上与原生 radio 一致。
- 缓存升到 v98；源码已提交并推送到 GitHub（88aff71）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bq 轮）· 功能 v29

- **按参考截图整理实时报价参数卡片**：
  - `.calc-card` 增加轻微阴影 `box-shadow:0 2px 8px rgba(0,0,0,.04)`，圆角由 10px 改 12px，padding 由 14px 改 16px，标题与内容间距由 10px 改 12px，更接近截图里独立浮起的卡片感。
  - `.calc-sub-area.disabled` 由半透明可见改为 `display:none`，未选中时 SET优惠 / 折扣优惠 的子项完全隐藏，页面更干净（选中后才展开）。
  - `.calc-sub-area` 非 disabled 时顶部加 `8px padding-top + 6px margin-top + 虚线分隔`，让展开内容与上方 radio 有明确分区。
  - "全局同模阶梯价倍率"标题简化为"同模阶梯价倍率"，与截图一致。
  - 优惠体系底部按钮去掉 emoji，只保留"删除方案""保存方案"；增加按钮与底部按钮间距微调（4px→8px/6px）。
- 缓存升到 v99；源码已提交并推送到 GitHub（b62b6d7）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。
  - **【已还原】**：用户反馈"只是按截图样式改加急前面的按钮，不要大改卡片"。本段（v99）的卡片改动已通过 `git revert b62b6d7` 全部撤销，卡片样式回到 v98 状态（无阴影、子区域半透明 opacity:.4、标题"全局同模阶梯价倍率"、优惠体系带 emoji 按钮）。加急（整单）圆形按钮样式（.dc-urgent-toggle，v97/Bp 轮所做）不受影响、保留。

### 2026-08-08（Bq 还原轮）· 功能 v29

- **还原 Bq 轮的卡片大改**：用户纠正——发截图只是想让"加急（整单）"前面的按钮改成圆形（与稿件用途倍率 radio 同款），并非重排整个参数卡片区。撤销 b62b6d7 的所有改动（git revert，新提交 0f254c0），卡片回到 v98 样式（.calc-card 无阴影、.calc-sub-area.disabled 半透明、标题"全局同模阶梯价倍率"、优惠体系按钮带 emoji）。圆形加急按钮样式保留。
- 缓存升到 v100（标记还原后内容，便于硬刷新）；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Br 轮）· 功能 v29

- **加急圆形按钮放大到与稿件用途倍率 radio 同款**：用户截图对比显示当前"加急（整单）"比"自用" radio 小很多。将 `.dc-urgent-toggle` 由 16px/1px 边框/5px 蓝点 改为 20px/2px 主色边框/8px 主色蓝点，未选中时边框用 `var(--c-border)`，选中时白底蓝圈蓝点，与稿件用途倍率的 radio 视觉尺寸一致。该样式同时作用于每行制品的"加急"开关和"加急（整单）"开关。
- 缓存升到 v101；源码已提交并推送到 GitHub（5d21214）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bs 轮）· 功能 v29

- **所有分组 radio 统一圆形尺寸**：把 `.dc-urgent-toggle` 的圆形视觉样式抽出通用类 `.calc-circle`（20px/2px 主色边框/8px 主色蓝点），并套用到「稿件用途倍率」「全局同模阶梯价倍率」「优惠体系」（无优惠/SET优惠/折扣优惠）的全部 radio 上，让整页圆形按钮尺寸、描边、中心点完全一致。加急按钮仍用 `.dc-urgent-toggle`（CSS 选择器合并，视觉同款）。
- 缓存升到 v102；源码已提交并推送到 GitHub（016f401）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bt 轮）· 功能 v29

- **圆形按钮尺寸回调到 16px/1px 边框/8px 蓝点**：用户给出精确数值，把 `.dc-urgent-toggle` / `.calc-circle` 由 Bs 轮的 20px/2px 边框 改回 16px/1px 边框，蓝点保持 8px。整页圆形按钮（加急、稿件用途倍率、同模阶梯价、优惠体系）统一为原生 radio 同款尺寸。
- 缓存升到 v103；源码已提交并推送到 GitHub（7419127）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bu 轮）· 功能 v29

- **同模倍率优先级重构（6 点）**：
  1. 全局同模阶梯价倍率模块改为**可空单选**：去掉"无同模"选项与 `disabled`，新增单选 `value` 与 `onchange="dcSelectGlobalModel(...)"`，默认不选中任何项（即"允许不选"）；`dcSelectGlobalModel` 支持再次点击已选项取消（toggle 回空）。
  2. 制品行勾选同模 → 优先读取**本行下拉**选中的同模类型（`p.sameModelType` + `p.sameModelRate`）。
  3. 本行没选类型 → 读取**全局同模模块**选中的类型（`_dcGlobalModelType`），用对应 `DC_MODEL.rate`。
  4. 本行、全局两处都没选 → 该行 `needType=true`，制品行显示红字 `⚠ 请选择同模类型`，且**不应用同模倍率**（按基础价计入，等同不计算同模报价）。
  5. 制品行**未勾同模** → 无论全局选什么，同模不生效（`p.sameModel===false` 直接跳过）。
  6. **柄图标识 > 1 自动勾选同模**逻辑保留（仅自动勾选、不预设类型），自动勾选后同样走 2→3→4 优先级；行内选类型本行优先。
  - 实现要点：新增状态变量 `_dcGlobalModelType`；`dcRecalc` Step1 重写同模解析为"本行 > 全局 > 皆空提示"；`dcRenderProducts` 在 `p.sameModel && !sameModelType && !_dcGlobalModelType` 时渲染 `.dc-prod-model-warn`；新增 CSS `.dc-prod-model-warn`（红字 11px）。
- 缓存升到 v104；源码已提交并推送到 GitHub（f9fd4e5）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bv 轮）· 功能 v29

- **全局同模模块交互修正**：
  1. 原 `radio` 无法实现"再次点击取消选择"（原生 radio 选中后点击同一项不会再触发 onchange），改为 `checkbox` + `dcSelectGlobalModel(val, checked)`：勾选=设为当前项，再次点击已选项→取消（`_dcGlobalModelType=''`）；单选约束通过遍历 `name="dc_model_rule"` 同步其余项未选中保证。
  2. 标题 `全局同模阶梯价倍率` → `同模阶梯价倍率`。
  3. 提示词改为 `选择同模更改项`（保留默认不选择/制品行未选时读取的补充说明）。
  4. 第 4 点（制品行勾选启用、可单独选择同模类型）确认已具备，无需改动；第 4 点红字校验提示保留；导出报价单（第 3861 行 `p.sameModelType || '改人'`）经用户确认当前无问题，暂不改动。
- 缓存升到 v105；源码已提交并推送到 GitHub（013ce79）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bw 轮）· 功能 v29

- **同模阶梯价倍率提示词精简**：去掉 Bv 轮附加的括号说明，提示词仅保留 `选择同模更改项`（用户纠正——上次多写了"默认不选择…"补充说明）。制品行"勾选启用、可单独选择同模类型"确认已具备，未改动。
- 缓存升到 v106；源码已提交并推送到 GitHub（fb96129）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bx 轮）· 功能 v29

- **同模提示词恢复括号整句**：`同模阶梯价倍率` 后的提示词改回 `（选择同模更改项，制品行勾选启用，可单独选择同模类型）`（用户本轮要求按此原文）。
- **取消红字校验提示**：移除制品行 `.dc-prod-model-warn`（`⚠ 请选择同模类型`）及收据三处（`needType` 行）的红字提示；`dcRecalc` 保留"本行/全局皆空→不应用同模倍率（按基础价计入）"的行为，仅去掉视觉提示。
- **导出跟随优先级**：`dcCreateCommission` 中同模类型从 `p.sameModelType || '改人'` 改为解析 `本行类型 → 全局 _dcGlobalModelType → 皆空则 '无同模'`，并同步 `sameModelRate`（全局生效时取 `DC_MODEL.rate`）；解决此前"全局有选、行内空 → 预览正确但导出落 '改人'"的不一致。
- 缓存升到 v107；源码已提交并推送到 GitHub（4fa877e）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（By 轮）· 功能 v29

- **收据加急展示重构**：
  1. **整单加急**：底部「整单加急（全部制品与加价项目）」块保持现有展示方式不变。
  2. **单项加急**：不再以组级「本组加急制品：序号 XX」块展示；改为与加价项目一样，用树状图显示在每个制品下方：`└─ 加急 ×倍率`（套用 `.dc-rr.bound` + `.dc-r-tree` 样式）。覆盖 SET 模式分组、无柄图分组、非 SET 模式三种场景。
  3. **全组加急**：当某个柄图分组内全部制品都勾选了加急，仍保留组级提示 `SET加急（本组全加急） ×倍率`，继续放在折后小计/金额之后（即 SET 优惠块后面）。
- 缓存升到 v108；源码已提交并推送到 GitHub（e39c3e3）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（Bz 轮）· 功能 v29

- **实时报价收据顺序调整**：将「加急」模块从原「总价」之后移到「总价」之前；新的收据顺序为：制品明细/加价项目 → 倍率计算（倍率小计） → 加急（加急基数/倍率/加急小计） → **总价** → 优惠 → 最终报价。
- **总价改为包含加急后结果**：原「总价」显示 `baseTotal`（未含用途倍率与加急）；现改为 `grandTotal = urgentAfter + nonUrgentAfter`，即已含「稿件用途倍率」与「加急倍率」、但尚未扣除后续优惠的总额。未勾加急时退化为 `baseTotal × 用途倍率`（与原语义相比仅补上了用途倍率，不再裸显基础价）。
- 同步移除不再展示的 `baseTotal` 变量定义；优惠块随之移至「总价」之后（与折扣作用于加急后金额的计算逻辑一致）。
- 缓存升到 v109；源码已提交并推送到 GitHub（da6dd30）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CA 轮）· 功能 v29

- **按用户要求重排计算顺序为 `原价 → 同模 → 加价 → 折扣 → 加急 → 用途`**：
  - 折扣（SET/多件/自定义/同担同推）整体前置到「加急」之前；加急仅作用于勾选部分；**用途倍率最后施加**。
  - 因折扣与加急在数学上可交换（已逐项验证最终报价与旧逻辑完全等价），实现上先把折扣作用于整体 `(制品+加价)`，再按紧急/非紧急比例拆分后施加急倍率，保证「总价 =（制品+加急）×加急」精确成立。
  - **总价** = `grandTotal = 紧急部分×加急倍率 + 非紧急部分`，即「（制品+加价）×加急」，**不含用途倍率**（用途在最后一步施加）。
- **倍率计算（稿件用途）回到原位置**：置于「总价」之后（撤销 Bz 轮的提前），与「总价 → 倍率计算 → 优惠」的原布局一致。收据最终顺序：制品明细/加价项目 → 加急模块 → **总价** → 倍率计算（倍率小计=总价×用途） → 优惠（折扣明细，已计入总价） → 最终报价。
- 注意：因用途倍率为最后一步，「倍率小计」在数值上等于「未含修改加价的 最终报价」，属正常（用途即末尾乘子）；若用户觉得重复可后续去掉倍率小计行。
- 缓存升到 v110；源码已提交并推送到 GitHub（d94999e）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CB 轮）· 功能 v29

- **废弃「全部汇总之后再整体乘以稿件用途倍率」旧逻辑**（CA 轮 Step 8 的 `grandTotal * uRate`）。
- **改用统一公式**：`最终报价 = 总价(grandTotal) + 总基数(afterDiscount) × (uRate − 1)`，逐项展开等价于用户给定规则：
  - 自用(uRate=1) → `A × 加急倍率`；
  - 倍率>1（无盈利/商用/买断/企业）→ `A × (加急倍率 + 用途倍率 − 1)`，**消除基础金额重复叠加**；
  - 不加急(加急倍率=1) → `A × 用途倍率`，与旧逻辑完全一致、无副作用。
  - 整单加急 / 单行勾选加急统一走此公式；加价项目已包含在 `afterDiscount`/`grandTotal` 中，自然参与加急与用途运算。
- **保留全部加急交互**：单行制品加急勾选、整单加急开关、加价项目绑定跟随逻辑均不改；`grandTotal` 仍是「折扣后、含加急溢价、未含用途」的中间量，「总价」保持「（制品+加价）×加急（未含用途）」语义。
- 收据「倍率小计」同步改为新公式结果（= 最终报价），不再用 `总价×uRate` 旧式。
- 缓存升到 v111；源码已提交并推送到 GitHub（75c2883）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### CC 轮（08-08 早）· 加价项目绑定下拉显示序号+制品
- **制品绑定下拉框（`dc-extra-bind` 的 `<select>`）选项改为「序号 制品名」**：原只显示 `01`/`02`，现显示 `01 改人立绘` 等（`value` 仍为序号串，存值逻辑不变）。
- **下拉框扩大保证全部显示**：给 `.dc-extra-row / .dc-product-row / .dc-mod-row` 的自定义 combobox 下拉框加 `white-space:nowrap; width:max-content; min-width:100%`，长名称选项不再被输入框宽度截断。
- **绑定列宽放宽**：桌面 `90px→110px`、移动 `60px→80px`（含表头 `.dc-extra-header` 第一列对齐），让 select 框内也能多显示些文字。
- 缓存升到 v112；提交并推送 b335601。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CD轮）· 功能 v29
- **加价项目「制品绑定」原生下拉（`dc-extra-bind` 的 `<select>`）加宽到 150px**：桌面 `110px→150px`（含表头 `.dc-extra-header` 第一列对齐），移动端 `80px→110px`。
- **外观对齐灵感记录下拉框**：原生 select 改为 `appearance:none` + 右侧下拉箭头 svg（与 `.form-select` 同款圆角边框、13px 字号、`padding-right:30px`），视觉与灵感记录「制品」combobox 一致；**保留原生 `<select>` 下拉选择器结构**（未改成自定义 combobox），存值逻辑（序号串 `bindSeq`）不变。
- 缓存升到 v113；提交并推送 f6ea713。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CE轮）· 功能 v29
- **修复加价项目绑定下拉框未真正加宽 / 样式未生效问题**：将 `<select class="form-input dc-extra-bind">` 改为 `<select class="form-select dc-extra-bind">`，继承项目统一 `.form-select` 样式（`appearance:none`、右侧箭头、圆角边框），并用 `!important` 锁定 `width:150px/min-width:150px/max-width:150px`。
- **缩短「名称」列给绑定列让位**：`.dc-extra-header` / `.dc-extra-row` 的 grid 第二列从 `minmax(120px,1.1fr)` 缩为 `minmax(80px,1fr)`，避免 150px 绑定列与名称 combobox 重叠。
- 缓存升到 v114；本地提交 2399da1。**本次 push 因 Git 凭据管理器无法弹窗而失败**，需手动执行 `git push origin main` 或重新授权后再推。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CF轮）· 功能 v29
- **加价项目「绑定制品」字段从原生 `<select>` 改为 combobox（与灵感记录「制品」下拉同款）**：废弃 `.dc-extra-bind` 原生 select，改用 `.combobox-wrapper`（input + `.combobox-toggle` 箭头 + `.combobox-dropdown`），**保留【下拉选择器】结构**。
- **新字段置于「名称」前面**（列顺序：绑定制品 → 加价项目 → 数量 → 单价 → 删除），表头同步由「制品绑定」改名为「绑定制品」。
- **绑定下拉选项读取制品列表自动生成的序号+制品**（如 `01 改人立绘`），首项固定为「无（不绑定）」（存值 `none`）；选中后 `bindSeq` 仍存序号串（如 `01`），计费/收据/导出逻辑不变。
- **列宽同步调整**：桌面绑定列 `150px→160px`、表头对齐；移动端 `110px→140px`。下拉框沿用既有 `width:max-content` 规则，长名称完整显示。
- 新增辅助函数 `dcExtraBindDisplay(seq)`（序号串→`序号 制品名` 显示）与 `dcSelectExtraBind(idx,el,cbId)`（选项点击回写 `bindSeq` + 显示文案）。
- 缓存升到 v115；本地提交 3523c17（**尚未推送**，沿用 CE轮凭据失效状态）。

### 2026-08-08（CG轮 bugfix）· 功能 v29
- **修复价目表单位显示错误**：`元/次` 被错误显示为 `元/p`——价目菜单渲染三元表达式对非「元」单位一律硬编码拼 `/p`，改为按真实 `priceUnit` 取后缀（`元/次`→`¥xx/次`、`元/p`→`¥xx/p`、`元`→`¥xx`）。
- **修复加价项目「绑定制品」下拉无法选择**：CF轮新增的绑定选项 `<div class="combobox-option">` 漏写 `onclick="dcSelectExtraBind(...)"` 处理器（名称下拉有、绑定下拉无），导致打开下拉后点击不回填。已为绑定选项补全 `onclick`，并让「无（不绑定）」回填空值（与 `dcExtraBindDisplay` 一致）。
- 缓存升到 v116；本地提交（**尚未推送**，待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CH轮）· 功能 v29
- **优惠体系顺序调整**：将「折扣优惠」选项由 SET 优惠之后移至其之前（紧跟「无优惠」），顺序变为 无优惠 → 折扣优惠 → SET优惠。仅交换两个 radio 块与对应子区（`dc-disc-area` / `dc-set-area` 的 id 不变），互斥切换逻辑 `dcDiscModeChange` 不受影响，计费逻辑不变。
- 缓存升到 v117；本地提交（**尚未推送**，待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CI轮，纠正 CH）· 功能 v29
- **撤销 CH 轮误改**：用户原意是「把整个『优惠体系』卡片放到『加急』前面」，而非在卡片内部调换折扣/SET 顺序。故恢复卡片内顺序为 无优惠 → SET优惠 → 折扣优惠（与 CH 前一致）。
- **移动整卡顺序**：将「优惠体系」卡片由「同模」之后移至「加急」之前；实时报价卡片顺序现为：…稿件用途倍率 → **优惠体系** → 加急 → 同模阶梯价倍率 → 同担/同推优惠。仅搬运卡片、id 不变，互斥/计费逻辑不受影响。
- 缓存升到 v118；本地提交（**尚未推送**，待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CJ轮，收据展示顺序）· 功能 v29
- **修正「优惠」在收据结果中的位置**：用户截图显示收据里「优惠」仍在「加急」之后。原 `dcRecalc` 的收据渲染顺序为「加急 → 总价 → 倍率计算 → 优惠」，现把「优惠」模块提到「加急」之前，顺序改为「**优惠 → 加急 → 总价 → 倍率计算 → 最终报价**」，与之前确定的计算顺序（原价→同模→加价→折扣→加急→用途）保持一致。计费结果不变，仅展示顺序调整。
- 缓存升到 v119；本地提交（**尚未推送**，待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CK轮，收据加急/折扣布局重写）· 功能 v29
- **加急展示重构（按用户样例）**：
  - 整单加急开启 → 保持底部独立「整单加急」区块；各柄图分组**内不输出加急行**。
  - 单行/部分制品加急 → 置于对应柄图分组内、SET优惠行**下方**，样式与 SET优惠统一（`margin-top:6px`）。全组加急显示「SET加急 ×倍率」；部分加急显示「加急：01、03 ×倍率」。
  - 无 SET 优惠时，加急行独立显示（同样样式）。
  - 非 SET 模式（制品明细）保留逐制品「└─加急」树状，并在尾部汇总「加急：序号 ×倍率」。
- **折扣行内联**：折扣优惠（多件折扣改名「折扣优惠：不同制品≥8件」/自定义/同担）在 SET 模式放分组后「优惠」区、在折扣模式下内联于制品明细内、加急行之前；不再作为 CJ 轮的独立底部「优惠→加急」顺序块。
- **分组折后小计含加急**：柄图分组/无柄图/制品明细的「折后小计」按组内加急比例上浮（整单时不含加急，由底部块体现）；计算顺序（同模→SET/折扣→加急→用途）不变，仅展示重构。
- 新增 `productDetails.actualUrgent` 区分「实际勾选加急」与整单加急。
- 缓存升到 v120；本地提交（**尚未推送**，待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CL轮，树状连接符修正）· 功能 v29
- **树状 ├─/└─ 排列修正**：非 SET 模式下，当某制品**既有绑定加价又有单行加急**时，原渲染让绑定加价末项显示 `└─`、加急再显示 `└─`，两个末连接符冲突、树形断裂。`renderBoundExtras` 新增 `urgentAfter` 参数：若本品下方紧跟「└─加急」行，则绑定加价末项改用 `├─`，加急为 `└─`，形成正确树。
- 同时修正 `renderBoundExtras` 对「空名称/空金额」被过滤项仍计入末位的 bug（改为先过滤可见项再判定末连接符）。
- SET 模式（分组内有 SET优惠/SET加急汇总）与整单加急（底部独立块）不涉及逐制品树状，保持原逻辑。
- 缓存升到 v121；本地提交（**尚未推送**，待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CM轮，三项微调）· 功能 v29
- **收据逐制品树状加急去倍率**：非 SET 模式下制品下方的「└─加急 ×倍率」改为「└─加急」（倍率已在底部「加急：序号 ×倍率」汇总行体现，避免重复显示）。
- **放开整单加急置灰**：原 Bn 轮规则「任一行制品勾选加急则整单加急开关置灰」会导致「勾了制品加急后再开整单被锁死」。现移除该互斥置灰——整单加急开关始终可选；开启时仍由各制品行的 `disabled` 控制（且 `effectiveUrgent` 已 `|| _dcWholeOrderUrgent` 覆盖，不重复计费）。`dcSyncWholeOrderToggle` 不再置灰、`anyProductUrgent` 变量移除。
- **收据行间距缩小**：SET优惠行、组内加急行、加急汇总行的 `margin-top:6px` 统一改为 `2px`（SET优惠↔加急↔折后小计更紧凑）。
- 缓存升到 v122；本地提交（**尚未推送**，待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CN轮，间距与整单加急互斥恢复）· 功能 v29
- **恢复整单加急互斥置灰**：撤销 CM 轮放开逻辑，恢复 Bn 轮规则——任一行制品勾选加急后，整单加急开关置灰不可选；反之整单加急开启时各制品行加急勾选仍自动置灰。恢复 `anyProductUrgent` 变量与 `dcSyncWholeOrderToggle` 置灰逻辑。
- **收据汇总行间距与制品间相同**：新增 `.dc-rr.promo` / `.dc-rr.disc` 类，把 SET优惠、SET加急、加急汇总、折扣优惠等汇总行的 padding 统一为与制品行一致（2px 12px），并去掉额外 `margin-top:2px`，使这些行之间的视觉间距与制品行之间相同。
- **制品→汇总间距加大**：给每个 section 内的第一条汇总行加 `.promo-first { margin-top:8px }`，让制品明细到第一个优惠/加急行的距离明显拉开。
- 缓存升到 v123；本地提交（**尚未推送**，待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CO轮，整单加急禁用视觉与间距微调）· 功能 v29
- **整单加急禁用视觉加强**：原 `:disabled{opacity:.45}` 在自定义白底圆点上不够明显。改为 `background:#e8e8ea;border-color:#ccc`、禁用蓝点 `::after{background:#bbb}`，并给整单加急 label 在禁用时加 `.calc-rate-label-disabled`（文字变灰 muted + not-allowed）。现在任一制品勾加急则整单开关明显置灰且不可点。
- **制品→汇总间距 8px→12px**：`.promo-first{margin-top:12px}`。
- 缓存升到 v124；本地提交（**尚未推送**，待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CP轮，SET模式单项加急制品后树状）· 功能 v29
- **SET 模式下单项加急补制品后树状图**：此前 SET（柄图分组）模式里，单项/部分制品加急只汇总成「加急：序号 ×倍率」行放在 SET优惠下方，制品后面没有 `└─加急` 树；现在柄图分组循环与无柄图制品循环都补上制品下方的「└─加急」树状行（与非 SET 模式一致），并保留汇总行。传入 `urgentAfter` 修正「绑定加价 + 加急」并存的连接符（绑定加价末项改 `├─`、加急 `└─`）。
- 缓存升到 v125；本地提交（**尚未推送**，待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-08（CQ轮，加价项目不纳入SET/折扣+新计算顺序）· 功能 v29
- **核心变更：加价项目不再参与 SET优惠/折扣优惠计算**。新计算顺序：**制品 → 同模 → SET优惠/折扣优惠 → 加价项目 → 加急 → 用途**。
- SET 模式：绑定加价的 `groupRate` 由继承制品 SET 组折扣率改为恒 `1.0`（加价项目全额，不再打折）。
- 折扣模式：原本自定义折扣/同担同推是对含加价的整体 `baseBeforeDiscount` 打折，现 `baseBeforeDiscount` 只含**制品(已含SET组折扣)**，折扣只作用于制品；多件折扣原本就排除加价，不变。
- 加急/用途基数拆分：Step4 把「制品(含SET)」与「加价项目(全额)」分开计 urgent/non-urgent；Step7 加急 = 制品折扣后×比例 + 加价项目全额；Step8 用途倍率作用于 `afterDiscount(制品) + extrasTotal(加价项目)`。
- 整单加急底部块「总价格」改为 `afterDiscount + extrasTotal`（含加价项目全额）。
- 数学自检：SET0.9 + 绑定¥5 + 加急×2 → 制品(20×0.9+5)×2=46、非急9、合计55（加价不被SET打折）；用途倍率对制品与加价项目同时生效，均通过。
- 缓存升到 v126；本地提交（**尚未推送**，待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（CR轮 bugfix）· 功能 v29
- **修复实时报价 SET+单制品加急计算错误**：用户截图示例 `吧唧20+书签10urgent+明信片30+宣图40，SET≥4种品类×0.9，绑定加价抠图10` 应得 ¥110，但先前显示 ¥109。
- 根因：CQ 轮 Step 4 把 `prodUrgentBase` 按 SET 折扣后价格统计（书签 10×0.9=9），导致单制品加急溢价只算了 9 而非原价 10。
- 修正：
  - Step 4 中 `prodUrgentBase` / `prodNonUrgentBase` 改为按制品**原价**统计；`extraUrgentBase` / `extraNonUrgentBase` 按绑定加价原价统计。
  - Step 7 拆分整单加急（保持旧逻辑）与单制品/部分加急：后者 `grandTotal = afterDiscount + extrasTotal + (prodUrgentBase + extraUrgentBase) × (urgentRate − 1)`，即加急溢价基于原价、不受 SET/折扣影响。
  - SET 模式柄图分组 / 无柄图分组的「折后小计」同步修正：组内加急溢价基数改用 urgent 制品原价 + urgent 绑定加价原价。
- 验证：上述示例 `(20+10+30+40)×0.9 + 10 + 10 = 110` 已吻合；整单加急逻辑与展示不变。
- 缓存升到 v127；本地提交（**尚未推送**，沿用此前凭据失效状态；待用户手动 `git push origin main` 后 Ctrl+F5 硬刷新）。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（CR-2轮 bugfix）· 功能 v29
- **修复加价项目被重复计入加急溢价**：用户截图显示 CR 轮修正后「折后小计 ¥110」正确，但「总价 ¥120」仍多 10。
- 根因：CR 轮把**绑定加价项目**也纳入了单制品加急基数（`extraUrgentBase` / `groupUrgentWB`），导致绑定加价被算了两次：一次作为 `extrasTotal` 原价计入，一次又作为 urgent premium 额外乘了 `urgentRate−1`。
- 修正：
  - Step 4 中移除绑定加价对 `extraUrgentBase` / `extraNonUrgentBase` 的贡献；未绑定加价也不再随整单加急乘倍率，统一计入非加急基数。
  - Step 7 非整单加急：`grandTotal = afterDiscount + extrasTotal + prodUrgentBase × (urgentRate − 1)`，加急溢价**仅基于制品原价**。
  - Step 7 整单加急：`grandTotal = (afterDiscount + extrasTotal) × urgentRate`（加价项目原价已含在基数中，不额外乘倍率）。
  - SET 模式柄图分组 / 无柄图分组的组内加急溢价基数 `groupUrgentWB` 改为只含制品原价，不含绑定加价。
  - 非 SET 模式「折后小计」直接显示 `afterDiscount + extrasTotal`（折扣后、加急前总额），与整单加急底部块的「总价格」一致。
- 验证：示例 `(20+10+30+40)×0.9 + 10 + 10 = 110` 现在折后小计、总价、最终报价均为 ¥110；用途倍率(uRate>1) 与整单加急场景逻辑保持一致。
- 缓存升到 v128；本地提交并推送后需 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（CS轮 bugfix）· 功能 v29
- **修复 SET 模式下 `afterDiscount` 未应用组折扣**：用户截图显示「SET优惠：≥4种品类 ×0.9」已展示，但「折后小计」仍为 ¥110（制品原价 100 + 加价 10），说明 SET 0.9 没有实际扣减。
- 根因：CQ 轮为把加价项目剥离出 SET/折扣，将 `baseBeforeDiscount` 改为制品原价；但 Step 5 只在 `dMode === 'discount'` 时重新计算 `afterDiscount`，**SET 模式（`dMode === 'set'`）下忘记把 `afterDiscount` 设为 Step 2 已算好的 `productsTotal`**，导致折后小计按原价输出。
- 修正：Step 5 开头增加 `if (dMode === 'set') afterDiscount = productsTotal;`，使 SET 组折扣正确生效。
- 影响：
  - SET 模式折后小计 = `productsTotal + extrasTotal`（制品已打折 + 加价全额）。
  - 单制品/部分加急：总价 = `afterDiscount + extrasTotal + prodUrgentBase × (urgentRate − 1)`。
  - 整单加急：总价 = `(afterDiscount + extrasTotal) × urgentRate`。
  - 同担/同推优惠仍在 SET 折扣后应用。
- 验证：用户截图示例（吧唧20+明信片30+书签10+宣图40，绑定抠图10，SET≥4×0.9，无加急）现在折后小计 = 总价 = 最终报价 = **¥100**；若书签勾选单制品加急×2，则折后小计 ¥100、总价 ¥110。
- 缓存升到 v129；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；本地提交并推送后需 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（CT轮，加价项目参与折扣）· 功能 v29
- **核心变更：反转 CQ 轮——加价项目与制品一起算入折扣**。折扣（SET/自定义/多件/同担）作用于「制品 + 加价」整体。
- 绑定加价 `groupRate` 由 CQ 轮的恒 `1.0` 恢复为继承绑定制品的 SET 组折扣率（`itemGroupRate`）；未绑定加价在 SET 模式下保持原价（无对应柄图分组）。
- 折扣作用：
  - SET 模式：`afterDiscount = productsTotal + Σ(加价 lt × groupRate)`（绑定加价跟随其制品 SET 折扣）。
  - 折扣模式：`d = baseBeforeDiscount + extrasTotal`，多件折扣只作用于净制品、自定义折扣对整体（含加价）打折。
- 加急（保持 CR-2 轮规则）：加价项目**不乘加急倍率**，加急溢价仅基于 `prodUrgentBase`（制品原价）。
  - 整单加急：`grandTotal = afterDiscount × urgentRate`（折扣后整体含加价乘倍率）。
  - 单制品加急：`grandTotal = afterDiscount + prodUrgentBase × (urgentRate−1)`。
- 用途倍率：`finalPrice = grandTotal + afterDiscount × (uRate−1)`，`afterDiscount` 已含加价折扣后金额。
- 渲染同步：SET 分组/无柄图分组的 `groupWithBound` 用 `ex.lt × ex.groupRate`；整单加急底部块与折后小计改用 `afterDiscount`（已含加价）。
- 数学自检：吧唧20+明信片30+书签10+宣图40+绑定抠图10，SET≥4×0.9，无加急 → 折后小计 = (100+10)×0.9 = **¥99**；若书签勾单制品加急×2 → 折后小计 ¥99、总价 ¥109。
- 缓存升到 v130；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；本地提交并推送后需 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（CU轮 bugfix，SET模式NaN）· 功能 v29
- **修复 SET 模式下总价/倍率小计/最终报价/定金尾款显示 `¥NaN`**：用户截图显示「折后小计 ¥99」正确，但后续金额全部 NaN。
- 根因：CT 轮 Step 5 SET 模式里写成 `_dcExtras.forEach(ex => extrasAfterSet += ex.lt * (ex.groupRate || 1.0))`。`_dcExtras` 是原始输入数组，元素只有 `name/price/quantity/bindSeq`，没有 `lt/groupRate` 字段；`undefined * 1.0 = NaN`，导致 `afterDiscount` 变成 NaN，污染后续所有金额。
- 修正：改用已预处理好的 `extraDetails` 数组（其中每个对象已含 `lt` 和 `groupRate`）。
- 验证：吧唧20+明信片30+书签10+宣图40+绑定抠图10，SET≥4×0.9，无加急 → 折后小计 = 总价 = 倍率小计 = 最终报价 = **¥99**；定金(50%) = 尾款(50%) = **¥49.50**。
- 缓存升到 v131；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；本地提交并推送后需 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（CV轮，非SET模式文案修正）· 功能 v29
- **把非 SET 模式下的「折后小计」改为「制品小计」**：用户截图（吧唧+宣图+明信片+logo，logo 单制品加急×2）显示非 SET 模式（制品明细）下汇总行仍显示「折后小计」。该模式无折扣，文案不准确。
- 修正：`dcRecalc` 非 SET 分支（制品明细）的汇总行文案由「折后小计」改为「制品小计」。SET 模式仍保留「折后小计」以体现组折扣。
- 金额逻辑不变：制品小计 = 制品原价合计（含加价折扣后金额）；总价 = 制品小计 + 单制品加急溢价。
- 验证：截图示例制品小计 **¥100.00**、总价 **¥110.00**、最终报价 **¥110.00**、定金/尾款各 **¥55.00**。
- 缓存升到 v132；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；本地提交并推送后需 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（CW轮，制品小计含加急）· 功能 v29
- **用户要求非 SET 模式的「制品小计」包含单制品加急溢价**（即显示 ¥110 而非 ¥100）。
- 修正：`dcRecalc` 非 SET 分支的「制品小计」金额由 `afterDiscount`（加急前）改为 `grandTotal`（已含制品+加价+单制品加急溢价）。SET 模式分支的「折后小计」本就用 `groupDisplay`（含加急），不需改。
- 说明：当前场景（无整单加急、无用途倍率）下，`制品小计 = 总价 = 倍率小计 = 最终报价 = grandTotal`；当存在整单加急时，制品小计（不含整单倍率）与总价（含整单倍率）会自然分开。
- 验证：截图示例（吧唧20+宣图40+明信片30+logo10，logo 单制品加急×2）→ 制品小计 **¥110.00**、总价 **¥110.00**、最终报价 **¥110.00**、定金/尾款各 **¥55.00**。
- 缓存升到 v133；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；本地提交并推送后需 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（CX轮，手动SET分组按钮）· 功能 v29
- **新增「🏷️ 归为SET」按钮**：制品列表每行新增「选」勾选框；勾选若干制品后点击该按钮，将其归为同一个手动 SET 组（`setGroup`）。
- 手动分组优先级高于自动按柄图分组：SET 模式下，按 `setGroup` 优先、`patternId` 次之、无柄图兜底 进行分组与折扣计算。适用于**单一柄图**（无须手填柄图标识即可成组）或**无柄图制品**（原本无法进入 SET）的场景。
- 分组键内仍按「不同品类数量」套用 SET 档位（≥4 九折 / ≥9 八折），沿用既有业务规则；同一组的制品在收据中以「SET组（N种品类）」标题呈现。
- 再次点击已同组制品的「归为SET」可取消分组；切换录入模式/清除会清空选择集。非 SET 模式点击按钮会提示先选 SET优惠。
- 制品新增稳定 id（`_pid`）支撑选择态跨重渲染保留；`newProduct`/`导入接稿` 均赋值。
- 缓存升到 v134；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 通过。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（CY轮 bugfix，页面空白）· 功能 v29
- **修复 CX 轮导致页面空白的运行时错误**：`newProduct()` 工厂函数在 2921 行使用 `_dcProdSeq`，但 `let _dcProdSeq = 1;` 声明在 2942 行，且 `_dcProducts = [newProduct()]` 初始化（2935 行）在声明之前，触发 `Uncaught ReferenceError: Cannot access '_dcProdSeq' before initialization`，`renderDesignCalc` 中断，首页主体空白。
- 修正：将 `_dcProdSeq` 与 `_dcSelSet` 的声明提前到 `_dcProducts` 初始化之前。
- 缓存/脚本版本号升到 v135；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（CZ轮，简化归为SET）· 功能 v29
- **用户要求简化「归为SET」**：不需要单独勾选制品，也不需要先到优惠体系选 SET 优惠；填完制品后点「归为SET」即把**当前全部制品**归为同一个 SET 组，并**自动开启 SET 优惠模式**（确保折扣生效）。
- **改动**：
  - 移除制品列表每行「选」勾选框、配套 `_dcSelSet` 状态集合、`dcToggleSel` 函数、`dc-sel-hint`「请先勾选/请先选SET优惠」提示分支；`_dcProducts` 全部直接归组。
  - `dcMakeSet` 重写：已全同组→再次点击取消分组；否则全部赋同一 `setGroup`；随后将「优惠体系」radio 自动切到 `SET优惠` 并调用 `dcDiscModeChange()` 启用 set-area。
  - 标题提示文案改为「点『归为SET』可将当前所有制品归为一组并自动开启SET优惠」。
  - `index.html` 三处 `.dc-product-row` grid（桌面 + 两处媒体/容器）去掉 `sel` 列，`header` nth-child 映射与 `.dc-prod-sel` CSS 同步清理。
  - 仍保留 `.dc-prod-grouped` 高亮（已归组行浅蓝背景），手动组优先于自动按柄图分组、按同组不同品类数套 SET 档位的既有逻辑不变。
- 缓存/脚本版本号升到 v136；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DA轮，归为SET去高亮+去重复无柄图）· 功能 v29
- **修复「无柄图制品」重复出现**：SET 优惠模式下，手动归为 SET 的制品若未填柄图标识，会同时进入 `groupDetails` 的 SET 组，又因 `!d.patternId` 被收据的「无柄图制品」区块再次渲染，导致同一批制品重复列出、总价被重复累加（截图中 01-04 与 05-08 实为同一组）。修正：`noPatternItems` 过滤条件增加 `!d.setGroup`，已归组的手工 SET 制品不再落入「无柄图制品」。
- **移除归组浅蓝高亮**：按用户要求，已归为 SET 的制品行不再显示浅蓝背景。`app.js` 中去掉 `groupedCls`，`index.html` 中删除 `.dc-product-row.dc-prod-grouped` 背景样式。
- 缓存/脚本版本号升到 v137；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。本地已提交 `ce4c729`；GitHub 推送失败（无错误输出，疑凭据管理器无法在非交互环境弹窗），可手动 `git push origin main` 补推。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DB轮，归组序号蓝色示意）· 功能 v29
- **用户要求**：已归为 SET 的制品，其列表「序号」改为蓝色以示意（取代 DA 轮删除的整行浅蓝高亮）。
- **改动**：`dcRenderProducts` 为每个制品序号 `<div class="dc-prod-seq">` 在 `p.setGroup` 非空时追加 `setgroup` 类；`index.html` 新增 `.dc-product-row .dc-prod-seq.setgroup{color:var(--c-primary)}`（仅序号蓝色，行背景保持白）。
- 缓存/脚本版本号升到 v138；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DC轮，制品列表提示词更新）· 功能 v29
- **用户要求**：制品列表标题提示词改为「（单个柄图SET选完制品后，可点击「归为SET」归组并自动开启SET优惠）」。
- **改动**：`dcRenderProducts` 的 `calc-card-hint` 文案替换（仅提示词，逻辑不变）。
- 缓存/脚本版本号升到 v139；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DD轮，制品列表提示词微调）· 功能 v29
- **用户要求**：提示词由「（单个柄图SET选完制品后…」改为「（单个SET选完制品后，可点击「归为SET」归组并自动开启SET优惠）」（去掉"柄图"）。
- **改动**：`dcRenderProducts` 的 `calc-card-hint` 文案微调（仅文字，逻辑不变）。
- 缓存/脚本版本号升到 v140；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DE轮，修复无优惠模式加价项目未计入总价）· 功能 v29
- **问题**：用户截图反馈「无优惠」模式下，制品 01 下方的绑定加价「纹路 ×1 ¥10.00」已显示，但「制品小计/总价/最终报价」均为 ¥230.00（仅含 8 个制品，未含 ¥10 加价）。
- **根因**：`dcRecalc` Step 5 中，`afterDiscount` 默认初始化为 `baseBeforeDiscount`（仅制品原价），只在 SET/折扣模式分支里把加价项目加回去；「无优惠」模式没有分支，导致 `extrasTotal`（含绑定/未绑定加价）完全没进入总价。
- **修正**：将 `afterDiscount` 默认值改为 `baseBeforeDiscount + extrasTotal`。SET/折扣模式仍各自覆盖该值，逻辑不变；无优惠模式现在正确含加价。
- 缓存/脚本版本号升到 v141；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DF轮，整单加急时制品小计不含加急）· 功能 v29
- **问题**：用户截图反馈整单加急开启后，「制品小计」显示为 ¥460.00（制品原价 ¥230 × 2 倍），与下方「整单加急-总价格 ¥230.00 / 加急倍率 ×2 / 加急小计 ¥460.00」重复叠加，造成视觉上总价被 double。
- **根因**：收据非 SET 分支的「制品小计」取了 `grandTotal`，而 `grandTotal` 在整单加急时已乘 `urgentRate`，导致制品小计提前把加急溢价算入。
- **修正**：将「制品小计」改为取 `afterDiscount`（折扣后整体，含制品+加价，但不含加急溢价）；加急溢价仅在下方「整单加急」独立区块展示。
- 缓存/脚本版本号升到 v142；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DG轮，单项加急时制品小计含加急溢价）· 功能 v29
- **问题**：用户截图反馈单项/部分制品加急时，「制品小计」显示 ¥217.00，未包含 logo 单制品加急的 ¥10 溢价，导致与下方「总价 ¥227.00」不一致。
- **根因**：DF 轮把非 SET 分支的「制品小计」统一改为 `afterDiscount`（不含任何加急），但单项加急时加急溢价已内联在明细中（`加急：08 ×2`），用户期望制品小计包含该溢价。
- **修正**：非 SET 分支「制品小计」按是否整单加急分支：整单加急取 `afterDiscount`（加急在下方独立块）；非整单加急取 `grandTotal`（已含单项加急溢价）。SET 模式组内「折后小计」仍维持原逻辑。
- 缓存/脚本版本号升到 v143；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DH轮，提示词+整单加急文案+删除解锁）· 功能 v29
- **加价项目提示词**：由「（不参与同模计价、SET折扣）」改为「（不参与同模计价）」（CT 轮起加价项目已参与 SET 折扣，故去掉 SET 折扣表述）。
- **整单加急文案**：
  - 区块标题「整单加急（全部制品与加价项目）」→「整单加急」（去掉括号说明）。
  - 区块内「整单加急-总价格」→「制品小计」（折扣后整体）。
  - 非 SET 分支汇总行「制品小计」→「折后小计」（对应折扣优惠后的总结）。
- **删除制品解锁整单加急**：`dcRemoveProduct` / `dcClearProducts` 增加 `dcSyncWholeOrderToggle()` 调用。当删除/清空最后一个带「加急」的制品时，解除整单加急开关的禁用（置灰）状态，使其可重新勾选。`dcSyncWholeOrderToggle` 同步更新复选框 `disabled` 与标签 `calc-rate-label-disabled` 类（单行勾选/取消加急本已调用，逻辑一致）。
- 缓存/脚本版本号升到 v144；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DK轮，同担/同推优惠独立列于倍率计算后）· 功能 v29
- **诉求**：实时报价中「同担/同推优惠」单独列出来，放在「倍率计算」之后。
- **改动**：将 Step6 中「同担/同推优惠」的行由 `discHTML` 抽出为独立的 `fanHTML`（计算逻辑 `afterDiscount -= _dcFanReduce` 不变，仍计入折扣与后续加急/用途倍率）；收据渲染在「倍率计算」区块之后单独输出 `fanHTML`（独立 `.dc-r-section`，仅含该行「同担/同推优惠 -¥X」）。
- 注意：该优惠仍是订单级折扣、作用于折扣后基数（在加急/用途倍率之前），因此会对最终报价产生乘倍率后的实际影响；收据展示的固定 -¥X 为用户填写的原始减价额。
- 缓存/脚本版本号升到 v145；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DL轮，同担/同推优惠作为最终抵扣）· 功能 v29
- **问题**：DK 轮把「同担/同推优惠」展示移到了「倍率计算」之后，但计算仍按原逻辑在 Step6 从 `afterDiscount` 扣除（即加急/用途倍率之前），导致用户截图中「倍率小计 ¥148.00 → 同担/同推优惠 -¥6.00 → 最终报价 ¥148.00」，优惠未在最终报价中体现。
- **修正**：将 同担/同推优惠 改为**最终抵扣**。Step6 不再修改 `afterDiscount`，仅生成展示 HTML；在 Step8 计算完用途倍率后，从 `finalPrice` 直接扣除 `_dcFanReduce`（`Math.max(0, ...)` 防止负数）。这样展示位置与计算顺序一致：倍率小计 → 同担/同推优惠 → 最终报价。
- 影响：折后小计、总价、倍率小计均按未扣同担/同推优惠的金额展示；最终报价、定金、尾款会扣除该优惠。
- 缓存/脚本版本号升到 v146；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DM轮，倍率小计保持不变，同担/同推优惠仅影响最终报价）· 功能 v29
- **问题**：DL 轮把 同担/同推优惠 作为最终抵扣，但「倍率小计」也取了扣减后的 `finalPrice`，导致截图中倍率小计从 ¥200 变成 ¥192，用户期望倍率小计保持未扣优惠前的金额。
- **修正**：拆分 `priceBeforeFan` 与 `finalPrice` 两个变量。「倍率小计」显示 `priceBeforeFan`（未扣同担/同推优惠）；「最终报价」、定金、尾款显示 `finalPrice = Math.max(0, priceBeforeFan - _dcFanReduce)`（已扣优惠）。
- 缓存/脚本版本号升到 v147；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DN轮，首页发布状态单选+日历图例改"开稿+截稿重合"+接稿制品勾选）· 功能 v29
- **需求1：首页平台发布记录增加「发布状态」单选**
  - `MODULES['home']` 的 `fields` 新增 `status`（`multiselect` + `single:true`，默认 `待发布`，选项「待发布 / 已发布」）；`listFields` 在「平台」后新增「状态」列（`tag:true`）。
  - `renderHome()` 在平台按钮栏之后、`main` 视图之前插入状态分段栏：左「待发布 <数量>」、右「已发布 <数量>」，点击调用 `homeSetStatusFilter(s)` 切换筛选（再点取消），同时过滤平台视图记录（`statusOf(r)` 判定）。卡片上对应展示 `tag-warning`（待发布）/ `tag-success`（已发布）状态标签，样式与开团记录进度状态一致。
  - 新增 `homeSetStatusFilter(s)`：切换/取消 `pageState.home.statusFilter` 并重置 `homePageNo` 后重渲染。
- **需求1续：日历图例文案**
  - `renderCalendar`（首页日历，~806行）与 `renderCommissionCalendar`（接稿排期日历，~1807行）底部图例「开稿+截稿同天」统一改为「开稿+截稿重合」。
- **需求2：接稿排期制品可勾选完成**
  - `openDetail()` 的 `dynamic-list/dynamic-products` 渲染：当 `pageKey==='design-commission' && f.key==='products'` 时，`<th>` 增加「完成」列，`<td>` 增加勾选框 `commissionToggleProductDone('${id}',${idx},this.checked)`，`<tr>` 加 `prod-done` 类（已勾选行加删除线 + 变灰）。
  - 新增 `commissionToggleProductDone(id,idx,checked)`：`DB.getById('commissions',id)` 取记录 → 设 `r.products[idx].done=checked` → `DB.update` 保存 → Toast → 重开详情。
  - `renderListPage()` 接稿排期卡片在 `</div>` 前增加「完成 x/y」计数（基于 `r.products.filter(p=>p.done).length`）。
- 缓存/脚本版本号升到 v148；同步 `workstation/` 四个文件到 `webview-app/app/src/main/assets/`；`node --check` 两个副本均通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DN-2轮修正，按用户截图反馈）· 功能 v29
- **问题1：待发布 / 已发布计数做成了平台按钮下方的单独一栏**。用户反馈应「直接在平台按钮上增加」。
- **修正1**：移除 `homeSetStatusFilter` 与 `statusFilter` 机制；在 `renderHome()` 每个平台按钮内部底部直接显示该平台「待发布 X · 已发布 Y」计数（待发布左、已发布右）。`MODULES.home.fields` 里的发布状态单选保留。
- **问题2：接稿排期制品可勾选仅在详情页生效，列表卡片无法勾选**。
- **修正2**：`renderListPage()` 的接稿排期卡片把「制品」字段渲染为可勾选列表（每行一个复选框 + 制品名），点击复选框不会打开详情；`commissionToggleProductDone(id,idx,checked,fromList)` 增加 `fromList` 参数，来自列表时重渲染列表页，来自详情时仍重开详情。
- 版本：v148→v149；index.html `app.js?v=149`、sw.js `xiao-workbench-v149`；四个文件已同步 `webview-app/assets/`；`node --check` 两个副本通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DN-3轮修正，取消总发布数）· 功能 v29
- **问题**：用户反馈平台按钮上仍保留原来的「总发布数」大数字，希望取消，只显示「待发布 / 已发布」两个数量。
- **修正**：移除 `renderHome()` 中平台按钮的 `platform-count`（总发布数）显示及多余的 `count` 变量；按钮结构改为：图标 + 平台名 +「待发布 X · 已发布 Y」。同步移除 `.platform-count` 相关 CSS，微调 `.platform-name` 与 `.platform-status-row` 字号/字重，使两行信息清晰可读。
- 版本：v149→v150；index.html `app.js?v=150`、sw.js `xiao-workbench-v150`；四个文件已同步 `webview-app/assets/`；`node --check` 两个副本通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DN-4轮修正，待发布/已发布全局筛选按钮）· 功能 v29
- **需求**：①「待发布」不记录在日历中（日历保持独立，不被状态筛选影响）；②在平台按钮下方新增一组全局「待发布 / 已发布」按钮，点击查看对应状态的记录列表；③平台按钮上原有的「待发布 X · 已发布 Y」计数**保留**。
- **实现**：`renderHome()` 在平台按钮之后新增 `.home-status-filter` 分段按钮（左待发布右已发布，带全局计数），点击调用重新加回的 `homeSetStatusFilter(s)`：再次点击取消并回到 main（日历）；否则设 `pageState.home.statusFilter` 并切到新视图 `view='status'`。
- 新增 `ps.view==='status'` 分支：列出全部平台中该状态（`statusOf(r)` 判定）的发布记录，含搜索框 / 日期筛选 / 清除筛选 / 分页 / 空态，顶部「‹ 返回日历」回到日历。平台按钮的 `platform-status-row` 计数不变。
- **CSS**：新增 `.home-status-filter / .home-status-btn / .home-status-head` 及 pending/published 激活配色（橙黄 / 绿），复用开团状态风格。
- 版本：v150→v152（中间 v151 仅放大平台按钮状态字号）；index.html `app.js?v=152`、sw.js `xiao-workbench-v152`；四个文件已同步 `webview-app/assets/`；`node --check` 两个副本通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DN-5轮修正，待发布/已发布按钮移至记录视图）· 功能 v29
- **问题**：用户要求「待发布 / 已发布」按钮**不在日历视图中展示**，改为放在「新增记录」按钮下方（即记录视图的工具栏下方）。
- **修正**：移除 `renderHome()` 中平台按钮下方「始终渲染」的 `.home-status-filter` 块；改为新增函数 `renderHomeStatusFilterBar()`（内部统计全局待发布/已发布数并渲染分段按钮），仅在 `view==='platform'` 与 `view==='status'` 两个记录视图、工具栏（`+ 新增记录` 之后）下方插入调用。日历（main 视图）不再出现该按钮。
- 版本：v152→v153；index.html `app.js?v=153`、sw.js `xiao-workbench-v153`；四个文件已同步 `webview-app/assets/`；`node --check` 两个副本通过。刷新前请 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DN-6轮修正，待发布/已发布再次点击回全部面板）· 功能 v29
- **问题**：`homeSetStatusFilter(s)` 在再次点击已激活按钮时 `ps.view='main'`（跳回日历），用户要求改为回到「全部面板」（不带状态过滤的记录列表）。
- **修正**：`if (ps.statusFilter === s)` 分支改为 `ps.statusFilter=''; if (ps.view==='status') ps.view='platform';` —— 清空状态过滤并回到平台记录视图（该平台全部记录），不再跳日历。状态视图仍为全局列出所有平台的待发布/已发布；平台按钮上「待发布 X · 已发布 Y」计数与记录视图下方分段按钮均保留。
- 版本：v153→v154；index.html `app.js?v=154`、sw.js `xiao-workbench-v154`；四个文件已同步 `webview-app/assets/`；`node --check` 两个副本通过。刷新前请 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DN-7轮修正，全部面板=全局全部记录且带新增记录）· 功能 v29
- **问题**：DN-6 的「全部面板」回到平台视图（单平台全部），用户要求「全部面板也要展示新增记录」，且应以与待发布/已发布一致的**全局全部记录**列表呈现。
- **修正**：① `renderHome()` 的 `view==='status'` 分支支持 `statusFilter=''` → `statusRecords = allRecords`（全局全部），标题显示「全部记录（N）」、空态「暂无全部记录」；② `homeSetStatusFilter(s)` 再次点击已激活按钮时仅 `ps.statusFilter=''`，**留在 status 视图**显示全局全部（不再跳平台视图/日历）。该状态视图工具栏本就含「+ 新增记录」按钮，故「全部面板」与「待发布/已发布」列表样式一致且都带新增记录。
- 版本：v154→v155；index.html `app.js?v=155`、sw.js `xiao-workbench-v155`；四个文件已同步 `webview-app/assets/`；`node --check` 两个副本通过。刷新前请 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DO轮，接稿排期加急/绑定制品/表单加宽）· 功能 v29
- **需求1**：接稿排期新增「是否加急」单选（是/否，默认否），`type:'multiselect' single:true`，UI 同「同柄图」；置于「报价金额」字段之前；「定金」「尾款」加 `row:'depositBalance'` 合并为同一行并列显示。
- **需求2**：① 制品列表（`products` dynamic-list）新增 `urgent` 复选列（单项加急），`buildDynamicListHTML`/`addDynamicRow`/`readDynamicList`/`openDetail` 均支持 `type:'checkbox'` 列（表单勾选、详情表显示 ✓）；② 加价项目（`extraItems`）新增 `productRef`（绑定制品，text 选填）列；数据结构与「报价计算」模块一致（per-product urgent + 绑定加价）。注：`报价金额` 仍为手动输入、定金/尾款=50%，未改自动计价逻辑（如要 urgent/绑定加价参与自动计价可再加）。
- **需求3**：电脑端新增/编辑弹窗整体加宽：`.modal` 600→780px、`.modal.lg` 800→1000px、`.modal.xl` 900→1120px，使接稿排期制品列表（新增加急列+绑定制品列）能顺利显示；其余模块新增表单同步加宽。新增 `.dl-urgent-cell` / `urgent` 列宽（50px）与 `productRef` 列宽（110px）CSS。
- 版本：v155→v156；index.html `app.js?v=156`、sw.js `xiao-workbench-v156`；四个文件已同步 `webview-app/assets/`；`node --check` 两个副本通过。刷新前请 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### 2026-08-09（DP轮，接稿排期制品序号+绑定制品下拉前置）· 功能 v29
- **需求1**：接稿排期制品列表（`products`）新增「序号」列（`{subkey:'_seq',type:'seq'}`），格式与报价计算 `dc-prod-seq` 完全一致：`String(i+1).padStart(2,'0')`（01、02…）。`buildDynamicListHTML`/`addDynamicRow` 按行序渲染，`openDetail` 详情表同步显示；`readDynamicList` 不读取该列（仅展示）；删除行后 `removeDynamicRow` 调用 `renumberSeqSpans` 重排。
- **需求2**：接稿排期加价项目（`extraItems`）的「绑定制品」改为 **下拉选择器**（combobox-wrapper 结构，与灵感记录/报价计算同款，保留【下拉选择器】结构），并 **移到「加价项目」名称列之前**。下拉选项读取制品列表自动生成的「序号+制品」，首项为「无（不绑定）」。`productRef` 由 `type:'text'` 改为 `type:'combobox', bindProducts:true`；新增 `buildCommissionBindCombobox`（初始读 `data.products`，新建表单读 `#products_rows` DOM）+ `refreshCommissionBindOptions`（表单注入后刷新）+ `removeDynamicRow`（删除并重排序号）。删除按钮统一改 `removeDynamicRow`。
- **需求3**：CSS 增加 `.dl-seq` 序号列宽（42px）；`extraItems` 的 `productRef` 表头列宽 110→140px 与新下拉一致；`.modal` 等宽度沿用 DO 轮（780/1000/1120）。
- 版本：v156→v157；index.html `app.js?v=157`、sw.js `xiao-workbench-v157`；四个文件已同步 `webview-app/assets/`；`node --check` 两个副本通过。刷新前请 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### DQ–DT轮（列宽微调合集）· 功能 v29（v158–v162）
- **DQ轮（v158）**：①接稿排期制品列表「尺寸」列 75→95px；②开团记录/售后记录「价格」(amount) 列 60→80px；③厂家合作记录「备注」(note) 列 min-width 80/flex1.2 → min-width 120/flex1.5。
- **DR轮（v159）**：①接稿排期修改类型「价格」(modifyPrice) 列 80→100px；②厂家合作备注 min-width 120→160px。
- **DS轮（v160）**：厂家合作备注 min-width 160→230px。
- **DT轮（v161→v162）**：厂家合作备注 min-width 230→330px（用户先误发 3300px，纠正后以 330px 为准）。
- 上述列宽均同步修改表头与输入框；`index.html`、`sw.js` 分段升版至 v162；四个文件已同步 `webview-app/assets/`；`node --check` 两副本通过。
- 状态：本地未提交（DA~DT 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### DU轮 · 接稿排期「修改项目」价格自动获取 · 功能 v29（v162→v163）
- **需求**：接稿排期「修改项目」的价格不要再手填，选中/输入「修改类型」后自动从价目表「修改类型」分类带出单价。
- **实现**：
  - 通用 dynamic-list 路径（接稿排期记录详情/编辑表单里的修改项目列表）：`MODULES['design-commission'].modifications` 的 `modifyType` 列新增 `priceLookup:'modify'`；扩展 `fillDynamicPrice(input, lookupType)` 支持 `lookupType==='modify'`——按 `category==='修改类型' && product===名称` 查 `priceList`，命中则写入该行 `[data-subkey="modifyPrice"]`（product/extra 仍写 `[data-subkey="price"]`）。选择或输入修改类型即自动带出单价，与制品/加价项目同逻辑。
  - 实时报价计算器面板（`renderDesignCalc` 的 `#dc-modifications`）：新增 `dcLookupModPrice(name)` 查价目表「修改类型」单价；`dcUpdateModification` 在 `field==='modifyType'` 时自动写入 `m.modifyPrice` 并同步更新对应行 `.dc-mod-price` 输入框；下拉选择（`dcSelectModification`）经此函数，选类型即带价。
- **同步/校验**：`app.js?v=162→v163`、`sw.js` `CACHE='xiao-workbench-v163'`；`workstation/index.html`、`app.js`、`sw.js` 三个文件已同步 `webview-app/assets/`；`node --check`（workstation + assets）通过。
- 状态：本地未提交（DA~DU 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

### DV轮 · 报价导入接稿过滤 + 修改项目总价强调 · 功能 v29（v163→v164）
- **需求 1**：报价计算「导入接稿」选项中，已交付且交付日期距今超过 7 天的订单不再显示。
- **实现 1**：
  - 接稿排期模块新增 `deliveredTime`（交付时间，date 类型，放在「稿件进度」之后；列表页 listFields 同步增加「交付」列）。
  - `saveForm`：当 `design-commission` 记录进度含「已交付」且 `deliveredTime` 为空时，自动写入当天（`todayStr()`）——之后满 7 天自动进入过滤。
  - `migrateData`：一次性回填——所有已「已交付」且无交付时间的旧记录，`deliveredTime` 设为当天（之后满 7 天自动从导入选项消失，平滑过渡、不丢数据）。
  - `renderDesignCalc` 的导入下拉改用 `visibleCommissions = commissions.filter(c => !isImportHidden(c))`；`isImportHidden` 规则：`进度含'已交付' && deliveredTime 存在 && 距今天数 > 7` 才隐藏（无交付时间的历史记录保守保留，用户可手动填日期触发过滤）。
- **需求 2**：实时报价「修改项目」里的「最终总价」「需付尾款」要更明显。
- **实现 2**：`index.html` 新增 `.dc-r-emph`（浅蓝卡片 + 主色 4px 左侧粗边，标签 13px/700、金额 20px/800 主色）与 `.dc-r-emph.balance`（暖橙 `#FA8C16`  variant，强调待收金额）；`dcRecalc` 收据把「最终总价」「需付尾款」两行由普通 `.dc-rr.total` 改为这两个强调块，明显区别于其他小计。
- **同步/校验**：`app.js?v=163→v164`、`sw.js` `CACHE='xiao-workbench-v164'`；`workstation/index.html`、`app.js`、`sw.js` 三个文件已同步 `webview-app/assets/`；`node --check`（workstation + assets）通过。
- 状态：本地未提交（DA~DV 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DW轮：修改项目「最终总价/需付尾款」样式重做（v164→v165）
- **诉求**：DU轮那版（两个分离卡片 + 硬编码橙色）用户觉得「不太好看」。
- **改动**：
  - `workstation/index.html`：删除 `.dc-r-emph` / `.dc-r-emph.balance` 两个分离卡片样式；新增聚合面板样式 `.dc-r-emph-panel`（主轴渐变 `linear-gradient(135deg,var(--c-primary-bg),#f7fbff)` + 1px `var(--c-primary-light)` 边框 + 轻阴影 `0 1px 4px rgba(122,181,245,.18)`，贴合 `.calc-receipt` 语法）、`.dc-r-emph-cap`（小标题「修改后结算」带主色短横线前缀）、`.dc-r-emph-row`（标签 13px/700 + 金额 21px/800 主色深色）、`.dc-r-emph-row.balance`（暖橙 chip：`#FFF8EC` 底 + `#FFE2B8` 边 + 3px `#F0A020` 左边条，金额 19px `#E8910C`）。
  - `workstation/app.js`：`dcRecalc` 修改项目区块把两行包进 `<div class="dc-r-emph-panel">` + 小标题，最终总价与需付尾款作为 panel 内两行（后者为橙色 chip），不再是悬浮双卡片。
- **同步/校验**：`app.js?v=164→v165`、`sw.js` `CACHE='xiao-workbench-v165'`；三文件同步 `webview-app/assets/`；`node --check` 通过。
- 状态：本地未提交（DA~DW 待补推）；浏览器需 **Ctrl+F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DX轮：修改项目结算面板改纯蓝无橙（v165→v166）
- **诉求**：DW轮仍保留暖橙 chip，用户回「换」→ 去掉所有橙色，整块统一主色系。
- **改动**：`workstation/index.html` 把 `.dc-r-emph-row.balance` 由暖橙（`#FFF8EC`/`#FFE2B8`/`#F0A020`/`#E8910C`）改为同色系蓝 chip（`#fff` 底 + `var(--c-primary-light)` 边 + `var(--c-primary)` 左边条，金额 `var(--c-primary-dark)` 19px）；最终总价保持 21px/800 主色深色。`index.html`/`sw.js` 升 v166。
- **同步/校验**：`app.js?v=165→v166`、`sw.js` `CACHE='xiao-workbench-v166'`；三文件同步 `webview-app/assets/`；`node --check` 通过；两端橙色计数 0。
- 状态：本地未提交（DA~DX 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DY轮：修改项目结算样式回退 v163（v166→v167）
- **诉求**：用户对 DW/DX 的强调面板（无论橙/蓝）都不满意，回「回到v163吧」→ 撤销 DV/DW/DX 全部强调样式。
- **改动**：`workstation/app.js` 把 `.dc-r-emph-panel` 聚合面板还原为 v163 原版普通小计行（`.dc-rr total` 两行：最终总价、需付尾款）；`workstation/index.html` 删除 `.dc-r-emph-panel`/`.dc-r-emph-cap`/`.dc-r-emph-row*` 全部 CSS（已无引用）。功能特性保留：价格自动获取（v163）、导入接稿过滤+交付时间（v164）不受影响。
- **同步/校验**：`app.js?v=166→v167`、`sw.js` `CACHE='xiao-workbench-v167'`；三文件同步 `webview-app/assets/`；`node --check` 通过；两端强调类计数 0、普通行计数 1。
- 状态：本地未提交（DA~DY 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DY2轮：修改项目「需付尾款」行删除 + 最终总价/需付尾款双按钮（v167→v168）
- **诉求**：删掉收据里「需付尾款」普通行；改做「最终总价」+「需付尾款」两个按钮，样式同「定金尾款」大块（grid 1fr 1fr、圆角、label+value），颜色做出区分。
- **改动**：
  - `workstation/app.js` `dcRecalc` 修改项目区：删除 `.dc-rr total` 的「最终总价」「需付尾款」两行，改为 `<div class="dc-r-final-balance">` 内含 `.dc-r-final-total`（最终总价）+ `.dc-r-pay-balance`（需付尾款），复用 `.dc-r-db-label`/`.dc-r-db-val`（与定金尾款一致）。
  - `workstation/index.html`：新增 `.dc-r-final-balance`（grid 1fr 1fr）、`.dc-r-final-total`（主蓝填充 `var(--c-primary)` 白字）、`.dc-r-pay-balance`（浅蓝 `#E0F2FF` 主色深字，同定金尾款）——两按钮靠「填充 vs 浅色」区分，无橙色。
- **同步/校验**：`app.js?v=167→v168`、`sw.js` `CACHE='xiao-workbench-v168'`；三文件同步 `webview-app/assets/`；`node --check` 通过；两端新类计数 3、旧行计数 0。
- 状态：本地未提交（DA~DY2 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DY3轮：复原最终总价普通行 + 双按钮改琥珀色区分定金尾款（v168→v169）
- **诉求**：① 最终总价的普通行还是要有的（恢复 .dc-rr total 行）；② 最终总价/需付尾款双按钮要与「定金尾款」（浅蓝）区分开——DY2 把需付尾款做成浅蓝，和定金尾款一模一样，造成混淆。
- **改动**：
  - `workstation/app.js` `dcRecalc` 修改项目区：恢复 `<div class="dc-rr total">最终总价</div>` 普通行；双按钮保留。
  - `workstation/index.html`：`.dc-r-final-total`（最终总价）改琥珀填充 `#FA8C16` 白字；`.dc-r-pay-balance`（需付尾款）改浅琥珀 `#FFF7E6` 底 + `#D46B08` 字 + `#FFD591` 边。整组琥珀色系，与蓝色「定金尾款」明显区分；两按钮内部靠「填充 vs 浅色」再区分。
- **同步/校验**：`app.js?v=168→v169`、`sw.js` `CACHE='xiao-workbench-v169'`；三文件同步 `webview-app/assets/`；`node --check` 通过；两端新类计数 3、普通行计数 1。
- 状态：本地未提交（DA~DY3 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DY4轮：双按钮改白底（v169→v170）
- **诉求**：用户回「白色呢」→ 不要琥珀色，改成白底按钮，仍与蓝色「定金尾款」区分。
- **改动**：`workstation/index.html` 把 `.dc-r-final-total`（最终总价）改白底 `#fff` + `var(--c-text)` 深色字 + `var(--c-primary)` 主色边框；`.dc-r-pay-balance`（需付尾款）改白底 `#fff` + `var(--c-text-muted)` 浅字 + `var(--c-border)` 灰边。两按钮均白底、靠边框色（主色 vs 灰）与文字深浅区分彼此，整体白底与浅蓝「定金尾款」明显拉开。琥珀色全部移除。
- **同步/校验**：`app.js?v=169→v170`、`sw.js` `CACHE='xiao-workbench-v170'`；三文件同步 `webview-app/assets/`；`node --check` 通过；两端白底计数 1、琥珀计数 0。
- 状态：本地未提交（DA~DY4 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。


## DY5轮：修改项目双按钮改与「定金尾款」同款浅蓝（v170→v171）
- **诉求**：用户对 DY4 白底四个备选方案回「和定金样式一样吧」→ 放弃白底，让「最终总价 / 需付尾款」双按钮与上方「定金尾款」(.dc-r-deposit/.dc-r-balance) 完全一致。
- **改动**：`workstation/index.html` 把 `.dc-r-final-total` 与 `.dc-r-pay-balance` 合并为同一条规则，统一为 `background:#E0F2FF;color:var(--c-primary-dark);border:1px solid var(--c-primary-light)`（即定金尾款同款浅蓝）；删除白底规则。
- **同步/校验**：`app.js?v=170→v171`、`sw.js` `CACHE='xiao-workbench-v171'`；三文件同步 `webview-app/assets/`；白底计数 0、统一浅蓝计数 1。
- 状态：本地未提交（DA~DY5 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DY6轮：修改项目双按钮改与「最终报价」同款实心蓝（v171→v172）
- **诉求**：用户回「按钮改成和最终报价样式一样吧」→ 放弃 DY5 浅蓝（和定金尾款同款），改与收据顶部「最终报价」(.dc-r-final) 同款：实心主蓝填充 + 白字 + 居中大数字。
- **改动**：
  - `workstation/app.js` `dcRecalc` 修改项目区：双按钮由 `.dc-r-final-total`/`.dc-r-pay-balance`（浅蓝）改成两个 `.dc-r-final` 块（label `.dc-r-final-label` + val `.dc-r-final-val`），并列排于 `.dc-r-final-balance`（grid 1fr 1fr）。
  - `workstation/index.html`：删除 `.dc-r-final-total,.dc-r-pay-balance` 浅蓝规则；`.dc-r-final-balance` 仅保留 grid 布局（背景/文字/圆角由 `.dc-r-final` 提供）。
- **同步/校验**：`app.js?v=171→v172`、`sw.js` `CACHE='xiao-workbench-v172'`；三文件同步 `webview-app/assets/`；`node --check` 通过；两端新渲染计数 1、浅蓝类计数 0。
- 状态：本地未提交（DA~DY6 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DY7轮：修改项目「最终总价」单独栏数字放大（v172→v173）
- **诉求**：按钮上方那条单独的「最终总价」普通行，数字部分字号加大。
- **改动**：`workstation/app.js` 该行加修饰类 `mod-grand`（`<div class="dc-rr total mod-grand">`），不影响其他 `.dc-rr.total`（综合单位成本/折后小计/加价合计/倍率小计等共用类）；`workstation/index.html` 新增 `.dc-r total.mod-grand` 规则——标签 14px/700、数字 19px/800 主色深字（原 13px/700）。
- **同步/校验**：`app.js?v=172→v173`、`sw.js` `CACHE='xiao-workbench-v173'`；三文件同步 `webview-app/assets/`；`node --check` 通过；两端 mod-grand 计数一致（app.js 1 / index.html 4）。
- 状态：本地未提交（DA~DY7 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DY7修正：改蓝色「总价」总结栏数字（v173→v174）
- **澄清**：用户截图说明「不是这个，这个改回去」，指的是中间蓝色「总价」栏（`.dc-rr.grand-bar`），而非按钮上方「最终总价」。
- **改动**：
  - 回退 DY7 对「最终总价」行的误改：删除 `mod-grand` 修饰类及相关 CSS，恢复为普通 `.dc-rr total`。`app.js` 对应行还原为 `<div class="dc-rr total"><span>最终总价</span>...`。
  - 正确目标：`.dc-rr.grand-bar`（蓝色「总价」栏）——高度由 32px 提到 40px，整体字号 15px/700，右侧金额数字 `font-size:21px;font-weight:800`。
  - CSS 覆盖放在原 `.dc-rr.grand-bar` 规则之后，保证优先级生效。
- **同步/校验**：`app.js?v=173→v174`、`sw.js` `CACHE='xiao-workbench-v174'`；三文件同步 `webview-app/assets/`；`node --check` 通过；两端 `mod-grand` 计数 0、总价栏覆盖规则在目标行后。
- 状态：本地未提交（DA~DY7 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DY8轮：蓝色「总价」栏数值定型（v174→v175）
- **诉求**：用户给定精确值——高度 32px（还原）、栏内文字 16px/700、右侧金额数字 16px/800。
- **改动**：`workstation/index.html` 把 DY7 修正的覆盖规则改为 `height:32px;font-size:16px`（整体）、`span{font-size:16px}`、`span:last-child{font-size:16px;font-weight:800}`。
- **同步/校验**：`app.js?v=174→v175`、`sw.js` `CACHE='xiao-workbench-v175'`；三文件同步 `webview-app/assets/`；`node --check` 通过。
- 状态：本地未提交（DA~DY8 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DY9轮：回退到 v172（撤销 DY7修正/DY8 蓝色总价栏放大）
- 用户指令「回到v172」→ 撤销 DY7修正(v174) 与 DY8(v175) 对蓝色「总价」栏(.dc-rr.grand-bar) 的放大覆盖规则。
- `workstation/index.html`：删除 DY8 覆盖规则（高度32px/16px/700/16px/800），恢复原始 13px/700 样式；`app.js?v=175 → v172`。
- `workstation/sw.js`：`CACHE='xiao-workbench-v175' → 'xiao-workbench-v172'`（sw 激活逻辑按缓存名清理旧缓存，回到 v172 名不会与历史冲突）。
- `workstation/app.js`：v173 的 `mod-grand` 误改已于 v174 撤销，当前已是 v172 状态（最终总价普通行 + 双实心蓝按钮），无需改动。
- **同步/校验**：`workstation/index.html`、`app.js`、`sw.js`、`manifest.json` 覆盖同步到 `D:/wbkey/webview-app/app/src/main/assets/`；`node --check`（workstation + assets）通过。
- **状态**：本地未提交（DA~DY9 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DYA轮：蓝色「总价」栏右侧金额数字 15px（v172→v176）
- 用户提供「右侧改成15px」→ 在 `.dc-rr.grand-bar` 原 13px/700 基础上，新增 `.dc-rr.grand-bar > span:last-child{font-size:15px}`（仅右侧金额放大，左侧「总价」文字保持 13px）。
- `workstation/index.html`：新增 DYA 规则；`app.js?v=172 → v176`。
- `workstation/sw.js`：`CACHE='xiao-workbench-v172' → 'xiao-workbench-v176'`。
- **同步/校验**：四文件同步 `webview-app/assets/`；`node --check` 通过。
- **状态**：本地未提交（DA~DYA 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DYB轮：修改项目区块微调（间距对齐 + 修改加价→修改类型×次数）（v176→v177）
- **间距对齐**：收据「修改项目」区块（`.dc-r-section` 内联 `margin-top`）由 `12px` 改为 `4px`，与「倍率计算」区块相对「总价」区块的 `margin-top`（`.dc-r-section + .dc-r-section{margin-top:4px}`）一致。
- **修改加价→修改类型×次数**：收据「修改项目」区块原单行聚合「修改加价 ¥XXX」改为遍历 `_dcModifications`，每行输出「修改类型 × 次数」并带该项小计（数据从每项 `modifyType`/`modifyCount` 提取）；聚合总价仍由 `dcCalcModTotal` 计入「最终总价」。
- `workstation/app.js`：`dcRecalc` 修改项目区块渲染改动；`app.js?v=176 → v177`。
- `workstation/sw.js`：`CACHE='xiao-workbench-v176' → 'xiao-workbench-v177'`。
- **同步/校验**：四文件同步 `webview-app/assets/`；`node --check` 通过。
- **状态**：本地未提交（DA~DYB 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DYC轮：修改项目区块 margin-top 改为 14px（v177→v178）
- 用户「修改项目区块的内联 margin-top 14px」→ 收据「修改项目」区块（`.dc-r-section` 内联 `margin-top`）由 DYB 的 `4px` 改为 `14px`。
- `workstation/app.js`：`dcRecalc` 修改项目区块 style 改动；`app.js?v=177 → v178`。
- `workstation/sw.js`：`CACHE='xiao-workbench-v177' → 'xiao-workbench-v178'`。
- **同步/校验**：四文件同步 `webview-app/assets/`；`node --check` 通过。
- **状态**：本地未提交（DA~DYC 待补推）；浏览器需 **Ctrl:F5** 硬刷新。APK 仍待打包 v1.1.17 上机。

## DYD轮：首页/接稿排期日历底部图例改两行（v178→v179）
- 用户「首页和接稿排期日历底部示意区分成两行」。
- `workstation/index.html`：`.cal-legend` 拆为 `.cal-legend-wrap`（只保留 border-top）+ `.cal-legend`（去 border-top、加 flex-wrap）；两行各自一个 `.cal-legend`。
- `workstation/app.js`：`renderCalendar`（首页日历）图例拆两行——第1行平台（小红书/抖音/视频号/公众号）、第2行开稿/截稿/开稿+截稿重合；`commissionCalGrid` 图例拆两行——第1行开稿/截稿/重合、第2行紧急/正常/充裕。
- `app.js?v=178 → v179`；`sw.js CACHE → v179`。四文件同步 assets；node --check 通过。

## DYE轮：报价计算两处提示词修正（v179）
- 制品列表 hint：`（单个SET选完制品后，可点击「归为SET」归组并自动开启SET优惠）` → `（单个柄图SET选完制品后，可点击「归为SET」归组）`。
- 同模阶梯价倍率 hint：`（选择同模更改项，制品行勾选启用，可单独选择同模类型）` → `（制品行勾选启用，可单独选择同模类型）`。
- 仅 hint 文案改动，无逻辑变更。

## DYF轮：手机手势返回修复（原生 Java + JS，v1.1.18 / versionCode 29）
- 根因：APP 用 `loadDataWithBaseURL` 内联加载，无历史记录 → `WebView.canGoBack()` 恒 false → 系统侧滑返回 `super.onBackPressed()` 直接 `finish()` 退出 APP；`initModalSwipe` 只拦模态内触摸，拦不住系统级手势。
- JS：`openModal` 时 `history.pushState({wbModal:1})` 使 canGoBack 为 true；`closeModal` 时 `history.back()`；新增 `popstate` 监听——系统返回手势触发时关闭当前模态回到上一级。
- Java：`MainActivity` 注册 `OnBackInvokedCallback`（API33+，预测性返回手势）与 `onBackPressed` 一致路由：`canGoBack()?goBack():finish()`。
- 构建：`build_v1.1.18.sh`（wk_118）；APK `D:\小筱工作台\小筱工作台-webview-v1.1.18.apk`（versionCode 29）。校验通过：包名 io.github.quella_x.webview、v1.1.18。
- 提交：本地 commit 838fbee（DYD-DYF）；GitHub push 本环境被网络/凭据拦截，未推送成功，需手动 push 或重试。
- 残险：若 ColorOS WebView 的 `canGoBack()` 不随 pushState 变化，手势仍会退出；届时需要加原生 modal 状态桥（addJavascriptInterface）兜底。

## DYG轮：日历图例两行加分隔线 + 重合文案（v179→v180）
- 新增 CSS `.cal-legend-sep`（1px 分隔线，margin:2px 16px）。
- 首页与接稿排期日历：两行 `.cal-legend` 之间插入 `.cal-legend-sep` 分隔线。
- 两处「开稿+截稿重合」图例文案 → 「当天同时存在开稿和截稿项目」。

## DYH轮：接稿排期紧急标时间 + 用途倍率/加急提示词（v179→v180）
- 接稿排期日历紧急/正常/充裕图例标注时间阈值：紧急（截稿≤3天）/ 正常（4-7天）/ 充裕（>7天）。
- 报价计算「稿件用途倍率」hint：`（选择稿件用途，可改数值）` → `（可更改数值）`。

## DYI轮：日历图例分隔线左右无空隙（v180→v181）
- 两行图例之间 `.cal-legend-sep` margin:2px 16px → 2px 0，分隔线左右贴合贯穿整条。
- 版本：app.js?v=180→v181、sw.js v181；APK v1.1.20（versionCode 31）。commit c9a49d2；GitHub 由用户手动 push 同步。

## DYJ轮：代码清理 + 打包 v1.1.21（v181→v182）
- 删 app.js 旧版 limitSingleCheckbox 重复定义；exportData 局部 stores 改引用全局 SYNC_STORES；删 index.html DY9 废注释。
- 版本：app.js?v=181→v182、sw.js v182；APK v1.1.21（versionCode 32）。commit a9069a2；用户手动 push 同步。

## DYK轮：加「筱色」预设 + 自定义保存/删除配色方案（v182→v183）
- 预设主题新增「筱色」（6色映射：主色#4472A8/浅#E0EDF8/深#3A6799/背景#E0EDF8/侧栏起#882424/侧栏终#641828，#703472 存 accent 备选）。
- 主题设置模块新增「保存为方案」（读6色输入框+命名存 customThemes）+「已保存方案」列表（应用/删除，删除二次确认）。
- 新增 saveCustomTheme/applyCustomTheme/deleteCustomTheme；DEFAULT_SETTINGS 加 customThemes:[]。
- 版本：app.js?v=182→v183、sw.js v183；**用户要求后续修改暂不打包 APK**，故未构建 v1.1.22。
- 本回合 Bash/PowerShell 工具故障，未同步 assets / 未 node --check / 未 git commit；代码已写入 workstation 工作区。
- 报价计算「加急」hint：`（单个制品加急请单选，此为整单加急）` → `（此为整单加急，制品行勾选，可单独选择部分制品加急）`。
- 版本升 v180：`app.js?v=179→v180`、`sw.js CACHE→v180`；APK 打包 v1.1.19（versionCode 30）。
- 提交：本地 commit 4443e9f（DYG-DYH）；GitHub push 仍被本环境网络拦截，未推送。

## DYI轮：日历图例分隔线左右无空隙贯穿整条（v180→v181）
- CSS `.cal-legend-sep` 由 `margin:2px 16px` 改为 `margin:2px 0`，两行图例之间的分隔线左右贴合、贯穿整条（不再左右留 16px 空隙）。首页与接稿排期日历均生效。
- 版本升 v181：`app.js?v=180→v181`、`sw.js CACHE→v181`；APK 打包 v1.1.20（versionCode 31）。
- 提交：本地 commit c9a49d2（DYI）；GitHub push 仍被本环境网络拦截，未推送。

## DYJ轮：代码清理（多余代码/内容删除，v181→v182）
- 删除 app.js 中旧版 `limitSingleCheckbox` 重复定义（756 行旧版仅取消其他勾选，已被 833 行增强版——含重要性胶囊样式同步——覆盖，旧版为死代码）。
- `exportData` 局部 `stores` 列表改为引用全局 `SYNC_STORES`，消除两份相同 store 列表的重复数据。
- 删除 index.html 中 DY9 轮遗留的废注释（描述已完成的「回退到 v172」操作，纯噪音）。
- 版本升 v182：`app.js?v=181→v182`、`sw.js CACHE→v182`；APK 打包 v1.1.21（versionCode 32）。
- 提交：本地 commit（DYJ）；GitHub push 待执行（本环境曾拦截）。

## DYK2轮：筱色色卡更换为新6色（v184→v185）
- 筱色预设 6 色全替换为用户新色卡：primary `#712258`、primaryLight `#d9edff`、primaryDark `#620712`、primaryBg `#ebf5ff`、sidebarStart `#466fa1`、sidebarEnd `#dff1ff`。
- 沿用「红主色 + 蓝侧边栏」结构（主界面不用全蓝），6 色全部取自新卡，未引入卡外颜色。
- 版本升 v185：`app.js?v=184→v185`、`sw.js CACHE→v185`；APK 仍不打包（用户要求后续修改暂不打包）。
- 本回合 Bash 工具仍故障，未同步 assets / 未 node --check / 未 git commit；代码已写入 workstation 工作区。

## DYK3轮：筱色改为「蓝色主色 + 红色辅助」（v185→v186）
- 用户要求由「红主色 + 蓝侧边栏」翻转为「蓝色主色、红色辅助」。
- 重映射 筱色预设：primary `#466fa1`(蓝) / primaryLight `#d9edff`(浅蓝) / primaryDark `#712258`(酒红，作按钮 hover 与强调文字 = 红色辅助) / primaryBg `#ebf5ff`(极浅蓝) / sidebarStart `#466fa1`(蓝) / sidebarEnd `#dff1ff`(极浅蓝)。
- 主内容区（按钮/标签/表头/选中态/侧边栏）统一蓝色系；红色仅作为辅助强调（酒红 hover、强调数字/价格），不再大面积出现。6 色取自新卡，未引入卡外颜色；`#620712`(深红) 本版未用（卡内冗余极浅蓝三选一）。
- 版本升 v186：`app.js?v=185→v186`、`sw.js CACHE→v186`；APK 仍不打包。
- 状态：Bash 工具仍故障，未同步 assets / 未 node --check / 未 git commit；改动已落 workstation 三文件 + 版本历史.md。

## DYK4轮：删除「筱色」预设主题（v186→v187）
- 用户要求删除筱色主题。从 `PRESET_THEMES` 移除筱色条目，仅保留「海豚蓝」「暖橙」两个预设。
- 安全性：主题以颜色对象存储（`applyPresetTheme` 复制字段到 `s.theme`，非名称引用），故此前已应用筱色的用户保留其配色不受影响；默认主题仍为「海豚蓝」，无需迁移。
- 版本升 v187：`app.js?v=186→v187`、`sw.js CACHE→v187`；APK 仍不打包。
- 状态：Bash 工具仍故障，未同步 assets / 未 node --check / 未 git commit；改动已落 workstation 三文件 + 版本历史.md。

## 生活模块：新增【生活】一级菜单 + 每日打卡/每日记录（v187→v188）
- 来源：桌面 `新增.docx`（两部分提案中先落「生活模块」；「接稿详情」含 AI 解析/腾讯问卷导入需后端，暂未做）。
- 导航：`NAV` 末加 `生活` 分组（每日打卡 `life-checkin` ✅ / 每日记录 `life-record` 📝）；`PAGE_TITLES` 两行；`navigate()` 两分支。
- 每日打卡 `renderLifeCheckin()`：内置 深空打卡(日签,同步日历蓝点)/运动打卡(日签)/每周按摩打卡(周签)/吸尘打卡(周签)；连续天数统计（日签按天、周签按自然周、断签自动清零、含昨日 grace）；补打卡(日期≤今天)+撤销；顶部「今日待打卡」一键打卡；自由新增打卡类型(`lifeCheckinTypes`)。
- 每日记录 `renderLifeRecord()`：内置 睡眠(入睡/起床/清醒次数/睡眠时长自动算)/三餐/零食；按日期切换、增删改多条；睡眠时长自动统计(当日+近7日平均)；自由新增记录类型(`lifeRecordTypes`，3 通用字段)。
- 数据独立存储 `lifeCheckins`/`lifeRecords`，不参与业务计算；未列入 `SYNC_STORES`（不与现有云端同步耦合）。
- 主页日历 `renderCalendar` 加深空打卡蓝点(读 `lifeCheckins` deepspace 日期,蓝点 `#9DC8FF` 置于开稿/接稿/同天下方) + 图例「深空打卡」。
- 新增 CSS：`.life-*` 系列（index.html `<style>` 末尾）。
- 版本升 v188：`app.js?v=187→v188`、`sw.js CACHE→v188`；四文件同步 `D:/wbkey/webview-app/app/src/main/assets/`；`node --check` 两份 app.js 通过。APK 暂不打包。
- 待办：git commit/push（本环境此前曾拦截，待执行）。

## 生活模块：今日打卡网格卡片化（v188→v189）
- 按截图重构「今日待打卡」区域：顶部 📌 图钉 + 「今日打卡」标题 + 粉色副标题「完成今日任务，点亮每一天✨」。
- 待打卡项改为 2 列网格卡片，每卡左侧拟态复选框、右侧 emoji + 名称；点击整卡即打卡。
- 复选框用粉色调渐变（`#f4a6b8`→`#f0c0cc`），hover/active 微动效，匹配截图风格。
- 保留原有日签/周签逻辑、连续天数统计、补卡、撤销、自定义打卡类型。
- 版本升 v189：`app.js?v=188→v189`、`sw.js CACHE→v189`；四文件同步 assets；`node --check` 两份通过；已 `git commit`（`7c344be`）并 `git push origin main` 成功。APK 暂不打包。

## 生活模块重写 + 导航前置 + 图例对齐（v189→v190）
- 删除旧 `LIFE_CHECKIN_TYPES`/`LIFE_RECORD_TYPES` 数组与 `renderLifeCheckin`/`renderLifeRecord` 旧实现，重写为新规范。
- 导航 `NAV`：生活(life)组移到开团之前，顺序为 首页/生活/开团/美工/OC；life 仍为 group，折叠行为与其余一致。
- 每日打卡：两类——日常(深空打卡🌌/运动打卡🏃，日签) + 周期周(每周按摩💆/吸尘🧹，周签)。
  - 日类：当日未打卡按钮可点、已打卡置灰显示「今日已打卡 ✓」；支持历史日期补打卡(`lifeCheckinDo`)；统计 连续天数 + 本月次数。
  - 周类：自然周去重(每周一次)，支持往周补打卡(`lifeCheckinDoWeekDate`)；统计 已完成周数 + 连续完成周数。
  - 每类型独立卡片(名称/状态/双统计)；顶部「今日打卡」网格快捷打卡；内嵌小日历(可切月)区分日打卡(主色圆点)/周打卡(橙色方块)标记；深空打卡仍同步主页日历蓝点。
  - 数据字段：`{type,date,week,isMakeup,createdAt}`，isMakeup 由补卡自动判定。
- 每日记录：睡眠(入睡/起床/清醒次数→时长自动算；同日多条、历史编辑删除；睡眠周统计表按自然周聚合平均时长/次数)、三餐(餐次选择+备注；一天多条)、零食(内容+数量备注；多条)；顶部日期选择器切历史；每类常驻「+ 新增记录」按钮；弹窗录入；每条编辑/删除。
  - 数据字段：`{type,date,createdAt,...扁平字段}`；`normLifeRec()` 兼容旧 `fields` 格式(含自定义类型 f1→note/f2→time/f3→qty)。
- 通用边界：生活数据加入 `SYNC_STORES`(lifeCheckins/lifeRecords/lifeCheckinTypes/lifeRecordTypes) 实现云端同步；与开团/美工/OC/报价计算零耦合。
- 日历图例：首页 `renderCalendar` 与接稿排期日历图例统一 `.cal-legend`/`.legend-item` 行高(line-height:18px) + `white-space:nowrap`，修复两行高度不一致。
- 版本升 v190：`app.js?v=189→v190`、`sw.js CACHE→v190`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## 删除生活模块（v190→v191）
- 用户要求删除「每日打卡」「每日记录」两个生活模块。
- 删除范围：
  - 导航：移除 NAV 中 `life` 分组、`PAGE_TITLES` 两条、`DEFAULT_NAV_ICONS` 两条、`navigate()` 两个分支。
  - 代码：整块删除生活模块函数（LIFE_CHECKIN_DEFS/LIFE_RECORD_DEFS、getLifeCheckinTypes/getLifeRecordTypes、renderLifeCheckin/renderLifeCheckinCalendar/renderLifeRecord 及全部打卡/记录辅助函数），约 410 行。
  - 主页日历：移除深空打卡蓝点缓存、深空 tag、图例「深空打卡」项。
  - 样式：index.html 删除全部 `.life-*` CSS（含移动端 media 规则）。
  - SYNC_STORES：移除 lifeCheckins/lifeRecords/lifeCheckinTypes/lifeRecordTypes（生活数据不再参与云端同步/导出）。
- 说明：浏览器本地 localStorage 中既有的生活数据未主动清除（非破坏性），但因模块与同步项已移除，数据成为孤儿、不再展示也不被同步；如需彻底清空可在应用内「清空数据」或告知我加一次性清理。
- 版本升 v191：`app.js?v=190→v191`、`sw.js CACHE→v191`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## 清理生活模块孤儿数据（v191→v192）
- 用户要求清除删除生活模块后残留在 localStorage 的孤儿数据。
- 在 `migrateData()` 末尾追加一次性清理：启动即删除 `xx_workbench_lifeCheckins` / `lifeRecords` / `lifeCheckinTypes` / `lifeRecordTypes` 四个 key（幂等，仅当存在才删，并 `console.info` 记录清除数量）。
- 附带：若 `ui_state.lastPage` 仍指向已删除的生活页（`life-*`），自动回退到首页，避免打开即空白。
- 说明：数据已在设备上本地清除，刷新（Ctrl+F5）后生效；云端既不再同步也不再包含这些 key，无需服务端清理。
- 版本升 v192：`app.js?v=191→v192`、`sw.js CACHE→v192`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## 重新加回生活模块（v192→v193）
- 用户重新要求增加「生活」版块（与 v190 同规格）：排序 生活/开团/美工/OC，含 每日打卡 + 每日记录 两个二级模块。
- 关键前置修复：移除 v192 在 `migrateData()` 末尾的「孤儿清理块」（否则每次刷新会删掉新建的生活数据），仅保留对极早期 `fields` 包裹生活记录的兼容展平。
- NAV：生活组前置（首页/home → 生活 → 开团 → 美工 → OC）；`DEFAULT_NAV_ICONS`/`PAGE_TITLES` 加 life-checkin/life-record；`navigate()` 加两分支。
- 每日打卡 `renderLifeCheckin`：日签(深空🌌/运动🏃)+周签(按摩💆/吸尘🧹)。独立卡片(名称/状态/双统计)；日类置灰「今日已打卡」+补打卡+连续天数/本月次数；周类按自然周、一周可多次打卡、往周补打卡、已完成周数/连续完成周数；内嵌小日历(可切月)区分日(主色圆点)/周(橙色方块)标记；深空仍同步主页蓝点。
- 每日记录 `renderLifeRecord`：睡眠(入睡/起床/清醒次数→时长自动算;同日多条;周统计表按自然周聚合平均时长+次数;历史编辑删除)/三餐(餐次+备注)/零食(内容+数量);顶部日期选择器切历史;每类常驻「+ 新增记录」;弹窗录入;每条编辑/删除。
- 主页日历 `renderCalendar`：深空打卡蓝点 `#9DC8FF`（置于开稿/截稿/同天下方）+ 图例「深空打卡」。
- 数据字段：打卡 `{type,date,week,period,isMakeup,createdAt}`；记录 `{type,date,week,createdAt,...扁平字段}`。
- `SYNC_STORES` 重新加入 `lifeCheckins`/`lifeRecords`（云端同步）。
- CSS：`.life-*` 全部改用主题 CSS 变量（跟随主题），含移动端适配。
- 版本升 v193：`app.js?v=192→v193`、`sw.js CACHE→v193`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v194（2026-08-10）
- 需求：重做「每日打卡」与「每日记录」布局样式；每日记录各记录单独成块；补上睡眠记录的数据图。
- 「每日打卡」`renderLifeCheckin` 重做：
  - 顶部 hero 标题区「每日打卡 / 日签每日坚持·周签每周达成」。
  - 四张打卡卡改为面板化：左侧色条（日签主色 / 周签橙色）区分周期；状态点 + 连续/本月(或已完成周/连续周)双统计；操作改为「日期选择 + 撤销(幽灵) + 整宽主按钮(打卡/补打卡)」，今日已打卡按钮置灰。
  - 打卡日历独立为 `.life-panel` 面板，头部含翻月按钮。
- 「每日记录」`renderLifeRecord` 重做：
  - 顶部 hero 标题；日期条 + 当日睡眠/近7日均/当日条数三统计。
  - **每个记录类型独立成「版块」面板**（睡眠😴/三餐🍚/零食🍟），面板头含图标+名称+条数徽标+「+ 新增记录」。
  - **每条记录单独成卡片 `.life-rec-item`**（浅底描边，hover 高亮），含字段明细 + 时间/日期 + 编辑/删除；空状态居中提示。
- **新增睡眠数据图** `renderSleepChart`：近 14 天睡眠时长柱状图（纯 CSS 柱+数值标签），含「8h 推荐」虚线参考、今日柱橙色高亮、日均标注；睡眠版块内置于周统计表之前。
- CSS：`.life-*` 全套重写为「hero + panel + item」体系，沿用主题变量与移动端适配。
- 版本升 v194：`app.js?v=193→v194`、`sw.js CACHE→v194`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v195（2026-08-10）
- 需求：按参考截图（Health/每日迹）重做每日打卡与每日记录布局样式，UI 风格跟随主题、与其他模块保持一致。
- 「每日打卡」`renderLifeCheckin` 重制：
  - 顶部日历卡片：月导航、回到今天、今日日期渐变圆形高亮、日/周打卡点标记。
  - 「今日打卡」环形图标网格：每个习惯独立圆形卡片，日签主色环、周签橙色环，已完成显示绿色对勾遮罩；点击图标快速打卡。
  - 「本周打卡」矩阵：习惯为行、周一~周日为列，显示每日是否完成及本周合计。
  - 「习惯统计」卡片：每个习惯一张卡片，图标+连续/本月（或已完成周/连续周）双统计，下方带当月迷你点阵热图（日签蓝色、周签橙色）。
- 「每日记录」`renderLifeRecord` 重制：
  - 日期条增加「今天」按钮；顶部四张统计摘要卡（当日睡眠/近7日平均/今日三餐/今日零食）。
  - 睡眠/三餐/零食各自独立 `.life-panel` 版块：头部分图标+名称+目标+条数+新增按钮。
  - 每个版块顶部左侧环形进度（睡眠目标 8h、三餐目标 3 餐），右侧文字提示；睡眠版块保留 14 天柱状图与周统计表。
  - 记录列表改为左侧时间线 `.life-timeline`：竖线+圆点，每条记录独立卡片，含字段明细、时间、编辑/删除。
- 新增辅助函数：`renderCheckinHeatmap`、`renderCheckinTodayGrid`、`renderCheckinWeekMatrix`、`renderHabitStatCards`、`renderRecordTimeline`、`renderRecordSummaryText`、`lifeCheckinMonthReset`。
- CSS：`.life-*` 全面重写为「hero + card + section + ring + matrix + heatmap + timeline」体系，全部使用主题变量，含移动端适配。
- 版本升 v195：`app.js?v=194→v195`、`sw.js CACHE→v195`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v196（2026-08-10）
- 需求：每日打卡参考「每日迹」目标列表界面重做；每日记录回退到上一版（v194）。
- 「每日打卡」重制为目标卡片列表：
  - 顶部 hero + 4 张摘要卡（累计打卡/今日完成/本周完成/最长连续）。
  - 「快捷打卡」矩阵：习惯为行、周一~周日为列，显示每日完成状态。
  - 「目标列表」：每个习惯一张白色圆角卡片，左侧图标+名称+右箭头，中部三个大数字统计（累计打卡/最长连续/完成率），右侧 28 天点阵热图（日签主色、周签橙色）。
  - 点击目标卡片弹出详情 modal：日期选择补打卡/撤销、35 天点阵热图、最近 10 条打卡记录。
  - 卡片底部状态点 + 今日打卡按钮。
- 「每日记录」回退到 v194：独立版块面板、日期条+三统计卡、睡眠版块含 14 天柱状图+周统计表、每条记录独立卡片（字段+时间+编辑/删除）。
- CSS：`.life-*` 重写为目标卡片+点阵热图+快捷矩阵+modal 样式；保留并恢复 v194 每日记录样式（stat-row/panel/rec-item/sleep-chart/life-week-table）。
- 版本升 v196：`app.js?v=195→v196`、`sw.js CACHE→v196`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v197（2026-08-10）
- 需求：每日打卡页面与参考图「完全不一样」，要求去掉前面的表情、去掉最顶上的每日打卡块。
- 改动 `renderLifeCheckin`：删除顶部 hero 标题块（🌿 每日打卡）、4 张摘要卡、快捷打卡矩阵 section，以及所有装饰表情（section 标题 ⚡ / 🎯）。
- 现在页面只渲染「目标列表」卡片网格（`renderGoalCard`），每个习惯一张白色圆角卡片：左侧习惯图标 + 名称、中部三个大数字（累计/最长连续/完成率）、右侧 28 天点阵热图、底部状态点 + 今日打卡按钮；点击弹出详情 modal。整体回归参考图的目标列表风格。
- 删除不再调用的 `renderCheckinWeekMatrix`、`renderLifeCheckinSummary` 两个函数（保留 `renderCheckinHeatmap`、`renderGoalCard`）。
- 版本升 v197：`app.js?v=196→v197`、`sw.js CACHE→v197`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v198（2026-08-10）
- 需求：打卡填色的圆改成圆角正方形，按日历视图走，展示本月，不是本月的方块变灰或变白、明显区分。
- `renderCheckinHeatmap` 重写为日历视图：默认展示当前月份（传入 year/month 可定制），顶部加「日一二三四五六」微型表头；当月空白格用 `--c-bg`（白/暗底）带浅边框，非本月补位格用 `--c-border-light`（灰）明显区分；打卡完成格按日签/周签分别填充主色/橙色；今日加内阴影高亮。
- 圆点改为圆角正方形：`border-radius:5px`，不再使用 `border-radius:50%`。
- 调用点更新：目标卡片 `renderCheckinHeatmap(t.key)`、详情 modal `renderCheckinHeatmap(typeKey)` 均展示本月。
- CSS：`.lgc-heatmap` 宽度由 96px 调整到 110px；`.life-heatmap` 全局加表头样式；modal 热图最大宽度由 220px 改为 280px。
- 版本升 v198：`app.js?v=197→v198`、`sw.js CACHE→v198`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v199（2026-08-10）
- 需求：每日打卡各版块前面的表情删掉；非本月方块改成白色；填色格缩小放在右侧；支持切换上月/下月查看历史打卡；本日格子不要阴影高亮。
- 删除表情：`renderGoalCard` 去掉 `lgc-ico`（目标图标 emoji）；`lifeCheckinOpenCard` 去掉 `lm-ico`。
- 非本月方块改白：`.life-heatmap .hm-day.other-month` 背景由 `--c-border-light`（灰）改为 `#fff`（白）、边框透明，与当月网格明显区分。
- 本日格子去阴影：`.life-heatmap .hm-day.today` 去掉 `box-shadow` 内阴影高亮，改为 `1.5px solid var(--c-primary)` 细边框标记。
- 填色格缩小靠右：`.lgc-heatmap` 宽 110px→90px、与热图 `gap` 3px→2px（单元格更小）；热图在卡片右侧（`.lgc-body` 内 `.lgc-stats` 之后）。modal 热图 `max-width` 280px→240px。
- 上月/下月切换：新增全局视图状态 `lifeCheckinView` + `lifeCheckinInitView/PrevMonth/NextMonth`；`renderLifeCheckin` 顶部加 `.life-monthbar`（「‹ 上月」「2026年8月」「下月 ›」），所有目标卡片热图与详情 modal 热图统一跟随该月份；下月按钮在当前月时 `disabled`，不可进入未来月。
- 版本升 v199：`app.js?v=198→v199`、`sw.js CACHE→v199`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v200（2026-08-10）
- 需求：热图要放在卡片右侧而非底下；格子带可见浅边框网格；每周打卡模块整体变黄以与每日区分；「每周按摩打卡」改名为「按摩打卡」。
- app.js：`LIFE_CHECKIN_DEFS.massage.label` 由「每周按摩打卡」改为「按摩打卡」；`renderGoalCard` 给周签卡片加 `week-card` class。
- index.html：`.lgc-body` 由 `flex-wrap:wrap` 改为 `nowrap`，`.lgc-stats` 改 `gap:5px/min-width:0`，统计框 `min-width:56px/padding:8px 4px`，数字字号 22→18px，标签 11→10px 并加省略；`.lgc-heatmap` 宽 90→74px，确保与统计区在同一行。
- index.html：`.life-heatmap .hm-day` 背景由 `--c-bg` 改 `#fff`，边框由 `--c-border-light` 改 `var(--c-border)`，使月历网格可见；非本月仍白底透明边。
- index.html：周签完成格由橙色 `--c-orange` 改为黄色 `--c-warning`；新增 `.life-goal-card.week-card` 覆盖规则（统计背景淡黄 `rgba(250,173,20,.12)`、数字/已完成状态用 `--c-warning`）。
- 版本升 v200：`app.js?v=199→v200`、`sw.js CACHE→v200`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v201（2026-08-10）
- 需求：格子浅色但不要外框线；周签「本周已打卡」状态用绿色；日签/周签待打卡状态用红色；热图必须固定在右侧不要折到底部。
- index.html：`.life-heatmap .hm-day` 背景改回 `--c-bg`（浅色填充），`border:none` 去掉所有外框线；非本月仍白底无框。
- index.html：`.lgc-status.todo` 颜色由 `--c-warning`（橙）改为 `--c-danger`（红）；周签卡片 `.lgc-status.done` 改回 `--c-green`（绿）。
- index.html：修复热图折行问题——媒体查询 `@media(max-width:640px)` 中不再把 `.lgc-body` 改 `column`，改为 `flex-wrap:nowrap` 并保持 `.lgc-heatmap{width:74px}`；同时 `.lgc-stat` 改 `flex:0 0 auto`、字号再压小，让统计区更紧凑，确保与热图在同一行。
- 版本升 v201：`app.js?v=200→v201`、`sw.js CACHE→v201`；四文件同步 assets；`node --check` 两份通过。APK 暂不打包。
- 版本升 v202：`app.js?v=201→v202`、`sw.js CACHE→v202`；四文件同步 assets；`node --check` 两份通过。改动：热图去掉星期表头、圆角正方形(radius 6px)、周签热图底色橙色（`--c-orange`/浅橙填充）、本日格子去蓝色框线、左侧文字块放大（名称17/数字20/统计flex放大+热图缩到66px）、新增「本月进度 X/总」进度块（日签=天数、周签=本月周数，带进度条，周签橙色）。APK 暂不打包。
- 版本升 v203：`app.js?v=202→v203`、`sw.js CACHE→v203`；四文件同步 assets；`node --check` 两份通过。改动：详情热图每格可点（新增 `lifeCheckinCellClick` 点格直接补卡/撤销并刷新弹窗）；弹窗按钮「补打卡」→「补卡」、底部记录「打卡」→「已打卡」；卡片外热图格 `border-radius:3px` 改圆角方块（原 6px 在 66px 宽度下显圆）；修复周签卡片完成格被浅橙底覆盖 specificity 问题（单独 `.hm-day.week` 实橙）；周签详情弹窗底色也加浅橙（`.life-modal-heatmap.week`）；周签月进度 `1/5` 文字改橙色；弹窗热图格可点 `cursor:pointer`+hover 描边。APK 暂不打包。
- 版本升 v204：`app.js?v=203→v204`、`sw.js CACHE→v204`；四文件同步 assets；`node --check` 两份通过。改动：①顶部月份条居中 + 中间「年月 ▾」可点弹月份选择器（年份±、12月网格跳月）；②卡片整体高度收紧（padding/gap 缩小、每日/每周提示改为标题后内联小标签 `.lgc-tag`）；③支持「添加打卡」模块（虚线＋卡 → 弹窗填名称+每日/每周，`lifeCheckinDefs` 存入 DB 并加入 SYNC_STORES，自定义 key=`custom_*`），详情里自定义模块可「删除模块」；④详情弹窗日期选择改为「‹上月 / 下月›」切换（去掉 date input），热图上方带「日一二三四五六」星期表头，热图右侧新增「X/总」月进度块（日=天数、周=本月周数，橙/蓝），点格选中（`.hm-day.selected` 描边）后右下角「补卡/撤销」按所选日期执行；openCard 拆为 open+renderModal 避免翻月被重置。APK 暂不打包。
- 版本升 v205：`app.js?v=204→v205`、`sw.js CACHE→v205`；四文件同步 assets；`node --check` 两份通过。改动：①顶部新增「今日打卡」快捷块（`renderQuickCheckin` ＋ `lifeQuickCheckin`）：标题+「已完成 X/总」计数，下列各打卡模块圆角按钮，点击即今日打卡成功、按钮变主色，再次点击取消；②月份条与详情月份条的上月/下月按钮去底色（透明背景纯文字）；③每日/每周提示词回归灰色小字标签（`.lgc-tag` 去周签橙色覆盖）；④详情标题后加「每日打卡/每周打卡」标签（`.lm-tag`），仅改位置；⑤本日/选中格子去黑边，改主色/同色系高亮（`.selected{outline:2px solid var(--c-primary);background:var(--c-primary-light)}`，周签橙）；⑥卡片「本月进度」去掉文字与进度条，仅留放大居中数字「X/总」（`.lgc-mp-num` 20px 居中）；⑦详情补卡/撤销按钮移到热图右侧下方（`.life-modal-right` 列内 `.life-modal-btns`）。APK 暂不打包。
- 版本升 v206：`app.js?v=205→v206`、`sw.js CACHE→v206`；四文件同步 assets；`node --check` 两份通过。改动：①「今日打卡」快捷块各模块**平均分布**（`.lqc-grid` 改 `grid auto-fit minmax(72px,1fr)`，按钮去 `min-width` 强制等宽填满一行），并**去掉表情图标**（不再渲染 `.lqc-ico`）；②「添加打卡」弹窗改用标准 `.form-row`+`.form-label`（label→输入框间距 6px、组间距 14px，与其他添加表单一致），名称/类型两行的 label↔输入框距离统一；③类型下拉由原生 `<select>` 改为**「下拉选择器」combobox 结构**（`input+combobox-toggle▼+combobox-dropdown`，与制品名称下拉同款），新增 `lifeCheckinSelPeriod(val,el)` 选中后回填显示文字并存 `dataset.val`；④「每日记录」三餐 `餐次` 原生 `<select>` 同样改为同款 combobox（保留 `lrf-*` id，`lifeRecSave` 仍读 `.value`），确立「所有下拉框及后续新增下拉框统一用 combobox」规则。APK 暂不打包。
- 版本升 v207：`app.js?v=206→v207`、`sw.js CACHE→v207`；四文件同步 assets；`node --check` 两份通过。改动：①卡片提示词**回归标题下方灰色小字无底色**（`.lgc-name` 下加 `.lgc-desc`「每日打卡/每周打卡」，删 `.lgc-tag` 内联标签背景）；②卡片「本月进度」**恢复文字+进度条**（`.lgc-monthprog` 加 `本月进度` 标签 + `.lgc-mp-bar`/`.lgc-mp-fill`，日蓝周橙），仅详情保持纯数字（`.lmp-num`）；③详情补卡/撤销按钮**移到热图右下、与热图底部对齐**（`.life-modal-hmwrap{align-items:stretch}`，`.life-modal-prog{flex:1 居中}`，`.life-modal-btns{margin-top:auto;align-self:flex-end}`），**撤销改红色**（`btn life-modal-undo`）；④详情热图**本日高亮线改细**（`1px` 主色/周签橙 `outline`），**选中恢复黑色边框**（`outline:2px solid var(--c-text)`）；⑤周签热图基础底色**变浅**（`.16→.07` 卡片与详情）以与右侧记录块 `var(--c-bg)` 统一；⑥添加打卡表单**输入框加高**（`lc-add-form .form-input` padding 11px）；⑦底部记录**改为所选日期当天信息且打卡时间精确到分钟**（新增 `fmtHM(iso)`，`life-modal-list` 只列 `lifeCheckinSelDate` 当天记录，显示 `日期 HH:MM 已打卡/补卡`）。APK 暂不打包。

## v208（2026-08-10）
- 需求：「今日打卡」快捷块加高、已完成计数变小；单独打卡卡片的「每日打卡/每周打卡」提示词改到标题右侧同一行；卡片整体高度减少；详情提示词同样紧跟标题后且无底色；详情热图选择日期时黑边（不要灰线）、鼠标 hover 也不要灰线；本日高亮仅细线不改变已打卡格子的填充色；添加打卡弹窗的输入框还原默认高度，但弹窗整体加高。
- index.html：`.life-quick-checkin` padding 上下 14px→18px；`.lqc-count` 字号 13px→11px、字重 600→500、padding 减小；`.lgc-title-wrap` 改 `display:flex;align-items:center;gap:8px`，`.lgc-name` 加 `flex-shrink:1`，`.lgc-desc` 去 `margin-top` 加 `flex-shrink:0`，实现提示词与标题同行；`.life-goal-card` padding 12px 14px→10px 12px、gap 10px→8px，减少单卡高度；详情 `.lm-tag` 去背景/圆角/内边距，改为 12px 灰色字与标题同行；详情热图去掉两处 `.hm-day:hover` 灰色描边；`.hm-day.selected` 描边色由 `var(--c-text)` 改 `#000`（黑边）；`.lc-add-form` 输入框还原默认 padding（删除 11px/15px 覆盖），给 `.lc-add-form` 本身加 `min-height:260px` 让弹窗加高。
- app.js / sw.js：缓存与引用版本号 207→208。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v209（2026-08-10）
- 需求：详情提示词去掉加粗、与卡片提示词统一标准；详情热图选中其他日期和本日显示方式一致（细线高亮，不要黑边）；鼠标 hover 其他日期时显示与打卡色一致的细线描边；添加打卡弹窗 `.lc-add-form` min-height 改为 230px。
- index.html：`.life-modal-hd .lm-tag` 显式 `font-weight:400` 去加粗，字号/颜色保持 12px/`--c-text-light`，与卡片 `.lgc-desc` 统一；`.lgc-desc` 也显式 `font-weight:400` 确保标准一致；详情热图 `.hm-day.selected` 由 2px 黑边改为 1px 主色细线（周签橙色），与 `.today` 完全统一；新增 `.life-modal-heatmap .hm-day:hover{outline:1px solid var(--c-primary);outline-offset:-1px}` 及周签 `outline-color:var(--c-orange)`，实现 hover 时与打卡色一致的细线示意；`.lc-add-form` min-height 由 260px 改为 230px。
- app.js / sw.js：缓存与引用版本号 208→209。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v210（2026-08-10）
- 需求：①两侧提示词字号再缩小；②详情提示词垂直居中微调；③详情热图本日与选中已打卡日期改用浅色填充（非细线 outline）以区分普通已打卡深色格；④添加打卡弹窗 `.lc-add-form` min-height 改为 210px；⑤删除名称输入框 placeholder；⑥详情弹窗增加日/周视图切换按钮，周视图显示横向一周七天打卡网格。
- index.html：`.lgc-desc` 与 `.lm-tag` 字号 12px→11px；`.lm-tag` 加 `margin-top:1px` 下移微调；详情热图 `.hm-day.today`/`.hm-day.selected` 改为背景色填充（日签 `var(--c-primary-light)`，周签 `rgba(250,140,22,.18)`），删除原来的 outline 细线规则，使本日/选中与普通已打卡深色格区分开；`.lc-add-form` min-height 230px→210px；新增 `.life-modal-tabs`、`.life-modal-weekbar`/`.lmw-txt`、`.life-weekview`/`-hd`/`-grid`、`.wv-day`(+`.done`/`.week`/`.today`) 样式，实现周视图横向七天网格。
- app.js：`lifeCheckinAdd` 删除名称输入框 `placeholder="如 喝水、读书"`；新增状态 `lifeCheckinModalTab`（'day'/'week'）与 `lifeCheckinModalWeekOffset`；新增 `lifeCheckinSetTab(tab)`、`lifeCheckinModalPrevWeek()`、`lifeCheckinModalNextWeek()`；`lifeCheckinOpenCard` 初始化 tab='day'、weekOffset=0；`lifeCheckinRenderModal` 顶部加「日/周」切换按钮，tab='day' 保持原月历热图，tab='week' 调用新 `renderCheckinWeekView(typeKey,period)` 渲染本周七天网格（含上周/下周切换、日期范围、日一二三四五六表头、打卡格显示 ✓、底部本周打卡计数）；`lifeCheckinCellClick` 点格后切回 day 视图；补卡/撤销后同时 `renderLifeCheckin()` 刷新底层列表。
- app.js / sw.js：缓存与引用版本号 209→210。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。


## v211（2026-08-10）
- 需求：①修复详情热图「选中其他日期后本日颜色改变」的 bug——本日颜色仅随打卡状态区分（打卡=深色、未打卡=浅色标记），选中改为描边环不再改变填充色；②橙色（周签）本日标记太浅，加深到 `.28` 更易区分；③周视图从详情弹窗移除，改为主页面独立「本周概览」大模块（横向 7 天网格，每日项逐天格子、每周项整行状态），现有打卡卡片保持日视图不变。
- index.html：详情热图 `.hm-day.today`/`.hm-day.selected` 重做——`.today:not(.done)` 才给浅色填充（日签 `var(--c-primary-light)`、周签 `rgba(250,140,22,.28)` 由 .18 加深），使「今日已打卡」仍显示深色打勾格、「今日未打卡」显示浅色标记；`.hm-day.selected` 改为纯描边环（`outline:2px solid #333`，周签 `outline-color:var(--c-orange)`），不再覆盖填充色 → 选中其他日期时今日填充色保持不变。删除弹窗内 `.life-modal-tabs`/`.life-modal-weekbar`/`.lmw-txt`/`.life-weekview*`/`.wv-day*` 等周视图样式；新增主页面「本周概览」模块样式 `.life-week-module`/`.lwm-head`/`.lwm-title`/`.lwm-nav`/`.lwm-range`/`.lwm-grid`/`.lwm-row`/`.lwm-row-hd`/`.lwm-day`/`.lwm-habit`/`.lwm-tag`/`.lwm-cell`(+`.done`/`.today`)/`.lwm-weekcell`(+`.done`/`.todo`，`grid-column:2/-1` 跨 7 天列)。
- app.js：删除 `lifeCheckinModalTab`/`lifeCheckinModalWeekOffset` 状态、`lifeCheckinSetTab`/`lifeCheckinModalPrevWeek`/`lifeCheckinModalNextWeek`/`renderCheckinWeekView` 函数；`lifeCheckinOpenCard` 不再初始化 tab/weekOffset；`lifeCheckinRenderModal` 去掉「日/周」切换按钮与 week 分支（纯月历热图）；`lifeCheckinCellClick` 去掉 tab 复位；新增 `renderLifeWeekOverview()`（按 `lifeWeekOffset` 算本周一及 7 天，每日项逐格判断是否打卡并标今日环，每周项整行显示「✓ 本周已打卡 / 本周待打卡」）及其导航 `lifeWeekPrev()`/`lifeWeekNext()`；`renderLifeCheckin` 在月份条后插入 `renderLifeWeekOverview()` 输出。
- app.js / sw.js：缓存与引用版本号 210→211。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v212（2026-08-10）
- 需求：①详情热图取消深色框线，选中/本日恢复为已打卡/未打卡颜色；打卡后本日（选中状态）显示浅色填充，选中其他已打卡日期也显示浅色以区分，未选中时今日恢复深色打卡色。②详情弹窗恢复「日/周」视图切换；点击热图格子可直接打卡。③周视图每周打卡保持橙色且占满 7 格。④勾号样式改为「✔」。⑤报价计算/价目计算的 `.calc-card-title` 标题颜色改为与每日打卡标题一致的 `var(--c-text)`。⑥首页日历底部图例「深空打卡」改为「进入临空」，日期格中的「深空」标签改为「临空」并固定在开稿/截稿标签的下一排。⑦添加打卡弹窗 `.lc-add-form` min-height 保持 210px。
- index.html：详情热图 `.hm-day.selected` 去掉深色描边；`.selected.done` 改为浅色填充（日签 `var(--c-primary-light)`、周签 `rgba(250,140,22,.35)`），使选中+已打卡日期变浅以区分；`.selected:not(.done)` 保留 1px 主色/橙色细线轮廓以便识别选中；删除 `.today:not(.done)` 默认浅色填充，让今日仅在选中+未打卡时才显示轮廓。恢复弹窗内 `.life-modal-tabs`/`.life-modal-weekbar`/`.lmw-txt`/`.life-weekview*`/`.wv-day*` 等周视图样式。`.calc-card-title` 颜色 `var(--c-primary)→var(--c-text)`。
- app.js：恢复 `lifeCheckinModalTab`/`lifeCheckinModalWeekOffset` 状态；`lifeCheckinOpenCard` 初始化 tab='day'、weekOffset=0；新增 `lifeCheckinSetTab`/`lifeCheckinModalPrevWeek`/`lifeCheckinModalNextWeek`/`renderCheckinWeekView`/`lifeCheckinWeekCellClick`；`lifeCheckinRenderModal` 加入日/周切换按钮与 week 分支；`lifeCheckinCellClick` 改为点击格子直接打卡（已打卡则提示，未来日期禁止）。`renderLifeWeekOverview` 与 `renderCheckinWeekView` 中完成标记由「✓」改为「✔」。`renderCalendar` 中「深空」标签改为「临空」，并用独立 `home-deep-tags` 行放在开稿/截稿标签下一排；图例「深空打卡」改为「进入临空」。
- app.js / sw.js：缓存与引用版本号 211→212。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v213（2026-08-10）
- 需求：①详情热图本日未打卡时与普通未打卡格完全一致，每周/每日详情热图样式统一（仅颜色不同：日签蓝色、周签橙色）；②详情弹窗不再显示周视图，周视图只保留在主页面「本周概览」；③主页面「本周概览」中每周打卡项显示为 7 个橙色格子（本周已打卡则 7 格全橙+✔，待打卡则浅橙 7 格），点击任意格可直接切换本周打卡状态；④橙色本日已打卡颜色由 `.35` 加深到 `.45` 以提升区分度；⑤报价计算/价目计算标题字号统一为 16px（与每日打卡标题 `.lqc-title` 一致），价目计算「成本参数」标题单独加 `margin-bottom:16px` 拉开与下方文字距离；⑥首页日历中「深空打卡」全部改为「进入临空」，日期格内用 `.cal-tag-rows` 两行容器固定开稿/截稿在第一排、临空在第二排，避免无开稿/截稿时临空标签位置上移。
- index.html：详情热图 CSS 重写——新增 `.today.done` 浅色填充（日签 `--c-primary-light`、周签 `rgba(250,140,22,.45)`），删除 `.today:not(.done)` 特殊样式，使本日未打卡=普通未打卡；保留 `.selected.done` 浅色填充与 `.selected:not(.done)` 1px 主色/橙色细线；删除弹窗内 `.life-modal-tabs`/`.life-modal-weekbar`/`.lmw-txt`/`.life-weekview*`/`.wv-day*` 等周视图样式；本周概览 `.lwm-cell` 增加 `cursor:pointer`，新增 `.lwm-cell.week`/`.lwm-cell.week.done`/`.lwm-cell.week.today` 橙色系列，删除 `.lwm-weekcell` 文字条样式；`.calc-card-title` 字号 `14px→16px`，新增 `.price-calc-title{margin-bottom:16px}`；日历新增 `.cal-tag-rows`/`.cal-tag-row` 两行固定结构。
- app.js：删除详情弹窗的 `lifeCheckinModalTab`/`lifeCheckinModalWeekOffset` 状态及 `lifeCheckinSetTab`/`lifeCheckinModalPrevWeek`/`lifeCheckinModalNextWeek`/`renderCheckinWeekView`/`lifeCheckinWeekCellClick` 函数；`lifeCheckinOpenCard` 不再初始化 tab/weekOffset；`lifeCheckinRenderModal` 移除「日/周」切换按钮与 week 分支，恢复纯月历热图；`renderLifeWeekOverview` 中每周项改为循环生成 7 个 `.lwm-cell.week` 格子；新增 `lifeWeekCellClick(typeKey,dateStr)` 处理点击每周格子的打卡/撤销；`renderCalendar` 中用 `.cal-tag-rows` 包裹两行标签（第一行固定开稿/截稿、第二行固定临空），图例保持「进入临空」；`renderPriceCalc` 中「成本参数」标题加 `price-calc-title` 类。
- app.js / sw.js：缓存与引用版本号 212→213。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v214（2026-08-10）
- 需求：修复每周打卡详情热图颜色未生效的问题——每周打卡完成的那一周应在月历热图中整周 7 格均显示橙色（已打卡周实心橙、未打卡周保持默认未打卡色），而非只高亮实际点击打卡的那一天；同时保持选中其他日期时本日恢复为已打卡/未打卡颜色。
- index.html：无新增样式，复用已有的 `.life-modal-heatmap.week .hm-day.week` 橙色系列与 `.today.done`/`.selected.done` 浅色填充规则。
- app.js：`renderCheckinHeatmap` 中按 `period` 区分：日签仍按 `r.date === ds` 判断；周签改为先收集已完成周 `doneWeeks = new Set(checkins.map(r => r.week || weekKeyOf(r.date)))`，再判断 `doneWeeks.has(weekKeyOf(ds))`，使整周 7 格统一渲染。
- app.js / sw.js：缓存与引用版本号 213→214。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v215（2026-08-10）
- 需求：详情热图点击格子改为仅选择日期查看记录，不再自动打卡/补卡；选中其他日期时本日恢复为普通已打卡/未打卡颜色，不再用浅色特殊高亮；选中态统一为 2px outline（日签蓝、周签橙）。
- app.js：`lifeCheckinCellClick` 简化为只更新 `lifeCheckinSelDate` 并重绘弹窗，移除自动写库逻辑；未来日期提示改为"不能选择未来日期"。
- index.html：删除 `.life-modal-heatmap .hm-day.today.done` 与 `.selected.done` 浅色填充规则；新增 `.life-modal-heatmap .hm-day.selected` / `.week .hm-day.selected` 统一 outline 规则。
- app.js / sw.js：缓存与引用版本号 214→215。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v216（2026-08-10）
- 需求：恢复详情热图点击格子直接打卡的能力（v215 误改为仅选日期），同时保留本日不特殊高亮、选中态统一 outline 的样式。
- app.js：`lifeCheckinCellClick` 改回 v214 行为：点击未打卡日期自动写库打卡，已打卡日期仅提示并选中；仍阻止未来日期。
- index.html：保留 v215 的 `.hm-day.selected` outline 规则；本日无特殊填充色。
- app.js / sw.js：缓存与引用版本号 215→216。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v217（2026-08-10）
- 需求：每日打卡详情热图样式回到 v212（本日无特殊高亮，只显示已打卡/未打卡色；选中已打卡用浅填充，选中未打卡用 1px 细线）；点击详情热图格子仅选日期不打卡；周视图（主页本周概览格子）保持点击打卡。
- index.html：详情热图 CSS 还原为 v212 版本——删除 `.hm-day.selected` 2px outline 规则，恢复 `.hm-day:hover` 1px 描边、`week` 分支底色、`.selected.done` 浅填充（日签 `--c-primary-light` / 周签 `rgba(250,140,22,.35)`）、`.selected:not(.done)` 1px outline（日签蓝/周签橙）。无 `.today` 特殊样式。
- app.js：`lifeCheckinCellClick` 改为仅更新 `lifeCheckinSelDate` 并重绘弹窗，不写库打卡（未来日期提示"不能选择未来日期"）。
- app.js / sw.js：缓存与引用版本号 216→217。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v218（2026-08-10）
- 需求：每周打卡不要整周一起打，改为单日打卡；详情热图（周签）打卡显示格式与每日打卡一致（按单日）；主页「本周概览」每日打卡格子不可点击打卡；主页本周概览本日格子框线改细。
- app.js `lifeWeekCellClick`：由「按整周 weekKeyOf 切换」改为按单日 `dateStr` 切换打卡/撤销（点哪天记哪天）。
- app.js `renderLifeWeekOverview` 周签行：由整周 `weekDone` 高亮改为逐日 `checkins.some(r => r.type===t.key && r.date===ds)` 显示 ✔（每格独立）。每日行本就无 onclick（不可点击打卡），保持不变。
- app.js `renderCheckinHeatmap`：周签分支去掉 `doneWeeks` 整周高亮，统一用 `doneByDate.has(ds)` 按单日判断 `week` 类（橙色），与每日 `done` 类显示逻辑一致。
- index.html：`.lwm-cell.today` outline 由 2px 改为 1px（周签 `.week.today` 仍用橙色 outline，仅变细）。
- app.js / sw.js：缓存与引用版本号 217→218。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v219（2026-08-10）
- 需求：主页「本周概览」每日打卡格子也要可以点击打卡/撤销；周签详情热图选中态与每日打卡完全一致（选中已打卡变浅填充、未打卡变 1px 细线描边）。
- app.js `renderCheckinHeatmap`：周签完成格由只加 `week` 改为同时加 `week done`，使 `.life-modal-heatmap.week .hm-day.selected.done` 浅色填充规则生效；未打卡格逻辑不变。
- app.js `renderLifeWeekOverview`：每日打卡行的每个 `.lwm-cell` 增加 `title` 与 `onclick="lifeWeekCellClick(...)"`，点击即可单日打卡/撤销；每周行仍沿用同一函数（已实现单日）。
- app.js / sw.js：缓存与引用版本号 218→219。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v220（2026-08-10）
- 需求（三块共 6 项）：①每日打卡页增加「日版块 / 周版块」切换；②首页待发布按平台分组、发布状态去默认、日历格间距平衡 + 显示「进入临空」；③报价计算 / 价目计算标题字号减小。
- **日周版块切换**：新增状态 `lifeCheckinBlock`（默认 `'week'`）；`renderLifeCheckin` 顶部加 `.life-block-toggle` 两个按钮（`lifeCheckinToggleBlock('day'|'week')` 切换并重绘）；`lifeCheckinBlock==='week'` 时渲染「本周概览」（原样），`'day'` 时渲染 `.life-goal-grid.compact` 习惯卡片（深空/运动/按摩/吸尘等日打卡），「添加打卡」按钮两版块都显示。新增 `lifeCheckinToggleBlock` 函数。
- **日版块高度缩短**：`.life-goal-grid.compact` 整体压缩——gap 10px、卡片 padding 8px 10px、标题 15px、heatmap 52px、描述 10px、去掉底部上边距等。
- **首页待发布按平台分组**：`renderHome` main 视图新增「📋 待发布 N」按钮（`homeSetStatusFilter('待发布')`）；status 视图去掉原分页（`ps.homePageNo`/`homePageSize`），改为按平台分组（顺序：小红书 / 抖音 / 视频号 / 公众号 + 未指定平台），每组显示平台标签色 + 条数 + 记录卡列表。新增 `.home-pending-bar/.home-pending-btn/.home-status-group*` CSS。
- **发布状态去默认**：字段定义 `status` 删除 `default:'待发布'`，无默认选项。
- **日历间距平衡 + 进入临空**：`.cal-day .cal-dots` / `.cal-date-row` / `.cal-tag-rows` 上下 margin 统一为 3px（原 1–2px 不等）；临空相关标签文本由「临空」改为「进入临空」。
- **标题字号**：`.calc-card-title` 由 `font-size:16px` 改为 `14px`（覆盖报价计算、价目计算等全部 calc-card-title，与每日打卡标题字号一致）。
- app.js / sw.js：缓存与引用版本号 219→220；`index.html` 引用 `app.js?v=220`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v221（2026-08-11）
- 需求（三块）：①每日打卡日/周版块精修（按钮缩小右移、默认日版块、日版块添加打卡间距、周版块改四周展示并删除「打卡项」、标题字号加大）；②首页待发布按钮位置与平台范围调整；③日历日期格固定大小、内部间距再缩小。
- **每日打卡**：
  - 默认视图改为「日版块」；切换按钮缩小为「日/周」并放到月份条最右侧。
  - 日版块「添加打卡」与上方卡片间距加大（`.life-goal-grid.compact + .life-goal-add{margin-top:20px}`）。
  - 周版块改为展示最近 4 周，标题为「第四周 / 第三周 / 第二周 / 第一周」自上而下排列，当前周（含本日）固定在最上方（第四周）。删除表头「打卡项」文字；习惯名称列右对齐并缩窄（90px），打卡格整体左移。
  - 「今日打卡」标题字号由 16px 加大到 **18px**。
- **首页待发布**：
  - 日历主视图不再显示全局「待发布」按钮。
  - 进入各平台模块（小红书/抖音/视频号/公众号）后，模块内新增该平台专属的「待发布 N」按钮，点击仅展示该平台待发布记录。
  - 新增 `homeSetPlatformStatusFilter(platform, status)`、`homeBackToPlatform()`；`renderHomeStatusFilterBar` 在平台上下文中按平台统计；状态视图若 `ps.tab` 存在则只显示该平台记录，返回按钮回到对应平台模块。
- **日历**：
  - 日期格改为固定高度（桌面 76px、手机 70px），后续不再随内容伸缩。
  - 日期格内记录间距再缩小：`.cal-dots/.cal-date-row/.cal-tag-rows` 上下 margin 由 3px 降至 2px；`.cal-tag-row` gap 由 6px 降至 4px；`.cal-day-events` gap/margin 降至 1px/3px。
  - 平台发布/开稿/截稿标签样式保持 v1.1.21 固定效果不变。
- app.js / sw.js：缓存与引用版本号 220→221；`index.html` 引用 `app.js?v=221`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v222（2026-08-11）
- 需求（四块）：①首页日历格子移除条数、去重平台模块待发布按钮、状态列表仅展示平台/类型/发布时间；②每日打卡日版块添加打卡间距对齐、日期块放最左，周视图打卡项左对齐且四周改为当月四周；③接稿排期切换按钮加高加粗；④确认故事小记/时间线/约稿记录搜索框到「全部」按钮间距。
- **首页**:
  - `renderCalendar` 移除日历格内的 `N条` 计数，仅保留平台圆点。
  - 平台模块内删除重复的平台专属「待发布 N」按钮；`renderHomeStatusFilterBar` 在平台上下文中已按平台统计，保留顶部「待发布」按钮即可。
  - 状态视图（点击待发布进入）列表卡片改为极简模式：仅展示平台、类型、发布时间，去掉标题与状态标签；新增 `.record-card-minimal` / `.record-card-body-minimal` / `.record-card-actions-minimal` 样式。
- **每日打卡**:
  - 日版块「添加打卡」与上方卡片间距由 `20px` 改为 `10px`，与卡片之间 gap 相等。
  - 「今日打卡」头部新增日期块 `X月X日 周X`，放在最左侧；新增 `.lqc-date` 样式。
  - 周视图习惯名称改为左对齐，`.lwm-habit` `text-align:left;padding-left:5px`（5px 与打卡格 grid gap 相同），取消右侧 `padding-right:8px`。
  - 周视图改为展示当前月份的 4 周（从该月 1 日所在周的周一开始），仍按 4-3-2-1 倒序排列，本日所在周置顶。
- **接稿排期**: `.view-toggle-top .btn` 增加 `padding:10px 18px;font-weight:700`。
- **间距确认**: 故事小记、时间线、约稿记录的搜索框位于 `.toolbar`，其上方是 `.relation-person-row`（含「全部」按钮），两者间距由 `.relation-person-row{margin-bottom:12px}` 决定，即 **12px**。
- app.js / sw.js：缓存与引用版本号 221→222；`index.html` 引用 `app.js?v=222`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v223（2026-08-11）
- 需求（八块）：①每日打卡删日期块、周视图当月四周且打卡格左移；②首页日历恢复条数并缩字号、待发布/已发布再次点击回全部且展开模块与全部列表一致；③接稿排期切换按钮再缩高；④答复搜索框到「全部」间距；⑤打样加样品名称并重排展示；⑥灵感展示去来源；⑦授权展示去客户；⑧时间线重要性单选保留多选样式、时间轴去来源。
- **每日打卡**:
  - 删除「今日打卡」头部日期块（`X月X日 周X` / `.lqc-date`），恢复为「今日打卡 + 已完成 N/M」左右布局（`.lqc-hd{justify-content:space-between}`）。
  - 周视图打卡格整体左移：`.lwm-row` 习惯名列宽由 `90px` 改为 `80px`。
  - 周视图四个周改回当月真正落在本月内的 4 个周一到周日（`firstMonday` 从当月 1 日所在周一起算，若跨月则 +7 天）；labels 为「第一周/第二周/第三周/第四周」，本日所在周置顶、其余按 4→3→2→1 排列（纠正 v222 误改成「最近 4 周」）。
- **首页**:
  - 日历格恢复显示条数（`cal-day-count`「N条」），条数字号由 `10px` 缩至 `9px`（`font-size:9px`）。
  - 平台按钮拆分为 `platform-main`（点进平台）与 `platform-status-row`（待发布/已发布 筛选）；`homeSetStatusFilter` / `homeSetPlatformStatusFilter` 均改为「再次点击同状态返回全部」（toggle）；platform 视图与 status 视图合并为同一分页列表模块（去除平台分组），删除 `homeBackToPlatform()`。
  - 首页记录卡极简样式仅展示 平台、类型、发布时间（文案「发布:」→「发布时间:」），platform / status 视图复用同一 `renderHomeRecordCard`。
- **接稿排期**: `.view-toggle-top .btn` `padding` 由 `10px 18px` 再缩为 `8px 16px`（保留 `font-weight:700`）。
- **间距答复**: 故事小记、时间线、约稿记录的搜索框位于 `.toolbar`（无 margin-top），其上方「全部」按钮位于 `.relation-person-row`（`margin-bottom:12px`），二者垂直间距 = **12px**。
- **打样记录**: fields 新增「样品名称」（`sampleName`，位于 打样品类 后）；列表卡片标题回落 `r.sampleName`；listFields 改为「厂家、打样费用、打样数量、样品评价、打样时间」，字段命名按用户写法（样品费用→打样费用、样品数量→打样数量）。
- **灵感记录**: listFields 改为「制品、标签、创建时间」（去掉来源）；名称按用户写法。
- **授权记录**: listFields 改为「授权类型、授权范围、授权费用、授权日期」（去掉客户）；名称按用户写法。
- **时间线**: 「重要性」字段此前(v32)已为 `single:true` 单选 + 胶囊多选样式（重要/较重要/一般/次要），本次无需改字段；时间轴展示删除「来源」，listFields 改为「重要性、关联」。
- app.js / sw.js：缓存与引用版本号 222→223；`index.html` 引用 `app.js?v=223`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

## v224（2026-08-11）
- 需求（五块）：①首页展示列表恢复完整卡片（撤销 v222/v223 极简）、日历条数前留空隙；②每日打卡周视图月份导航置最左、周视图打卡格左右等距、右侧热图还原原大小；③接稿排期展示模块重排序（稿件进度/支付状态/稿件用途/制品/截稿日期）并答复搜索框到切换按钮间距；④故事小记/时间线/约稿记录「全部」按钮间距收窄；⑤时间线新增记录重要性改常规标签样式（去胶囊）。
- **首页**:
  - 记录卡恢复为完整卡片样式 `renderHomeRecordCard`（`record-card`：标题 + 编辑/删除图标 + 平台 tag + 状态 tag + 发布时间 + 类型 + 24h赞 + 查看作品），撤销 v222/v223 的极简 `record-card-minimal`；首页全部列表 / 待发布 / 已发布 视图统一复用完整卡片。
  - 日历条数前留空隙：`.cal-day-count` 新增 `margin-left:4px`（条数文本与左侧平台圆点之间留 4px）。
- **每日打卡**:
  - 周视图月份导航置最左：`.life-monthbar` 在 `week` 模式加 `week-mode` 类，`.life-monthbar.week-mode .life-monthbar-center{justify-content:flex-start}`，使「<上月 2026年8月 下月>」靠左。
  - 周视图打卡格左右等距：`.lwm-grid` 新增 `padding-right:5px`，使打卡格右边到外框距离 = 打卡项文字左边到边框距离（均为 5px）。
  - 右侧热图还原原大小：`.life-goal-grid.compact .lgc-heatmap{width:52px}` 改回 `width:66px`（与非 compact 一致，撤销 v220 缩小）。
- **接稿排期**:
  - 展示模块重排序并按用户命名：listFields 改为「稿件进度（progress, tag）、支付状态（paymentStatus, tag）、稿件用途（usageType）、制品（_firstProduct）、截稿日期（deadline, date）」，删除原「交付」「报价」两项。
  - 间距答复（#3）：搜索框位于 `.toolbar`（其上方为 `.view-toggle-top`，含「日历 / 列表」切换按钮），二者垂直间距 = `.view-toggle-top{margin-bottom:12px}` = **12px**；搜索框到下方内容的间距 = `.toolbar{margin-bottom:16px}` = 16px。
- **故事小记 / 时间线 / 约稿记录**: `.relation-person-row{margin-bottom}` 由 `12px` 改为 `8px`（「全部」按钮与下方搜索框间距收窄；同步修正 v222/v223 记录的 12px 为仅针对旧版，v224 起为 8px）。
- **时间线**: `buildFieldHTML` 删除 `importance-pills` 专用逻辑（去掉 `isImpPills` / `updateImportancePill` 钩子 / `data-value`），重要性字段恢复为常规 `.checkbox-item` 多选样式（与故事小记「剧情标签」一致），不再显示胶囊按钮；`single:true` 单选行为保留（`limitSingleCheckbox`）。`.checkbox-group.importance-pills` 旧 CSS 保留但不再被引用。
- app.js / sw.js：缓存与引用版本号 223→224；`index.html` 引用 `app.js?v=224`。
- 四文件同步 assets；`node --check` 两份通过（注：本环境 Node 22 的 `node --check` 会实际执行脚本，浏览器全局 `window` 在 Node 下报 `ReferenceError`，属环境差异非语法错误；语法本身合法）。APK 暂不打包。

## v225（2026-08-11）
- 需求（三块）：①首页列表展示模块仅展示平台、类型、发布时间，名称按用户写法；日历条数仅计算发布条数。②每日打卡日视图左对齐；周视图小字改到后面、文字与格水平居中、格缩小、仅展示本周、增加总结（打卡次数/完成率/日常打卡时间段）。③接稿排期搜索框到切换按钮间距改为 8px。
- **首页**:
  - `renderHomeRecordCard` 改为极简卡片（`.record-card-minimal`），仅输出 平台 / 类型 / 发布时间 三行；标签严格按用户命名。
  - 日历条数：`.cal-day-count` 使用 `dayRecords.length`（已只过滤 `publishTime` 的 home 发布记录），无需额外改动，保持「仅计算发布条数」。
- **每日打卡**:
  - 日视图左对齐：`.lqc-hd{justify-content:flex-start}`，「今日打卡 + 已完成 N/M」均靠左排列。
  - 周视图仅展示本周：`renderLifeWeekOverview` 改为只取今天所在周的周一至周日，渲染单一 `renderLifeSingleWeek('本周', ..., true)`。
  - 周视图打卡项小字后置：`renderLifeSingleWeek` 将 `每日/每周` 标签从 `.lwm-habit` 内移到行尾 `.lwm-tag-r`。
  - 水平居中：`.lwm-habit{align-items:center;text-align:center;padding-left:0}`；`.lwm-cell` 保持 flex 居中；日头 `.lwm-day` 保持居中。
  - 打卡格缩小：`.lwm-row` 列宽 `80px repeat(7,1fr)` → `70px repeat(7,1fr) 38px`，gap `5px → 4px`；`.lwm-cell` 字号 `13px → 11px`，圆角 `6px → 4px`。
  - 周视图总结：`renderLifeSingleWeek(withSummary=true)` 在模块顶部输出三列卡片：打卡次数、完成率、日常打卡时间段（取本周最早/最晚打卡时间 HH:mm - HH:mm）。
- **接稿排期**: `.view-toggle-top{margin-bottom:12px → 8px}`，搜索框到「日历 / 列表」切换按钮间距改为 8px。
- app.js / sw.js：缓存与引用版本号 224→225；`index.html` 引用 `app.js?v=225`。
- 四文件同步 assets；`node --check` 两份通过（Node 环境 `window` 未定义报 ReferenceError，非语法错误）。APK 暂不打包。

## v226（2026-08-11）
- 需求（三块）：①首页列表改为竖排卡片，仅展示平台 / 内容类型 / 发布时间，名称按用户写法；标题置顶并保留编辑/删除图标；日历条数仅计算已发布的发布条数；告知待发布/已发布按钮到新增记录按钮的间距。②每日打卡日视图月份导航左对齐；周视图「每日/每周」小字放到打卡项字段后面（紧跟标签名），总结数字字号加大，日常打卡时间段取最频繁出现的时间段，打卡格再缩小一点。③人物关系思维导图节点仅显示名字。
- **首页**:
  - `renderHomeRecordCard` 恢复标题栏（`.record-card-header`），标题左侧、编辑/删除图标右侧；下方仅展示「平台」「内容类型」「发布时间」三行，竖向堆叠（`.record-card-body-minimal{flex-direction:column;align-items:flex-start}`）。
  - 字段标签严格按用户命名：「平台」「内容类型」「发布时间」。
  - 首页日历 `.cal-day-count` 改为仅统计当天 `status === '已发布'` 的发布记录。
  - 间距答复（#1）：平台页内「新增记录」按钮在 `.toolbar` 内，其 `margin-bottom:16px`；`.home-status-filter`（待发布 / 已发布按钮）无额外 margin-top，因此**新增记录按钮底部到待发布/已发布按钮顶部的间距 = 16px**。
- **每日打卡**:
  - 日视图月份导航左对齐：`.life-monthbar-center{justify-content:flex-start}`（不再仅周视图生效，日/周统一左对齐）。
  - 周视图「每日/每周」小字位置：`renderLifeSingleWeek` 将 `.lwm-tag-r` 移除，改为在 `.lwm-habit` 内紧跟标签名输出 `<span class="lwm-habit-name">` + `<span class="lwm-tag">`。
  - 打卡项文字与格水平居中：`.lwm-habit` 保持 `align-items:center;justify-content:center;text-align:center`。
  - 打卡格再缩小：`.lwm-row` 列宽 `70px repeat(7,1fr) 38px` → `65px repeat(7,1fr) 34px`，gap `4px → 3px`；`.lwm-cell` 字号 `11px → 10px`，圆角 `4px → 3px`。
  - 周总结数字字号加大：`.lwm-sum-val` 由 `15px`（当前）改为 `18px`（已告知用户）。
  - 日常打卡时间段算法：由「本周最早打卡-最晚打卡」改为统计打卡时间所在小时，取出现次数最多的小时段，输出 `HH:00-(HH+1):00`；无记录时显示 `--`。
- **OC · 人物关系**:
  - `drawMindMap` 节点 HTML 去掉 `c.alias` 副名，思维导图节点仅显示人物名字。
- app.js / sw.js：缓存与引用版本号 225→226；`index.html` 引用 `app.js?v=226`。
- 四文件同步 assets；`node --check` 两份通过（Node 环境 `window` 未定义报 ReferenceError，非语法错误）。APK 暂不打包。

## v227（2026-08-11）
- 需求（六块）：①价目计算成本参数去掉前面表情。②手机端单选/多选选择点击时去掉浅蓝色底纹。③接稿排期选择加急=整单加急，制品列表单独选择加急=单项加急，导入时同步。④价目表其他说明展开后与下方模块隔一点间距，标题改成与每日打卡标题一样的颜色和字号。⑤首页平台页内「新增记录」按钮 `.toolbar{margin-bottom:14px}`，平台 tag 与前面「平台：」间距缩小。⑥每日打卡周视图打卡格右边到外框距离与打卡项文字左边到边框距离一致，周视图日期切换由「上月/下月」改成「上一周/下一周」。
- **价目计算**:
  - 成本参数标题 `🔧 成本参数` → `成本参数`。
- **全局交互**:
  - 移动端单选/多选标签点击浅蓝色底纹去除：`.checkbox-item`/`.radio-item` 增加 `-webkit-tap-highlight-color:transparent`、`-webkit-touch-callout:none`、`user-select:none`；`:active` 背景强制透明。
- **接稿排期 / 报价计算器**:
  - 接稿表单字段 `isUrgent`（是否加急）语义明确为**整单加急**。
  - 报价计算器 `dcCreateCommission` 与 `dcUpdateQuote` 保存时把 `_dcWholeOrderUrgent` 映射为 `isUrgent: ['是']/['否']`。
  - `dcImportRecord` 导入接稿记录时，根据 `rec.isUrgent` 还原 `_dcWholeOrderUrgent` 并同步勾选整单加急开关；制品行的 `urgent` 仍作为**单项加急**保留。
- **价目表**:
  - 其他说明展开容器 `margin-bottom:16px → 10px`；内层面板上下 `margin:0 → 8px`，让展开内容与下方价目菜单模块留出间距。
  - 展开标题 `.pricelist-note-title` 字号 `13px → 18px`，颜色 `var(--c-primary-dark) → var(--c-text)`，与每日打卡 `.lqc-title` 一致。
- **首页平台页**:
  - `.toolbar{margin-bottom:16px → 14px}`。
  - 平台 tag 与「平台：」间距：去掉 `.record-card-body-minimal .field-label` 的 `min-width:56px`，标签宽度按内容自适应，tag 紧挨文字。
- **每日打卡**:
  - 周视图左右等距：`.lwm-grid{padding-right:5px}`，`.lwm-habit{padding-left:5px;justify-content:flex-start}`，两边均为 5px。
  - 周视图导航改为「上一周 / 下一周」，中间显示当前周范围（如 `8/4 - 8/10`）；新增 `lifeCheckinPrevWeek`/`lifeCheckinNextWeek` 控制 `lifeWeekOffset`，未来周禁用「下一周」。
- app.js / sw.js：缓存与引用版本号 226→227；`index.html` 引用 `app.js?v=227`。
- 四文件同步 assets；`node --check` 两份通过（Node 环境 `window` 未定义报 ReferenceError，非语法错误）。APK 暂不打包。

## v228（2026-08-11）
- 需求：①报价计算导入接稿加急修复与下拉框改造。②价目表标题字号、折叠条颜色调整。③每日打卡周视图打卡格间距再修正。④时间线导入故事小记下拉框改造并过滤已导入。⑤所有导入选择器支持手动输入筛选。
- **报价计算 / 接稿排期**:
  - 修复 `renderDesignCalc()` 在导入模式下用 `calcSettings.wholeOrderUrgent` 覆盖 `_dcWholeOrderUrgent` 的 bug，确保导入加急记录时整单加急开关能正确勾选。
  - 报价计算「导入接稿」下拉框由原生 `<select>` 改为与「灵感记录·制品名称」同款的 combobox 下拉选择器（`combobox-wrapper` + `combobox-input` + 可筛选 + 隐藏下拉按钮 + `combobox-dropdown`），保留支持手动输入筛选。
  - 时间线「导入故事小记」下拉框同样改为 combobox 下拉选择器；导入前过滤掉已导入过的小记（按 `source === '故事小记: ' + title` 判断），避免重复导入。
- **价目表**:
  - `.pricelist-menu-header` 字号 `22px → 14px`，颜色 `var(--c-primary-dark) → var(--c-text)`。
  - `.collapsible-toggle` 颜色 `var(--c-primary-dark) → var(--c-text)`，与价目表标题颜色一致。
- **每日打卡**:
  - 修正周视图打卡格右侧空白：`.lwm-row` 列宽由 `65px repeat(7,1fr) 34px` 改为 `65px repeat(7,1fr)`，去掉之前遗留的空 34px 列；「每日/每周」小字仍保留在 `.lwm-habit` 内部紧跟标签名。现在打卡格右边到外框距离 = 左侧文字到边框距离 = 模块 `padding-right(14px)` + grid `padding-right(5px)` = 19px（两侧对称）。
- app.js / sw.js：缓存与引用版本号 227→228；`index.html` 引用 `app.js?v=228`。
- 四文件同步 assets；`node --check` 两份通过（Node 环境 `window` 未定义报 ReferenceError，非语法错误）。APK 暂不打包。

## v229（2026-08-11）
- 需求：①实时报价无折扣时「折后小计」改为「制品小计」。②价目表标题 18px、折叠条内容标题 14px、去掉「其他说明」表情。③每日打卡周视图 habit 文字与 tag 不再被挤。④导入故事小记弹窗高度 200px。
- **报价计算 / 实时报价**:
  - `dcRecalc()` 收据渲染：当没有折扣（`!discHTML && !_dcFanReduce`）时，底部「折后小计」文案改为「制品小计」。
- **价目表**:
  - `.pricelist-menu-header` 字号 `14px → 18px`。
  - 折叠条 `.pricelist-note-title` 字号 `18px → 14px`。
  - 去掉「其他说明」折叠条标题与 toolbar 按钮前的 `📝` 表情。
- **每日打卡 / 周视图**:
  - `.lwm-row` 列宽由 `65px repeat(7,1fr)` 改为 `85px repeat(7,1fr)`，把更多宽度让给 habit 列，打卡格反而略小（不是填大）。
  - `.lwm-habit` 内部 `gap:3px → 2px`；`.lwm-habit .lwm-tag` 字号 `10px → 9px`； habit 名称与「每日/每周」tag 不再被挤。
- **时间线 / 导入故事小记**:
  - 弹窗调用增加 size 参数 `'import-story'`；新增 `.modal.import-story{height:200px}`。
- app.js / sw.js：缓存与引用版本号 228→229；`index.html` 引用 `app.js?v=229`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

### v230（2026-08-11）
**需求来源**：v230 反馈图 4 点。
- **报价计算 / 导入接稿下拉框**：修复同名接稿记录导致下拉选项全蓝的 bug。`showComboboxDropdown` 现在优先读取 `.combobox-wrapper` 内 `.combobox-value` hidden input 的值与 `data-value` 匹配；导入接稿 combobox 新增 hidden input `#dcImportSelectValue`，避免用 label 文本匹配造成同名项全部高亮。
- **价目表**：
  - 恢复 toolbar「其他说明」按钮与折叠条标题前的 `📝` 表情。
  - `.pricelist-menu-header`「价目表」三字字号恢复为 v1.1.21 的 `22px`，颜色恢复 `var(--c-primary-dark)`。
  - 主列表删除 `.menu-dots` 虚线；制品条统一左缩进 `12px`，与分类标题留出层级空位。
  - 主列表条目去掉右侧独立的 ✏️/🗑️，改为点击整行打开编辑；编辑/删除按钮合并到「调整排序」弹窗的排序按钮旁。
- **每日打卡 / 周视图**：
  - `.lwm-row` 列间距 `3px → 4px`，让打卡格略小。
  - `.lwm-habit` 内部 `padding-left:5px → 3px`、`gap:2px → 1px`；`.lwm-habit-name` 改为 `flex:0 1 auto`，让「每日/每周」提示 tag 紧跟字段文本末尾，不再被挤到最右。
- **时间线 / 导入故事小记**：弹窗高度由 `200px` 调整为 `240px`。
- app.js / sw.js：缓存与引用版本号 229→230；`index.html` 引用 `app.js?v=230`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

- **v231（2026-08-11）**：网页源码升至 v231。需求来源：用户 3 点反馈（价目表细节 / 周视图打卡格 / 导入故事弹窗高度）。
  - 价目表 toolbar「其他说明」保留 📝 表情；**折叠条标题去掉 📝 表情**（仅保留文字「其他说明」）。
  - 价目表排序按钮文案「调整排序」→「排序编辑」。
  - 价目表内容对称内边距：`.pricelist-menu-item` 由 `padding:5px 0 5px 12px` 改为 `padding:5px 12px`，制品距左与价格距右均为 `12px`，内容整体向中间收拢、左右间距一致。
  - 每周打卡 / 周视图：打卡格**实际尺寸**缩小——`.lwm-cell` 由 `aspect-ratio:1`（填满 1fr 列宽）改为固定 `width:24px;height:24px` 并在网格内 `justify-self/align-self:center`；列间距 `gap:4px` 保持不变（格子变小、间距不变）。
  - 时间线 / 导入故事小记：弹窗高度由 `240px` 调整为 `420px`。
  - app.js / sw.js：缓存与引用版本号 230→231；`index.html` 引用 `app.js?v=231`。
  - 四文件同步 assets；`node --check` 两份通过。APK 暂不打包。

- **v232（2026-08-11 06:17 起）**：网页源码升至 v232。需求来源：用户 3 点反馈（价目表对称内边距 / 周视图打卡格 28px / 导入故事弹窗与添加打卡弹窗同高）。
  - 价目表内容对称内边距：`.pricelist-menu-item` 由 `padding:5px 12px` 改为 `padding:5px 16px`（制品距左、价格距右均为 16px，整体向中间收拢更多）。
  - 每周打卡 / 周视图：打卡格实际尺寸 `24px → 28px`；列间距 `.lwm-row{gap:4px → 6px}`（格子放大、间距相应放宽，保持比例协调）；列宽与满宽布局不变。
  - 时间线 / 导入故事小记：去掉 `.modal.import-story` 固定 `height:420px`，改为**内容自适应高度**，与「添加打卡」弹窗（默认 `.modal` 无固定高度、按内容排布）一致。
  - app.js / sw.js：缓存与引用版本号 231→232；`index.html` 引用 `app.js?v=232`。
  - 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。

- **v233（2026-08-11 06:34 起）**：网页源码升至 v233。需求来源：用户 3 点反馈（周视图打卡格 30px+紧凑间距 / 添加打卡弹窗高度不一致 / 导入故事弹窗 310px）。
  - 每周打卡 / 周视图：打卡格实际尺寸 `28px → 30px`；`.lwm-row{gap:6px → 3px}`、`.lwm-grid{gap:6px → 4px}`，整体更紧凑。
  - 时间线 / 导入故事小记：由 v232 的内容自适应改回**固定高度** `.modal.import-story{height:310px}`（与「添加打卡」弹窗 `.lc-add-form{min-height:210px}` 的视觉高度统一为 310px 内容区）。
  - app.js / sw.js：缓存与引用版本号 232→233；`index.html` 引用 `app.js?v=233`。

- **v234（2026-08-11 06:45 起）**：网页源码升至 v234。需求来源：用户 2 点反馈（导入故事弹窗文案/颜色/高度 + 周视图格子中间空档）。
  - 时间线 / 导入故事小记：
    - 下拉框 placeholder「请选择或输入故事小记」改为「选择故事小记」。
    - 标签「默认重要性：」改为「重要性：」。
    - 重要性下拉框文字与边框颜色随选项变化（红/橙/绿/蓝），默认红色 `#e74c3c`。
    - 弹窗高度 `.modal.import-story{height:310px → 370px}`。
  - 每日打卡 / 周视图：把打卡格列由 `1fr`（会拉伸、格子居中后两侧留白）改为固定 `30px`， habit 列保持 `85px`，中间空档消失，格子紧凑排列。
  - app.js / sw.js：缓存与引用版本号 233→234；`index.html` 引用 `app.js?v=234`。
  - 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。

- **v235（2026-08-11 06:57 起）**：网页源码升至 v235。需求来源：用户纠错 + 1 点调整。
  - 导入故事小记弹窗（纠正 v234 误操作）：v234 误把「重要性」下拉框本身改成彩色（红/橙/绿/蓝随选项变化），但用户从没要求改重要性下拉框；用户要的是「选择故事小记」下拉框颜色与重要性下拉框一致。修正：撤销重要性下拉框的彩色样式与 onchange（恢复默认 `<select class="form-select" id="importImportance">`），标签「重要性：」保留；「默认重要性」→「重要性」仍生效（v234 已改）。选择故事小记下拉框保持默认控件样式，与重要性下拉框一致。placeholder「选择故事小记」、弹窗高度 `370px`（v234 已改）保持不变。
  - 每日打卡 / 周视图：打卡项文字往右挪，使「打卡项文字左边到边框」=「打卡格右边到外框」。做法：`.lwm-grid{padding-right:5px → padding:0 5px}`（左右各 5px），`.lwm-habit{padding-left:3px → 0}`；打卡格列宽固定 `30px`（v234 已改），因此打卡格右边到外框 = 网格右 padding 5 + 卡片 padding 14，打卡项文字左边 = 卡片 padding 14 + 网格左 padding 5 + habit 0，两侧一致（均为 19px）。
  - app.js / sw.js：缓存与引用版本号 234→235；`index.html` 引用 `app.js?v=235`。
  - 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。

- **v236（2026-08-11 07:07 起）**：网页源码升至 v236。需求来源：用户继续纠错 + 2 点调整。
  - 导入故事小记弹窗：
    - 在「选择故事小记」下拉框上方补标签 `<div>选择故事小记:</div>`，与「重要性：」标签样式一致。
    - 把重要性 native `<select>` 改成 `.combobox-wrapper` 自定义下拉框，与故事小记下拉框 UI 样式统一；选项带颜色，选中后输入框文字/边框显示对应颜色。
  - 每日打卡 / 周视图：`.lwm-grid` 去掉 padding 并设 `align-items:center`，`.lwm-row` 加 `justify-content:center`，让整行网格在卡片内水平居中，使 habit 文字左侧留白 = 最右侧打卡格右侧留白。
  - app.js / sw.js：缓存与引用版本号 235→236；`index.html` 引用 `app.js?v=236`。
  - 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。

- **v237（2026-08-11 07:19 起）**：网页源码升至 v237。需求来源：用户明确要求下拉框统一规范 + 3 点修正。
  - 确立并记录规范：所有后续新增/修改的下拉框必须严格复用「灵感记录制品名称下拉框」的 `.combobox-wrapper` 结构（`input.combobox-input` + `button.combobox-toggle` + `.combobox-dropdown` + `.combobox-option`）。
  - 导入故事小记弹窗：
    - 故事小记下拉框 placeholder 改回「请选择或输入故事小记」。
    - 重要性下拉框按统一规范改为 `.combobox-wrapper` 自定义下拉框，但**不**随选项改变颜色，保持默认中性样式（不要红色）。
  - 每日打卡 / 周视图：
    - 打卡格尺寸 `30px → 35px`，字号 `10px → 11px`。
    - 打卡项（habit）字号 `12px → 13px`。
    - habit 列加 `padding-right:10px`，与打卡格之间留出更多距离。
    - habit 列宽 `85px → 95px`，列 gap `3px → 4px`，配合 35px 格子让整行更宽，减少左右留白。
  - app.js / sw.js：缓存与引用版本号 236→237；`index.html` 引用 `app.js?v=237`。
  - 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。

## v238（2026-08-11）

### 需求来源
用户 v238 消息：每日打卡周视图打卡格尺寸 45px；每日/每周小字离打卡项文字有点距离；每日/每周小字和打卡格中间的距离多一点。

### 变更内容
- 每日打卡 / 周视图：
  - 打卡格尺寸 `35px → 45px`，格内字号 `11px → 12px`；`.lwm-row` 列宽 `repeat(7,35px) → repeat(7,45px)`。
  - 每日/每周小字（`.lwm-tag`）与打卡项文字（habit name）距离拉开：`margin-left:1px → 5px`，habit 内 `gap` 置 0（距离全由 tag margin 控制）。
  - 每日/每周小字与打卡格之间距离加大：habit 列 `padding-right:10px → 16px`。
  - 整行更宽（45px×7 + 95px 列），居中后左右留白进一步减少。
- app.js / sw.js：缓存与引用版本号 237→238；`index.html` 引用 `app.js?v=238`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。

## v239（2026-08-11）

### 需求来源
用户反馈：导入故事小记弹窗顶部仍显示「选择要导入的故事小记：」，重要性下拉框文字仍是红色。

### 变更内容
- app.js 导入故事小记弹窗：
  - 顶部说明文案 `选择要导入的故事小记：` → `选择故事小记：`（v237 只改了 combobox 标签，漏了顶部说明，本次修正）。
  - 重要性下拉框 combobox 代码中已没有红色样式；版本号刷新后缓存（v237→v239）将强制拉取新代码，清除旧红色缓存。
- app.js / sw.js：缓存与引用版本号 238→239；`index.html` 引用 `app.js?v=239`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。

## v240（2026-08-11）

### 需求来源
用户 v240 消息：每日打卡周视图打卡格尺寸 39px；时间线导入故事小记删除弹窗顶部那行说明。

### 变更内容
- 每日打卡 / 周视图：打卡格尺寸 `45px → 39px`；`.lwm-row` 列宽 `repeat(7,45px) → repeat(7,39px)`（格内字号保持 12px）。
- 时间线导入故事小记弹窗：删除顶部说明行「选择故事小记：」，弹窗直接以 combobox 标签「选择故事小记：」开头。
- app.js / sw.js：缓存与引用版本号 239→240；`index.html` 引用 `app.js?v=240`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。

## v241（2026-08-11）

### 需求来源
用户 v241 消息：每日打卡周视图打卡格尺寸 37px；时间线导入故事小记弹窗「选择故事小记」「重要性」标签去掉后面的冒号。

### 变更内容
- 每日打卡 / 周视图：打卡格尺寸 `39px → 37px`；`.lwm-row` 列宽 `repeat(7,39px) → repeat(7,37px)`。
- 时间线导入故事小记弹窗：combobox 标签「选择故事小记：」「重要性：」去掉末尾冒号，改为「选择故事小记」「重要性」。
- app.js / sw.js：缓存与引用版本号 240→241；`index.html` 引用 `app.js?v=241`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。

## v242（2026-08-11）

### 需求来源
用户 v242 消息：每日打卡周视图「本周」字号 16px 并往右挪；日期「08-10 - 08-16」往左挪；上方总结打卡次数文本也往右挪、与「本周」左对齐。

### 变更内容
- 每日打卡 / 周视图头部 `.lwm-head`：加 `padding-left:14px;padding-right:14px`——「本周」左边缘右移 14px、「日期」从右侧收回左移 14px（两张卡片框宽度不变，仅文本缩进）。
- `.lwm-title`（本周）：`font-size:15px → 16px`。
- `.lwm-summary`（上方总结打卡次数框）：左内边距 `14px → 28px`，使「打卡次数」文本左边缘与「本周」左边缘对齐（两者同右移 14px）。
- app.js / sw.js：缓存与引用版本号 241→242；`index.html` 引用 `app.js?v=242`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。

## v243（2026-08-11）

### 需求来源
用户 v243 消息：每日打卡周视图「本周」字号改成 18px 并往左挪；打卡格改成 33px；本周打卡块自适应手机尺寸。

### 变更内容
- 每日打卡 / 周视图「本周」标题 `.lwm-title`：`font-size:16px → 18px`。
- `.lwm-head`：`padding-left:14px;padding-right:14px → 0`——「本周」左边缘移回卡片内左边（相对 v242 右移状态，本轮回退为往左挪）。
- 打卡格改为自适应尺寸：`.lwm-row` 列模板从 `95px repeat(7,37px)` 改为 `minmax(54px,1fr) repeat(7,minmax(0,1fr))`、`gap:4px → 3px`、`width:100%`；`.lwm-cell` 去掉固定 `width:37px;height:37px`，改 `width:100%;aspect-ratio:1/1`（方格随容器宽度自适应，典型手机下约 30–35px，等效原先 33px 诉求、不再溢出）。
- 「本周打卡块自适应手机尺寸」：`.life-week-module` 与 `.lwm-summary` 各加 `max-width:420px;margin:0 auto`（手机满宽、宽屏居中，不再因固定 382px 宽溢出横向滚动）；`.lwm-grid` `align-items:center → stretch` 使行铺满宽度；`.lwm-summary` 左内边距 `28px → 14px` 与「本周」左边缘重新对齐；`.lwm-habit` 右内边距 `16px → 8px` 让习惯名列在窄列里多留名空间。
- app.js / sw.js：缓存与引用版本号 242→243；`index.html` 引用 `app.js?v=243`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。

## v244（2026-08-11）

### 需求来源
用户 v244 消息：每日打卡周视图打卡格再改小一点，现在打卡项文字（习惯名列）显示不完全。

### 变更内容
- 根因：v243 把 7 天列设成 `repeat(7,minmax(0,1fr))` 等分，习惯列仅 `minmax(54px,1fr)`；在 ~360px 手机上 7 天格各约 40px、习惯列被压到 54px，习惯名被 `text-overflow:ellipsis` 截断。
- `.lwm-row` 列模板改为 `minmax(72px,1fr) repeat(7,minmax(0,30px))`：打卡格封顶 30px（比之前 ~40px 更小、符合要求"再改小一点"），习惯列下限从 54px 提到 72px 并吸收剩余宽度（~360px 手机上习惯列约 129px，名字完整显示）；`minmax(0,30px)` 在极窄屏仍会缩到 30px 以下以防溢出，保持自适应手机尺寸。
- `app.js?v=243 → v244`、`sw.js CACHE = 'xiao-workbench-v244'`；`index.html` 引用 `app.js?v=244`。
- 四文件同步 assets；`node --check` 两份通过。APK 暂不打包（待用户确认）。git commit `8fb3993` 本地完成；`git push` 因 GitHub Basic-auth 凭据交互挂起，需用户手动 push。

## v245（2026-08-11）

### 需求来源
用户 v245 消息（附截图）：严格按照截图布局重构「每日记录」版块。背景浅灰色（`var(--c-bg)`）划分模块；颜色跟随主题自适配；睡眠记录/三餐记录/零食记录保留现有外框样式。要点：
1. 顶部睡眠时长圆环：8 小时=满圈；未满 8h 按比例填充且颜色变化（满/≥8h 绿，否则主题主色）；底部小字 ≥8h 显示「睡眠时长达到8小时」、未满显示「睡眠时长不满8小时」；右侧饮食快捷按钮与记录同步，有记录即变色（`.filled`）。
2. 睡眠记录 / 饮食记录 按钮可点击进入历史查询视图，再次点击返回全部视图：
   - 睡眠历史：顶部一周睡眠折线图 + 当周睡眠记录（同外卡样式），列标题 周一~周日，底部 上一周/下一周 切换。
   - 饮食历史：顶部一周各餐次数统计柱状 + 当周饮食记录，列标题 周一~周日，底部 上一周/下一周 切换。
3. 记录结构：睡眠 夜晚/午间 框常驻（填写即更新、留空即空）；饮食 早餐/午餐/晚餐/宵夜/零食/奶茶 框常驻；宵夜/零食/奶茶 支持多条目（框向下堆叠）。零食数量字段单位单选但用多选（pill）样式，默认 包/个/颗/根（可自定义增删）；奶茶口味字段多选（不额外加糖/三分糖/五分糖/七分糖/全糖/热/常温/正常冰/少冰/去冰/小杯/中杯/大杯），并像人物关系关系类型一样可自定义增删。

### 变更内容
- 数据结构重构：新增 `LIFE_RECORD_SUBTYPES`（sleep: night/noon；diet: breakfast/lunch/dinner/midnight/snack/milktea，`midnight/snack/milktea` 标 `multi:true`）；每条生活记录现携带 `type`+`subtype`。`migrateData()` 内新增 v245 迁移：旧 `type:'sleep'`→subtype `night`；`type:'meal'`→`type:'diet'` 并按 `mealType` 映射 subtype；`type:'snack'`→`type:'diet'` subtype `snack`；并规整奶茶 `flavors` 为数组、零食 `qty` 为数字。
- 新增 `renderSleepRing(totalHours)`：SVG 圆心环 r=38，`pct=min(total/8,1)`，`stroke-dashoffset=circumference*(1-pct)`；≥8h 绿、否则主色；状态文案按规格。
- 新增 `renderLifeRecordTopCard(all,date)`：圆环 + 入睡/清醒时间、清醒次数、午休时长 四个统计 + 饮食快捷按钮（各 subtype 有记录即加 `.filled`）。
- 新增 `renderSleepRecordCard(date)` / `renderDietRecordCard(date)`：各渲染 `.lr-card`，每个 subtype 一个 `.lr-subtype-box` 常驻；空行可点新增；`ps.formType===typeKey && ps.subtype===st.key` 时插入行内表单。
- 行内表单 `renderLifeRecordInlineForm(typeKey,subtypeKey)`：零食单位用 `renderUnitSingleSelect(selected)`（pill 单选 + `customOpts_lifeRecord_snack_unit` 自定义增删）；奶茶口味用 `renderFlavorMultiselect(selected)`（tag 多选 + `customOpts_lifeRecord_milktea_flavors` 自定义增删）；复用人物关系 custom-option 增删模式。
- 历史视图 `renderLifeRecordHistory(typeKey)`：周一~周日 网格，顶部 `renderSleepWeekLineChart(days)`（SVG 折线 + 8h 虚线参考，x 坐标固定 5–95 防出界）或 `renderDietWeekStats(days)`（各餐次数柱状），底部 上一周/下一周。
- `renderLifeRecord()` 重写：pageState `{date,formType,editId,values,view:'home',weekOffset:0,subtype:null}`；view 为 `sleep-history`/`diet-history` 时路由到历史视图。
- 处理器：`lifeRecordToggleView`（点同一视图返回 home）、`lifeRecordHistoryPrevWeek/NextWeek`、`lifeRecordHistoryCellClick`（跳该日+subtype 表单）、`lifeRecOpenForm/Cancel/Edit/Delete/Save`、`addLifeRecordUnit/removeCheckedLifeRecordUnits`、`addLifeRecordFlavor/removeCheckedLifeRecordFlavors`。
- index.html 新增 `/* ===== Daily Record v245: screenshot layout ===== */` 大段 CSS（`.life-record-page` 浅灰底、`.lr-top-card`、`.sleep-ring*`、`.lr-top-stats`、`.lr-diet-btns/.lr-diet-qbtn.filled`、`.lr-toggle-row/.lr-toggle-btn.active`、`.lr-card*/.lr-subtype-box*`、`.lr-empty-row/.lr-record-row`、`.pill-group/.tag-group` 复选 pill、历史视图 `.lr-week-*` 等）及 `@media (max-width:640px)` 移动端覆盖。
- `app.js?v=244 → v245`、`sw.js CACHE = 'xiao-workbench-v245'`；`index.html` 引用 `app.js?v=245`。
- 四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。APK 暂不打包（待用户确认）。git commit `4eb6713` 本地完成；`git push` 因 GitHub Basic-auth 凭据交互挂起，需用户手动 push。

## v246（2026-08-11）
### 每日记录版块 v245 后续精修
1. **样式**：每日记录页模块间距/卡片阴影加强，顶部圆环放大到 104px，快捷按钮与切换按钮字号/内边距加大，模块分界更清晰；移动端同步微调。
2. **交互修复**：
   - 零食单位单选 pill 与奶茶口味多选 tag，点击后同步更新 `.selected` 视觉状态；自定义新增口味也带上点击事件。
   - 宵夜记录的时间标签从「吃饭时间」改为「享用时间」，与截图一致。
3. **版本**：`app.js?v=245 → v246`、`sw.js CACHE = 'xiao-workbench-v246'`；`index.html` 引用 `app.js?v=246`。
4. **校验/交付**：四文件同步 assets；两份 `app.js` `node --check` 通过；git commit `1afbe0f` 并 `git push origin main` 成功。APK 暂不打包。

## v247（2026-08-11）
### 每日记录版块按用户截图 1:1 复刻布局
1. **顶部睡眠统计卡片**：中间数据从单列竖排改为「入睡时间/清醒时间」「清醒次数/午休时长」左右两列，标签小字+数值大字横向排列。
2. **睡眠/饮食记录模块**：去掉子类型标题条，改为左侧大字号子类型名（夜晚/午间/早餐/午餐…），右侧小字号内容分两列展示；操作按钮（编辑/删除）紧跟右侧；与截图一致。
3. **每条子类型独立新增**：每个 `.lr-subtype-box` 右上角都有「+」按钮，宵夜/零食/奶茶均可单独增加条目，不再依赖卡片顶部唯一的新增按钮。
4. **历史查询顶部**：去掉「一周睡眠折线统计 / 一周饮食统计」标题框，改为纯色底卡片内大字标题+小字副标题两列；折线图/柱状图保留在卡片内。
5. **历史查询记录列表**：去掉周一~周日表格网格，改为和外面一样的 `.lr-card` 模块列表，按天排列，每天一个卡片，里面是各子类型记录（同首页样式）。
6. **版本**：`app.js?v=246 → v247`、`sw.js CACHE = 'xiao-workbench-v247'`；`index.html` 引用 `app.js?v=247`。
7. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `5dfaaa6` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，已终止进程并请用户手动 push。APK 暂不打包（待用户确认是否打 v1.1.22/versionCode 33）。

## v248（2026-08-11）
### 每日记录删除顶部日期栏
1. **移除首页日期选择区**：删除原来的 `📅 日期` 标签、日期输入框、「前一天/后一天」切换按钮，以及对应的 `.life-record-datebar` CSS。
2. **版本**：`app.js?v=247 → v248`、`sw.js CACHE = 'xiao-workbench-v248'`；`index.html` 引用 `app.js?v=248`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `9f52573` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

## v249（2026-08-11）
### 每日记录顶部卡片左右等分、统计单列竖排
1. **HTML 结构调整**：将睡眠圆环 + 统计文本包进 `.lr-top-left`，饮食快捷按钮单独 `.lr-diet-btns`。
2. **左右等分**：`.lr-top-left` 与 `.lr-diet-btns` 均 `flex:1`，中间用 `border-left` 分隔线，左右视觉宽度相等。
3. **统计单列竖排**：`.lr-top-stats` 改为 `flex-direction:column`，每个 `.lr-top-stat` 内标签小字在上、数值大字在下，与截图一致。
4. **圆环放大**：桌面端圆环从 104px 放大到 120px，字号同步加大；移动端适当缩小到 90px。
5. **版本**：`app.js?v=248 → v249`、`sw.js CACHE = 'xiao-workbench-v249'`；`index.html` 引用 `app.js?v=249`。
6. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `88ab568` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

## v250（2026-08-11）
### 每日记录顶部卡片细节修正（按反馈）
1. **分隔竖线顶格全高**：去掉 `.lr-diet-btns` 的 `border-left`，改为独立的 `.lr-top-divider` 元素，`align-self:stretch` + `margin:-16px 12px`（移动端 `-12px 8px`）抵消卡片内边距，竖线从卡片顶边直通底边，上下不留白。
2. **左侧文字右移**：`.lr-top-left` 改为 `align-items:flex-start` 且圆环与统计列间距从 16px 加到 20px（移动端 14px），统计文字相对圆环更靠右。
3. **圆环上移**：卡片与左列均 `align-items:flex-start`，圆环不再垂直居中，顶端对齐；圆环自身 `margin-top:-2px`（移动端 -1px）略微上提。
4. **状态小字与圆环拉开间距**：`renderSleepRing` 把状态小字移出绝对定位，包进 `.sleep-ring-col`（环 + 状态竖排），`.sleep-ring-status` 改用 `margin-top:14px`（移动端 10px），与圆形保持明显间隔。
5. **版本**：`app.js?v=249 → v250`、`sw.js CACHE = 'xiao-workbench-v250'`；`index.html` 引用 `app.js?v=250`。
6. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `1f0d930` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

#### 四、后续变更记录

**v251（2026-08-11）每日记录模块左右外边距与全站对齐**
1. **问题**：每日记录版块左右外边距明显大于其他模块（截图对比每日打卡页）。
2. **根因**：`.life-record-page` 自身又加了一层 `padding:8px`（移动端 `6px`），叠加全站 `.main-body` 已有的 `padding:16px`，导致每日记录模块左右实际留白 = 16+8 = 24px，而其他模块只有 16px。
3. **修复**：将 `.life-record-page` 的 `padding` 改为 `0`（桌面与移动端均改），模块左右外边距回归全站统一的 `main-body` 16px；卡片内部 padding 在 v250 已收紧（顶部卡 12px、卡片头/体 10-12px、子类型框 10-12px 等），保持。
4. **版本**：`app.js?v=250 → v251`、`sw.js CACHE = 'xiao-workbench-v251'`；`index.html` 引用 `app.js?v=251`。
5. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `02f636c` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

**v252（2026-08-11）每日记录顶部卡片与底部记录条样式微调**
1. **需求（用户逐条）**：① 顶部卡片左侧整体往右移；② 圆环下小字往上移一点；③ 左侧文字数字部分字号 16px；④ 右侧按钮整体居中；⑤ 底部记录卡片标签（夜晚/午间/早餐/午餐/晚餐/宵夜/零食/奶茶）水平居中；⑥ 加宽单个记录条高度；⑦ 宵夜/零食/奶茶有新增后「+」按钮保持原位（顶部右对齐）。
2. **修改（index.html CSS）**：`.lr-top-left` 加 `padding-left:10px`（移动端 `4px`）实现左侧整体右移；`.sleep-ring-status` 的 `margin-top` 由 `14px`→`8px`（移动端 `10px`→`5px`）使小字上移；`.lts-val` 字号 `18px`→`16px`（移动端 `15px`→`16px`）；`.lr-diet-btns` 改用 `grid-template-columns:repeat(2,minmax(78px,108px))` + `justify-content:center;align-content:center`（移动端 `minmax(60px,90px)`）使按钮块整体居中；`.lr-subtype-label` 加 `text-align:center` 使 8 个标签水平居中；`.lr-record-row` padding `8px 0`→`11px 0`、`.lr-empty-row` `6px 0`→`10px 0` 加高记录条；`.lr-subtype-add` 加 `align-self:flex-start` 确保多条目子类型新增后「+」始终贴顶部右对齐（原 `align-items:flex-start` 已满足，显式固化）。
3. **版本**：`app.js?v=251 → v252`、`sw.js CACHE = 'xiao-workbench-v252'`；`index.html` 引用 `app.js?v=252`。
4. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `cecc5fb` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

**v253（2026-08-11）每日记录顶部卡片数值放大 + 右侧按钮真正整体居中**
1. **问题（用户截图反馈）**：左侧统计数值字号仍显小；右侧 6 个饮食按钮在右列里偏上，底部空出一大块，没有「整体居中」。
2. **修改（index.html CSS）**：`.lts-val` 字号由 `16px`→`22px`（移动端 `20px`），让入睡/清醒时间、清醒次数、午休时长更醒目；`.lr-diet-btns` 在已有的 `justify-content:center`（水平居中）基础上，增加 `align-self:center`（垂直居中），使整个按钮块在顶部卡片右半区水平和垂直都居中，消掉底部空档；移动端同步增加 `align-self:center`。
3. **版本**：`app.js?v=252 → v253`、`sw.js CACHE = 'xiao-workbench-v253'`；`index.html` 引用 `app.js?v=253`。
4. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `0bd48f5` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

**v254（2026-08-11）每日记录顶部卡片左右真正平分**
1. **问题（用户截图反馈）**：顶部卡片看起来不是左右平分，中间分隔线明显偏右，左侧区域比右侧宽。
2. **修改（index.html CSS）**：
   - `.lr-top-left` 去掉仅向右推的 `padding-left:10px`，改为 `align-items:center; justify-content:center`，让左侧圆环+统计文字作为整体在左半区居中。
   - `.sleep-ring-col` 加 `flex-shrink:0` 避免圆环被压缩。
   - `.lr-top-stats` 改为 `flex:0 1 auto; justify-content:center`，不再强行 `flex:1` 撑开过宽。
   - `.lr-top-divider` 去掉两侧 margin，仅保留 `margin:-12px 0`，避免分割线本身占额外空间。
   - `.lr-diet-btns` 由固定 `minmax(78px,108px)` 的按钮块改为 `grid-template-columns: repeat(2, 1fr)`，并 `align-self:stretch; padding:0 14px`，让 6 个饮食按钮真正占满整个右半区，整体在右区水平+垂直居中；移动端同步 `padding:0 8px`。
3. **版本**：`app.js?v=253 → v254`、`sw.js CACHE = 'xiao-workbench-v254'`；`index.html` 引用 `app.js?v=254`。
4. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `7b764df` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

**v255（2026-08-11）每日记录顶部卡片：两个严格平均盒子 + 内容从左铺开**
1. **问题（用户反馈）**：v254 把左盒内容整体居中、右盒内容铺满，两边对齐逻辑不一致，视觉上中线偏、整体显歪。用户要求「先把顶部划分成两个平均的盒子再往里放内容」，且「不改内容」。
2. **修改（index.html CSS，仅结构调整不动数值/按钮样式）**：
   - `.lr-top-left` 与 `.lr-diet-btns` 均改为 `flex:1 1 0`（basis 0，严格平分可用宽度，不受内容影响），中间 `.lr-top-divider` 仅占 1px。
   - `.lr-top-left` 由 `justify-content:center` 改回 `flex-start`，让圆环+统计文字**从各自左盒左边界开始铺**；`.lr-top-stats` 由 `flex:0 1 auto` 改回 `flex:1 1 0` 撑满左盒剩余宽度，文字列左对齐贴到右边界——与右盒按钮从左铺到右的对齐逻辑一致，两侧对称不再显歪。
   - 数值字号 22px、右侧按钮铺满垂直居中、移动端布局均保持 v253/v254 不变。
3. **版本**：`app.js?v=254 → v255`、`sw.js CACHE = 'xiao-workbench-v255'`；`index.html` 引用 `app.js?v=255`。
4. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `9a90739` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

**v256（2026-08-11）每日记录顶部卡片：改用 grid 强制左右严格等宽**
1. **问题（用户截图反馈）**：即便 flex-basis 0，左右两盒看起来仍不平均，右侧明显更大。
2. **修改（index.html CSS）**：
   - 顶部卡片 `.lr-top-card` 由 `display:flex` 改为 `display:grid; grid-template-columns: 1fr 1px 1fr`，用 Grid 三列强制左、右各占 `1fr`（等宽），中间 1px 竖线单独一列。
   - `.lr-top-left` 去掉 `flex:1 1 0`，仅保留 flex 内部布局（圆环+文字列），并加 `overflow:hidden` 防止撑破网格。
   - `.lr-top-divider` 去掉 `flex-shrink:0`，宽度仍为 1px，由 grid 列宽控制。
   - `.lr-diet-btns` 去掉 `flex:1 1 0` 和 `align-self:stretch`，作为 grid 右列内容自然占满。
   - 移动端媒体查询同步去掉 flex 残留属性（`align-self:stretch`）。
3. **版本**：`app.js?v=255 → v256`、`sw.js CACHE = 'xiao-workbench-v256'`；`index.html` 引用 `app.js?v=256`。
4. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `3ad5f1f` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

**v257（2026-08-11）每日记录顶部卡片：圆环加大 + 数值30px + 右盒按钮对称加高**
1. **修改（index.html CSS，内容微调，grid 等宽盒子结构不变）**：
   - 左侧睡眠圆环 `.sleep-ring-wrap` 由 `120px×120px` 加大到 `144px×144px`（移动端 `84px→100px`）；**圆环内数字 `.sleep-ring-text` 保持 `26px`（移动端 `18px`）不变**。
   - 左侧统计数值 `.lts-val` 由 `22px` 提到 **`30px`**（移动端 `20px→24px`，避免手机端溢出）。
   - 右侧饮食按钮 `.lr-diet-qbtn` 垂直内边距由 `11px→16px`（移动端 `9px→13px`）**增高**；右盒 `.lr-diet-btns` 保留 `padding:0 14px`（移动端 `0 8px`），即按钮组左侧到分割线、右侧到外框**留白相等**。
2. **版本**：`app.js?v=256 → v257`、`sw.js CACHE = 'xiao-workbench-v257'`；`index.html` 引用 `app.js?v=257`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `c3e1c81` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

**v258（2026-08-11）每日记录顶部卡片：圆环/文字/按钮继续精调**
1. **修改（index.html CSS + app.js 渲染，grid 等宽盒子结构不变）**：
   - 左侧睡眠圆环 `.sleep-ring-wrap` 上移：`margin-top` 桌面 `-2px→-8px`、移动端 `-1px→-5px`。
   - 圆环内文字 `.sleep-ring-text` **取消加粗**：`font-weight:800→400`；其中 `h/m` 单位加 `<span class="dur-unit">` 并单独设为桌面 `13px`、移动端 `10px`（圆环内数字 `26px/18px` 不变）。
   - 左侧统计数值 `.lts-val` 由 `30px→28px`（移动端维持 `24px`）；其中 `h/m` 单位同样加 `<span class="dur-unit">`，桌面 `13px`、移动端 `11px`。
   - 右侧饮食按钮区域 `.lr-diet-btns` 改为固定列宽 `repeat(2,minmax(0,78px))`（移动端 `68px`），并加 `justify-content:center`，使按钮块在右半区**左右对称居中**；去掉 `padding`，避免之前 `padding:0 14px` 造成的两侧留白不均。
   - 按钮 `.lr-diet-qbtn` 继续加高：垂直内边距桌面 `16px→20px`、移动端 `13px→17px`。
2. **版本**：`app.js?v=257 → v258`、`sw.js CACHE = 'xiao-workbench-v258'`；`index.html` 引用 `app.js?v=258`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `4ded415` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

**v259（2026-08-11）每日记录顶部卡片：修正 grid 等宽与圆环/按钮细节**
1. **修改（index.html CSS）**：
   - 修复上一版 CSS 中 `.sleep-ring-text` / `.lts-val` 与其 `.dur-unit` 子规则被错误写成同一行（`\n` 字面字符导致选择器失效），改为真正独立两行。
   - 顶部卡片 Grid 列由 `1fr 1px 1fr` 改为 **`minmax(0,1fr) 1px minmax(0,1fr)`**，强制左右两列各占 50% 可用宽度（原 `1fr` 默认 `minmax(auto,1fr)` 会被左盒圆环内容撑大，导致右侧被压缩、边距不对称）。
   - 左侧睡眠圆环 `.sleep-ring-wrap` `margin-top` 桌面 `-8px→-20px`、移动端 `-5px→-12px`。
   - 圆环内文字 `.sleep-ring-text` `font-weight` 由 `400→600`；其中 `h/m` 单位桌面 `13px→8px`、移动端 `10px→7px`。
   - 右侧饮食按钮 `.lr-diet-btns` 固定列宽桌面 `78px→92px`、移动端 `68px→78px`，仍 `justify-content:center` 保持按钮块在右半区居中。
2. **版本**：`app.js?v=258 → v259`、`sw.js CACHE = 'xiao-workbench-v259'`；`index.html` 引用 `app.js?v=259`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `c2b5ee8` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

**v260（2026-08-11）每日记录顶部卡片：圆环贴顶与右侧边距真正对称**
1. **修改（index.html CSS）**：
   - 顶部卡片 `.lr-top-card` 左右内边距拆分到两列：整体 `padding:12px 10px` 改为 `padding:12px 0`，左列 `.lr-top-left` 加 `padding-left:10px`（移动端 `8px`），右列 `.lr-diet-btns` 加 `padding:0 10px`（移动端 `0 8px`）。这样左右两列内容到卡片外框的距离严格相等，避免之前卡片统一 padding 导致右侧额外多出 10px。
   - 圆环真正上移：之前负 margin 被 `.lr-top-left align-items:center` 抵消，改为给 `.sleep-ring-col` 加 `align-self:flex-start`，让圆环列贴顶；`.sleep-ring-wrap` 的 `margin-top` 由 `-20px→0`（移动端 `-12px→0`）。
   - 圆环内 `h/m` 单位字号桌面 `8px→12px`、移动端 `7px→10px`。
2. **版本**：`app.js?v=259 → v260`、`sw.js CACHE = 'xiao-workbench-v260'`；`index.html` 引用 `app.js?v=260`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `5bf0b5a` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

**v261（2026-08-12）每日记录顶部卡片：圆环下移、内数字加大、左数值加字重+字间距、按钮加宽**
1. **修改（index.html CSS）**：
   - 圆环整体下移：`.sleep-ring-wrap` 的 `margin-top` 由 `0→8px`（移动端 `0→6px`），让圆环不再贴顶。
   - 圆环内数字加大：`.sleep-ring-text` 桌面 `font-size 26px→30px`、移动端 `18px→22px`；`h/m` 单位 `.dur-unit` 维持 `12px / 10px` 不变。
   - 左侧文字数值部分：`.lts-val` 明确 `font-weight:700` 并新增 `letter-spacing:1px` 加一点字间距。
   - 右侧按钮加宽：`.lr-diet-btns` 单按钮宽 `minmax(0,92px)→minmax(0,108px)`（移动端 `78px→88px`），grid 仍两列居中。
2. **版本**：`app.js?v=260 → v261`、`sw.js CACHE = 'xiao-workbench-v261'`；`index.html` 引用 `app.js?v=261`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `47aa5b6` 本地完成；`git push` 仍受 GitHub Basic-auth 凭据交互挂起，需用户手动 push。APK 暂不打包。

**v262（2026-08-12）每日记录顶部卡片：左数值与圆环同字重、字距归零、右按钮文字加大**
1. **修改（index.html CSS）**：
   - 圆环内数字字重确认为 `600`（`.sleep-ring-text{font-weight:600}`）。
   - 左侧数值 `.lts-val`：字重由 `700` 改为 `600`（与圆环一致），`letter-spacing` 由 `1px` 改回 `0px`。
   - 右侧按钮文字加大：`.lr-diet-qbtn` 桌面 `font-size 14px→16px`、移动端 `13px→15px`。
2. **版本**：`app.js?v=261 → v262`、`sw.js CACHE = 'xiao-workbench-v262'`；`index.html` 引用 `app.js?v=262`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `bb0f5b5` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

**v263（2026-08-12）每日记录顶部卡片：进度线变细、圆环加大、内字号33/hm14、状态文案、整体右移、右按钮24px**
1. **修改**：
   - 圆环进度线改细：`.sleep-ring-bg` 与 `.sleep-ring-fill` 的 `stroke-width` 由 `8 → 6`。
   - 圆环加大：`.sleep-ring-wrap` 桌面 `144px → 160px`、移动端 `100px → 112px`（svg 等比缩放）。
   - 圆环内字号：`.sleep-ring-text` 桌面 `30px → 33px`、移动端 `22px → 26px`；`.dur-unit`（h/m）桌面 `12px → 14px`、移动端 `10px → 12px`。
   - 圆环下小字文案（app.js `renderSleepRing`）：`'睡眠时长达到8小时'/'睡眠时长不满8小时'` → `'睡眠达到目标'/'睡眠未达到目标'`。
   - 整体右移：`.sleep-ring-col` 新增 `margin-left:10px`。
   - 右侧按钮文字：`.lr-diet-qbtn` 桌面 `16px → 24px`、移动端 `15px → 20px`。
2. **版本**：`app.js?v=262 → v263`、`sw.js CACHE = 'xiao-workbench-v263'`；`index.html` 引用 `app.js?v=263`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `8b2d3c8` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

**v264（2026-08-12）顶卡修正：左移4px / h-m显差 / 状态文案8小时 / 右按钮字大盒不变**
1. **修改**：
   - 圆环左移收敛：`.sleep-ring-col` `margin-left` `10px → 4px`。
   - h/m 显差修复：`.sleep-ring-text .dur-unit` 桌面 `14px → 18px`、移动端 `12px → 15px`。原因：v263 同时把数字 30→33px 与单位 12→14px，二者比例未变（约40%）故肉眼无差；本次仅放大单位使其明显大于数字旁比例。
   - 状态文案（app.js `renderSleepRing`）：`'睡眠达到目标'/'睡眠未达到目标'` → `'睡眠达到8小时'/'睡眠未达到8小时'`。
   - 右侧按钮「只放大字、不动按钮盒」：`.lr-diet-qbtn` 保持 `font-size` 桌面 `24px` / 移动端 `20px`，但将垂直 `padding` 由 `20px→15px`（桌面）、`17px→14px`（移动端），使按钮高度回落到 v262 水平，仅文字变大、盒子不胀。
2. **版本**：`app.js?v=263 → v264`、`sw.js CACHE = 'xiao-workbench-v264'`；`index.html` 引用 `app.js?v=264`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `55fab75` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

**v265（2026-08-12）顶卡：状态字加大、圆环红、左标签同数字、右字重600、按钮底浅**
1. **修改**：
   - 圆环下小字加大：`.sleep-ring-status` 桌面 `11px → 14px`、移动端 `9px → 12px`。
   - 圆环颜色改红：app.js `renderSleepRing` 进度线 `color` 由 `'var(--c-green)'/'var(--c-primary)'` 改为固定 `'#620712'`（达到/未达均红）。
   - 左侧标签同数字：`.lts-label` 桌面 `12px → 28px`、移动端 `10px → 24px`（与 `.lts-val` 字号一致）。
   - 右侧文字字重同左数字：`.lr-diet-qbtn` `font-weight` `700 → 600`（左数字 `.lts-val` 为 600）。
   - 初始按钮底色改浅：`.lr-diet-qbtn` 背景由 `var(--c-primary-bg)`(#E0F2FF) 改为更浅 `#EFF7FF`。
2. **版本**：`app.js?v=264 → v265`、`sw.js CACHE = 'xiao-workbench-v265'`；`index.html` 引用 `app.js?v=265`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `6b408fe` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

### v266（2026-08-12）
1. **修改**：
   - 左侧"次"字同数字大：app.js `renderLifeRecordTopCard` 中清醒次数的"次"改为 `<span class="lts-unit">次</span>`；新增 `.lts-val .lts-unit` 桌面 `28px`、移动端 `24px`，与对应 `.lts-val` 数字同字号。
   - 左侧标签改回：`.lts-label` 桌面 `28px → 12px`、移动端 `24px → 10px`。
   - 右侧按钮底色改回记录条浅蓝：`.lr-diet-qbtn` 背景由 `#EFF7FF` 改回 `var(--c-primary-bg)`（与记录条等组件一致）。
   - 右侧按钮字重改 500：`.lr-diet-qbtn` `font-weight` `600 → 500`。
2. **版本**：`app.js?v=265 → v266`、`sw.js CACHE = 'xiao-workbench-v266'`；`index.html` 引用 `app.js?v=266`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `f931d44` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

### v267（2026-08-12）
1. **修改**：
   - 左侧"次"字重改 550：`.lts-val .lts-unit` 新增 `font-weight:550`（桌面/移动端均 550）。
   - 右侧按钮字重改 550：`.lr-diet-qbtn` `font-weight` `500 → 550`（桌面/移动端均 550）。
   - 颜色说明：用截图取色验证，右侧按钮中心与记录条空白均为 `#E0F2FF`，已和 `--c-primary-bg` 对齐；若仍有视觉差，主要是 1px 边框/阴影造成的边界反差。
2. **版本**：`app.js?v=266 → v267`、`sw.js CACHE = 'xiao-workbench-v267'`；`index.html` 引用 `app.js?v=267`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `3530240` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

### v268（2026-08-12）
1. **修改**：
   - 左侧"次"字重改回 500：`.lts-val .lts-unit` `font-weight` `550 → 500`。
   - 右侧按钮底色改对用户给的浅色：由 `var(--c-primary-bg)`（#E0F2FF）改为 **#F4FCFF**（与之前截图纯色一致）。
2. **版本**：`app.js?v=267 → v268`、`sw.js CACHE = 'xiao-workbench-v268'`；`index.html` 引用 `app.js?v=268`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `881168c` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

### v269（2026-08-12）
1. **修改**：
   - 左侧"次"字重改 520：`.lts-val .lts-unit` `font-weight` `500 → 520`。
   - 午休时长 h/m 字号加大：`.lts-val .dur-unit` 桌面 `13px → 16px`、移动端 `11px → 14px`。
   - 右侧已完成按钮：文字颜色由 `#fff` 改为 `var(--c-text)`（与未完成一致）；背景由 `var(--c-primary)` 改为 **#E0F2FF**（用户截图取色）。
2. **版本**：`app.js?v=268 → v269`、`sw.js CACHE = 'xiao-workbench-v269'`；`index.html` 引用 `app.js?v=269`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `f76957c` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

### v270（2026-08-12）
1. **修改**：
   - 左侧"次"字重同数字：`.lts-val .lts-unit` `font-weight` `520 → 600`。
   - 圆环缩小：`.sleep-ring-wrap` 桌面 `160px → 152px`、移动端 `112px → 106px`。
   - h/m 字号再加大（让肉眼明显）：圆环内 `.sleep-ring-text .dur-unit` 桌面 `18px → 22px`、移动端 `15px → 18px`；左侧 `.lts-val .dur-unit` 桌面 `16px → 18px`、移动端 `14px → 16px`。
   - 右侧文字颜色改为用户截图「累计打卡」数字蓝 `#a2cdf9`。
   - 已完成按钮底色改为用户截图卡片背景 `#e0f2ff`。
2. **版本**：`app.js?v=269 → v270`、`sw.js CACHE = 'xiao-workbench-v270'`；`index.html` 引用 `app.js?v=270`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `e0f4c42` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

### v271（2026-08-12）
1. **修改**：
   - 右侧文字颜色改回默认：`.lr-diet-qbtn`（及 hover/filled）`color` 由 `#a2cdf9` 退回 `var(--c-text)`。
   - 圆环内数字缩小：`.sleep-ring-text` 桌面 `33px → 28px`、移动端 `26px → 22px`。
   - 圆环下状态字缩小 1 号：`.sleep-ring-status` 桌面 `14px → 13px`、移动端 `12px → 11px`。
2. **版本**：`app.js?v=270 → v271`、`sw.js CACHE = 'xiao-workbench-v271'`；`index.html` 引用 `app.js?v=271`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `6f8c10f` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

### v272（2026-08-12）
1. **修改**：
   - 圆环改大：`.sleep-ring-wrap` 桌面 `152px → 158px`、移动端 `106px → 110px`。
   - 圆环下状态字颜色：`.sleep-ring-status` `color` 由 `var(--c-text-muted)` 改为 `var(--c-text)`（与数字同色）。
   - 左侧标题颜色：`.lts-label` `color` 由 `var(--c-text-light)` 改为 `var(--c-text)`（与数字同色）。
   - 右侧已完成按钮底色改蓝：`.lr-diet-qbtn.filled` 背景 `#e0f2ff → var(--c-primary-light)`（#BDE7FF），边框同步。
   - 鼠标移上底色：`.lr-diet-qbtn:hover` 背景改 `var(--c-primary-light) → #e0f2ff`（即 v271 的已完成底色）；新增 `.lr-diet-qbtn.filled:hover` 同样落到 `#e0f2ff`。
2. **版本**：`app.js?v=271 → v272`、`sw.js CACHE = 'xiao-workbench-v272'`；`index.html` 引用 `app.js?v=272`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `1939c12` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

### v273（2026-08-12）
1. **修改**：
   - 右侧已完成按钮底色：`.lr-diet-qbtn.filled` 背景 `var(--c-primary-light)`（#BDE7FF）→ **#D6F0FF**（用户反馈不要该浅蓝、要更浅一档）；边框同步 `#D6F0FF`。
2. **版本**：`app.js?v=272 → v273`、`sw.js CACHE = 'xiao-workbench-v273'`；`index.html` 引用 `app.js?v=273`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `14584cd` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

### v274（2026-08-12）
1. **修改**：
   - 任务1：每日记录右侧「已完成」底色：`.lr-diet-qbtn.filled` 背景/边框 `#D6F0FF` → **#e0f2ff**（用户要求改回）。
   - 任务2：每日打卡顶部卡片 `.life-quick-checkin` 底色 `var(--c-primary-bg)` → **#fff（白）**。
   - 任务2：打卡按钮 `.lqc-btn` 宽高样式对齐每日记录右侧按钮 `.lr-diet-qbtn`：border 1px `var(--c-border)`、radius `var(--radius-sm)`、背景 `#F4FCFF`、文字 `var(--c-text)`、`font-size:24px`、`font-weight:550`、`padding:15px 6px`；网格 `.lqc-grid` 改 `repeat(auto-fit,minmax(0,108px))` + 居中（等同 diet 列宽）；`.lqc-btn:hover` 与 `.lqc-btn.done` 均落到 `#e0f2ff`/边框 `#e0f2ff`（等同 diet 的 filled 态）；`.lqc-label` 字号 `12px→24px`、`font-weight:600→550`、加 `white-space:nowrap` 防换行增高。移动端同步：`.lqc-grid`→`repeat(auto-fit,minmax(0,88px))`、`.lqc-btn`/`.lqc-label`→`20px`/`550`。
2. **版本**：`app.js?v=273 → v274`、`sw.js CACHE = 'xiao-workbench-v274'`；`index.html` 引用 `app.js?v=274`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。git commit `8a89a3e` 本地完成；`git push` 仍挂起待用户手动。APK 暂不打包。

### v275（2026-08-12）
1. **修改**（每日打卡版块，共 8 项）：
   - 进度徽章三态着色：`.lqc-count` 新增 `.lqc-c0`（0/4 红 `#e53935`/底 `#fdecea`）、`.lqc-cpart`（有进度 橙 `#f57c00`/底 `#fff3e0`）、`.lqc-cfull`（4/4 绿 `#2e7d32`/底 `#e8f5e9`）；app.js `renderQuickCheckin` 按 `doneCount===0 / ===total / 其他` 动态加 class。
   - 打卡按钮平均分布：`.lqc-grid` 改 `grid-template-columns:repeat(auto-fit,minmax(80px,1fr))` + `gap:10px`，新增项自动换行到下一排（v274 的 `minmax(0,108px)` 居中方案废弃）。
   - 按钮文字缩短：`LIFE_CHECKIN_DEFS` 标签由「深空打卡/运动打卡/按摩打卡/吸尘打卡」改「深空/运动/按摩/吸尘」，去掉「打卡」二字。
   - 底部统计模块缩小：`.lgc-num` `20px→17px`、`.lgc-unit` `11px→10px`、`.lgc-stat` padding `10px→8px 4px`。
   - 底部模块左右边距加大：`.lgc-stats` 新增 `margin:0 4px`（配合 `.life-goal-card` 容器已留白）。
   - 移动端同步：`.lqc-grid` 改 `repeat(auto-fit,minmax(64px,1fr))` + `padding:0 8px`、`gap:6px`。
2. **版本**：`app.js?v=274 → v275`、`sw.js CACHE = 'xiao-workbench-v275'`；`index.html` 引用 `app.js?v=275`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。**APK 已打包 v1.1.22**（versionCode 33，189499 字节，脚本 `build_v1.1.22.sh`，交付 `D:\小筱工作台\小筱工作台-webview-v1.1.22.apk`，已签名验证通过）。git commit 本地完成；`git push` 仍挂起待用户手动。OPPO/ColorOS 16 安装须先卸载旧版 v1.1.21 再装新版。

### v276（2026-08-12）
1. **修改**（每日打卡版块微调，共 4 项）：
   - 底部模块名称恢复带「打卡」：`LIFE_CHECKIN_DEFS` label 由「深空/运动/按摩/吸尘」改回「深空打卡/运动打卡/按摩打卡/吸尘打卡」。
   - 顶部快速打卡按钮仍显示 2 字：`renderQuickCheckin` 渲染按钮时去掉 label 末尾「打卡」（`.replace(/打卡$/, '').slice(0,4)`），自定义项无「打卡」后缀时自动取前 4 字。
   - 底部模块卡片内容左右间距对齐每日记录记录条：`.life-goal-grid.compact .life-goal-card` padding `8px 10px → 10px 12px`（同 `.life-rec-item`）；`.lgc-stats` margin `0 4px → 0`，让内部统计区左右边距与卡片 padding 一致。
   - 周视图「本周」标题字号加大：`.lwm-title` `font-size:18px → 24px`。
2. **版本**：`app.js?v=275 → v276`、`sw.js CACHE = 'xiao-workbench-v276'`；`index.html` 引用 `app.js?v=276`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过（exit 0）。新增 `preview-phone.html`（手机效果桌面预览页）一并提交。git commit `c4238a0` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v277（2026-08-12）
1. **每日记录顶部卡片缩放与对齐**：圆环 `158px→120px`（桌面）/ `110px→92px`（移动）；左侧文字（`.lts-val` 等）缩小；右侧打卡按钮 `24px→20px`（桌面）/ `18px`（移动）。
2. **每日打卡顶部按钮对齐每日记录右侧按钮**：`.lqc-btn` 与 `.lr-diet-qbtn` 同尺寸（20/18px、同 padding），左缘对齐底部「深空打卡」文字左缘、右缘对齐夜晚条右缘。
3. **底部打卡模块卡片内容左右间距对齐每日记录记录条**：`.life-goal-card` 沿用 `10px 12px`；`.lgc-stats` margin 归零。底部统计模块（累计/最长/完成率）左缘对齐深空打卡文字左缘。
4. **新增三项打卡 + 零食联动**：`LIFE_CHECKIN_DEFS` 增 `早睡打卡(🌙,day)`、`零食打卡(🍟,day)`、`拉屎打卡(🚽,day)`；`syncSnackCheckin()` 实现每日记录填「零食」自动打卡、删零食记录自动取消；调用点 `lifeRecSave` / `lifeRecDelete`。
5. **周视图文案与字号**：「本周」→「本周概览」、`.lwm-title` `24px→20px`；「每日/每周」小字 →「每日打卡/每周打卡」。
2. **版本**：`app.js?v=276 → v277`、`sw.js CACHE = 'xiao-workbench-v277'`；`index.html` 引用 `app.js?v=277`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过。git commit `4e712b2` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22。

### v278（2026-08-12）
1. **打卡项重排 + 新增面膜打卡**：`LIFE_CHECKIN_DEFS` 顺序调整为「每日组：深空→运动→早睡→零食→拉屎；每周组：按摩→吸尘→面膜」，新增 `面膜打卡(🧖,week)`；约定**每日打卡优先级高于每周打卡，以后新增打卡项每日放每周前、周打卡项跟在现有周组后**。
2. **每周打卡顶部卡片橙色**：`renderQuickCheckin` 给 `period==='week'` 的项加 `.lqc-weekly` 类，未完成时橙色（`#fff3e0` 底 / `#ffcc80` 边 / `#f57c00` 字），完成后仍走浅蓝 `.done`。
3. **每日记录顶部卡片微调**：圆环描边加粗（`stroke-width 6→9`）；圆环内 `h/m` 单位缩小（`.sleep-ring-text .dur-unit` `18px→14px` 桌面 / `16px→12px` 移动）；左侧数字加粗（`.lts-val` `font-weight 600→700`）；右侧打卡按钮高度减少（`.lr-diet-qbtn` padding `13px→10px` 桌面 / `12px→9px` 移动）；顶部卡片左右边距相等（`.lr-top-card` `padding:12px 0 → 12px`）。
2. **版本**：`app.js?v=277 → v278`、`sw.js CACHE = 'xiao-workbench-v278'`；`index.html` 引用 `app.js?v=278`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过。git commit 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v279（2026-08-12）
1. **每日记录顶部卡片右侧按钮左右间距修复**：给 `.lr-diet-btns` 加 `width:100%;box-sizing:border-box`，让 grid 容器撑满右列、内部按钮居中，左右留白严格相等。
2. **每日记录顶部卡片圆环数字加大**：`.sleep-ring-text` `22px→26px`（桌面）、`18px→22px`（移动）。
3. **每日打卡顶部周打卡项颜色对齐底部模块**：未完成时文字颜色由橙色改回 `var(--c-text)`（与日打卡一致）；已完成时底色改为和底部模块按摩打卡「已完成周」统计卡片相同的 `rgba(250,140,22,.12)`，文字同为 `var(--c-text)`。
2. **版本**：`app.js?v=278 → v279`、`sw.js CACHE = 'xiao-workbench-v279'`；`index.html` 引用 `app.js?v=279`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过。git commit `86721ac` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v280（2026-08-12）
1. **每日记录圆环描边改回 7**：`.sleep-ring-bg` / `.sleep-ring-fill` 的 `stroke-width` 由 `9 → 7`（桌面/移动端均 7）。
2. **每日记录右侧按钮区域左右边距严格相等**：移动端 `.lr-diet-btns` 改为 `grid-template-columns: repeat(2, minmax(0, 1fr))`，按钮均分撑满右列；配合 `padding:0 8px`，左右留白严格相等，避免 `justify-content:center` 带来的 subpixel 偏移。
3. **每日打卡顶部周打卡项未完成底色更浅**：`.lqc-weekly:not(.done)` 底色 `#fff3e0 → #fff8e6`、边框 `#ffcc80 → #ffe0b2`，文字保持 `var(--c-text)`。
4. **每日打卡顶部周打卡项 hover 对齐已完成色**：`.lqc-weekly:not(.done):hover` 设为 `background/border: rgba(250,140,22,.12)`，与已完成周打卡底色一致。
2. **版本**：`app.js?v=279 → v280`、`sw.js CACHE = 'xiao-workbench-v280'`；`index.html` 引用 `app.js?v=280`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过。git commit `e052037` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v281（2026-08-12）
1. **每日记录右侧按钮区域真正撑满**：v280 的 `repeat(2, 1fr)` 在部分浏览器/视口下仍出现右侧留白远大于左侧的问题（用户截图右侧明显更宽）。
   - 桌面端 `.lr-diet-btns` 同样改为 `repeat(2, minmax(0, 1fr))`（不再固定 108px）。
   - 给 `.lr-diet-qbtn` 加 `box-sizing:border-box; width:100%`，确保每个按钮严格占满所属 grid cell，左右留白完全由容器 `padding:0 8px` 决定。
2. **版本**：`app.js?v=280 → v281`、`sw.js CACHE = 'xiao-workbench-v281'`；`index.html` 引用 `app.js?v=281`。
3. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过。git commit `9268f04` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v282（2026-08-12）
1. **修复预览页缓存导致 v281 样式不生效**：`preview-phone.html` 的 iframe 加载时自动附加时间戳 `index.html?_t=...`，并在 `onload` 后卸载 iframe 内旧 Service Worker，确保预览始终拉取最新 `index.html/app.js/sw.js`。
2. **右侧按钮区域再加 CSS 保险**：`.lr-diet-btns` 增加 `justify-items:stretch`；`.lr-diet-qbtn` 增加 `min-width:0`，确保 grid cell 内的按钮严格撑满，避免浏览器对 `width:100%` 在 shrink-to-fit 场景下的差异处理。
3. **版本**：`app.js?v=281 → v282`、`sw.js CACHE = 'xiao-workbench-v282'`；`index.html` 引用 `app.js?v=282`。
4. **校验/交付**：四文件同步 assets；两份 `app.js` 均 `node --check` 通过。git commit `e41efab` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v283（2026-08-12）
1. **修正每日记录顶卡左右边距不对称**：定位到 v278「等距」未完全达成——左列（圆环区）实际左边距 = 卡片 padding 12px + 圆环 `margin-left:4px` = 16px，右列（按钮区）右边距 = 卡片 12px + `.lr-diet-btns` `padding:0 8px` 的 8px = 20px，右侧比左侧宽 4px。
   - `.sleep-ring-col` 的 `margin-left:4px → 0`。
   - 桌面/移动端 `.lr-diet-btns` 的 `padding:0 8px → 0`，按钮区域左右留白统一由卡片 `padding:12px` 决定，左右严格相等（均为 12px）。
2. **预览页加一键强刷清缓存按钮**：`preview-phone.html` 工具栏/悬浮栏新增「🧹 强刷清缓存」按钮，点击时卸载 iframe 内 Service Worker 并清空其 Cache Storage，再以新时间戳重载，彻底解决「按浏览器刷新没用」的残留缓存问题。
3. **版本**：`app.js?v=282 → v283`、`sw.js CACHE = 'xiao-workbench-v283'`；`index.html` 引用 `app.js?v=283`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `1b66476` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v284（2026-08-12）修正顶卡左右内边距——恢复 4px 呼吸间距
1. **问题**：v283 把两侧内边距都压到 0，导致按钮区左侧直接贴中间分隔线，且按钮视觉上被加宽。
2. **修正**：让左右内边距相等（均为 16px），而非都归零。
   - `.sleep-ring-col` 的 `margin-left:0 → 4px`。
   - 桌面/移动端 `.lr-diet-btns` 的 `padding:0 → 0 4px`。
   - 最终：左侧 = 卡片 padding 12px + 圆环 margin-left 4px = 16px；右侧 = 卡片 padding 12px + 按钮区 padding-right 4px = 16px。两侧严格对称，同时保留 4px 呼吸间距避免贴线。
3. **版本**：`app.js?v=283 → v284`、`sw.js CACHE = 'xiao-workbench-v284'`；`index.html` 引用 `app.js?v=284`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `590a06d` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v285（2026-08-12）减窄按钮并恢复对称外边距
1. **问题**：v284 把按钮区 padding 设为 4px 后，用户反馈按钮还是太宽，且按钮区左侧到分隔线、右侧到外框的距离看起来仍不对称。
2. **修正**：把按钮区 padding 从 4px 加回 8px，使按钮变窄；同时把左侧圆环 margin-left 从 4px 加回 8px，保持左右两列外边距相等。
   - `.sleep-ring-col` 的 `margin-left:4px → 8px`。
   - 桌面/移动端 `.lr-diet-btns` 的 `padding:0 4px → 0 8px`。
   - 最终：左侧 = 卡片 padding 12px + 圆环 margin-left 8px = 20px；右侧 = 卡片 padding 12px + 按钮区 padding-right 8px = 20px。两侧严格对称，按钮宽度回到较窄状态。
3. **版本**：`app.js?v=284 → v285`、`sw.js CACHE = 'xiao-workbench-v285'`；`index.html` 引用 `app.js?v=285`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `a3baf6d` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v286（2026-08-12）左侧圆环区整体往左移
1. **问题**：用户反馈 v285 左侧整体仍偏右，且左右间距看起来不对称。
2. **修正**：将左侧圆环区 `margin-left` 从 8px 减到 4px，使左侧内容整体往左移动；保持按钮区 `padding:0 8px`，按钮宽度维持较窄状态。
   - `.sleep-ring-col` 的 `margin-left:8px → 4px`。
   - 桌面/移动端 `.lr-diet-btns` 保持 `padding:0 8px`。
   - 当前状态：左侧外边距 = 卡片 padding 12px + 圆环 margin-left 4px = 16px；右侧外边距 = 卡片 padding 12px + 按钮区 padding-right 8px = 20px。
3. **版本**：`app.js?v=285 → v286`、`sw.js CACHE = 'xiao-workbench-v286'`；`index.html` 引用 `app.js?v=286`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `4d66bf4` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v287（2026-08-12）同步加宽两侧间距并进一步减窄按钮
1. **问题**：用户反馈 v286 右侧仍比左侧宽，且按钮宽度还需减小。
2. **修正**：把左右两侧外边距同步加宽到 22px，同时让按钮进一步减窄。
   - `.sleep-ring-col` 的 `margin-left:4px → 10px`（左侧外边距 = 12 + 10 = 22px）。
   - 桌面/移动端 `.lr-diet-btns` 的 `padding:0 8px → 0 10px`（右侧外边距 = 12 + 10 = 22px，按钮宽度减小）。
   - 最终左右两侧外边距均为 22px，严格对称；按钮每侧收窄 2px。
3. **版本**：`app.js?v=286 → v287`、`sw.js CACHE = 'xiao-workbench-v287'`；`index.html` 引用 `app.js?v=287`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `53f18ff` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v288（2026-08-12）左侧不动，仅右侧减窄按钮且外边距=左侧
1. **需求**：用户明确「左边不动」，只调右边；并要求按钮更窄、同时右侧外边距仍与左侧相等（22px）。注意：若用「加大 .lr-diet-btns padding」减窄按钮，外边距会超过 22px，无法同时满足，故改用按钮自身收窄。
2. **修正**：
   - `.sleep-ring-col` 的 `margin-left:10px` **保持不变**（左侧外边距 22px 不动）。
   - 桌面/移动端 `.lr-diet-btns` 的 `padding:0 10px` **保持不变**（右侧外边距仍为 22px）。
   - `.lr-diet-qbtn` 的 `width:100% → 86%` 并加 `margin:0 auto` 居中，使按钮明显变窄，但容器外边距不增、仍与左侧相等。
3. **版本**：`app.js?v=287 → v288`、`sw.js CACHE = 'xiao-workbench-v288'`；`index.html` 引用 `app.js?v=288`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `7333326` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v289（2026-08-12）回调按钮贴边，减小 gap，左移圆环区
1. **需求**：用户反馈 v288 按钮中间隔得太开，要像之前截图那样按钮 100% 宽、左右边距相等；同时左侧圆环区 `margin-left` 改为 6px。
2. **修正**：
   - `.sleep-ring-col` 的 `margin-left:10px → 6px`（左侧整体往左移，左侧外边距 = 12 + 6 = 18px）。
   - 桌面/移动端 `.lr-diet-btns` 的 `padding:0 10px → 0 6px`（右侧外边距 = 12 + 6 = 18px，与左侧相等）。
   - `.lr-diet-qbtn` 的 `width:86% → 100%`，去掉 `margin:0 auto`，回到按钮贴边效果。
   - 桌面端 `.lr-diet-btns` 的 `gap:10px → 8px`，移动端保持 6px，让按钮不要隔太开。
3. **版本**：`app.js?v=288 → v289`、`sw.js CACHE = 'xiao-workbench-v289'`；`index.html` 引用 `app.js?v=289`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `217cf8a` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v290（2026-08-12）左移圆环区并确认按钮宽度
1. **需求**：用户要求左侧圆环区 `margin-left` 改为 4px，并确认右侧按钮宽度配置。
2. **修正**：
   - `.sleep-ring-col` 的 `margin-left:6px → 4px`（左侧整体再往左移；左侧外边距 = 12 + 4 = 16px）。
   - 右侧按钮宽度仍为 `100%`（贴边，即每个按钮占满所在 grid 列宽），`.lr-diet-btns` 的 `padding:0 6px` 不变（右侧外边距 = 18px）。注意：此时左侧 16px、右侧 18px，右侧比左侧宽 2px，需用户确认是否要把右侧 padding 同步减到 `0 4px` 以保持对称。
3. **版本**：`app.js?v=289 → v290`、`sw.js CACHE = 'xiao-workbench-v290'`；`index.html` 引用 `app.js?v=290`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `381ffae` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v291（2026-08-12）右侧按钮缩窄
1. **需求**：用户要求右侧饮食按钮缩窄。
2. **修正**：
   - 桌面/移动端 `.lr-diet-qbtn` 的 `width:100% → 84%` 并加 `margin:0 auto` 居中，按钮明显变窄；容器 `.lr-diet-btns` 的 `padding:0 6px` 保持不变（右侧外边距仍 18px，未靠加大 padding 缩窄，避免外边距变宽）。
   - 桌面端 `.lr-diet-btns` 的 `gap:8px → 6px`、移动端 `gap:6px → 5px`，让按钮之间不要隔太开。
   - 注意：左侧圆环区 `margin-left:4px`（外边距 16px），右侧 18px，左右仍差 2px，待用户确认是否要对称。
3. **版本**：`app.js?v=290 → v291`、`sw.js CACHE = 'xiao-workbench-v291'`；`index.html` 引用 `app.js?v=291`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `88fd80a` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v292（2026-08-12）按钮缩窄但中间间距不加大
1. **需求**：用户要求右侧饮食按钮缩窄，但中间间距不要加大。v291 把按钮设成 `width:84%` + `margin:0 auto`，导致每个按钮两侧都空出留白，中间间距 = gap + 两侧留白，视觉上隔得很开。
2. **修正**：
   - 桌面/移动端 `.lr-diet-qbtn` 改回 `width:100%` 并去掉居中 margin，按钮贴边不留白。
   - `.lr-diet-btns` 的 `padding:0 6px → 0 14px`，靠容器内边距压缩内部可用空间，使按钮变窄。
   - 桌面/移动端 `.lr-diet-btns` 的 `gap` 分别收紧到 `4px`，中间间距只由 gap 决定，不会额外加大。
   - 左侧 `.sleep-ring-col` 的 `margin-left:4px → 14px`，使左右外侧边距均为 26px，保持对称。
3. **版本**：`app.js?v=291 → v292`、`sw.js CACHE = 'xiao-workbench-v292'`；`index.html` 引用 `app.js?v=292`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `8af7e5f` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v293（2026-08-12）收窄左右外侧边距
1. **需求**：用户反馈 v292 右侧还是宽了，外侧留白太多。
2. **修正**：
   - `.sleep-ring-col` 的 `margin-left:14px → 8px`，左侧外边距从 26px 收回到 20px。
   - `.lr-diet-btns` 的 `padding:0 14px → 0 8px`，右侧外边距从 26px 收回到 20px，与左侧对称。
   - 按钮水平 padding 收紧：桌面 `10px 6px → 10px 2px`、移动 `9px 4px → 9px 2px`，让按钮视觉上更窄。
   - `gap` 保持 `4px`，中间间距不加大。
3. **版本**：`app.js?v=292 → v293`、`sw.js CACHE = 'xiao-workbench-v293'`；`index.html` 引用 `app.js?v=293`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `d440fda` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v294（2026-08-12）回退到v289满意状态
1. **需求**：用户说"改回去"，要求回到截图2认可的满意状态（即 v289）。
2. **修正**：回退 v292/v293 的缩窄尝试——
   - `.sleep-ring-col` 的 `margin-left:8px → 6px`
   - `.lr-diet-btns` 的 `padding:0 8px → 0 6px`，`gap` 恢复 `8px`（桌面）/ `6px`（移动）
   - `.lr-diet-qbtn` 水平 padding 恢复 `10px 6px`（桌面）/ `9px 4px`（移动），`width` 保持 `100%` 贴边
   - 左右外侧边距恢复为 18px，对称。
3. **版本**：`app.js?v=293 → v294`、`sw.js CACHE = 'xiao-workbench-v294'`；`index.html` 引用 `app.js?v=294`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `fb6caee` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v295（2026-08-12）回退到v292状态
1. **需求**：用户要求"改回v292"（即撤销 v294 对 v289 的回退）。
2. **修正**：回退 v294 到 v292 视觉——`.sleep-ring-col` 的 `margin-left:6px → 14px`；`.lr-diet-btns` 的 `padding:0 6px → 0 14px`、`gap` 恢复 `4px`（桌面/移动一致）；按钮水平 padding 保持 `10px 6px`（桌面）/ `9px 4px`（移动），`width` 保持 `100%` 贴边。
3. **版本**：`app.js?v=294 → v295`、`sw.js CACHE = 'xiao-workbench-v295'`；`index.html` 引用 `app.js?v=295`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `8ba81f2` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v296（2026-08-12）左侧圆环区 margin-left 改 4px
1. **需求**：用户要求左侧圆环区 `margin-left:4px`。
2. **修正**：`.sleep-ring-col` 的 `margin-left:14px → 4px`。左侧外边距变为 16px；右侧按钮区 `padding:0 14px` 外边距仍为 26px，左右暂不对称（用户仅要求动左侧，右侧未提）。
3. **版本**：`app.js?v=295 → v296`、`sw.js CACHE = 'xiao-workbench-v296'`；`index.html` 引用 `app.js?v=296`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `ba5a9c0` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v297（2026-08-12）顶卡左右对称：两侧均 margin-left:3px
1. **需求**：左侧圆环区 `margin-left:3px`；右侧饮食按钮区也改为 `margin-left:3px`（与左侧对称）。
2. **修正**：
   - 桌面 `.sleep-ring-col` 的 `margin-left:4px → 3px`；
   - 桌面 `.lr-diet-btns` 的 `padding:0 14px → margin-left:3px;padding:0`（左间隙 14px → 3px，与左侧一致）；
   - 移动端 `.lr-diet-btns` 同步 `padding:0 14px → margin-left:3px;padding:0`。
   - 结果：左右两侧内容距各自列左缘均为 3px，顶卡左右对称。
3. **版本**：`app.js?v=296 → v297`、`sw.js CACHE = 'xiao-workbench-v297'`；`index.html` 引用 `app.js?v=297`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `6834127` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v298（2026-08-12 22:21 后）修正：恢复右侧按钮宽度，仅调左间隙到 3px
1. **问题**：v297 把 `.lr-diet-btns` 由 `padding:0 14px` 改为 `margin-left:3px;padding:0`，因按钮是 `width:100%`，等于把右侧按钮拉宽（约多 25px），违背用户"调间距不要动按钮宽度"的要求。
2. **修正**：恢复按钮像素宽度不变——左右内边距之和保持 28px（与 `padding:0 14px` 等效），仅把左内边距 `14px → 3px`、右内边距 `14px → 25px`：`padding:0 25px 0 3px`（桌面+移动）。左侧圆环区 `.sleep-ring-col` `margin-left:3px` 不变。结果：右侧左间隙=3px 与左侧对齐，按钮宽度与 v296 完全一致。
3. **版本**：`app.js?v=297 → v298`、`sw.js CACHE = 'xiao-workbench-v298'`；`index.html` 引用 `app.js?v=298`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `df90c89` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v299（2026-08-12 23:33 后）修复：预览页 iframe 空白
1. **问题**：用户反馈 `preview-phone.html` 内 iframe 内容不显示（全白），点「强刷」也无效。
2. **根因**：原预览页 iframe 初始 `src="index.html?_t="`（空时间戳），且强刷仅尝试从 iframe 内部清除 service worker；若 iframe 尚未加载或 SW 缓存异常，`getRegistrations()` 逻辑可能卡住，导致 iframe 始终空白。
3. **修复**：
   - iframe 初始 `src` 改为 `about:blank`，避免加载任何带缓存风险的资源。
   - `hardReloadApp()` 改为 async，同时从**父窗口**和 **iframe 窗口**清理 `serviceWorker` 注册与 `caches`，再设置带时间戳的 `index.html?_t=...`。
   - 页面加载时直接调用 `hardReloadApp()`，确保首次打开就绕过旧缓存。
4. **版本**：`app.js?v=298 → v299`、`sw.js CACHE = 'xiao-workbench-v299'`；`preview-phone.html` 同步更新。
5. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `af57649` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v300（2026-08-12 23:44 后）修复：预览页 iframe 仍空白
1. **问题**：v299 修复后，用户反馈预览页 iframe 仍空白（截图显示加载状态无内容）。
2. **根因**：service worker 在 `file://` 协议下行为不稳定，可能缓存了异常响应；原 preview-phone.html 仅清除缓存但仍让 iframe 正常注册 SW，导致反复空白。
3. **修复**：
   - `index.html` 检测到 URL 含 `preview=1` 时**完全跳过 service worker 注册**。
   - `preview-phone.html` 加载 `index.html?_t=...&preview=1`，从源头避免预览模式下的 SW 干扰。
   - 新增加载状态条与 iframe 错误透传：index.html 内嵌脚本把 `error` / `unhandledrejection` / `console.log` 通过 `postMessage` 发到 preview 父页，父页实时显示状态或报错。
4. **版本**：`app.js?v=299 → v300`、`sw.js CACHE = 'xiao-workbench-v300'`；`preview-phone.html` 同步更新。
5. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `5c67717` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v301（2026-08-12 23:57 后）每日记录右侧按钮回退到 v274
1. **需求**：用户要求每日记录顶部卡片右侧饮食按钮改回 v274 版本外观。
2. **回退内容**（取自 git 提交 `8a89a3e` 的真实 CSS）：
   - 桌面 `.lr-diet-btns`：`repeat(2,minmax(0,1fr))` + `width:100%` + `justify-items:stretch` + `padding:0 25px 0 3px` → **`repeat(2,minmax(0,108px))` + `justify-content:center` + `padding:0 10px`**（按钮固定列宽、在右列居中，不再撑满）。
   - 移动 `.lr-diet-btns`：`repeat(2,minmax(0,1fr))` + `padding:0 25px 0 3px` → **`repeat(2,minmax(0,88px))` + `padding:0 8px` + `justify-content:center`**（gap 4px→6px）。
   - 桌面 `.lr-diet-qbtn`：`font-size:20px;padding:10px 6px;box-sizing:border-box;width:100%` → **`font-size:24px;padding:15px 6px`**（去掉 width:100%）。
   - 移动 `.lr-diet-qbtn`：`font-size:18px;padding:9px 4px` → **`font-size:20px;padding:14px 4px`**。
   - 左侧 `.sleep-ring-col` `margin-left:3px` 保持不变（用户仅要求回退右侧按钮）。
   - `.lr-diet-qbtn.filled` 已是 `#e0f2ff`（v274 当年引入），无需改动。
3. **版本**：`app.js?v=300 → v301`、`sw.js CACHE = 'xiao-workbench-v301'`；`index.html` 引用 `app.js?v=301`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `d112422` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。


### v302（2026-08-13 00:15 后）每日记录右侧盒子重做：等边距+2列铺满+单个按钮
1. **需求**：用户确认左侧盒子已固定（`.sleep-ring-col margin-left:3px`）后续勿动；右侧盒子改为「上下左右等边距」，放弃之前的固定列宽居中布局，按钮改单个、2 个一排、共 3 排（6 个饮食子类型：早餐/午餐/晚餐/宵夜/零食/奶茶）。
2. **改动（仅右侧，左侧未动）**：
   - 桌面 `.lr-diet-btns`：`repeat(2,minmax(0,108px))`+`justify-content:center`+`padding:0 10px` → `repeat(2,1fr)`+`padding:12px`（等边距）+`width:100%;box-sizing:border-box`，gap 10px。
   - 移动 `.lr-diet-btns`：`repeat(2,minmax(0,88px))`+`padding:0 8px` → `repeat(2,1fr)`+`padding:10px`（等边距），gap 8px。
   - 桌面 `.lr-diet-qbtn`：去固定列宽，加 `box-sizing:border-box;width:100%`（填满格子），`font-size:24px→20px;padding:15px 6px→14px 6px`。
   - 移动 `.lr-diet-qbtn`：加 `box-sizing:border-box;width:100%`，`font-size:20px→18px;padding:14px 4px→11px 4px`。
   - `.lr-diet-qbtn.filled` 保持 `#e0f2ff` 不变。
3. **版本**：`app.js?v=301 → v302`、`sw.js CACHE = 'xiao-workbench-v302'`；`index.html` 引用 `app.js?v=302`。
4. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `1c96773` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。


### v304（2026-08-13 01:00 后）修复：预览页 file:// 缓存导致手机框丢失与 iframe 白屏
1. **问题**：用户截图显示 preview-phone.html 仍是旧版（toolbar 只有 3 个机型按钮且显示「显示手机框」，与当前文件 4 个机型按钮/「隐藏手机框」不符），iframe 白屏且处于全屏模式，内容窄。根因是浏览器用 `file://` 打开本地文件时顽固缓存旧版 preview-phone.html。
2. **修复**：
   - `preview-phone.html` 脚本开头默认强制移除 `content-only`，确保每次打开都是手机框模式。
   - `file://` 协议下若 URL 没有 `_refreshed=1`，自动 `location.replace` 到带 `_refreshed=1&_t=...` 的新 URL，尝试绕过缓存。
   - 全屏模式 `.phone` 的 `max-width:520px` 改为 `max-width:none`，全屏时内容真正撑满窗口。
3. **版本**：`app.js?v=303 → v304`、`sw.js CACHE = 'xiao-workbench-v304'`；`preview-phone.html` 内 `APP_VERSION=304`，iframe 加载 `index.html?preview=1&v=304&_t=...`。
4. **交付**：将 `workstation/` 部署到 CloudStudio，获得稳定线上预览链接，避免用户继续受 file:// 缓存困扰。
5. **校验/交付**：四文件同步 assets；`app.js` `node --check` 通过。git commit `ac34547` 本地完成；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。


### v305（2026-08-13 02:00 后）修复预览页：手机框内白屏「什么都没有」+ 去框后全屏过窄
1. **问题**：用户截图反馈 preview-phone.html 两处异常——① 手机框模式内整片空白「什么都没有」；② 点「隐藏手机框」全屏后内容很窄「去掉手机框是窄的」。
2. **根因**：
   - ① 白屏：本地 `file://` 打开时被浏览器顽固缓存旧版 preview-phone.html / index.html，且 `file://` 下 Service Worker 与缓存策略易异常，iframe 取到的仍是旧/空版本。
   - ② 过窄：全屏 CSS 的 `.phone` 用 `width:100% !important`（100% 是「居中 flex 子元素」的宽而非视口宽）且 `height:auto`，导致去框后内容仍窄、且不撑满。
3. **修复（preview-phone.html）**：
   - 全屏改 `width:100vw !important;max-width:none;height:100vh !important`；`.phone-wrap` 加 `width:100% !important`；`setSize()` 在 content-only 时直接 return 不再覆盖宽度；切换按钮进入全屏时清空 `phone.style.width/height` 让 CSS 接管。
   - 新增 `preview-ready` 消息处理（子页就绪即清超时、显示「应用已加载」）；iframe 加 `allow="fullscreen"`。
   - 加 5 秒加载超时诊断：超时提示「点强刷清缓存重试」并建议用线上链接；`hardReload` 先 `unregister` SW + 清 `caches` 再重载 `index.html?preview=1&v=305&_t=...`。
   - `APP_VERSION` 304→305。
4. **配套（index.html / sw.js）**：index.html 预览 IIFE 在 DOM 就绪后 `postMessage('preview-ready')` 给父页；`app.js?v=304→v305`；`sw.js CACHE='xiao-workbench-v305'`。
5. **交付**：`workstation/` 重新部署 CloudStudio（入口 index.html，`/preview-phone.html`），v305 线上链接稳定可用，绕开 file:// 缓存。四文件同步 assets；`app.js` `node --check` 通过。
6. **校验/交付**：git 待提交（index.html / preview-phone.html / sw.js 已改）；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。


### v306（2026-08-13 02:04 后）修正预览页全屏模式：固定机型宽度 + 隐藏顶部 toolbar
1. **问题**：用户反馈「隐藏手机框后尺寸不是应该固定吗？不固定跟之前的 index.html 预览有什么区别，而且上面的依旧挡住了」。v305 的全屏模式把内容拉伸到 `100vw`，且顶部机型/切换 toolbar 仍然显示，遮挡应用内容。
2. **根因**：v305 把「全屏」理解成了「铺满视口」，但实际上用户想要的是「去掉手机框后仍然保持该机型固定宽度、且不显示预览工具栏」的纯净预览。
3. **修复（preview-phone.html）**：
   - 新增 CSS 变量 `--phone-width`，默认 412px；切换机型时同步更新该变量。
   - 全屏模式 `.phone` 宽度由 `100vw` 改为 `var(--phone-width)`（固定为当前机型宽），高度 `100vh`；`.phone-wrap` 宽度 auto、由 flex 居中。
   - 全屏模式隐藏 `.header`（机型选择、隐藏手机框按钮、hint 等全部隐藏），并隐藏仿真 `.statusbar` 与 `.notch`。
   - 新增浮动按钮 `#exitFrame`（默认隐藏，全屏时显示在右上角「📱 显示手机框」），点击或按 `Esc` 可切回手机框模式。
   - `setSize()` 不再在 content-only 时 return，确保全屏下切换机型也能即时改变固定宽度。
   - `APP_VERSION` 305→306；iframe 加载 `index.html?preview=1&v=306&_t=...`。
4. **配套**：应用核心版本保持 v305（`app.js?v=305`、`sw.js CACHE='xiao-workbench-v305'` 不变）；未同步 assets（preview-phone.html 不属于 APK）。
5. **交付**：重新部署 CloudStudio（同沙盒 036c0ec20b0f490ab25f63b72a3168ef，入口 index.html，`/preview-phone.html`）。
6. **校验/交付**：`node --check app.js` 通过；git 待提交（preview-phone.html / 版本历史.md 已改）；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22。


### v307（2026-08-13 02:20 后）每日记录顶卡右侧按钮：宽度缩窄 + 高度加高
1. **需求**：用户要求「每日记录顶部卡片右侧按钮宽度缩窄，高度加高」。
2. **改动（仅右侧按钮，左盒 .sleep-ring-col margin-left:3px 未动）**：
   - 桌面 `.lr-diet-btns`：`grid-template-columns:repeat(2,1fr)` → `repeat(2,minmax(0,100px))`，并加 `justify-content:center`（列宽收窄并整体居中，按钮不再占满半卡片宽）。
   - 桌面 `.lr-diet-qbtn`：纵向 padding `14px 6px` → `20px 6px`（更高）；`width:100%` 保持填满 100px 列。
   - 移动 `.lr-diet-btns`：`repeat(2,1fr)` → `repeat(2,minmax(0,84px))` + `justify-content:center`。
   - 移动 `.lr-diet-qbtn`：纵向 padding `11px 4px` → `16px 4px`（更高）。
   - 字号不变（桌面 20px / 移动 18px）；`.filled` 态 `#e0f2ff` 不变。
3. **版本**：应用核心 v305→v306（`app.js?v=305→v306`、`sw.js CACHE='xiao-workbench-v305'→'v306'`）；`index.html` 引用 `app.js?v=306`。preview-phone.html `APP_VERSION=306→307`（iframe 重新拉取新 index.html）。
4. **交付**：四文件同步 assets（`index.html`/`app.js`/`sw.js`/`manifest.json`）；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交（index.html / sw.js / preview-phone.html / 版本历史.md 已改）；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v308（2026-08-13 02:37 后）每日记录顶卡：清空内容只留空白盒子
1. **需求**：用户要求「每日记录顶部卡片内容全部删除，留一个空白的盒子」。
2. **改动**：`renderLifeRecordTopCard()`（app.js）改为仅返回 `<div class="lr-top-card"></div>`，移除左侧睡眠环+统计、分隔线、右侧 6 个饮食按钮的全部渲染。卡片仍保留边框/内边距样式，呈现为空白盒子。
3. **版本**：应用核心 v306→v307（`app.js?v=306→v307`、`sw.js CACHE='xiao-workbench-v306'→'v307'`）；preview-phone.html `APP_VERSION=307→308`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v309（2026-08-13 02:48 后）每日记录顶卡 1:1 复刻截图布局
1. **需求**：用户截图要求「严格按照这个布局样式1:1复刻。底部白色模块宽度不变，左右两侧分为两个盒子平均分布，盒子内固定好内边距再做内容」。截图特征：浅蓝背景卡片，左侧大紫色睡眠环+「睡眠达到8小时」+右侧入睡/清醒/清醒次数/午休统计，右侧2列×3排浅蓝圆角按钮（早餐/午餐/晚餐/宵夜/零食/奶茶）。
2. **改动**：
   - `renderLifeRecordTopCard()` 恢复渲染：左侧 `.lr-ring-col`（睡眠环+状态）与 `.lr-top-stats`（4 项统计）并排，右侧 `.lr-diet-btns` 6 个饮食按钮。
   - 卡片背景 `#EDF8FF`；布局改为 `grid-template-columns:1fr 1fr`（左右均分），移除旧的分隔线列。
   - 左右两盒分别固定内边距（桌面 16px / 移动 10px）。
   - 睡眠环轨道色 `#DDEAF3`，进度/状态文字 `#7B2D4D`；环尺寸不变（桌面 120px / 移动 86px）。
   - 右侧按钮：2 列等分 `repeat(2,1fr)`，背景 `#D6EBFF`，无边框，圆角 12px（移动 10px），字号/高度微调以匹配截图。
3. **版本**：应用核心 v307→v308（`app.js?v=307→v308`、`sw.js CACHE='xiao-workbench-v307'→'v308'`）；preview-phone.html `APP_VERSION=308→309`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交（app.js / index.html / sw.js / preview-phone.html / 版本历史.md 已改）；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v310（2026-08-13 03:08 后）每日记录顶卡微调：分隔线 + 紫环加大粗细7 + 按钮配色修正
1. **需求**：中间加分隔线；左侧圆环加大、粗细7、往左往上移动；圆环底部文字单行；睡眠≥8h 显示「睡眠达到8小时」、否则「睡眠未达到8小时」；左侧数字字号加大；右侧已完成与 hover 底色改 `#e0f2ff`、未完成底色 `#F4FCFF`、文字字重500加粗。
2. **改动**：
   - 卡片布局改回 `grid-template-columns:1fr 1px 1fr`，新增 `.lr-top-divider`（1px 浅线，移动端 `margin:-10px 0` 撑满）。
   - 睡眠环加大：桌面 `.sleep-ring-wrap` 120→140px、移动 86→92px；轨道与进度 `stroke-width` 8→7。
   - 左盒 `.lr-top-left` 对齐改 `align-items:flex-start;justify-content:flex-start`（环往左往上靠）。
   - `.sleep-ring-status` 保持 `white-space:nowrap`（单行）；状态文案逻辑不变（`total>=8` → 达到，否则 未达到）。
   - `.lts-val` 字号桌面 22→24px、移动 18→20px（数字加大）。
   - 右侧按钮：未完成背景 `#D6EBFF`→`#F4FCFF`；已完成 `.filled` 与 `:hover` 底色均 `#e0f2ff`；文字 `font-weight:500` 加粗保持。
3. **版本**：应用核心 v308→v309（`app.js?v=308→v309`、`sw.js CACHE='xiao-workbench-v308'→'v309'`）；preview-phone.html `APP_VERSION=309→310`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v311（2026-08-13 03:18 后）每日记录顶卡：恢复左右两盒均分、分隔线改左盒右边框、底色变白
1. **需求**：用户反馈 v310 右侧按钮「飞了」，要求改回左右两个盒子；卡片底色改成白色。
2. **改动**：
   - 卡片布局恢复为 `grid-template-columns:1fr 1fr`（左右两盒平均分布），移除 v310 的 `1px 1fr` 分隔线列，改为给左侧 `.lr-top-left` 加 `border-right:1px solid var(--c-border-light)`，避免分隔线列破坏 grid 宽度分配。
   - 卡片背景由 `#EDF8FF` 改为 `#FFFFFF`（纯白）。
   - 左右两个 grid 子项均加 `min-width:0`，防止右侧按钮因内容溢出被挤到左侧上方。
   - 桌面内边距保持左/右各 16px；移动左/右各 10px；紫环 140px/86px、状态单行、数字加大、按钮未完成 `#F4FCFF`/已完成+hover `#e0f2ff` 等 v310 需求保留。
3. **版本**：应用核心 v309→v310（`app.js?v=309→v310`、`sw.js CACHE='xiao-workbench-v309'→'v310'`）；preview-phone.html `APP_VERSION=310→311`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v312（2026-08-13 03:32 后）每日打卡按钮去外圈细线 + 每日记录圆环加大左移、右侧按钮间距缩窄文字加粗
1. **需求**：
   - ① 每日打卡顶部卡片按钮（`.lqc-btn`）未完成时外圈细线去掉。
   - ② 每日记录左侧圆环加大、往左移；圆环下小字保持单行（一条）；右侧按钮间距缩窄、文字加大加粗。
2. **改动**：
   - 每日打卡按钮 `.lqc-btn` 由 `border:1px solid var(--c-border)` 改为 `border:none`（已完成/hover 的 `border-color` 在无边框时无效，干净无细线）。
   - 每日记录左盒 `.lr-top-left` 左边距减小（`padding:16px 16px 16px 8px`，移动 `10px 10px 10px 4px`），环往左靠；睡眠环尺寸桌面 140→160px、移动 86→100px。
   - 圆环下状态 `.sleep-ring-status` 保持 `white-space:nowrap`（单行一条）。
   - 右侧 `.lr-diet-btns` 间距 `gap` 桌面 12→8px、移动 8→6px；`.lr-diet-qbtn` 字号桌面 20→22px、移动 16→18px，`font-weight` 由 500 加粗到 700。
3. **版本**：应用核心 v310→v311（`app.js?v=310→v311`、`sw.js CACHE='xiao-workbench-v310'→'v311'`）；preview-phone.html `APP_VERSION=311→312`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v313（2026-08-13 19:22 后）每日记录圆环下文字去重 + 右侧按钮间距再缩窄
1. **需求**：
   - ① 每日记录圆环下文字出现两行「睡眠达到8小时」重复，要求只保留一条。
   - ② 右侧按钮间间距再缩窄一点。
2. **改动**：
   - 去重：`renderSleepRing()` 内部已渲染 `<div class="sleep-ring-status">${statusText}</div>`；`renderLifeRecordTopCard()` 之前又额外拼了一次 `<div class="sleep-ring-status">${statusText}</div>`，导致圆环下方出现两行重复文字。已删除 `renderLifeRecordTopCard()` 中重复的状态文字节点及其未使用的 `statusText` 变量。
   - 右侧按钮间距：桌面 `gap` 8→6px，移动 6→5px。
3. **版本**：应用核心 v311→v312（`app.js?v=311→v312`、`sw.js CACHE='xiao-workbench-v311'→'v312'`）；preview-phone.html `APP_VERSION=312→313`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v314（2026-08-13 19:35 后）每日记录顶卡左右位移 + 配色/数字微调
1. **需求**：① 左侧整体往左挪；② 圆环下文字颜色改成和圆环内文字颜色一致；③ 左侧数字加大；④ 右侧整体往右挪。
2. **改动**：
   - 左侧左移：桌面 `.lr-top-left` padding-left `8px→4px`，移动 `4px→2px`。
   - 圆环下状态文字配色：`.sleep-ring-status` 颜色 `#7B2D4D→var(--c-text)`（与环内 `.sleep-ring-text` 同色）。
   - 左侧数字加大：`.lts-val` 桌面 24→28px、移动 18→22px；`.dur-unit`/`lts-unit` 同步 16→18 / 14→16 px。
   - 右侧右移：`.lr-diet-btns` padding-left 桌面 `16px→28px`、移动 `10px→18px`。
3. **版本**：应用核心 v312→v313（`app.js?v=312→v313`、`sw.js CACHE='xiao-workbench-v312'→'v313'`）；preview-phone.html `APP_VERSION=313→314`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v315（2026-08-13 19:49 后）每日记录顶卡：左更靠左 + 数字 26/20 + 间距微调 + 右侧按钮细边框加宽加高
1. **需求**：① 左侧整体更左（桌面 padding-left 3px / 移动 1px）；② 左侧数字 `.lts-val` 桌面 26px、移动 20px；③ 数字与上面小标题间距缩短；④ 各统计行间距增加；⑤ 右侧按钮增加细边框并加宽加高。
2. **改动**：
   - 左侧左移：`.lr-top-left` padding-left 桌面 `4px→3px`、移动 `2px→1px`。
   - 数字尺寸：`.lts-val` 桌面 `28→26px`、移动 `22→20px`；`.dur-unit`/`lts-unit` 同步（桌 18→16 / 移 16→14）。
   - 数字↔标题间距缩短：`.lr-top-stat` gap 桌面 `2px→1px`（移动已 1px 不变）。
   - 各统计行间距增加：`.lr-top-stats` gap 桌面 `12→14px`、移动 `6→8px`。
   - 右侧按钮：`.lr-diet-qbtn` 加 `border:1px solid var(--c-border)`（细边框）；桌面 padding `18px 6px→24px 8px`、移动 `12px 2px→18px 6px`，移动字号 `18→20px`，加宽加高。
3. **版本**：应用核心 v313→v314（`app.js?v=313→v314`、`sw.js CACHE='xiao-workbench-v313'→'v314'`）；preview-phone.html `APP_VERSION=314→315`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v316 右侧按钮高度回调（20:07）
1. **需求**：用户反馈 v315 右侧饮食按钮加太高，要求在 v314 基础上只加约 2px 高度。
2. **改动**：保留 v315 的细边框与加宽（横向 padding 桌面 8px / 移动 6px）；纵向 padding 回调——桌面 `24px→20px`、移动 `18px→14px`，移动字号 `20px→18px`（恢复 v314 的 18px，去掉 v315 多加的字号）。最终高度≈v314+2px。
3. **版本**：应用核心 v314→v315（`app.js?v=314→v315`、`sw.js CACHE='xiao-workbench-v314'→'v315'`）；preview-phone.html `APP_VERSION=315→316`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v317 每日记录顶卡微调（20:23）
1. **需求**：① 左侧圆环改细；② 左侧数字与上方小标题间距再小一点；③ 数字字号加大一号、粗细不变。
2. **改动**：睡眠环 `stroke-width` 7→5；`.lr-top-stat` gap 1px→0；`.lts-val` 桌 26→28px、移 20→22px（粗细不变：桌面 700 / 移动 lts-unit 600），`dur-unit`/`lts-unit` 同步放大（桌 16→18、26→28；移 14→16、20→22）。
3. **版本**：应用核心 v315→v316（`app.js?v=315→v316`、`sw.js CACHE='xiao-workbench-v315'→'v316'`）；preview-phone.html `APP_VERSION=316→317`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v318 每日记录顶卡微调（20:41）
1. **需求**：① 睡眠环 `stroke-width` 由 5 改 6（加粗）；② 数字 `.lts-val` 桌面 28→26px、移动 22→20px；③ 桌面 `.lts-val` `font-weight` 700→600；④ 移动 `.lts-unit` `font-weight` 600→550。
2. **改动**：`.sleep-ring-bg/fill` `stroke-width:5`→`6`；`.lts-val` 桌面 `font-size:28px;font-weight:700` → `font-size:26px;font-weight:600`，移动 `font-size:22px`→`font-size:20px`；移动 `.lts-val .lts-unit` `font-weight:600`→`550`。轨道色 `#DDEAF3`、进度色 `#7B2D4D`、环半径与状态文案不变。
3. **版本**：应用核心 v316→v317（`app.js?v=316→v317`、`sw.js CACHE='xiao-workbench-v316'→'v317'`）；preview-phone.html `APP_VERSION=317→318`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v319 每日记录顶卡微调（21:08）
1. **需求**：① 移动端 `.lts-unit` 字重 550→600（回退）；② 圆环内文字加大一号。
2. **改动**：移动 `.lts-val .lts-unit` `font-weight:550`→`600`；`.sleep-ring-text` 桌面 `28px`→`30px`、移动 `20px`→`22px`，其内 `.dur-unit` 同步 `14px`→`16px`（桌）/`12px`→`14px`（移）。轨道/进度色、stroke-width:6、状态文案不变。
3. **版本**：应用核心 v317→v318（`app.js?v=317→v318`、`sw.js CACHE='xiao-workbench-v317'→'v318'`）；preview-phone.html `APP_VERSION=318→319`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v320 每日记录顶卡：恢复左侧数字字重 700（21:32）
1. **需求**：用户截图反馈左侧数字（入睡/清醒/清醒次数/午休时长）字重没有恢复。
2. **根因**：v318 为响应「桌面 font-weight:600」把 `.lts-val` 整体字重改为 600，导致左侧数字变轻；`.lts-unit`（单位）应保持 600，但数字本体应恢复为 700。
3. **改动**：`.lts-val` 桌面 `font-weight:600`→`700`；`.lts-val .lts-unit` 桌面/移动仍保持 `font-weight:600`。字号 26/20px、环内文字、stroke-width:6 等不变。
4. **版本**：应用核心 v318→v319（`app.js?v=318→v319`、`sw.js CACHE='xiao-workbench-v318'→'v319'`）；preview-phone.html `APP_VERSION=319→320`。
5. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
6. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v321 每日记录顶卡：修复左侧数字字重未生效（21:55）
1. **问题**：用户截图反馈左侧数字（入睡/清醒/清醒次数/午休时长）字重仍细，v320 改动未生效。
2. **根因**：v320 提交里 index.html 的 `.lts-val{font-weight:600→700}` 与 v319 的桌面 `.sleep-ring-text{font-size:28→30px}`、`.sleep-ring-text .dur-unit{font-size:14→16px}` 均未实际写入文件（仅版本号与移动端样式被提交），导致桌面数字仍维持 v318 的 600 字重。
3. **改动**：重新写入桌面规则——`.lts-val` `font-weight:700`；`.sleep-ring-text` `font-size:30px`；`.sleep-ring-text .dur-unit` `font-size:16px`。移动端规则（sleep-ring-text 22px/dur-unit 14px、.lts-unit 600）保持不变。
4. **版本**：应用核心 v319→v320（`app.js?v=319→v320`、`sw.js CACHE='xiao-workbench-v319'→'v320'`）；preview-phone.html `APP_VERSION=320→321`。
5. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
6. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v322 每日打卡顶部卡片按钮加细边框（22:11）
1. **需求**：每日打卡顶部卡片快捷按钮（`.lqc-btn`）加细边框。
2. **改动**：`.lqc-btn` `border:none` → `border:1px solid var(--c-border)`（恢复 v312 去掉的外圈细线）；hover/done 已有的 `border-color` 规则随之生效（hover 边框变主色浅色、done 边框融入背景）。每日记录顶卡右侧饮食按钮 `.lr-diet-qbtn` 的细边框（v314 加）不受影响。
3. **版本**：应用核心 v320→v321（`app.js?v=320→v321`、`sw.js CACHE='xiao-workbench-v320'→'v321'`）；preview-phone.html `APP_VERSION=321→322`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v323 每日记录视图：保留主页顶部「今日打卡」卡片 + 再点按钮回主页（22:25）
1. **需求**：①点「每日记录」进入后，保留主页（每日打卡）顶部的「今日打卡」卡片；②在每日记录视图再次点击「每日记录」按钮回到每日打卡主页。
2. **改动**：
   - `renderLifeRecord()` 在 `renderLifeRecordTopCard`（每日记录顶卡）之前新增 `renderQuickCheckin()`，使每日记录页顶部先显示「今日打卡」卡片再显示每日记录顶卡。`renderQuickCheckin` 仅依赖 DB/今日日期，可安全复用；历史查询子视图（sleep-history/diet-history）不渲染，保持原「‹ 返回」逻辑。
   - `navigate(page)` 增加 toggle：当 `page==='life-record' 且 currentPage==='life-record'` 时改为跳转 `life-checkin`，实现「再点一次回主页」。
3. **版本**：应用核心 v321→v322（`app.js?v=321→v322`、`sw.js CACHE='xiao-workbench-v321'→'v322'`）；preview-phone.html `APP_VERSION=322→323`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v324 修正：撤销误加的「今日打卡」卡片（22:47）
1. **问题**：v323 把每日打卡主页的「今日打卡」按钮组（`renderQuickCheckin`）插入到了每日记录视图顶部，用户反对——「每日记录，你拿每日打卡的东西进来干嘛」，即不应把每日打卡内容混入每日记录页。
2. **纠正**：撤销 `renderLifeRecord()` 中插入的 `renderQuickCheckin()`，每日记录页顶部恢复为仅显示其自身的「每日记录顶卡」（睡眠环+饮食按钮，本就在该页顶部，进入即保留，无需额外加入）。`navigate()` 的 toggle（再点「每日记录」按钮回每日打卡主页）保留。
3. **版本**：应用核心 v322→v323（`app.js?v=322→v323`、`sw.js CACHE='xiao-workbench-v322'→'v323'`）；preview-phone.html `APP_VERSION=323→324`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v325 每日记录：内部「睡眠/饮食记录」toggle + 顶卡保留 + 左侧标签垂直居中（23:02）
1. **需求明确**：用户截图说明——①点每日记录页内的「睡眠记录」「饮食记录」按钮进入历史视图时，**每日记录顶部卡片（睡眠环+饮食按钮）保留**；历史内容更新在下方；再次点击同一按钮返回每日记录主界面。②左侧导航栏的「每日记录」按钮点击几次都应停留在每日记录页面。③底部记录条左侧大字标题（夜晚/午间/早餐/午餐…）垂直居中，不再上移。
2. **改动**：
   - 移除 `navigate()` 中误加的 `life-record` toggle（v323/v324 引入），恢复左侧导航栏点击始终进入 `life-record`。
   - 重构 `renderLifeRecord()`：不再在历史视图时直接替换整个 body，而是始终渲染「每日记录顶卡 + 睡眠/饮食切换按钮」，再按 `ps.view` 渲染主界面卡片（home）或历史内容（sleep-history/diet-history）。这样历史视图下顶卡和切换按钮始终保留。
   - `renderLifeRecordHistory()` 去掉外层 `<div class="fade-in life-record-page">` 包裹，改由 `renderLifeRecord()` 统一包裹；内部的「‹ 返回」按钮保留作为额外返回方式。
   - CSS：`.lr-subtype-box` 桌面与移动端均改为 `align-items:center`；`.lr-subtype-label` 移除 `padding-top:2px`，使左侧标题在记录条内垂直水平居中。
3. **版本**：应用核心 v323→v324（`app.js?v=323→v324`、`sw.js CACHE='xiao-workbench-v323'→'v324'`）；preview-phone.html `APP_VERSION=324→325`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v326 每日记录记录条左侧文字固定顶部对齐（23:21）
1. **问题**：v325 让 `.lr-subtype-box` 用 `align-items:center`，导致记录条内容（尤其宵夜/零食/奶茶「有新增」记录后变多行）高度变化时，左侧 `.lr-subtype-label`（宵夜/零食/奶茶等大字标题）在垂直方向跟着漂移，而右侧「+」按钮因 `align-self:flex-start` 始终钉在顶部，二者不再水平对齐——即「左侧文字位置不跟随变化」。
2. **改动**：`.lr-subtype-label` 桌面与移动端均加 `align-self:flex-start; margin-top:2px`，使其与「+」按钮顶部对齐、固定位置（不再随内容高低漂移）；`text-align:center`（水平居中）保持不变。`.lr-subtype-box` 的 `align-items:center` 对其他内容（记录行）仍生效。
3. **版本**：应用核心 v324→v325（`app.js?v=324→v325`、`sw.js CACHE='xiao-workbench-v324'→'v325'`）；preview-phone.html `APP_VERSION=325→326`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
5. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v327 每日记录弹窗重构与样式修正（00:06）
1. **样式修正**：
   - 底部记录条左侧大字标签恢复**垂直+水平居中**（v326 顶部对齐导致偏上）；字号放大为桌面 22px / 移动 20px，字重 700。
2. **新增按钮位置**：
   - 「睡眠记录」「饮食记录」卡片标题栏右侧各放一个 `+` 按钮（`.lr-head-add`）；移除每条记录条右侧的 `+` 按钮；历史视图每天卡片的每条右侧 `+` 按钮也移除，保留「点击添加」文字触发弹窗。
3. **弹窗重构（Modal）**：
   - 移除原来的 inline form，睡眠/饮食记录统一用 `openLifeRecordModal` 弹窗新增/编辑。
   - **睡眠弹窗**：记录项下拉（夜晚/午间，combobox）、入睡时间、清醒时间、睡眠时长自动计算、清醒次数。
   - **饮食弹窗**：记录项下拉（早餐/午餐/晚餐/宵夜/零食/奶茶，combobox）、吃饭/享用时间、餐食/零食/奶茶记录文本；零食显示数量+单位选择（包/个/颗/根/袋/盒/瓶/片/粒/份/条/罐/把）；奶茶显示规格（mini杯/小杯/中杯/标准杯/大杯/超大杯）、糖分（不另外加糖/三分糖/五分糖/七分糖/微糖/半糖/标准糖/全糖）、温度（热/常温/标准冰/少冰/去冰/多冰）、口味配方（可自定义增删）。
   - 记录项下拉切换时，动态更新时间/内容标签、placeholder 及零食/奶茶额外字段显隐。
   - 旧奶茶记录兼容：编辑时自动把混在 `flavors` 中的规格/糖分/温度拆分回新字段。
4. **版本**：应用核心 v325→v326（`app.js?v=325→v326`、`sw.js CACHE='xiao-workbench-v325'→'v326'`）；preview-phone.html `APP_VERSION=326→327`。
5. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
6. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v328 每日记录弹窗样式统一与记录条视觉调整（00:53）
1. **弹窗样式统一**：
   - 每日记录新增/编辑弹窗改为与其他弹窗一致的纵向表单：使用 `.form-row` + `.form-label` + `.form-input`；标签位于输入框上方；输入框通宽；`.life-rec-form` 移除虚线框与背景色。
   - 记录项下拉继续使用标准 `combobox`；规格/糖分/温度/单位/添加小料等字段标签统一样式。
2. **时间字段改为可手动输入**：
   - 入睡/清醒/吃饭/享用时间均改为 `type="text"`，占位符 `HH:MM`，限制 `maxlength="5"`；输入时自动插入冒号，支持纯键盘手动输入。
   - 睡眠时长仍根据入睡/清醒时间自动计算。
3. **新增记录按钮改成长按钮**：
   - 「睡眠记录」「饮食记录」卡片标题栏右侧的 `.lr-head-add` 由小方块 `+` 改为圆角长按钮，显示 `+ 新增记录`。
4. **记录条视觉调整**：
   - 记录条（`.lr-subtype-box`）加高：桌面 `padding:14px 12px`、移动 `12px 10px`；记录行 `.lr-record-row` 纵向内边距 `14px 0`。
   - 左侧分类文字缩小：桌面 18px / 移动 16px；卡片标题缩小：桌面 14px / 移动 13px。
   - 编辑/删除按钮间距缩小为 `gap:2px`；记录详情标签/数值字号分别缩小到 11/12（移动 10/11）。
5. **奶茶口味配方改为添加小料**：
   - `LIFE_RECORD_SUBTYPES.diet.milktea` 的 `flavorOptions` 重命名为 `toppingOptions`；弹窗内标签改为「添加小料」；自定义添加/删除提示改为小料；记录行中显示为「小料:xxx」。
6. **版本**：应用核心 v326→v327（`app.js?v=326→v327`、`sw.js CACHE='xiao-workbench-v326'→'v327'`）；preview-phone.html `APP_VERSION=327→328`。
7. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
8. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v329 每日记录新增按钮字重统一、记录条布局与弹窗多选自定义（01:47）
1. **新增按钮字重统一**：`.lr-head-add` 字体字重 `600→500`（与全站 `.btn` 的「+ 新增记录」一致）；`::before` 的 `+` 字重同步 `700→500`。
2. **记录条内容字号加大**：`.lr-info-val` 桌面 `12→13`、移动 `11→12`；`.lr-info-label` 桌面 `11→12`、移动 `10→11`。
3. **编辑/删除移到首排并缩小**：重构 `renderSleepRecordRows` / `renderDietRecordRows`，把操作按钮移入第一行（时间行）右侧 `.lr-info-line-head`；`.lr-record-ops .btn` 缩小为 `padding:2px 7px` / `11px`（移动 `1px 6px` / `10px`）。
4. **饮食记录内容自动换行**：`.lr-info-full .lr-info-val` 取消 `nowrap`/`ellipsis`，改为 `white-space:normal` + `word-break:break-word`，一行放不下自动换行。
5. **规格/糖分/温度支持自定义添加**：`renderSizeSingleSelect` / `renderSugarSingleSelect` / `renderTemperatureSingleSelect` 合并自定义选项并加「添加/删除」输入；新增 `addLifeRecordSize` / `addLifeRecordSugar` / `addLifeRecordTemperature` 与对应 `remove` 函数，存储键 `customOpts_lifeRecord_milktea_{size,sugar,temperature}`；保存逻辑复用现有单选读写。
6. **多选样式确认**：零食单位、奶茶规格/糖分/温度保持 pill-group 多选样式但单选（`limitSingleCheckbox`）；添加小料保持 tag-group 多选（`toggleCheckboxItem`）。
7. **版本**：应用核心 v327→v328（`app.js?v=327→v328`、`sw.js CACHE='xiao-workbench-v327'→'v328'`）；preview-phone.html `APP_VERSION=328→329`。
8. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒，入口 index.html，`/preview-phone.html`）。
9. **校验/交付**：git 待提交；`git push` 仍挂起待用户手动。APK 仍为 v1.1.22，未重新打包。

### v330 每日记录按钮与记录条布局回退修正（02:0x）
1. **加号垂直居中**：`.lr-head-add::before` 的 `+` 原偏下，改为 `display:inline-flex;align-items:center;justify-content:center;align-self:center` + `transform:translateY(-1px)`，与文字垂直居中对齐。
2. **睡眠记录条改回两行两列**：`renderSleepRecordRows` 撤回 v329 首行内联按钮；恢复 4 条信息行（入睡/清醒时间、睡眠时长、清醒次数）在 `.lr-record-info-2col` 网格内，编辑/删除改为末尾 `.lr-record-ops` 右列（两行两列原版）。
3. **零食记录条改回两行两列**：`renderDietRecordRows` 的 `snack` 分支撤回首行内联按钮，首行恢复普通 `.lr-info-line`（享用时间），数量行保留，零食记录用 `.lr-info-full` 承载并换行；编辑/删除改末尾 `.lr-record-ops` 右列。奶茶记录、餐食记录（含宵夜）保持 v329 首行按钮样式。
4. **编辑/删除按钮字号匹配小标题**：`.lr-record-ops .btn` 字号 `11→12`（桌面）/`10→11`（移动），与「吃饭时间」等小标题（12px）同量级、与餐食记录值（13px）接近。
5. **换行范围确认**：仅 餐食记录 / 零食记录 / 奶茶记录 的 `.lr-info-full` 内容行一行放不下才换行；睡眠记录无长文本换行需求，维持两列网格。
6. **版本**：应用核心 v328→v329（`app.js?v=328→v329`、`sw.js CACHE='xiao-workbench-v328'→'v329'`）；preview-phone.html `APP_VERSION=329→330`。
7. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22，未重新打包；`git push` 仍挂起。

### v331 记录条按钮平齐首行、字号统一加大（02:2x）
1. **编辑/删除按钮与首行时间平齐**：`renderSleepRecordRows` 与 `renderDietRecordRows` 的 `snack` 分支撤掉 v330 末尾右列 `trailingOps`，改回首行内联 `.lr-info-line-head`（与 入睡时间 / 享用时间 同一行）；meal/milktea 首行按钮保持不变。四类记录按钮均与时间标签平齐。
2. **按钮间距缩小**：`.lr-record-ops` 的 `gap` 由 `2px→1px`（编辑/删除更紧凑）。
3. **记录条文字/数字字号统一加大**：`.lr-info-label` 与 `.lr-info-val` 统一为 `14px`（桌面）/`13px`（移动）（原 12/13 → 14、11/12 → 13）；编辑/删除按钮同步 `12→14`（桌）/ `11→13`（移），保持 v330「按钮=小标题字号」规则一致。
4. **换行与左文字稳定**：餐食记录（早/午/晚/宵夜）保持首行按钮 + `.lr-info-full` 换行，不论换行或新增记录，左侧标签/数值位置不变；睡眠/零食/奶茶同理。
5. **版本**：应用核心 v329→v330（`app.js?v=329→v330`、`sw.js CACHE='xiao-workbench-v329'→'v330'`）；preview-phone.html `APP_VERSION=330→331`。
6. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v332 每日记录样式精修（08-14）
1. **记录条小标题字号还原**：`.lr-info-label` 由 v331 的 `14/13px` 还原为 `12/11px`（保持不变），仅 `.lr-info-val` 保持加大后的 `14/13px`，实现「小标题不变、记录值加大」。
2. **弹窗选项改为可见复选框样式**：`.life-rec-form` 内的 `.pill-group`、`.tag-group` 不再隐藏 `input[type="checkbox"]`，改为显示 □ 复选框 + 圆角矩形标签（同关系类型截图）；选中态改为浅色主色背景 + 主色文字 + 主色边框，单选组仍受 `limitSingleCheckbox` 限制只能选一个。
3. **记录属性渲染为角标**：`renderDietRecordRows` 中：
   - 零食记录的 `数量+单位` 不再单独成行，改为跟在记录文字后的 `.lr-corner-tag`（如「薯片 <span class='lr-corner-tag'>2包</span>」）；
   - 奶茶记录的规格、糖分、温度、添加小料分别渲染为独立角标（小料前加 `+`），跟在奶茶记录文字后，形如价目表尺寸标注。
4. **睡眠/饮食按钮去表情**：底部切换按钮 `lr-toggle-btn` 去掉 `<span>😴</span>`、`<span>🍚</span>`，仅保留文字「睡眠记录」「饮食记录」；历史查询标题保留表情但去掉「历史查询」后缀，改为「😴 睡眠记录」「🍚 饮食记录」。
5. **分页改为文字链无白底**：`.pagination` 的上一页/下一页改用 `.page-link`（透明背景、无边框、文字色），中间页码信息保持不变，整体去除按钮白底。
6. **版本**：应用核心 v330→v331（`app.js?v=330→v331`、`sw.js CACHE='xiao-workbench-v330'→'v331'`）；preview-phone.html `APP_VERSION=331→332`。

### v333 每日记录样式回退与修正（08-14）
1. **编辑/删除按钮大小改回**：`.lr-record-ops .btn` 字号由 v331/v332 的 `14/13px` 改回 `12/11px`（与 v330 一致）。
2. **睡眠/零食记录条改回两行两列**：
   - `renderSleepRecordRows` 撤掉首行内联按钮，恢复 4 条信息行在 `.lr-record-info-2col` 网格内，编辑/删除回到末尾 `.lr-record-ops` 右列；
   - `renderDietRecordRows` 的 `snack` 分支撤掉首行内联按钮，恢复「享用时间 + 数量」两列 + 「零食记录」整行，编辑/删除回到末尾右列；
   - 奶茶记录、餐食记录（含宵夜）保持首行内联按钮不变。
3. **左侧标签置顶固定**：`.lr-subtype-box` 由 `align-items:center` 改为 `align-items:flex-start`，「早餐/午餐/晚餐/宵夜/零食/奶茶」等左侧标签始终置顶，不再因内容换行高度不同而上下偏移。
4. **角标改为括号小字**：删除 `.lr-corner-tag` 的 pill 标签样式，新增 `.lr-size-note`：属性以括号内小字跟在记录文字后，如「吧唧（20cm）」；零食只把**单位**放括号（如「薯片（包）」），数量仍保留在「数量」列；奶茶把规格/糖分/温度/小料以「 / 」分隔后整体括起来（如「奶茶（中杯 / 三分糖 / 常温 / +珍珠）」）。
5. **历史查询标题行整行删除**：`renderLifeRecordHistory` 的 `.lr-history-hd` 只保留「‹ 返回」按钮，标题（含 emoji 和「睡眠记录/饮食记录」文字）整行移除。
6. **周导航条去掉白色底色**：`.lr-week-nav` 去除 `background` 与 `border`；上一周/下一周改用 `.page-link` 文字链，中间日期使用 `.page-info` 样式，整体与分页风格一致且无白底。
7. **版本**：应用核心 v331→v332（`app.js?v=331→v332`、`sw.js CACHE='xiao-workbench-v331'→'v332'`）；preview-phone.html `APP_VERSION=332→333`。
8. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v334 历史页返回按钮删除（08-15）
1. **历史查询页「‹ 返回」按钮删除**：`renderLifeRecordHistory` 移除仅含返回按钮的 `.lr-history-hd` 整块；返回主页改由顶部「睡眠记录 / 饮食记录」切换按钮承担（`lifeRecordToggleView` 中 `if (ps.view === view) view='home'`，点当前选中项即回主页）。
2. **清理死 CSS**：删除已不再使用的 `.lr-history-hd`、`.lr-history-title` 规则。
3. **版本**：应用核心 v332→v333（`app.js?v=332→v333`、`sw.js CACHE='xiao-workbench-v332'→'v333'`）；preview-phone.html `APP_VERSION=333→334`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v335 记录条高度/左侧标签/时间输入（08-15）
1. **记录条高度缩短**：`.lr-record-row` 桌面 `padding:14px 0` → `8px 0`，底部记录条更紧凑。
2. **左侧标签固定顶部（含移动端）**：之前 v333 仅桌面端 `.lr-subtype-box` 设 `align-items:flex-start`，移动端 media query 被覆盖回 `center`；本次移动端同步改为 `flex-start`，手机视图下「早餐/午餐」等左侧大字标签固定顶部，不随记录行高度浮动。
3. **时间输入去限制**：删除 `lifeRecordModalTimeInput`（原会清除非数字、强制插冒号、限长4位）；入睡/清醒/吃饭/享用时间 input 改为纯文本自由输入（`maxlength` 与自动格式化均移除），仅保留入睡/清醒时间实时计算睡眠时长。
4. **版本**：应用核心 v333→v334（`app.js?v=333→v334`、`sw.js CACHE='xiao-workbench-v333'→'v334'`）；preview-phone.html `APP_VERSION=334→335`。
5. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v336 记录条高度再压/左侧标签居中（08-15）
1. **左侧标签改垂直居中**：`.lr-subtype-box` 桌面与移动端 `align-items:flex-start` → `center`，「早餐/午餐」等左侧大字标签相对内容垂直居中（不再贴顶偏高）。
2. **记录条高度再缩短**：`.lr-record-row` `padding:8px 0` → `4px 0`；`.lr-subtype-box` 桌面 `padding:14px 12px` → `10px 12px`、移动 `12px 10px` → `8px 10px`，整体更紧凑且单条记录也可见缩短。
3. **时间显示说明**：保存/显示均直接用输入值，无重新格式化；旧版自动补零存的「08:23」为历史数据，新输入原样显示。
4. **版本**：应用核心 v334→v335（`app.js?v=334→v335`、`sw.js CACHE='xiao-workbench-v334'→'v335'`）；preview-phone.html `APP_VERSION=335→336`。
5. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v337 左侧标签固定顶部 + 时间自动格式化为 HH:MM（08-15）
1. **左侧标签真正固定**：`.lr-subtype-box` 从 `display:flex;align-items:center` 改为 `display:grid;grid-template-columns:auto 1fr;align-items:start`，左侧 `.lr-subtype-label` 用 `align-self:start;padding-top:2px` 钉在左上角；桌面/移动端同步调整，右侧内容行数变化不再影响左侧标签位置。
2. **时间保存时自动标准化**：新增 `normalizeTime()`，支持 `1:29` / `01:29` / `129` / `0130` 等输入，保存时统一格式为 `01:29`/`02:30`；无法解析则保留原值。`lifeRecSave` 中对 `sleepTime`/`wakeTime`/`time` 统一调用。
3. **输入体验不变**：弹窗内时间输入仍为自由文本，不做实时限制或自动插冒号；仅在保存时统一显示格式。
4. **版本**：应用核心 v335→v336（`app.js?v=335→v336`、`sw.js CACHE='xiao-workbench-v335'→'v336'`）；preview-phone.html `APP_VERSION=336→337`。
5. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v338 字号层级调整 + 左侧标签下移（08-15）
1. **标题加大**：顶部切换按钮 `.lr-toggle-btn` 桌面 `16px→18px`、移动 `15px→17px`；卡片标题 `.lr-card-title` 桌面 `14px→16px`、移动 `13px→15px`。
2. **记录条内小标题缩小**：`.lr-info-label`（入睡时间/清醒时间/睡眠时长/清醒次数/吃饭时间/餐食记录/享用时间/零食记录/奶茶记录）桌面 `12px→11px`、移动 `11px→10px`。
3. **记录数字加大**：`.lr-info-val`（时间数字、数量数字等）桌面 `14px→16px`、移动 `13px→15px`。
4. **左侧标签下移**：`.lr-subtype-label` `padding-top:2px→6px`（桌面/移动同步），不再贴顶偏上，视觉与右侧首行对齐。
5. **版本**：应用核心 v336→v337（`app.js?v=336→v337`、`sw.js CACHE='xiao-workbench-v336'→'v337'`）；preview-phone.html `APP_VERSION=337→338`。
6. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v339 小标题回到 12px + 左侧标签垂直居中于两排记录中间（08-15）
1. **记录条内小标题改回 12px**：`.lr-info-label`（入睡时间/清醒时间/睡眠时长/清醒次数/吃饭时间/餐食记录/享用时间/零食记录/奶茶记录）桌面 `11px→12px`、移动 `10px→11px`（即回到 v337 之前的原始字号）。
2. **左侧标签垂直居中于两排记录中间**：`.lr-subtype-label` 由 `align-self:start;padding-top:6px` 改为 `align-self:center` 并去掉 `padding-top`；`grid` 容器内始终以整个记录内容块的正中垂直对齐，永久固定在「第一条小标题与第二条小标题的正中间」，不论记录自动换行还是新增记录都不再移动。桌面/移动端同步。
3. **版本**：应用核心 v337→v338（`app.js?v=337→v338`、`sw.js CACHE='xiao-workbench-v337'→'v338'`）；preview-phone.html `APP_VERSION=338→339`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v340 normalizeTime 支持中文时间词 + 缓存强制刷新（08-15）
1. **中文时间词自动转 HH:MM**：增强 `normalizeTime()`，支持「两点半→02:30、一点→01:00、三点一刻→03:15、十二点整→12:00、零点半→00:30、2点半→02:30」等；小时支持中文一~十二/零/两，分钟支持整/正/一刻/半/三刻/阿拉伯数字分。
2. **缓存刷新**：三处版本号递增至 v339/v340，强制 Service Worker 重新拉取 v339 CSS，解决用户端仍显示 v338（左侧标签贴顶、小标题 11px）的缓存问题。
3. **版本**：应用核心 v338→v339（`app.js?v=338→v339`、`sw.js CACHE='xiao-workbench-v338'→'v339'`）；preview-phone.html `APP_VERSION=339→340`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v341 左侧标签改为 flex 居中 + 强制刷新缓存（08-15）
1. **左侧标签真正垂直居中**：把 `.lr-subtype-box` 从 `display:grid;grid-template-columns:auto 1fr;align-items:start` 改回 `display:flex;align-items:center`，`.lr-subtype-label` 用 `align-self:center` 钉在整条内容块的垂直正中。无论右侧是 1 行、2 行还是新增到多行，左侧「早餐/午餐/零食/奶茶/夜晚/午间」都固定在第一条与第二条小标题之间的中位，不再贴顶。桌面/移动端同步。
2. **缓存刷新**：三处版本号递增至 v340/v341，强制 Service Worker 重新拉取最新 CSS，消除用户端仍显示旧版布局的缓存问题。
3. **版本**：应用核心 v339→v340（`app.js?v=339→v340`、`sw.js CACHE='xiao-workbench-v339'→'v340'`）；preview-phone.html `APP_VERSION=340→341`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v342 再次强制刷新缓存（08-15）
1. **问题**：用户反馈 v341 左侧标签仍贴顶、小标题未缩小。核查当前代码实际已是 flex 居中 + 12px 小标题，判断为 Service Worker/浏览器缓存未完全刷新，用户端仍显示 v340 的 grid 贴顶样式。
2. **处理**：三处版本号再次递增至 v341/v342（`app.js?v=340→v341`、`sw.js CACHE='xiao-workbench-v340'→'v341'`、`preview-phone.html APP_VERSION=341→342`），强制重新拉取最新 CSS。
3. **版本**：应用核心 v340→v341；preview `APP_VERSION=341→342`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v343 左侧标签固定在两行小标题正中间（08-15）
1. **问题分析**：用户反馈 v342「没变化」。核查代码已是 flex 居中，但用户截图仍显示贴顶。重新理解需求：用户要的不是整个内容块的几何居中，而是左侧标签**固定在第一行小标题与第二行小标题之间的正中间**——例如午餐的「吃饭时间」和「餐食记录」两行小标题之间；不论第二行以下折行多长、新增多少记录，左侧大字标签位置都不再移动。
2. **改动**：`.lr-subtype-label` 由 `align-self:center` 改为 `align-self:flex-start; margin-top:16px`（桌面），移动端 `margin-top:14px`。通过固定上外边距，把标签钉在第一条与第二条小标题之间的垂直中点，不再随下方内容折行/新增而下移。桌面/移动端同步。
3. **版本**：应用核心 v341→v342（`app.js?v=341→v342`、`sw.js CACHE='xiao-workbench-v341'→'v342'`）；preview-phone.html `APP_VERSION=342→343`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v344 空记录左侧居中 + 记录内容汉字缩小（08-15）
1. **问题**：用户反馈 v343 空记录（早餐/晚餐/宵夜/奶茶）左侧标签偏下，有记录的才居中；记录内容汉字（餐食/零食/奶茶记录文字）字号仍太大。
2. **改动1（空记录左侧居中）**：新增 `.lr-subtype-box:has(.lr-empty-row) .lr-subtype-label` 规则，空记录时 `align-self:center;margin-top:0`，让左侧大字标签在没有记录时垂直居中，不再被 `margin-top:16px` 顶下去；有记录时仍保持 `margin-top:16px` 钉在第一/二行小标题正中间。桌面/移动端同步。
3. **改动2（记录内容汉字缩小）**：`.lr-info-full .lr-info-val`（餐食记录、零食记录、奶茶记录内容文字）单独缩小为桌面 `13px`、移动 `12px`；`.lr-info-line .lr-info-val`（时间数字、数量数字）保持桌面 `16px`、移动 `15px` 不变，实现「时间/数量数字大、记录内容汉字小」的层级。
4. **版本**：应用核心 v342→v343（`app.js?v=342→v343`、`sw.js CACHE='xiao-workbench-v342'→'v343'`）；preview-phone.html `APP_VERSION=343→344`。
5. **交付**：四文件同步 assets；`node --check app.js` 通过；重新部署 CloudStudio（同沙盒）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v345 左侧标签 margin-top 调小（偏下修复）（08-15）
1. **反馈**：v344 常规记录与空记录左侧标签都偏下。
2. **改动**：`.lr-subtype-label` 桌面/移动端 `margin-top` 由 16/14px 改为 `2px`；空记录 `:has` 居中规则不变。
3. **版本**：应用核心 v343→v344（`app.js?v=343→v344`、`sw.js CACHE='xiao-workbench-v343'→'v344'`）；preview-phone.html `APP_VERSION=344→345`。
4. **交付**：四文件同步 assets；`node --check app.js` 通过；CloudStudio 同沙盒重部署。APK 仍为 v1.1.22；`git push` 仍挂起。

### v346 左侧标签 margin-top 回调（偏上修复）（08-15）
1. **反馈**：v345（margin-top:2px）左侧标签仍偏上，未落在两行小标题中点。
2. **改动**：`.lr-subtype-label` 桌面 `margin-top:2px→8px`、移动 `2px→7px`，取两行小标题中点附近折中，固定不变；空记录 `:has` 居中规则不变。
3. **版本**：应用核心 v344→v345（`app.js?v=344→v345`、`sw.js CACHE='xiao-workbench-v344'→'v345'`）；preview-phone.html `APP_VERSION=345→346`。
4. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署。APK 仍为 v1.1.22；`git push` 仍挂起。

### v347 左侧标签分态微调（有记录偏上 / 空记录偏下修复）（08-15）
1. **反馈**：v346 有记录时左侧标签仍偏上，空记录（早餐/宵夜/奶茶等）左侧标签偏下。
2. **改动1（有记录下移）**：`.lr-subtype-label` 桌面 `margin-top:8px→12px`、移动 `7px→11px`，让有记录时标签真正落在第一行与第二行小标题正中间。
3. **改动2（空记录上移）**：`.lr-subtype-box:has(.lr-empty-row) .lr-subtype-label` 由 `align-self:center;margin-top:0` 改为 `align-self:flex-start;margin-top:2px`，让空记录标签贴顶一点、与单行提示文字对齐。
4. **版本**：应用核心 v345→v346（`app.js?v=345→v346`、`sw.js CACHE='xiao-workbench-v345'→'v346'`）；preview-phone.html `APP_VERSION=346→347`。
5. **交付**：四文件同步 assets；`node --check workstation/app.js` 与 assets/app.js 均通过；CloudStudio 同沙盒重部署。APK 仍为 v1.1.22；`git push` 仍挂起。

### v348 左侧标签分态再微调（有记录偏下 / 空记录偏上修复）（08-15）
1. **反馈**：v347 有记录时左侧标签偏下一点点（幅度要小），空记录时左侧标签偏上。
2. **改动1（有记录小幅上收）**：`.lr-subtype-label` 桌面 `margin-top:12px→10px`、移动 `11px→9px`，仅轻微上移。
3. **改动2（空记录下移）**：`.lr-subtype-box:has(.lr-empty-row) .lr-subtype-label` 由 `align-self:flex-start;margin-top:2px` 改为 `margin-top:8px`，空记录标签不再贴顶偏上。
4. **版本**：应用核心 v346→v347（`app.js?v=346→v347`、`sw.js CACHE='xiao-workbench-v346'→'v347'`）；preview-phone.html `APP_VERSION=347→348`。
5. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署。APK 仍为 v1.1.22；`git push` 仍挂起。

### v349 有记录标签精确值（用户指定）（08-15）
1. **反馈/指令**：用户直接指定有记录时 `.lr-subtype-label` 桌面 `margin-top:11px`、移动 `10px`（即 v348 基础上各 +1px）。空记录保持 `8px` 不变。
2. **改动**：桌面 `margin-top:10px→11px`、移动 `9px→10px`（基础规则，不含空记录例外）。
3. **版本**：应用核心 v347→v348（`app.js?v=347→v348`、`sw.js CACHE='xiao-workbench-v347'→'v348'`）；preview-phone.html `APP_VERSION=348→349`。
4. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署。APK 仍为 v1.1.22；`git push` 仍挂起。

### v350 奶茶新建默认选中（标准杯/三分糖/去冰）（08-15）
1. **指令**：奶茶记录新建时默认选中规格「标准杯」、糖分「三分糖」、温度「去冰」。
2. **改动**：`renderSizeSingleSelect` / `renderSugarSingleSelect` / `renderTemperatureSingleSelect` 三处 `const sel = selected || opts[0]` 改为 `selected || '标准杯' / '三分糖' / '去冰'`。编辑已有记录时 `selected` 有值不受影响；仅新建回退到指定默认项。
3. **版本**：应用核心 v348→v349（`app.js?v=348→v349`、`sw.js CACHE='xiao-workbench-v348'→'v349'`）；preview-phone.html `APP_VERSION=349→350`。
4. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署（首次 409 重试成功）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v351 SET组全加急报价修正（08-15）
1. **问题**：SET组 4 项均加急时，用户手算 `(25+15+35+30+10)*0.9*2=207`，但实时报价显示 ¥208.50。
2. **根因**：组内全部加急时仍按「单制品/部分加急」公式计算——加急溢价基于制品原价 `prodUrgentBase*(urgentRate-1)`，未把 SET 折扣后的绑定加价（抠图）纳入加急基数，导致比「SET加急 ×2」语义多 1.5 元。
3. **改动**：
   - 分组对象新增 `isAllUrgent` 标记（组内全部 `actualUrgent`）。
   - 全加急分组的加急溢价改为基于分组折扣后总额（含绑定加价），即折后整体乘加急倍率。
   - 整单总价：在 `prodUrgentBase*(urgentRate-1)` 基础上，对每个全加急分组追加 `(groupWithBound - groupSubtotal)*(urgentRate-1)` 修正量，使总价与分组小计之和保持一致。
   - 「无柄图制品」分组同步增加全加急判断，保持逻辑一致（因其无 SET 折扣，数值不变）。
4. **版本**：应用核心 v349→v350（`app.js?v=349→v350`、`sw.js CACHE='xiao-workbench-v349'→'v350'`）；preview-phone.html `APP_VERSION=350→351`。
5. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署。APK 仍为 v1.1.22；`git push` 仍挂起。

### v352 折后小计不含加急（08-15）
1. **问题**：v351 后整单加急时，「折后小计」显示为 ¥207.00，但用户要求折后小计仅含 SET 折扣，应为 ¥103.50，加急部分由底部「整单加急」块或组内「SET加急」行单独展示。
2. **根因**：v351 把 `groupDisplay` 在全加急/整单加急时设为 `groupWithBound * urgentRate`，导致折后小计直接包含了加急倍率。
3. **改动**：SET 组与无柄图制品分组均将 `groupDisplay` 恢复为 `groupWithBound`（仅 SET 折扣后金额，含绑定加价折扣），加急部分仍由独立加急块/行展示；整单总价计算逻辑不变。
4. **版本**：应用核心 v350→v351（`app.js?v=350→v351`、`sw.js CACHE='xiao-workbench-v350'→'v351'`）；preview-phone.html `APP_VERSION=351→352`。
5. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署。APK 仍为 v1.1.22；`git push` 仍挂起。

### v353 每日记录历史页倒序（08-16）
1. **指令**：睡眠记录 / 饮食记录按钮点击进去后的历史查询页，日卡片需倒序排放（周日、周六、周五…周一）。
2. **改动**：`renderLifeRecordHistory` 中构造 `daysRev = days.slice().reverse()` 与 `wdRev`，日卡片渲染与周折线图/周统计均改用倒序数组，使整体从周日排到周一；周导航「上一周/下一周」日期范围仍用正序 `days`（周一 - 周日）不受影响。
3. **版本**：应用核心 v351→v352（`app.js?v=351→v352`、`sw.js CACHE='xiao-workbench-v351'→'v352'`）；preview-phone.html `APP_VERSION=352→353`。
4. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署。APK 仍为 v1.1.22；`git push` 仍挂起。

### v354 饮食周统计改文字 & 周导航去灰底（08-16）
1. **反馈**：
   - 周导航「上一周 / 下一周」按钮有灰色底色，要去掉。
   - 睡眠记录折线图保留；饮食记录周统计不要柱状图，直接显示文字。
2. **改动**：
   - CSS 新增 `.lr-week-nav .page-link` 规则：`background:transparent`、`border:none`，hover/disabled 状态同步去底；`.lr-week-nav` 仍无背景。
   - JS `renderDietWeekStats` 从柱状图改为纯文字网格：每项显示「早餐 3次 / 午餐 2次 …」；新增 `.lr-hsc-diet-grid / .lr-hsc-diet-item / .lr-hsc-diet-count` 样式（桌面/移动端）。
   - 睡眠折线图 `renderSleepWeekLineChart` 保持原样，仅随 `daysRev` 倒序。
3. **版本**：应用核心 v352→v353（`app.js?v=352→v353`、`sw.js CACHE='xiao-workbench-v352'→'v353'`）；preview-phone.html `APP_VERSION=353→354`。
4. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署。APK 仍为 v1.1.22；`git push` 仍挂起。

### v355 饮食/睡眠周统计视觉重设计（08-16）
1. **指令**：饮食周统计改成「累计点亮数 / 最长连续 / 累计点亮率」风格的三列大数字指标卡；睡眠周折线图改成带空心圆点、数据标签在点上方、底部横轴标签的简洁绿色折线图。
2. **改动**：
   - `renderDietWeekStats`：改为三列指标卡，显示「累计记录数 / 最长连续 / 本周完成率」；指标基于本周 7 天饮食记录统计（总条数、连续有记录天数最大值、有记录天数 / 7 百分比）。
   - `renderSleepWeekLineChart`：重绘为 680×240 SVG，折线 `#2A9D8F`、圆点为白色描边空心圆、每日时长标签显示在点正上方；横轴改为「周日-周一」文字并随日卡片倒序；移除 8 小时参考虚线；保留日均时长副标题。
   - CSS 新增 `.lr-hsc-metrics`、`.lr-hsc-metric-val`、`.lr-hsc-metric-label`，调整 `.lr-hsc-x` 字号与 `.lr-hsc-chart-body` 移动端高度。
3. **版本**：应用核心 v353→v354（`app.js?v=353→v354`、`sw.js CACHE='xiao-workbench-v353'→'v354'`）；preview-phone.html `APP_VERSION=354→355`。
4. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署（首次 409 重试成功）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v356 饮食次数卡 + 睡眠橙色折线图（08-16）
1. **反馈**：一周饮食统计仅参考 v355 三指标卡样式，统计内容仍要各餐次数；睡眠折线图要橙色；此前时间标签端点被裁、显示不完整。
2. **改动**：
   - `renderDietWeekStats` 改回统计各餐次数（早餐/午餐/晚餐/宵夜/零食/奶茶），沿用三列大数字卡样式（`.lr-hsc-metrics` 3 列 × 2 行），每格显示「次数 + 餐名」。
   - `renderSleepWeekLineChart` 折线色 `#2A9D8F` → 橙色 `#FA8C16`；`xOf(i)=44+(W-88)*i/6` 左右各留边距修复端点标签溢出；viewBox 高度 240→250、TOP 32→38、容器 180→190px、标签字号 13→14、y 偏移 12→14，确保时间标签完整显示。
3. **版本**：应用核心 v354→v355（`app.js?v=354→v355`、`sw.js CACHE='xiao-workbench-v354'→'v355'`）；preview-phone.html `APP_VERSION=355→356`。
4. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署。APK 仍为 v1.1.22；`git push` 仍挂起。

### v357 睡眠折线图改面积图风格（08-16）
1. **指令**：参考金价走势截图，把一周睡眠折线图改成折线下方带浅色渐变填充的面积图风格，线条用橙色。
2. **改动**：
   - `renderSleepWeekLineChart` 重绘为 680×260 SVG，折线色改为金橙色 `#F5A623`；折线下方添加同色系线性渐变填充（上 32% 透明度 → 下 3% 透明度），形成面积图效果。
   - 新增淡色横向网格线（`--c-border-light`），与参考截图的背景网格风格一致。
   - 圆点改为实心橙 + 白边；时长标签仍显示在点上方，但画布顶部留白由 38 扩到 48、标签 y 偏移 16、左右边距 50，确保周日/周一端点的时长标签完整不被裁。
   - 图表高度改由 CSS 控制：桌面 `.lr-hsc-chart-body` 200px、移动端 160px；移除函数内联 `style="height:..."` 避免覆盖媒体查询。
3. **版本**：应用核心 v355→v356（`app.js?v=355→v356`、`sw.js CACHE='xiao-workbench-v355'→'v356'`）；preview-phone.html `APP_VERSION=356→357`。
4. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署（首次 409 重试成功）。APK 仍为 v1.1.22；`git push` 仍挂起。

### v358 睡眠面积图放大 + 底部日期间距收紧（08-16）
1. **指令**：睡眠折线图（面积图）整体放大一点；底部星期日期标签的横向间距缩一下。
2. **改动**：
   - `renderSleepWeekLineChart`：SVG viewBox 高度 260→280、TOP 48→52；横向边距 `SIDE` 50→36，折线横向更舒展；圆点半径、标签字号保持不变。
   - CSS：`.lr-hsc-chart-body` 高度桌面 200→240px、移动端 160→200px，折线纵向放大。
   - CSS：`.lr-hsc-x` 左右 padding 由 `2px` 改为 `5.3%`（对应 SIDE/680≈5.29%），使 7 个星期标签贴合折线端点，不再两端外扩，日期间距收紧并与折线对齐。
3. **版本**：应用核心 v356→v357（`app.js?v=356→v357`、`sw.js CACHE='xiao-workbench-v356'→'v357'`）；preview-phone.html `APP_VERSION=357→358`。
4. **交付**：四文件同步 assets；`node --check` 通过；CloudStudio 同沙盒重部署。APK 仍为 v1.1.22；`git push` 仍挂起。

### v359 睡眠图改周一→周日 + 加 4/6/8/10h 线 + 饮食去标题（08-16）
1. **指令**：去掉折线图「一周睡眠记录」字样；日均统计上边距/右边距扩大；折线图日期排序改从左到右 周一、周二…周日；折线时间字号加大；增加 4/6/8/10 小时刻度线；饮食记录统计去掉「一周饮食统计」「各餐食用次数」文字。
2. **改动**：
   - `renderSleepWeekLineChart`：`wdLabels` 改为正序 `['周一'…'周日']`，调用方改传正序 `days`（仅图表正向，日卡片仍 `daysRev` 倒序）；删除标题 span，仅留「日均」并加 class `.lr-hsc-avg`（`margin-left:auto;margin-top:6px;margin-right:8px`）；时间标签 `font-size 13→16`、圆点 r 5→5.5；`maxV` 由 `Math.max(8,…)` 改为 `Math.max(10,…)`，新增 `hourLines`（4/6/8/10h 实线 + 左端 `Nh` 标签）；`SIDE` 36→40 给小时标签留位，`.lr-hsc-x` padding 5.3%→5.9%。
   - `renderDietWeekStats`：删除 `.lr-hsc-row`（标题「一周饮食统计」+ 副标题「各餐食用次数」），仅保留各餐次数三列大数字卡。
3. **版本**：应用核心 v357→v358（`app.js?v=357→v358`、`sw.js CACHE='xiao-workbench-v357'→'v358'`）；preview-phone.html `APP_VERSION=358→359`。

### v398 接稿详情九项整改（08-22）
1. **需求**：去掉顶部标题；三导入按钮并排且生成标准化约稿单改为给单主链接；删搜索下新增按钮、四分类改名并排均分；日期导航对齐每日打卡；约稿单字段分「单主信息/交付规范/制品信息」、删两项、饭圈/二次支持追加制品、展示视图绑定接稿排期后增加开/截稿日期；并按用户指定顺序重排土味/封面/饭圈/二次字段。
2. **改动**：`renderCommissionDetailPage` 重写（无页头、3 并列导入按钮、4 分类按钮均分、仅搜索工具条、仿打卡月导航、接稿条按月分页折叠）；`setCdTab` toggle；`renderCdFullRecord` 分组展示并加开/截稿；新增 `buildCdLocalForm/readCdLocalForm/buildCdExtraProductsHTML/...` 支持追加制品；`openCdClientForm/runCdClientForm/buildCdClientForm/saveCdClientForm` 生成单主表单并直接存本地；四个 MODULES 重分组/重排序；`migrateData` v398 兼容旧数据。
3. **版本**：应用核心 v397→v398（`app.js?v=397→v398`、`sw.js CACHE='xiao-workbench-v397'→'v398'`）；preview-phone.html `APP_VERSION=398→399`。
4. **交付**：`node --check` 通过；CloudStudio 同沙盒重部署。APK 未打包；git push 挂起。

### v399 接稿详情五项精修（08-22 续）
1. **需求**：①三导入按钮一排，生成标准化约稿单需生成单主填写链接且结果直接回流本地；②删除日期下方「本月共...」小字；③「是否可以打码展示」改为「是否可以展示」；详情小标题去粗、section 标题加大；关联接稿排期仅显示稿件进度/开稿时间/截稿时间三排且与交付规范样式一致；④追加制品放在制品信息栏内、是否同模单选保留样式、固定接收邮箱不可更改；⑤接稿排期列表视图左右两列+装订环连接，右侧按品类精简字段，点击跳转到接稿详情对应块。
2. **改动**：
   - `openCdClientForm`/`runCdClientForm` 改为生成分享链接（`cdShowClientLink`/`copyCdClientLink`/`cdOpenClientFormFromLink`/`cdCheckClientFormFromUrl`），`init()` 延迟检查 URL 参数自动打开单主表单。
   - `renderCommissionDetailPage` 删除 `.cd-month-summary`；接稿条卡片加 `data-id`。
   - `commDetailDisplayField` 标签改为「是否可以展示」；`commDetailFixedEmailField` 改为 `readonly`。
   - `buildCdLocalForm`/`buildCdClientForm` 按「交付规范」切分，追加制品放在制品信息与交付规范之间。
   - `cdExtraProductRowHTML` 同模 checkbox group 加 `data-single="true"`，确保单选。
   - `renderCdFullRecord`：section 标题字号加大；小标题去粗；readonly 字段静态展示；关联排期改为 `.cd-link-block` 三排：稿件进度/开稿时间/截稿时间。
   - `openCdDetail` readonly 字段展示实际值。
   - `renderCommDetailPreview` 重写：按品类展示关键字段（土味/封面：书名/文字/类型/尺寸/颜色；饭圈：姓名/制品/尺寸/风格颜色；二次：角色名/制品/尺寸/风格颜色），点击跳转 `cdJumpToDetail`。
   - `renderListPage` 双栏中间插入 `.comm-binder` 装订环。
   - CSS：`.cd-rec-section` 加大；`.cd-link-block`/`.cd-link-box`；`.comm-dual` 三列布局+装订环；`.comm-preview-thumb` 可点击；移动端隐藏装订环。
3. **版本**：应用核心 v398→v399（`app.js?v=398→v399`、`sw.js CACHE='xiao-workbench-v398'→'v399'`）；preview-phone.html `APP_VERSION=399→400`。
4. **交付**：`node --check` 通过；CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署。APK 未打包；git push 仍挂起。

### v400 接稿详情六项复修（08-22 续，预览不传 github）
1. **需求（5 点 + 复修）**：①顶部三导入按钮要「一排三个」并排（非三排），给单主的链接打开后应是干净独立填写页、不得暴露用户工作台与数据；②接稿单小标题**加粗**（撤回之前「去粗」要求），展示视图隐藏「单主」「固定接收邮箱」，所有小标题及文字内容与关联接稿排期视觉左对齐；③追加制品填写样式与第一条制品一致；④「风格颜色」拆成并排两个框（风格 / 颜色），提示词「可以给参考图/色卡 请把主色写最前面」，交付方式「直发附件」→「直发」；⑤接稿排期列表视图双栏+装订环在手机预览框内完全失效（被移动端媒体查询压成单列、隐藏装订环）。
2. **改动**：
   - `cdShowClientLink` 链接追加 `&standalone=1`；新增 `cdRenderClientStandalone(catKey)`：`document.body.classList.add('cd-standalone')`，仅把单主表单渲染进 `#mainBody`（隐藏 `#sidebar`/`.sidebar-overlay`/`.main-header`），提交按钮调用 `saveCdClientForm(catKey, true)`；`cdCheckClientFormFromUrl` 检测 `standalone===1` 改走独立页；`saveCdClientForm(pageKey, standalone)` 取 `container=$('#modalBody')||$('#mainBody')`，standalone 时显示感谢页、**不跳转工作台、不暴露任何用户数据**。
   - CSS：`.cd-import-row{grid-template-columns:repeat(3,1fr)}`（一排三个）；`.commission-detail-page .cd-rec-k{font-weight:600}`（小标题加粗）；`.cd-ep-grid` 改单列 `1fr`（追加制品样式与首条一致）；移动端媒体查询修正为 `.comm-dual{grid-template-columns:1fr 8px 150px}` + `.comm-binder{display:flex}`（手机框内双栏+装订环生效）；新增 `.cd-standalone *` 隐藏侧栏/顶栏、表单居中 680px 的整套样式。
   - `renderCdFullRecord`：展示视图 `if (f.key==='clientInfo'||f.key==='fixedEmail') return`（隐藏单主/固定邮箱）；小标题加粗由 CSS 承担；追加制品「风格颜色」拆为 `cdRecRow('风格',p.style)` + `cdRecRow('颜色',p.color)`。
   - MODULES（饭圈/二次 同款字段，replace_all）：`styleColor` 单字段 → `theme`(企划/主题名称) + `style`(风格,占位提示) + `color`(颜色,占位提示) + `elementsRequired`；`cdExtraProductRowHTML` 同步拆两框带占位提示；`renderCommDetailPreview` 缩略字段改为 `['风格',d.style]`/`['颜色',d.color]`；聊天解析器 `/(风格|风格颜色)[：:]/` → `style`；`COMM_DETAIL_DELIVERY_OPTS` `直发附件`→`直发`。
3. **版本**：应用核心 v399→v400（`app.js?v=399→v400`、`sw.js CACHE='xiao-workbench-v399'→'v400'`）；preview-phone.html `APP_VERSION=400→401`。
4. **交付**：`node --check` 通过；CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified）。APK 未打包；**git push 仍挂起（用户要求暂不上传）**。

### v401 接稿详情四项精修（08-22 晚，预览不传 github）
1. **需求**：①接稿详情顶部导入按钮、品类按钮、搜索框间距过大，改成与其他模块顶部按钮间距一致；②接稿单小标题**取消加粗**，小标题与文字整体向右挪，左侧与「单主信息」等 section 标题文字左对齐；③追加制品仍有蓝底、风格颜色未并排，需与第一条制品样式统一；④接稿排期列表视图中接稿详情预览模块应与接稿排期记录列表**同级并列均分**，不要占顶部搜索/筛选/新增区域；右侧详情信息取最新一条；中间装订环样式错误。
2. **改动**：
   - CSS：`.cd-import-row` gap 12→8px、margin-bottom 14→8px，按钮内边距/字号微调；`.cd-cat-bar` margin-bottom 12→8px、按钮内边距/字号微调；`.cd-toolbar` 保持 toolbar 标准 14px 底边距。`.commission-detail-page .cd-rec-k` 字重 600→400；`.cd-rec-row` 加 `padding-left:11px`，使小标题/文字左边缘与 section 标题文字（border-left 3px + padding-left 8px = 11px）对齐。
   - 追加制品：`index.html` `.cd-extra-product-row` 去背景/去边框/去内边距，`.cd-ep-grid` 改 flex column；新增 `.cd-ep-inline` 让 风格/颜色 并排。`app.js` 饭圈/二次 MODULES 给 `style`、`color` 加 `row:'styleColor'`，buildForm 自动同行；`cdExtraProductRowHTML` 里风格/颜色用 `.cd-ep-inline` 包裹。
   - 接稿排期双栏：`renderListPage` 把工具栏移到 `.comm-dual` 之外（全宽置顶），仅记录列表与详情预览进入双栏；`.comm-dual` 桌面 `1fr 32px 1fr`、移动端 `1fr 8px 1fr`（真正均分）；`renderCommDetailPreview` 对匹配详情按 `_ct` 降序取最新一条。
   - 装订环：改 `.comm-binder` 为带竖向书脊渐变线 + 圆环（白底、灰边、内凹阴影、中心孔），更像真实活页环；移动端同步缩小比例。
3. **版本**：应用核心 v400→v401（`app.js?v=400→v401`、`sw.js CACHE='xiao-workbench-v400'→'v401'`）；preview-phone.html `APP_VERSION=401→402`。
4. **交付**：`node --check` 通过；CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified）。APK 未打包；**git push 仍挂起（用户要求暂不上传）**。
### v402 接稿详情五点复修（08-22 续，预览不传 github）
1. **需求（5 点）**：①接稿详情的导入按钮、信息按钮、搜索框按钮内边距/字号还原（导入按钮 margin-bottom 6px、信息按钮 margin-bottom 6px）；②接稿单「关联接稿排期」小标题和文字位置不变，改回去；③接稿单风格和颜色为同一个大类中的两个小类；④追加制品的小标题改成和常规小标题字号字重颜色一致，稿件用途和是否同柄并列，是否同柄的下拉框格式按项目标准 combobox 选择器；⑤接稿排期列表接稿详情信息展示模块高度保持和它绑定的接稿排期展示模块高度一致，详情内容默认显示（之前空白），中间链接环改成摊开的线圈连接两边。
2. **改动**：
   - CSS：`.cd-import-btn` padding 12px10px→14px12px、图标 24→26px、名称 13→14px、描述 11→12px，并加 `margin-bottom:6px`；`.cd-cat-btn` padding 9px6px→11px6px、字号 13→14px，加 `margin-bottom:6px`；移动端媒体查询同步还原（导入 10px8px/22/12/10，品类 9px8px/13）。
   - 新增 `.form-section-title` 统一样式（font-size:14px;font-weight:700;color:var(--c-primary);padding-left:8px;border-left:3px solid var(--c-primary)），与列表页 `.cd-rec-section` 一致，消除详情弹窗内分组标题/文字缩进错位。
   - MODULES 饭圈/二次（replace_all）：`theme` 之后新增 `{ section:'风格/颜色' }`，`style`/`color` 保留 `row:'styleColor'` 并排，形成「制品信息」大类下的「风格/颜色」小类。
   - 追加制品：`.cd-ep-title` 改为与 `.form-section` 一致（13px/700/var(--c-primary-dark)/下边框）；`cdExtraProductRowHTML` 把「稿件用途」「是否同柄」用 `.cd-ep-inline` 包裹并列，且两个下拉框从原生 `<select>` 改为项目标准 `.combobox-wrapper` 选择器（sameHandle 选择后触发 `toggleCdExtraProductFull` 展开/收起同柄明细）。
   - 接稿排期双栏：`.comm-dual` `align-items:start`→`stretch`；`.comm-dual-right` 去掉 sticky/align-self:start，加 `height:100%`，与左栏等高；`renderListPage` 默认 `selectedCommissionId` 为空时取左侧第一条排期，确保右侧详情内容默认可见（点击切换、再次点同一张回退到默认第一条）；`.comm-binder` 重写为「摊开的线圈」——`viewBox 0 0 32 100` 的 SVG 竖排 18 个横向椭圆环，横贯左右两栏连接；删除旧竖脊+圆环节点样式。
3. **版本**：应用核心 v401→v402（`app.js?v=401→v402`、`sw.js CACHE='xiao-workbench-v401'→'v402'`）；preview-phone.html `APP_VERSION=402`（沿用）。
4. **交付**：`node --check` 通过；CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（待验证）。APK 未打包；**git push 仍挂起（用户要求暂不上传）**。

### v403 接稿详情五点复修（08-22 续二，预览不传 github）
1. **需求（5 点，依截图文字判断）**：①接稿详情导入按钮 margin-bottom **-2px**、信息按钮 margin-bottom **-2px**（v402 误设 6px，过松）；②接稿单「关联接稿排期」小标题左侧要对齐下方关联接稿排期文字左侧（消除 v402 加的 padding-left:8px+border-left:3px 造成的 11px 缩进）；③「风格/颜色」不是大类 section 标题，而是风格颜色输入填写框再分「风格」「颜色」两个输入框（删掉 v402 误加的 `{section:'风格/颜色'}`）；④追加制品小标题（"追加制品1"）颜色字号字重与常规小标题不一致，改回去（变深、变小、减顶距），以截图"备注"常规小标题为参照；⑤接稿排期左右展示模块高度不统一、右侧顶部标题文字多余、制品小标题与文字间距过大、✏️修改按钮多余（保留查看完整详情）、中间连接环仍是横向椭圆（"横的横的横的"）要改竖。
2. **改动**：
   - CSS：`.cd-import-btn`/`.cd-cat-btn` margin-bottom 6px→**-2px**；移动端媒体查询同步。`.form-section-title` 删 `padding-left:8px;border-left:3px`，与下方 `.cd-link-list` 内容左对齐。`.cd-ep-title` 改 `font-size:12px;font-weight:600;color:var(--c-text);margin:8px 0 4px`（去下边框/去蓝），"追加制品（可选）"父标题同步改用 `.cd-ep-title` 类，与"追加制品1"保持一致。
   - MODULES 饭圈/二次（replace_all）：删除 `{section:'风格/颜色'}`，仅保留 `style`/`color`（仍 `row:'styleColor'` 并排两个输入框，非大类标题）。
   - 接稿排期双栏：`.comm-dual-left`/`.comm-dual-right` 均改 `display:flex;flex-direction:column` + 右栏 `min-height:100%`，`.comm-preview-thumb{flex:1}` 实现左右真正等高；删除 `.comm-preview-head`（"接稿详情信息展示"标题整段移除）；`.comm-preview-row` gap 8→4px、`.comm-preview-k` 基宽 70→50px（小标题/文字更近）；`.comm-preview-actions` 删 ✏️修改按钮，保留「查看完整详情」并靠右。`.comm-binder` SVG 椭圆由横向（`rx=13 ry=3.2`）改为竖向（`rx=3.6 ry=11`）、16 个，呈现竖直活页环。移动端媒体查询同步去 head、缩 preview-k。
3. **版本**：app.js v402→v403、sw.js v402→v403、preview-phone.html APP_VERSION=402→403。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（待验证）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

### v404 关联接稿排期标题对齐修复（08-22，预览不传 github）
1. **问题**：v403 去掉 `.form-section-title` 的 `padding-left/border-left` 后，「关联接稿排期」标题文字与下方内容（位于 `.cd-link-block` 内）反而更不对齐；用户截图确认未对齐。
2. **根因**：关联接稿排期内容实际位于 `.cd-link-block{padding:8px 10px}` 内的 `.cd-rec-row{padding-left:11px}`，文字起点 = 容器左边缘 + 10px + 11px = 21px；而 `.form-section-title` 无 padding 时文字在 0px，差距 21px。
3. **改动**：
   - `index.html`：恢复 `.form-section-title` 默认 `padding-left:8px;border-left:3px`（文字起点 11px，与常规 `.cd-rec-row` 内容对齐）；新增 `.form-section-title.cd-link-section-title{padding-left:18px}`，使「关联接稿排期」标题文字起点 = 3 + 18 = 21px，与 `.cd-link-block` 内内容严格对齐。
   - `app.js`：「关联接稿排期」标题加 `cd-link-section-title` 类。
4. **版本**：app.js v403→v404、sw.js v403→v404、preview-phone.html APP_VERSION=403→404。`node --check` 通过。
5. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

### v405 关联接稿排期标题对齐再修（08-22，预览不传 github）
1. **问题**：v404 只修了弹窗内的 `.form-section-title`「关联接稿排期」标题，但用户截图显示未对齐的标题实际在接稿详情**列表/详情页内联区块**（`.cd-rec-section`，无 emoji），同样存在标题文字（11px）与 `.cd-link-block` 内容文字（21px）不对齐。
2. **改动**：
   - `app.js`：详情页内联「关联接稿排期」标题加 `.cd-rec-section-link` 类。
   - `index.html`：`.cd-rec-section.cd-rec-section-link` 同样设 `padding-left:18px`，与 `.form-section-title.cd-link-section-title` 统一处理，使标题文字起点 = 3px 蓝条 + 18px = 21px，对齐 `.cd-link-block` 内 `.cd-rec-row` 内容。
3. **版本**：app.js v404→v405、sw.js v404→v405、preview-phone.html APP_VERSION=404→405。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

### v406 关联接稿排期对齐方向修正（08-22，预览不传 github）
1. **问题**：用户要求标题保持不动，只移动下方「稿件进度 / 开稿时间 / 截稿时间」小标题来对齐；v404/v405 错误地把标题往右推。
2. **改动**：
   - `index.html`：去掉 `.cd-link-section-title`/`.cd-rec-section-link` 的 `padding-left:18px` 规则；新增 `.cd-link-block .cd-rec-row{padding-left:1px}`。
   - 对齐计算：标题文字起点 = 3px 蓝条 + 8px padding = 11px；内容在 `.cd-link-block{padding-left:10px}` 内，`.cd-rec-row{padding-left:1px}` 后内容文字起点 = 10 + 1 = 11px，两者严格对齐。
   - `app.js`：去掉「关联接稿排期」标题上的 `.cd-link-section-title` 和 `.cd-rec-section-link` 修饰类，标题保持默认位置。
3. **版本**：app.js v405→v406、sw.js v405→v406、preview-phone.html APP_VERSION=405→406。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

### v407 风格颜色/追加制品/连接环三处回正（08-22，预览不传 github）
1. **背景**：用户反馈 v403–v406 中第3/4/5点"全部没改"。经线上实测核对：第3点已对；第4点改反方向（`.cd-ep-title` 误设深灰 `var(--c-text)`，而"备注"常规小标题是项目蓝 `var(--c-primary)`，颜色仍不一致）；第5点连接环改反（v403 做成竖向椭圆，用户早期示意图为横向连线，要求"横的横的横的"）。
2. **改动**：
   - 第4点：`.cd-ep-title` 改回 `font-size:13px;font-weight:700;color:var(--c-primary)`（项目蓝，与"备注"常规小标题一致），保留 `margin:8px 0 4px`（上边距已减）。父标题"追加制品（可选）"已用同类，一致。
   - 第5点：`.comm-binder` SVG 椭圆改回横向 `rx=13 ry=3.2`（18 个），还原为"横的横的横的"水平连线环，符合示意图。
   - 第3点：确认饭圈/二次 MODULES 无 `{section:'风格/颜色'}`，`style`/`color` 仍 `row:'styleColor'` 并排两输入框，无需再改。
3. **版本**：app.js v406→v407、sw.js v406→v407、preview-phone.html APP_VERSION=406→407。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

### v408 风格颜色/追加制品/连接环三处按截图重改（08-22，预览不传 github）
1. **问题**：用户截图指出 v407 仍未改对：①风格颜色不是「风格」「颜色」两个普通并排输入框，而是要「风格颜色」一个大输入框 + 横线 + 下面「风格」「颜色」两个小输入框；②追加制品小标题（"追加制品1"）仍是蓝色加粗，与截图里"文案/备注/尺寸"等常规灰色不加粗小标题不一致，且上边距仍远；③连接环与用户给出的线圈示意图完全不像。
2. **改动**：
   - **风格颜色**：饭圈/二次 MODULES 中 `theme` 字段 label 改为「风格颜色」、type 改为 `textarea`、placeholder 保留「可以给参考图/色卡 请把主色写最前面」；在 theme 后插入 `{type:'custom',html:'<div class="style-color-divider"></div><div class="style-color-subfields"><span>风格</span><span>颜色</span></div>'}`；`style`/`color` 字段 label 置空、placeholder 分别改为「例：清新、复古、可爱」和「例：薄荷绿+浅紫」，仍 `row:'styleColor'` 并排。新增 CSS `.style-color-divider`（横线）与 `.style-color-subfields`（两个居中子标签）。
   - **追加制品小标题**：`.cd-ep-title` 改为 `font-size:13px;font-weight:400;color:var(--c-text-muted);margin:4px 0 4px`，与常规「文案/备注/尺寸」小标题同样灰色、不加粗、更紧凑。
   - **连接环**：将 `.comm-binder` 由横向椭圆改为**连续波浪形 path**（`M 2,0` + 9 组 cubic bezier，向右凸弧 + 向左凹弧交替），模拟线圈本从侧面看的一串弧形线圈；`stroke-linecap:round`。
3. **版本**：app.js v407→v408、sw.js v407→v408、preview-phone.html APP_VERSION=407→408。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

### v409 风格颜色/追加制品标题/连接环三处再按图修正（08-22，预览不传 github）
1. **问题**：用户再次截图指出 v408 仍未改对：①风格颜色不要大 textarea + 下面两小框，而要**一个大框内分「风格」「颜色」两列**；②追加制品真正要改的是「稿件用途 / 是否同柄 / 制品」等字段小标签，而不是「追加制品（可选）」「追加制品1」这两个标题，这两个标题应改回深色加粗；③连接环与用户给出的真实线圈本示意图完全不像，应是一列竖立的圆环。
2. **改动**：
   - **风格颜色**：饭圈/二次 MODULES 中删除 `theme` textarea 与 `style/color` row 字段，改为单个 `{type:'custom'}` 字段，渲染 `<label>风格颜色</label>` + `.style-color-box` 大边框容器，容器内左右两列分别放置 `<input data-key="style">` 与 `<input data-key="color">`，每列顶部小标签「风格」「颜色」。新增对应 CSS：大框白底浅边框圆角、两列 flex 等分、列标签 13px/500。
   - **追加制品标题**：`.cd-ep-title`（「追加制品（可选）」「追加制品1」）改回 `font-weight:700;color:var(--c-text)`（深色加粗）；真正的小字段标签 `.cd-ep-field .form-label`（稿件用途、是否同柄、制品、尺寸、备注等）改为 `font-weight:400;color:var(--c-text-muted)`（灰色不加粗），与截图里常规小标题一致。
   - **连接环**：将波浪 path 改回**竖直椭圆上下排列**（`rx=6 ry=12`，8 个），模拟真实线圈本侧面看到的一列竖立圆环。
3. **版本**：app.js v408→v409、sw.js v408→v409、preview-phone.html APP_VERSION=408→409。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

### v410 接稿单字段重组 + 生成约稿单路由修正 + 线圈本还原（08-22，预览不传 github）
1. **问题**：用户提出 7 点修正：①点击「生成标准化约稿单」会直接打开工作台；②风格/颜色输入框提示词要删、风格颜色后加小字提示；③元素也做成风格颜色同款大框、内分三列（必用/可选/避雷）；④姓名/角色名+昵称+英文名合并为「重要信息」大框、2×2（姓名、昵称、英文名、生日）；⑤饭圈/二次的企划/主题名称挪到尺寸后、制品+尺寸并排；⑥追加制品（可选）仍显蓝色、追加制品1 上边距缩短4px、小标题（稿件用途/制品等）灰色与常规不一致、新增制品按钮上边距+6px；⑦连接环与真实线圈本相差甚远。
2. **改动**：
   - **生成标准化约稿单路由**：`init()` 中将 `cdCheckClientFormFromUrl` 提前到 `navigate('home')` 之前并 `return`，避免先渲染工作台首页；`cdCheckClientFormFromUrl` 内移除 200ms/300ms 延迟，standalone 与弹窗模式同步接管，单主打开链接不再闪出用户工作台。
   - **风格颜色**：饭圈/二次 MODULES 改为 `{type:'custom'}` 大框（`.style-color-box`），内分两列 input（style/color），**删除 input placeholder**；label 后加 `<span class="form-label-hint">可以给参考图/色卡 请把主色写最前面</span>` 小字。新增 `.form-label-hint` 与 `.style-color-row>.form-label` 块级样式。
   - **元素**：新增 `{type:'custom'}` 大框 `.style-color-box.elements-box`，内分三列 input（elementsRequired/elementsOptional/elementsAvoid），无 placeholder。删除原三条 `元素-*` 字段。
   - **重要信息**：新增 `{type:'custom'}` 大框 `.style-color-box.info-box`（grid 2×2），饭圈放 姓名/昵称/英文名/生日（charName/nickName/englishName/birthday），二次放 角色名/昵称/英文名/生日；删除原 charName/nickName 独立字段。
   - **饭圈/二次顺序**：企划/主题名称（theme）移到 尺寸 之后；制品(product)+尺寸(size) 用 `row:'prodSize'` 并排。
   - **追加制品样式**：`.cd-ep-field .form-label` 由灰(`--c-text-muted`)改为常规 `var(--c-text)`/font-weight:500（与常规表单一致）；`.cd-ep-title` 维持深色加粗（非蓝）；首个追加制品行 head 加 `.row-first`（`margin-top:6px`，较默认 -4px）；「新增制品」按钮 `margin-top` 8px→14px（+6px）。
   - **连接环**：`.comm-binder` 由竖直椭圆改为**一列 C 形金属装订环**（9 个，每个开口朝右的 C 弧 + 内侧高光细线），viewBox `0 0 40 200`、`preserveAspectRatio="none"` 纵向撑满双栏高度，模拟真实线圈本。
3. **版本**：app.js v409→v410、sw.js v409→v410、preview-phone.html APP_VERSION=409→410。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

### v411 单主页零闪屏 + 真实线圈连接环重绘 + 部署入口修正（08-22，预览不传 github）
1. **问题**：用户核验 v410 反馈：①访问旧沙盒看到旧版，风格/元素/重要信息改动未生效；②单主 standalone 页仍闪出工作台且页面太窄；③连接环与真实线圈本相差甚远，给出多张线圈本实物/插画参考图。
2. **改动**：
   - **零闪屏**：在 `index.html` `<head>` 内增加内联脚本，DOM/CSS 主逻辑运行前即检测 `cd_client=1&standalone=1`，给 `<html>` 加 `cd-standalone` class，确保侧边栏/顶栏一开始就隐藏，单主打开链接**不会闪出工作台**。
   - **单主页加宽**：`.cd-standalone .main-body` 与 `.cd-client-standalone` 的 `max-width:680px` 改为 `max-width:none`，让单主填写页在桌面端占满可用宽度。
   - **真实线圈连接环**：`renderListPage` 中 `.comm-binder` SVG 重绘为**一列交替开口方向的 C 形金属装订环**（9 个，偶数朝左、奇数朝右），每个环带纸张孔洞椭圆、粗金属丝主体、内侧高光、截面高光圆点，模拟真实螺旋线圈本侧面。CSS 中双栏装订栏宽度 32px→38px。
   - **部署入口修正**：重新 CloudStudio 部署 `workstation/`，显式指定入口 `index.html`，根路径返回应用本体而非 preview-phone.html；手机预览仍通过 `/preview-phone.html` 访问。
3. **版本**：app.js v410→v411、sw.js v410→v411、preview-phone.html APP_VERSION=410→411。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

### v412 三个 modal 下拉框改用 combobox 规范 + 单主页彻底铺满 + 线圈连接环改为连续螺旋（08-22，预览不传 github）
1. **问题**：用户核验 v411 反馈：①三个弹窗（聊天记录解析导入 / 腾讯问卷 JSON 导入 / 生成标准化约稿单）里的分类下拉仍是原生 `<select>`，未遵守 combobox 规范，且下拉框贴着左边标题文字太近；②单主独立填写页「还是窄的」；③连接环仍不像真实线圈本（参考图白给了）。
2. **改动**：
   - **下拉框改用 combobox**：新增 `cdCatComboboxHTML(hiddenId, selectedKey)`，统一输出 `.combobox-wrapper`（input + toggle 按钮 + dropdown + 隐藏 `.combobox-value`），替换上述三个弹窗里的原生 `<select>`；隐藏 input 沿用原 `id`（cdChatCat / cdJsonCat / cdClientCat），故 `runCdChatParse` / `runCdJsonImport` / `runCdClientForm` 读取 `.value` 逻辑不变。下拉框加 `margin-left:14px`，与左侧标题文字拉开间距。
   - **单主页铺满**：`.cd-standalone #app` 改为 `display:block`；`.main-body` / `#mainBody` / `.cd-client-standalone` 均强制 `max-width:none;width:100%`，移除居中缩窄，单主填写页在桌面端彻底占满宽度（padding 16→24、表单内边距 16→20）。
   - **连接环改为连续螺旋**：`renderListPage` 中 `.comm-binder` 由「交替开口 C 形环」改为**一条连续螺旋金属丝 path**（11 个交替半圆环：偶数凸左、奇数凸右，首尾相接成正弦状螺旋），主丝 `#8c97a3` 5px + 高光 `#d7dee5` 1.6px，均用 `vector-effect="non-scaling-stroke"` 保证拉伸后粗细均匀；每环接缝处加纸张打孔椭圆。双栏装订栏宽度 38px→44px。
3. **版本**：app.js v411→v412、sw.js v411→v412、preview-phone.html APP_VERSION=411→412。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v413（2026-08-22）

1. **问题**：用户核验 v412 反馈 4 点：①连接环画成"连续螺旋正弦线"，与真实线圈本（一列独立圆环扣）完全不像；②三个导入弹窗不够高、标签文案不统一、下拉框离左边标题仍偏远；③生成的约稿单链接弹窗 1000px 太宽且不能滑动；④约稿单重要信息应在风格颜色前、风格颜色提示词应移到 label 右侧、删除"追加制品（可选）"标题、追加制品小标题字号要与常规一致。
2. **改动**：
   - **连接环重画为真实线圈本**：`renderListPage` 的 `.comm-binder` 弃用连续螺旋 path，改为**一列独立环形线圈扣**——12 只闭合金属环（rx8/ry6，纵向拉伸呈竖椭圆，主色 `#8c97a3` 3px + 高光 `#e3e9ef` 1px，均 `non-scaling-stroke`）+ 贯穿背脊的细金属丝（1.4px）+ 每只环的纸孔椭圆（`#eef2f6`），不再是连续线。
   - **导入弹窗增高 200px**：`index.html` 新增 `.modal.tall{height:calc(90vh + 200px);max-height:none}`；聊天记录解析导入 / 腾讯问卷 JSON 导入 / 生成标准化约稿单 三个弹窗的 `openModal` 由 `'lg'` 改为 `'lg tall'`。
   - **三处导入标签统一为「选择分类」**：原「导入到分类」「目标分类（JSON 内未带 category 时生效）」「选择约稿分类」全部改为「选择分类」。
   - **combobox 贴近标题**：`cdCatComboboxHTML` 内联 `margin-left:14px` → `6px`。
   - **链接弹窗收窄**：`cdShowClientLink` 的 `openModal` 由 `'lg'` 改为 `'link-narrow'`（`index.html` 新增 `.modal.link-narrow{width:560px}`），桌面端不再 1000px，链接框可完整选取/复制。
   - **约稿单字段顺序（fq/ec 模块）**：`重要信息` 自定义块移到 `风格颜色` 之前（顺序：重要信息 → 风格颜色 → 元素）。
   - **风格颜色提示词右移**：`.form-label-hint` 由 `display:block` 改为 `display:inline;margin-left:8px`，提示词从 label 下方挪到「风格颜色」右侧。
   - **删除「追加制品（可选）」标题**：`buildCdExtraProductsHTML` 移除顶部 `<div class="cd-ep-title">追加制品（可选）</div>`（每行「追加制品 N」标题保留）。
   - **追加制品小标题字号统一**：`.cd-ep-field .form-label` 由 `12px` 改为 `13px`，与常规小标题（稿件用途/制品等）一致。
3. **版本**：app.js v412→v413、sw.js v412→v413、preview-phone.html APP_VERSION=412→413。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，线上已确认 `app.js?v=413` / `APP_VERSION=413` / `xiao-workbench-v413`）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v414（2026-08-22）

1. **问题**：用户核验 v413 反馈 3 点：①导入弹窗 +200px 改回去，改为 +20px；②生成的约稿单链接直接打开（单主 standalone 页）在电脑端依旧太宽且无法滑动；③连接环要 1:1 复刻参考图（粉色线圈本一列独立白色圆环）。
2. **改动**：
   - **导入弹窗高度改回并仅 +20px**：`index.html` 删除 `.modal.tall{height:calc(90vh + 200px);max-height:none}`，改为 `.modal.add20{max-height:calc(90vh + 20px)}`；三个导入弹窗（聊天记录解析导入 / 腾讯问卷 JSON 导入 / 生成标准化约稿单）的 `openModal` 由 `'lg tall'` 改回 `'lg add20'`。
   - **单主 standalone 页收窄 + 可滚动**：`index.html` 中 `.cd-standalone body` 新增 `overflow-y:auto !important;height:auto !important`，解除 `body{overflow:hidden}` 导致的无法滚动；`.cd-standalone .main-body`、`.cd-standalone #mainBody`、`.cd-client-standalone` 的 `max-width` 由 `none` 改为 **720px** 并水平居中（`margin:0 auto`），桌面端不再铺满全屏。
   - **连接环 1:1 复刻线圈本正面视图**：`renderListPage` 中 `.comm-binder` 弃用 SVG 拉伸方案，改为 HTML/CSS 绝对定位：中间一条浅灰脊柱条 + 12 只独立白色正圆环（直径 30px，径向渐变 + 高光阴影 + 中心小孔），按百分比 `top` 均匀分布，不再被拉成椭圆；`index.html` 同步新增 `.comm-binder`/`.comm-binder-spine`/`.comm-ring` 样式。
3. **版本**：app.js v413→v414、sw.js v413→v414、preview-phone.html APP_VERSION=413→414。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，线上已确认 `app.js?v=414` / `APP_VERSION=414` / `xiao-workbench-v414`）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v415（2026-08-22）

1. **问题**：用户核验 v414 反馈：当前连接环是厚圆盘（12 只 30px 填充圆环堆叠），与参考图（粉色线圈本一列独立细金属螺旋环）完全不是一回事。
2. **改动**：
   - **连接环改为细金属环**：`index.html` 中 `.comm-ring` 由 30px 填充圆盘（径向渐变 + 中心小孔）改为 **20px 细线圆环**（`border:2px solid #fff` + 外层 1px 灰色描边 + 轻阴影），移除填充与中心孔，更像螺旋线圈。
   - **减少环数并加大间距**：`renderListPage` 中 `ringCount` 由 12 改为 **10**，百分比分布使环与环之间留出明显间隙，避免堆叠。
   - **替换粗脊柱条为细中轴**：移除 18px 浅灰脊柱条，改为 1px 渐变中轴线（`.comm-binder-axis`），让整体更像单纯的一列线圈。
3. **版本**：app.js v414→v415、sw.js v414→v415、preview-phone.html APP_VERSION=414→415。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，线上已确认 `app.js?v=415` / `APP_VERSION=415` / `xiao-workbench-v415`）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

### v416（2026-08-22）连接环再修正 + 右栏宽度限制

**用户反馈**：
1. 当前连接环（v415 一列细线圆环）与参考图仍不一致；参考图应为「左右两边圆形假装孔洞，中间小长矩形假装线圈连接」，而非单个圆环。
2. 接稿排期双栏右侧详情模块在桌面大屏下直接撑满右栏，显得过宽。

**改动**：
1. **连接环改为「中间细长条 + 左右圆孔」**：
   - `index.html`：新增 `.comm-binder-spine`（8px 宽浅灰竖条，模拟线圈连接）与 `.comm-hole`（14px 圆孔，左右各一列），移除 `.comm-binder-axis` / `.comm-ring`。
   - `app.js`：`renderListPage` 中生成 10 对左右圆孔，中间为 `.comm-binder-spine`。
   - 移动端中间条缩为 4px，圆孔隐藏。
2. **限制右侧详情区宽度**：`.comm-dual-right` 新增 `max-width:560px;width:100%;margin:0 auto`，桌面端不再无限撑满右栏；移动端恢复 `max-width:none`。
3. **版本**：app.js v415→v416、sw.js v415→v416、preview-phone.html APP_VERSION=415→416。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，线上已确认 `app.js?v=416` / `APP_VERSION=416` / `xiao-workbench-v416`）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

### v417（2026-08-22）连接环横放椭圆 + 右栏固定宽度

**用户反馈**：
1. v416 连接环圆孔未显示且方向不对，要求圆孔是「横着」的椭圆，左右两边各一列。
2. v416 右侧详情区因内部 max-width 居中，导致卡片右侧出现大片空白。

**改动**：
1. **连接环改为横放椭圆孔洞**：
   - `index.html`：`.comm-hole` 从 14px 正圆改为 **18px × 12px 横向椭圆**（`border-radius:50%`），左右各一列 10 个；中间脊柱 `.comm-binder-spine` 从 8px 缩为 **4px**。
   - 椭圆定位调整：左椭圆 `left:11px`、右椭圆 `right:11px`，紧贴中间脊柱。
2. **右栏改为固定宽度列**：
   - `.comm-dual` 由 `1fr 44px 1fr` 改为 **`1fr 44px 480px`**，右栏固定 480px，不再随屏幕撑满。
   - `.comm-dual-right` 移除 `max-width:560px;margin:0 auto`，改为占满 480px 列，消除卡片外空白。
3. **版本**：app.js v416→v417、sw.js v416→v417、preview-phone.html APP_VERSION=416→417。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，线上已确认 `app.js?v=417` / `APP_VERSION=417` / `xiao-workbench-v417`）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v418（2026-08-23）

**用户反馈**：
1. 接稿排期双栏中间的连接环反复调整仍未满意，要求直接删除。

**改动**：
1. **删除接稿排期双栏连接环**：
   - `app.js`：`renderListPage` 中删除 `.comm-binder`（中间脊柱 `.comm-binder-spine` + 左右两列 `.comm-hole` 椭圆孔洞）的生成代码，关闭左栏后直接渲染右侧详情。
   - `index.html`：删除 `.comm-binder` / `.comm-binder-spine` / `.comm-hole` 全部 CSS；`.comm-dual` grid 由 `1fr 44px 480px` 改为 **`1fr 480px`**（两栏）；移动端媒体查询中删除 `.comm-binder-spine` / `.comm-hole` 残留规则，`.comm-dual` 改 `1fr 1fr`。
2. **版本**：app.js v417→v418、sw.js v417→v418、preview-phone.html APP_VERSION=417→418。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
3. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，线上已确认 `app.js?v=418` / `APP_VERSION=418` / `xiao-workbench-v418`）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v419（2026-08-23）

**用户反馈**：
1. 接稿排期双栏中，右侧详情展示模块不应是一个占满高度的大面板，而应与左侧排期模块「并排平均放置、中间有间距、宽度/高度/圆角一致」。
2. 右侧详情需与左侧联动，狸狸等下方排期对应的详情应可见。
3. 之前要求过「接稿详情展示模块与接稿排期展示模块高度保持一致」。

**改动**：
1. **双栏改为左右一一对应的卡片列表**：
   - `index.html`：`.comm-dual` 由 `1fr 480px` 改为 **`1fr 1fr`**，中间 `gap:12px`（移动端 `gap:8px`）；卡片通过 `align-items:stretch` 自动等高。`.record-card` 在双栏内 `height:100%` 并 flex 纵向撑满。
   - `app.js`：`renderListPage` 中不再先渲染完整左侧列表再渲染单一右侧面板，改为**每条排期记录依次输出左卡片 + 右详情卡片**；CSS Grid 自动将左、右卡片放在同一行，保证同一排期对应的左右卡片高度一致。
   - 新增 `renderCommDetailCard(r, isSelected)`：按单主姓名关联 `commissionDetails`，为每个排期生成一个与左侧同款的 `.record-card` 详情卡片；空详情时显示「该单主暂无接稿详情档案」并提供「➕新建」按钮，有详情时显示品类关键字段并提供「👁查看」按钮。
   - 点击左侧排期卡片仍会触发 `commissionSelectCard`，重新渲染后同一行的左右两张卡片同时获得 `.selected` 高亮，实现联动。
2. **移除冗余代码**：`renderListPage` 中删除 `.comm-dual-left` 容器与旧的 `renderCommDetailPreview` 调用；保留 `renderCommDetailPreview` 函数本体但不再使用（供后续单独预览场景复用）。
3. **版本**：app.js v418→v419、sw.js v418→v419、preview-phone.html APP_VERSION=418→419。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，线上已确认 `app.js?v=419` / `APP_VERSION=419` / `xiao-workbench-v419`）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v420（2026-08-23）

1. **触发**：用户反馈 v419 右侧接稿详情卡片头部的「👁」与空态「➕」要删掉，恢复底部「查看完整详情」按钮，且详情卡片不摆单主名字；饭圈/二次把「风格」「颜色」合并为一行「风格颜色」。
2. **改动（app.js `renderCommDetailCard`）**：
   - 删除卡片头部 `👁` 图标与空态 `➕` 图标（`record-card-actions` 整段移除）。
   - 卡片底部新增「查看完整详情」按钮（`.comm-detail-actions` 内 `btn btn-primary btn-sm`，`onclick="cdJumpToDetail('${d.id}')"`），跳转对应品类完整详情。
   - 卡片标题由「排期标题/单主名」改为「详情品类」（土味/封面/饭圈/二次），不再把单主名字摆上面。
   - 饭圈（`姓名`）、二次（`角色名`）：字段精简为 `姓名/角色名 → 制品 → 尺寸 → 风格颜色`（原「风格」「颜色」两行合并为一行「风格颜色」，值取 `style + ' ' + color`）。
   - 土味/封面维持 `书名/文字 → 类型 → 尺寸 → 颜色` 不变。
3. **改动（index.html）**：新增 `.comm-detail-actions{margin-top:auto;padding-top:10px}` 与 `.comm-detail-actions .btn{width:100%}`，使底部按钮贴底并占满卡片宽度。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，线上已确认 `app.js?v=420` / `APP_VERSION=420` / `xiao-workbench-v420`）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v421（2026-08-23）

1. **触发**：用户反馈 4 点——①右侧接稿详情卡片标题不要、要回浅蓝底色、风格颜色拆开；②导入按钮删底部灰字、两处文案改名、弹窗 +60px；③聊天记录弹窗蓝底文案改指定句、且解析几乎全识别不到要修；④约稿单弹窗蓝底文案改指定句、链接弹窗「直接打开填写」改「直接填写」。
2. **右侧接稿详情卡片（app.js `renderCommDetailCard`）**：删除卡片标题渲染（不再摆品类名/单主名）；卡片头部 `.record-card-header` 改为空标签 + 浅蓝底色（index.html 新增 `.comm-detail-card .record-card-header{background:var(--c-primary-bg);...}`）；饭圈（`姓名`）/二次（`角色名`）原合并行「风格颜色」**拆开**为独立「风格」「颜色」两行（值分别取 `d.style` / `d.color`）。
3. **导入按钮区（app.js `renderCommissionDetailPage`）**：删除三个按钮底部灰色提示小字（`.cd-import-btn-desc` 整段移除）；文案「聊天记录解析导入」→「聊天记录导入」、「生成标准化约稿单」→「生成约稿单导入」（中间「腾讯问卷导入」不变）。
4. **弹窗高度（index.html + 三处 `openModal`）**：新增 `.modal.add60{max-height:calc(90vh + 60px)}`；`openCdImportChat`/`openCdImportJson`/`openCdClientForm` 三处由 `'lg add20'` 改为 `'lg add60'`（即较默认再 +60px）。
5. **聊天记录导入弹窗 + 解析修复（app.js `openCdImportChat`/`runCdChatParse`）**：
   - 顶部蓝底文案改为用户指定句：「粘贴与单主的聊天记录，系统将自动识别并回填到新建表单（标题一定要保持一致！否则可能识别不出来，有误可手动修改）」；标题改为「聊天记录导入」；textarea 占位示例补全颜色格式/昵称/英文名/生日/风格/元素等。
   - **修复解析**：原正则多处失败（昵称需紧跟「英文名」、文案需紧跟「小字」、缺「颜色格式」、交付仅支持「交付」不支持「交付方式」、元素写死 `elements` 而非表单真实字段、`m[1]||m[2]` 使两捕获组正则取错值）。重写 map：①覆盖中英别名（书名/文字、作者名、角色名、昵称/英文名、生日、IP、用途、制品、风格/风格颜色）；②新增「颜色格式」「英文名」「生日」字段回填；③元素按「必用/可选/避雷」分别回填 `elementsRequired`/`elementsOptional`/`elementsAvoid`（负向 lookbehind 防止前缀串扰）；④交付兼容「交付方式」；⑤取值统一取最后一个捕获组。用户截图反馈的姓名/昵称/颜色/元素/文案/颜色格式/交付方式现已可正确识别回填。
6. **约稿单导入弹窗（app.js `openCdClientForm`/`cdShowClientLink`）**：顶部蓝底文案改为「选择分类后生成可分享的填写链接，单主打开链接填写完成并提交后，信息会自动保存到本地接稿条中。」；弹窗标题改为「生成约稿单导入」；点击「生成分享链接」后弹窗内「直接打开填写」→「直接填写」；生成后蓝底文案维持「已生成「xxx」约稿单的填写链接」（xxx=分类名）。
7. **版本**：app.js v420→v421、sw.js v420→v421、preview-phone.html APP_VERSION=420→421。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
8. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（待验证，三处版本标记应一致为 421）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v422（2026-08-23）

1. **触发**：用户反馈 6 点——①聊天记录导入识别太严（标题字段必须完全一致才识别）要求放宽；②生成约稿单导入点击「生成分享链接」后蓝底文字未改成指定句；③约稿单制品/尺寸要与价目表联动、尺寸加提示词、追加制品格式统一；④约稿单移除「是否同柄」下拉、改「关联制品序号」联动表单；⑤追加制品展示归入「制品信息」列、左对齐；⑥右侧接稿详情蓝底作字段背景、空值不要横杠、追加制品也要展示、增加一页一柄的轮播模式。
2. **聊天记录导入识别放宽（app.js `runCdChatParse`）**：弃用 24 条严格正则，改用 `aliasMap`（字段 key→多位中英别名，如 单主/客户/宝子、书名/文字/标题、昵称/微信/qq、尺寸/大小、IP/原作/番名、制品/产品/物料、风格颜色/风格、必用元素/可选元素/避雷元素 等）+ `norm()` 归一化（去空白/标点/数字/emoji）+ 两路匹配：
   - 有冒号分支：直接对**原始标签**做 `before`/`after` 上下文校验（`before` 仅允许序号/emoji/空格等非中英前缀、`after` 仅允许空白/分隔符），严格命中才赋值，修复「尺寸 4:3」误把值 `3` 当尺寸的问题；
   - 无冒号分支：行长 ≤24 且别名出现在前 12 字符内则截取后缀赋值；
   - 交付兼容「交付」与「交付方式」；`assign(key,val)` 仅首次赋值防覆盖。弹窗 tip 改「系统会智能识别字段并自动回填（字段名不要求完全一致，识别有误可手动修改）」。
3. **生成约稿单导入分享后蓝底文案（`cdShowClientLink`）**：改为「已为你生成「${分类名}」约稿单的填写链接，复制后发送给单主即可（单主填写并提交后会自动存入本地接稿条）。」
4. **约稿单制品/尺寸与价目表联动（`prepareFields` + `buildFormField`）**：
   - 饭圈/二次模块 `product` 字段 `type` 改为 `combobox`，`options` 取自价目表中 `category∈['纸片类','其他材质类','线上&应援类']` 且 `product` 非空去重（支持下拉选择**也支持手动输入**）；`size` 字段加 `hint:'初始为默认尺寸'`。
   - 追加制品：制品与尺寸改为并排（`.cd-ep-inline`）；重要信息用 `.cd-ep-4col` 四列网格；风格/颜色/元素与初始制品对齐更新；追加制品填写格式与初始制品保持一致（`buildFormField` 新增 `custom` 类型按 `data-key` 回填逻辑）。
5. **关联制品序号替代「是否同柄」（app.js `cdHandleRefCombobox`/`cdExtraProductRowHTML`/`collectCdExtraRows`/`reRenderCdExtra`/`addCdExtraProduct`/`deleteCdExtraProduct`）**：移除「是否同柄」下拉，改为【关联制品序号】combobox，动态展示全部已创建制品序号（初始制品 0 + 追加制品 1/2…跳过自身 idx）。选「否（独立新柄）」→展示完整全套制品填写表单（`.cd-ep-full` 不隐藏）；选任意已有制品→`.cd-ep-full` 加 `hidden` 简化、隐藏重复基础信息；新增/删除制品行时 `collect→reRender` 整体重渲染，实时刷新全部行 idx 与下拉选项。`openCdDetail`/详情展示改用 `sameHandleRef`（保留 `sameHandle` 兼容映射）。
6. **右侧接稿详情卡片蓝底作字段背景（app.js `renderCommDetailCard` + index.html）**：`.record-card-header` 改 `background:transparent`（蓝底不再作标题背景）；`.comm-detail-card .field` 加 `background:var(--c-primary-bg);border-radius:6px;padding:6px 10px`（蓝底作字段背景）；`cdDetailRowsFor` 仅当值非空才 `add`（空值不再输出横杠占位）。
7. **追加制品展示归入制品信息列（app.js `renderCommDetailCard`）**：初始制品 + 各追加制品均按 `cdDetailRowsFor` 同款字段渲染，左侧文字与「制品信息」左对齐；追加制品额外显示「关联制品序号」（独立新柄 / 同柄于初始制品 / 同柄于追加制品 N）。
8. **详情卡片轮播图模式（一页一柄，可滑动翻页）（app.js `renderCommDetailCard` + index.html + 全局 scroll 监听）**：改为 `.cdc-track`（flex + `scroll-snap-type:x mandatory` + 隐藏滚动条）多页结构，每页 `.cdc-slide` 一个柄图内容（初始制品为「制品信息」页、追加制品为「制品 N」页）。底部 `.cdc-dots`/`.cdc-dot` 指示当前页；全局 `document.addEventListener('scroll',...,true)` 监听 `.cdc-track` 的 `scrollLeft` 与各 slide `offsetLeft` 距离，同步 `.cdc-dot.active`。
9. **版本**：app.js v421→v422、sw.js v421→v422、preview-phone.html APP_VERSION=421→422。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
10. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（待验证，三处版本标记应一致为 422）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v423（2026-08-23）

针对 v422 用户 6 点反馈未达标，逐项修复（仅预览，未传 GitHub / 未打包 APK）：

1. **分享蓝底文案改回指定句（`cdShowClientLink`）**：由 v422 长句改回简洁「已生成「${分类名}」约稿单的填写链接」。
2. **聊天记录解析垃圾值/冒号修复（`runCdChatParse` 的 `assign` + 无冒号行长上限）**：`assign` 新增 `isJunkVal`（空/纯冒号·标点·数字视为垃圾）—垃圾值不占位，且允许真值覆盖已被占用的垃圾值（修复「莫名其妙的冒号」与元素被占位导致识别不到）；无冒号分支行长上限 24→60（容纳多元素列表）。
3. **尺寸提示词放到 label 右侧（`buildFormField` + `prepareFields`）**：`buildFormField` 新增 `hintInline` 支持，渲染 `<label>尺寸<span class="form-label-hint">初始为默认尺寸</span></label>`；`prepareFields` 对 `size` 开 `hintInline`（不再输出在字段下方）。
4. **追加制品表单与初始制品格式一致（`cdExtraProductRowHTML`）**：追加制品完整表单改用与初始制品同款大框——`style-color-box info-box`（重要信息：姓名/昵称/英文名/生日 或 角色名/昵称/英文名/生日）、`style-color-box`（风格颜色 + `form-label-hint`「可以给参考图/色卡 请把主色写最前面」）、`style-color-box elements-box`（元素 必用/可选/避雷），字段 `data-ep` 保持不变。
5. **追加制品详情归入制品信息（不再单独成块）（`renderCdFullRecord` / `openCdDetail` + 新增 `renderCdExtraProductsRec` / `renderCdExtraProductsDetail`）**：列表页完整记录与详情弹窗均在「交付规范」section 之前把追加制品作为「制品信息」子项注入（保留「追加制品 N」小标题与各字段），移除原先独立的「追加制品」section / form-section-title。
6. **右侧详情卡片精简字段 + 去顶部标题 + 修复轮播乱跳（`cdDetailRowsFor` / `renderCommDetailCard` / `cdcGoTo` / 全局 scroll 监听）**：
   - `cdDetailRowsFor` 精简为仅 姓名(角色名)/制品/尺寸/风格/颜色（+追加制品的「关联制品序号」），去掉昵称/英文名/生日/企划/元素/文案/备注等；
   - 移除每页 `cd-detail-page-title` 顶部标题；
   - `cdcGoTo` 改用 `getBoundingClientRect` 计算相对 track 内容原点的偏移（原 `slide.offsetLeft` 因 offsetParent 错位导致点第二跳第三、无法回第一页），全局 scroll 同步圆点逻辑同步改用 `getBoundingClientRect`，轮播点击/滑动一致。
7. **版本**：app.js v422→v423、sw.js v422→v423、preview-phone.html APP_VERSION=422→423。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
8. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（待验证）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v424（2026-08-23）

针对 v423 用户 4 点反馈修复（仅预览，未传 GitHub / 未打包 APK）：

1. **右侧详情卡片改为整块蓝底（index.html `.comm-detail-card`）**：`.comm-dual .comm-detail-card.record-card` 背景改 `var(--c-primary-bg)`，覆盖原白色；`.field` 改为透明背景、无圆角、仅 padding，字段 value 加粗，实现「一整块底色」而非「一个字段一个字段的底色」。
2. **尺寸提示词重复与生日/元素识别（app.js `buildFormField` + `runCdChatParse`）**：
   - `buildFormField` 默认 text 分支改用 `belowHint`，避免 `hintInline=true` 时 label 右侧与 input 下方同时出现提示词；
   - `runCdChatParse` 的 `isJunkVal` 移除 `0-9`，避免纯数字/日期（尺寸 75、生日 2005.5.20 等）被误判为垃圾值；
   - 元素别名增加反向写法（元素必用/元素-必用、元素可选/元素-可选、元素避雷/元素-避雷）；生日别名增加「出生日期」「生日日期」。
3. **追加制品表单字段顺序与下拉框（app.js `cdExtraProductRowHTML` / `cdHandleRefCombobox` + index.html）**：
   - 「企划/主题名称」提到「重要信息」大框之前；
   - 「关联制品序号」label 改为「是否同模」，右侧加 `form-label-hint`「可选择同柄制品」；
   - 下拉选项文案改为「独立新柄」「初始制品 0」「追加制品 N」；默认值由「否（独立新柄）」改为「初始制品 0」；
   - 调整追加制品标题与大框/输入框间距（`.cd-ep-head` / `.cd-ep-title` / `.cd-extra-product-row .style-color-row`）。
4. **追加制品内外标题一致与左对齐（app.js `renderCdExtraProductsDetail` + index.html）**：详情视图中的追加制品小标题由「追加制品 2/3…」改为「追加制品 1/2…」（与表单中的追加制品序号从 1 起一致）；新增 `.detail-row.cd-extra-subtitle` 样式，蓝色加粗标题，与「制品信息」字段左侧对齐。
5. **版本**：app.js v423→v424、sw.js v423→v424、preview-phone.html APP_VERSION=423→424。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
6. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 424）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v425（2026-08-23）

针对 v424 用户反馈「右侧整块蓝底字段混在一起、看不懂」回滚样式（仅预览，未传 GitHub / 未打包 APK）：

1. **右侧详情卡片字段改回独立白底小卡片（index.html `.comm-detail-card`）**：
   - `.comm-detail-card .field` 由透明无背景改回 `background:#fff;border-radius:8px;padding:8px 10px`；
   - 字段排列由纵向堆叠改回横向 `label + value`，保持 `gap:8px` 与 `flex-wrap:wrap`；
   - `.record-card-body` 保持 `flex-direction:column` 并加 `gap:6px`，让字段卡片间有清晰间距；
   - 整体卡片仍维持浅蓝底（`--c-primary-bg`），只是字段本身恢复白底分隔。
2. **版本**：app.js v424→v425、sw.js v424→v425、preview-phone.html APP_VERSION=424→425。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
3. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 425）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v426（2026-08-23）

针对 v425 用户截图明确反馈「模块里面单独有一个框是蓝底，所有字段放框里」（仅预览，未传 GitHub / 未打包 APK）：

1. **右侧详情卡片改为白底卡片 + 内部蓝底字段框（index.html `.comm-detail-card`）**：
   - `.comm-dual .comm-detail-card.record-card` 背景由浅蓝改回白色（`var(--c-card)`），恢复卡片白底；
   - `.comm-detail-card .record-card-body` 设为蓝底圆角框（`background:var(--c-primary-bg);border-radius:12px;padding:12px`），所有字段集中放在这个框内；
   - `.comm-detail-card .field` 改为透明无背景、无圆角，字段直接铺在蓝底框上（`label + value` 横向排列），与截图一致。
2. **版本**：app.js v425→v426、sw.js v425→v426、preview-phone.html APP_VERSION=425→426。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
3. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 426）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v427（2026-08-23）

针对 v426 用户仍反馈不对、回顾最初要求「右侧底色是一整块的底色」（仅预览，未传 GitHub / 未打包 APK）：

1. **右侧详情卡片改为整块蓝底、无白色外框（index.html `.comm-detail-card`）**：
   - `.comm-dual .comm-detail-card.record-card` 背景改回 `--c-primary-bg`、边框透明，整个右侧详情模块直接是蓝底；
   - `.comm-detail-card .record-card-body` 改为透明无背景、无圆角、无内边距，不再套一个额外的蓝底框；
   - `.comm-detail-card .field` 保持透明无背景，字段横向排列（`label + value`），与 v424 的纵向堆叠区分。
2. **版本**：app.js v426→v427、sw.js v426→v427、preview-phone.html APP_VERSION=426→427。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
3. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 427）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v428（2026-08-23）

用户最终明确指令「v426 右侧内部样式，v423 并列布局排版」——即：**保留 v423 的并排双栏布局（左列表 + 右详情），但右侧详情内部底色回退到 v426 的白底卡片 + 内部蓝底字段框**。纠正 v427 自作主张又改回单栏布局的错误（该错误）。

1. **右侧详情卡片内部改为白底卡片 + 内部蓝底字段框（index.html `.comm-detail-card`，恢复 v426 内部样式）**：
   - `.comm-dual .comm-detail-card.record-card` 背景白底（`var(--c-card)`）+ 浅边框（v426 内部样式）；
   - `.comm-detail-card .record-card-body` 蓝底圆角框（`background:var(--c-primary-bg);border-radius:12px;padding:12px`），所有字段集中放框内；
   - `.comm-detail-card .field` 透明无背景，字段横向排列（`label + value`）。
2. **布局保留 v423 并排双栏**：`.comm-dual` 双栏 `1fr 1fr` 不改动（用户认可 v423 排版，仅错在后续 v427 又自作主张改单栏）。
3. **版本**：app.js 维持 v428（未改 JS 逻辑）、sw.js v427→v428、preview-phone.html APP_VERSION=427→428、index.html `app.js?v=427→428`。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 428）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v429（2026-08-23）

针对 v428 用户 5 点反馈（附 7 图）逐项修复（仅预览，未传 GitHub / 未打包 APK）：

1. **右侧详情卡片视觉精修（index.html `.comm-detail-card` + `.cdc-track`）**：
   - 白底卡片 `.comm-detail-card.record-card` 背景 `var(--c-card)` + 浅边框；
   - **轮播区（`.cdc-track`）浅蓝底** `background:var(--c-primary-bg)`（=#E0F2FF，图3 浅蓝色值，纠正 v428 误改 transparent 的错误方向）；
   - **字段改为独立浅蓝/白底小框** `.field{background:var(--c-card);border-radius:8px;padding:8px 10px}`，不再整块蓝底；
   - **轮播分页点置于蓝色底色外面的白底区域**（分页控件移到 `.cdc-track` 之外）；
   - **小标题后文字去加粗** `.field-value{font-weight:400}`；
   - 强调**均分双栏并排**（`.comm-dual` 维持 `1fr 1fr` 并排）。
2. **元素识别修复（app.js `runCdChatParse` → `parseElementLine`）**：重写解析逻辑——**优先从标签提取子类型**（`labelRaw.indexOf(必用/可选/避雷) > -1` 则归类），再退回按值内拆分，均无则整体进必用。修复「元素—必用：星星」这种**破折号前缀分行写法**以前只从值里找子类型导致可选/避雷永远为空的问题。
3. **约稿单纵向间距统一（index.html 追加制品编辑器 CSS）**：所有「标题→输入框」「输入框→下一个小标题」统一为 14px 节奏（`.cd-ep-grid`/`.cd-ep-full{gap:14px}`、`.form-row`/`.style-color-row{margin-bottom:0}`，靠 grid/full gap 控制，避免叠加重）；
4. **是否同模下拉修复（app.js `cdHandleRefCombobox`）**：文案「否」改回 **「否（独立新柄）」**；下拉选项**只列 sameHandleRef==='否'（独立新柄）的追加制品**（初始制品0 / 独立新柄追加制品 / 其他独立新柄追加制品），排除非独立新柄追加制品误入选项。
5. **追加制品外部标题左对齐 + 内外序号统一（app.js + index.html）**：
   - 外部小标题改用 `form-section-title cd-extra-subtitle` 同款（左边框 + 8px padding），与上方「制品信息」区块标题视觉左对齐（纠正原 `detail-label` 左缩进差 ~11px）；
   - 轮播页内标题 `制品 N` 改统一为 `追加制品 N`（内外序号一致）。
6. **版本**：app.js v428→v429、sw.js v428→v429、preview-phone.html APP_VERSION=428→429、index.html `app.js?v=428→429`。四文件已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
7. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 429）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v430（2026-08-23）

针对 v429 用户爆发式反馈（附 5 图，情绪激动）逐项复修（仅预览，未传 GitHub / 未打包 APK）：

1. **右侧详情卡片字段去白底小框（index.html `.comm-detail-card .field`）**：用户明确「单个字段不需要白色底色」。`background:var(--c-card)` 改回 `background:transparent;border-radius:0;padding:4px 0`，字段不再有独立白底小框；整体白底卡片 + `.cdc-track` 浅蓝底 #E0F2FF 维持不变。
2. **全局间距真正统一（index.html 多处 CSS）**：用户指出「间距依旧不一致」。把「标签→输入框」统一由 6px 加大到 8px（`.form-row{gap:8px}`、`.form-row-inline .form-inline-field{gap:8px}`、`.style-color-col{gap:8px}`、`.cd-ep-field{gap:8px}`）；`.form-row-label{margin-bottom:8px}`（原为 6px）；追加制品标题 `.cd-ep-head{margin:14px 0}`（上下对称，原为 `14px 0 0` 只有上边距）。
3. **追加制品「是否同模」默认初始制品 0（app.js）**：用户要求「追加制品是否同模默认选择初始制品」。`cdExtraProductRowHTML` 与 `addCdExtraProduct` 的 `sameHandleRef` 默认值由 `'否'` 改为 `'0'`（初始制品 0），新增/渲染追加制品默认即选中初始制品。
4. **外部完整记录标题内外序号 + 左对齐统一（app.js `renderCdExtraProductsRec`）**：用户指出「里面追加制品1外面追加制品2还是不一致啊」「说的左对齐也没有」。外部完整记录的追加制品小标题由 `cd-rec-subtitle` + `idx+2` 改为 `form-section-title cd-extra-subtitle` + `idx+1`，与弹窗内 `renderCdExtraProductsDetail`（`idx+1`）及轮播内 `renderCommDetailCard`（`i+1`）三者序号一致且同款左对齐。
5. **双栏并排均分布局不动**：用户强调「并排均分！！！」——`.comm-dual{grid-template-columns:1fr 1fr}` 本就存在且正确，本轮未改。
6. **版本**：app.js v429→v430、sw.js v429→v430、preview-phone.html APP_VERSION=429→430、index.html `app.js?v=429→430`。五文件（含 manifest.json）已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
7. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 430）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v431（2026-08-23）

针对 v430 用户三点严厉反馈（附 3 图，指责「只看一半、截图不给看、把 v423 正确样式改坏」）逐项强制修复（仅预览，未传 GitHub / 未打包 APK）：

1. **强制双栏真正均分（index.html `.comm-dual` + 字段值）**：用户指责「右边这么大一块左边都快没了叫均分？接稿排期列表改回 v423 版本」。根因是 CSS Grid `1fr 1fr` 等价于 `minmax(auto,1fr)`，右侧详情卡片内长字段值会撑开列宽。
   - `.comm-dual{grid-template-columns:1fr 1fr}` → `minmax(0,1fr) minmax(0,1fr)`（桌面 + 移动端 media query 同步）；
   - `.comm-dual .record-card` / `.record-card-body` 加 `min-width:0`；
   - `.comm-detail-card .field-label{flex-shrink:0}` + `.field-value{flex:1;min-width:0}`，长值换行不再撑宽右列。
   - 逻辑结构本就与 v423 一致（renderListPage 左右一一对应卡片），本轮只修 CSS 均分根因，满足「回退到真正均分的 v423 样式」。
2. **约稿单弹窗去除空行与叠加间距（index.html `.form-row-inline` 规则）**：用户指责「空这么大一块说间距统一了？」。根因：buildForm 同行 row 字段在无 rowLabel 时仍生成空 `.form-row-label` 占位（margin-bottom:8px）产生空洞，且 `.form-row-inline{margin-bottom:14px}` 与上一个 `.form-row` 的 14px 堆叠。
   - 新增 `.form-row-inline:has(> .form-row-label:empty){margin-bottom:0}` + `.form-row-inline:has(> .form-row-label:empty) > .form-row-label{display:none}`；
   - 间距由上一个 `.form-row` 的 `margin-bottom:14px` 单一控制，「稿件用途→制品/尺寸」之间只剩 14px 正常间距，无空洞。
3. **追加制品标题去竖线只对齐（index.html `.cd-extra-subtitle`）**：用户明确「展示模块追加制品不需要左侧的竖线，我只要对齐不要样式一致」。
   - `.cd-extra-subtitle` 去 `border-left` + `padding-left:0`（去掉蓝色竖线样式）；
   - 弹窗内（`.detail-view` 包裹）`padding-left:0` 与 `.detail-row`（无左 padding）对齐；
   - 完整记录内（`.cd-rec-body` 包裹）`padding-left:11px` 与 `.cd-rec-row` 的 11px 左内边距对齐。
4. **版本**：app.js v430→v431、sw.js v430→v431、preview-phone.html APP_VERSION=430→431、index.html `app.js?v=430→431`。五文件（含 manifest.json）已同步至 `D:/wbkey/webview-app/app/src/main/assets/`。`node --check` 通过。
5. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 431）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v432（2026-08-23）

针对 v431 用户三点严厉反馈（附 3 图）逐项修复（仅预览，未传 GitHub / 未打包 APK）：

1. **追加制品标题竖线仍未去掉（index.html）**：根因是 `.cd-extra-subtitle` 与 `.form-section-title` 优先级相同且 `.form-section-title` 后定义，`border-left` 仍生效。
   - 改为高优先级 `.form-section-title.cd-extra-subtitle{border-left:none;padding-left:0}`；弹窗/完整记录分别用 `.detail-view .form-section-title.cd-extra-subtitle{padding-left:0}` 与 `.cd-rec-body .form-section-title.cd-extra-subtitle{padding-left:11px}` 对齐。
2. **约稿单弹窗 IP 上边距异常 / 空 label 占位（app.js + index.html）**：根因是 `buildForm` 在无 rowLabel 的同行字段中仍生成空 `.form-row-label`，靠 CSS `:has` 隐藏；而部分 WebView 不支持 `:has`，空 label 继续占位导致间距不均。
   - `buildForm` 改为：无 rowLabel 时直接不生成 `.form-row-label` 元素。
   - 删除 `.form-row-inline:has(...)` 的 CSS hack；间距统一由 `.form-row`/`.form-row-inline{margin-bottom:14px}` 单一控制。
3. **右侧接稿详情卡片内部布局错乱（app.js + index.html 重写）**：用户指出「模块内全是乱的，直接删了重写」。直接删除轮播/圆点/浅蓝内框，改为**扁平白底卡片**：
   - `renderCommDetailCard` 不再生成 `.cdc-track`/`.cdc-slide`/`.cdc-dots`/`.cdcGoTo`，仅展示初始制品关键字段（`cdDetailRowsFor(d, cat, false)`）。
   - 删除 `document` scroll 监听器。
   - `.comm-detail-card .record-card-body` 改为普通 flex 列（无浅蓝背景、无内框 padding 嵌套），字段直接平铺；`.comm-detail-actions` 保留在底部。
4. **版本**：app.js v431→v432、sw.js v431→v432、preview-phone.html APP_VERSION=431→432、index.html `app.js?v=431→432`。五文件同步 webview assets，`node --check` 通过。
5. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 432）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v433（2026-08-23）

针对 v432 用户两点反馈（附 2 图）逐项修复（仅预览，未传 GitHub / 未打包 APK）：

1. **右侧详情卡片恢复轮播 + 浅蓝底模块（app.js + index.html）**：用户要求「右侧模块内增加一个模块填入浅蓝色底色，字段放在模块内，加回轮播图设计，底部查看完整详情按钮无法点击跳转」。
   - `renderCommDetailCard` 重写为轮播结构：每页一个柄图（初始制品 + 各追加制品），每页有「制品信息 / 追加制品 N」小标题，字段平铺在浅蓝底 `.cdc-track` 模块内；单页无圆点，多页显示圆点并可点击切换。
   - 恢复 `cdcGoTo` 与 `document` scroll 监听器，滑动时同步圆点高亮。
   - 新增 `.comm-detail-card .cdc-track`（`background:var(--c-primary-bg)`、`border-radius:12px`、`padding:12px`、`scroll-snap-type:x mandatory`）、`.cdc-slide`、`.cd-detail-page-title`、`.cdc-dots`/`.cdc-dot` 样式。
   - 底部「查看完整详情」按钮增加 `type="button"`、`event.stopPropagation()`，并为 `.comm-detail-actions` 与按钮加 `position:relative;z-index` 与 `pointer-events:auto`，避免被轮播区域遮挡导致无法点击。
2. **追加制品标题字号缩小（index.html）**：用户截图指出弹窗/详情展示模块中「追加制品 1/2」标题字号过大。
   - `.form-section-title.cd-extra-subtitle` 字号由默认 14px 改为 12px，保持去竖线、左对齐。
3. **版本**：app.js v432→v433、sw.js v432→v433、preview-phone.html APP_VERSION=432→433、index.html `app.js?v=432→433`。五文件同步 webview assets，`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 433）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v434（2026-08-23）

针对 v433 用户反馈（附 1 图）修复（仅预览，未传 GitHub / 未打包 APK）：

1. **右侧详情卡片浅蓝模块溢出卡片边界（index.html）**：用户截图显示 `.cdc-track` 浅蓝模块右侧超出白色卡片。
   - 根因：`.cdc-track` 未设 `width:100%` 与 `box-sizing:border-box`，`padding:12px` 把总宽度撑出卡片。
   - 给 `.comm-detail-card .cdc-track` 增加 `width:100%`、`max-width:100%`、`box-sizing:border-box`，确保模块严格限制在卡片内部。
2. **版本**：app.js v433→v434、sw.js v433→v434、preview-phone.html APP_VERSION=433→434、index.html `app.js?v=433→434`。五文件同步 webview assets，`node --check` 通过。
3. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 434）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v435（2026-08-23）

针对 v434 用户反馈（附 1 图）修复（仅预览，未传 GitHub / 未打包 APK）：

1. **右侧详情卡片仅保留底部按钮（app.js + index.html）**：用户明确指令「右侧卡片内除了底部按钮全部删除」。
   - `renderCommDetailCard` 删除标题、轮播浅蓝模块、字段行、圆点、空状态提示，仅保留「查看完整详情」按钮。
   - 删除 `cdDetailRowsFor`、`cdcGoTo`、轮播 scroll 监听器等仅被轮播使用的辅助代码（保留 `cdHandleRefLabel`，弹窗详情仍使用）。
   - 清理右侧卡片专用 CSS：删除 `.cdc-track`/`.cdc-slide`/`.cdc-dots`/`.cdc-dot`/`.cd-detail-page-title`/`.field`/`.field-label`/`.field-value`/`.comm-detail-empty`/`.comm-detail-empty-sm`/`.record-card-header` 等废弃规则。
   - 右侧卡片改为 `display:flex;flex-direction:column`，`.comm-detail-actions` 改为 `flex:1;display:flex;align-items:center;justify-content:center;padding:16px`，让按钮在空白卡片内垂直居中。
2. **版本**：app.js v434→v435、sw.js v434→v435、preview-phone.html APP_VERSION=434→435、index.html `app.js?v=434→435`。五文件同步 webview assets，`node --check` 通过。
3. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 435）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v436（2026-08-23）

针对 v435 用户反馈（附 1 图）重新实现右侧详情卡片（仅预览，未传 GitHub / 未打包 APK）：

1. **右侧详情卡片按截图样式恢复并优化（app.js + index.html）**：用户要求「右侧按钮内按这个样式做」，即右侧卡片内展示接稿详情信息。
   - 顶部增加「单主：xxx」标题（`.cdc-header`）。
   - 字段按品类精简：
     - 土味/封面：书名/文字、类型、尺寸、颜色；
     - 饭圈：姓名、制品、尺寸、风格、颜色；
     - 二次：角色名、制品、尺寸、风格、颜色；
     - 其他：类型、制品、尺寸、颜色。
   - 字段无内容时显示空白，不再用「—」横杠占位。
   - 恢复轮播模式：一页一个柄图内容，初始制品为第 1 页，追加制品依次分页；支持左右滑动翻页，多页时显示圆点指示器。
   - 每页字段放在浅蓝底模块（`.cdc-slide`）内，字段使用 `grid-template-columns:auto 1fr` 两列布局，label 右对齐、value 左对齐。
   - 底部「查看完整详情」按钮保留，点击仍跳转接稿详情对应展示块。
   - 恢复/新增 `cdDetailRowsFor`、`cdcGoTo`、轮播 scroll 同步监听器；清理废弃的 v435「仅按钮」样式。
2. **版本**：app.js v435→v436、sw.js v435→v436、preview-phone.html APP_VERSION=435→436、index.html `app.js?v=435→436`。五文件同步 webview assets，`node --check` 通过。
3. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 436）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v437（2026-08-23）

针对 v436 用户 8 点反馈（附 1 图）优化右侧详情卡片（仅预览，未传 GitHub / 未打包 APK）：

1. **内框蓝色底色调浅**：`.cdc-slide` 背景由 `var(--c-primary-bg)`(#E0F2FF，偏深) 改为更浅的 `#EFF6FF`，贴合用户截图参考色；`border-radius` 12→14px。
2. **内框加大**：`.cdc-slide` 的 `padding` 12→16px，网格 `gap` 8px 12px→10px 14px。
3. **顶部单主信息改为柄图标签**：`.cdc-header` 由「单主：xxx」改为动态显示当前页柄图标签——第 0 页「初始制品 0」、第 i 页「追加制品 i」；新增 `cdcUpdateHeader(id,i)`，`cdcGoTo` 与圆点滑动同步监听均会更新顶部标签。
4. **内框字段标题左对齐**：`.cdc-label` 由 `text-align:right` 改为 `left`；网格列宽由 `auto 1fr` 改为固定 `76px 1fr`，左对齐更整齐。
5. **同柄图追加制品同步字段**：`cdDetailRowsFor` 增加 `base`（初始制品）参数；当追加制品 `sameHandleRef==='0'`（同柄于初始制品）且自身 姓名/角色名、风格、颜色 为空时，从初始制品同步这些值。
6. **增加「是否同模」字段放最后**：每个 slide 末尾追加一行 `是否同模`，初始制品显示「否」，追加制品显示 `cdHandleRefLabel`（独立新柄 / 同柄于初始制品 / 同柄于追加制品 N）。
7. **轮播圆点改小**：`.cdc-dot` 由 8px→6px，`.cdc-dots` gap 6→5px。
8. **修复底部按钮不跳转（关键 bug）**：`cdJumpToDetail` 中 `pageState['design-commission-detail']` 在用户未访问过接稿详情页时为 `undefined`，直接 `ps.tab=...` 抛 `TypeError` 中断导致 `navigate()` 永不执行。改为先判空并初始化（带 `thisMonthStr()` 默认值）再赋值，按钮现已正常跳转。
9. **版本**：app.js v436→v437、sw.js v436→v437、preview-phone.html APP_VERSION=436→437、index.html `app.js?v=436→437`。五文件同步 webview assets，`node --check` 通过。
10. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 437）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v438（2026-08-23）

针对 v437 用户 3 图反馈修复右侧详情卡片与约稿单标签（仅预览，未传 GitHub / 未打包 APK）：

1. **内框蓝色按截图色值调浅**：用 Python 标准库解析截图 PNG，中心区域平均色为 `#F4FCFF`，`.cdc-slide` 背景由 `#EFF6FF` 改为 `#F4FCFF`。
2. **内框圆角 12px**：`.cdc-slide` `border-radius` 14px→12px。
3. **内框加宽**：`.cdc-slide` `padding` 16px→18px。
4. **字段文字离标题更近**：网格 `gap` 由 `10px 14px` 改为 `10px 10px`。
5. **字段标题字号**：`.cdc-label` 保持 `font-size:12px`。
6. **右侧卡片区分“同柄”与“是否同模”**：
   - 轮播标题显示同柄关系：第 0 页「初始制品 0」；第 i 页非独立新柄时显示「追加制品 i—与初始制品 0 同柄」或「追加制品 i—与追加制品 M 同柄」。
   - slide 末尾「是否同模」改为读取 `sameModel` 字段显示原值（如「否」「改人」等），不再把同柄标签错填到这里。
   - `renderCommDetailCard` 给每个 `.cdc-slide` 增加 `data-same-handleref`；`cdcUpdateHeader` 读取该属性拼标题。
7. **约稿单追加制品标签修正**：选择同柄制品的下拉框标签由「是否同模」改为「是否同柄」，提示文字保留；下方复选框组仍为真正的「是否同模」。
8. **版本**：app.js v437→v438、sw.js v437→v438、preview-phone.html APP_VERSION=437→438、index.html `app.js?v=437→438`。五文件同步 webview assets，`node --check` 通过。
9. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 438）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v439（2026-08-24）

针对 v438 用户 1 图反馈继续微调右侧详情卡片（仅预览，未传 GitHub / 未打包 APK）：

1. **无详情档案的空白态顶部标题统一**：`renderCommDetailCard` 空状态不再显示「单主：xxx」，改为和其他卡片一致的「初始制品 0」，避免两个展示模块标题不一致。
2. **内框继续加宽**：`.cdc-body` 内边距由 `12px 14px` 改为 `10px 12px`，`.cdc-slide` 内边距由 `18px` 改为 `22px`，蓝色底模块在卡片内更宽、更饱满。
3. **字段文字离标题更近**：网格列宽由 `76px 1fr` 改为 `64px 1fr`，横向 `gap` 由 `10px` 改为 `6px`；标签列更窄、间距更小，字段值明显左移靠近标题。
4. **明确字段标题字号**：`.cdc-label` 字号为 **12px**（已在 CSS 中写明，本轮不变）。
5. **修复同柄标题未生效**：v438 中 `.cdc-slide` 写的是 `data-same-handleref`，而 `cdcUpdateHeader` 读取的是 `dataset.sameHandleRef`，dataset 驼峰映射不匹配导致同柄后缀永远读成「否」。改为 `data-same-handle-ref`，现在追加制品同柄时标题会正确显示「追加制品 i—与初始制品 0 同柄」。
6. **版本**：app.js v438→v439、sw.js v438→v439、preview-phone.html APP_VERSION=438→439、index.html `app.js?v=438→439`。五文件同步 webview assets，`node --check` 通过。
7. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 439）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v440（2026-08-24）

针对 v439 用户 1 图反馈继续微调右侧详情卡片标题与字段间距（仅预览，未传 GitHub / 未打包 APK）：

1. **同柄标题改为右侧提示小字**：`cdcUpdateHeader` 不再把同柄信息用 em-dash 拼在标题后面（会换行），而是拆成左右两部分——左侧主标题「追加制品 i」，右侧小字提示「与初始制品 0 同柄」/`与追加制品 M 同柄`；初始制品仍只显示「初始制品 0」。新增 `.cdc-header-hint` 样式（11px，浅灰，不换行）。
2. **字段标题往左移 6px**：`.cdc-slide` 左内边距由 `22px` 改为 `16px`。
3. **字段整体往上移**：`.cdc-slide` 上内边距由 `22px` 改为 `16px`。
4. **字段行间距缩短**：网格纵向 `gap` 由 `8px` 改为 `6px`。
5. **标题和文字间距再缩短**：网格横向 `gap` 由 `6px` 改为 `4px`。
6. **版本**：app.js v439→v440、sw.js v439→v440、preview-phone.html APP_VERSION=439→440、index.html `app.js?v=439→440`。五文件同步 webview assets，`node --check` 通过。
7. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 440）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v441（2026-08-24）

针对 v440 用户 1 图反馈继续微调右侧详情卡片（仅预览，未传 GitHub / 未打包 APK）：

1. **提示小字数字两侧加空格**：同柄提示由「与初始制品0同柄」改为「与初始制品 0 同柄」，让数字与前后字词不再贴在一起。
2. **提示小字字号缩小**：`.cdc-header-hint` 由 `11px` 改为 `10px`。
3. **字段整体往左移 4px**：`.cdc-slide` 左内边距由 `16px` 改为 `12px`。
4. **字段整体往上移 8px**：`.cdc-slide` 上内边距由 `16px` 改为 `8px`。
5. **字段行间距再缩短**：网格纵向 `gap` 由 `6px` 改为 `4px`。
6. **标题和文字间距再缩短**：网格横向 `gap` 由 `4px` 改为 `3px`。
7. **版本**：app.js v440→v441、sw.js v440→v441、preview-phone.html APP_VERSION=440→441、index.html `app.js?v=440→441`。五文件同步 webview assets，`node --check` 通过。
8. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 441）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v442（2026-08-24）

针对 v441 用户 1 图反馈继续微调右侧详情卡片（仅预览，未传 GitHub / 未打包 APK）：

1. **提示小字靠左贴近标题**：`.cdc-header` 由 `justify-content:space-between` 改为 `flex-start`，gap 由 `8px` 改为 `6px`，提示小字紧跟在主标题右侧。
2. **提示小字数字两侧去掉空格**：同柄提示由「与初始制品 0 同柄」改回「与初始制品0同柄」。
3. **字段整体上移 2px**：`.cdc-slide` 上内边距由 `8px` 改为 `6px`。
4. **字段行间距缩短 4px**：网格纵向 `gap` 由 `4px` 改为 `0px`。
5. **标题和文字间距缩短 3px**：网格横向 `gap` 由 `3px` 改为 `0px`。
6. **蓝色底内框高度按内容自适应**：`.cdc-slide` 增加 `align-self:flex-start`，不再被轮播轨道拉伸，蓝色背景高度只包裹字段内容 + 内边距。
7. **版本**：app.js v441→v442、sw.js v441→v442、preview-phone.html APP_VERSION=441→442、index.html `app.js?v=441→442`。五文件同步 webview assets，`node --check` 通过。
8. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 442）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v443（2026-08-24）

针对 v442 用户 1 图反馈继续微调右侧详情卡片，并统一左侧排期展示框字段样式（仅预览，未传 GitHub / 未打包 APK）：

1. **提示词小字与标题水平居中**：`.cdc-header` 由 `align-items:baseline` 改为 `align-items:center`，提示小字与主标题垂直居中对齐。
2. **字段整体下移 1px**：`.cdc-slide` 上内边距由 `6px` 改为 `7px`。
3. **字段标题和文字间距缩短 3px**：`.cdc-slide` 网格第一列宽由 `64px` 改为 `61px`。
4. **蓝色底内框高度减少 / 上下边距对称**：`.cdc-slide` 内边距由 `6px 22px 22px 12px` 改为 `7px 12px 7px 12px`，上下边距相同且整体更紧凑。
5. **左侧排期展示框字段样式与右侧相同**：`.comm-dual .record-card-body` 增加蓝底圆角内框、两列网格布局（61px/1fr、gap 0）、字段标题/值字号颜色与右侧 `.cdc-label`/`.cdc-value` 一致；制品复选框单独保持块级显示。
6. **版本**：app.js v442→v443、sw.js v442→v443、preview-phone.html APP_VERSION=442→443、index.html `app.js?v=442→443`。五文件同步 webview assets，`node --check` 通过。
7. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 443）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v444（2026-08-24）

针对 v443 用户 1 图反馈继续微调右侧详情卡片与左侧排期展示框（仅预览，未传 GitHub / 未打包 APK）：

1. **右侧蓝色底内框底部增加 2px**：`.cdc-slide` 下内边距由 `7px` 改为 `9px`。
2. **视觉上下边距平衡**：`.cdc-slide` 上内边距由 `7px` 改为 `5px`；`.cdc-body` 内边距由 `10px 12px` 改为 `8px 12px`；`.cdc-label` `padding-top` 由 `1px` 改为 `0`，减少上方多余空白。
3. **左侧排期展示框去掉蓝色底**：移除 `.comm-dual .record-card-body` 的蓝底、圆角、padding 及字段 grid 样式，恢复原来普通字段排版。
4. **左侧字段标签去掉冒号**：`renderListPage` 中针对 `design-commission` 模块将字段 label 后的 `:` 改为空，其他模块保持不变。
5. **左侧「制品」复选框从本行开始**：`.comm-dual .record-card-body .comm-product-checklist` 改为 `flex-direction:row;flex-wrap:wrap`，第一个制品选项与「制品」标签同行开始。
6. **稿件进度 / 支付状态 tag 长度保持原状**：左侧恢复默认 `.field` flex 布局后，tag 按钮仍按内容自适应宽度。
7. **版本**：app.js v443→v444、sw.js v443→v444、preview-phone.html APP_VERSION=443→444、index.html `app.js?v=443→444`。五文件同步 webview assets，`node --check` 通过。
8. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 444）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v445（2026-08-24）

针对 v444 用户 1 图反馈修复左侧字段标题与文字间距（仅预览，未传 GitHub / 未打包 APK）：

1. **左侧普通字段恢复两列网格布局**：`.comm-dual .record-card-body .field:not(.comm-products-field)` 改为 `display:grid;grid-template-columns:61px 1fr;gap:0 0`，与右侧 `.cdc-slide` 列宽一致，标题列 61px，值列 1fr。
2. **左侧字段标题/值样式与右侧一致**：`.field-label` 字号 12px、颜色 `--c-text-muted`、无 padding-top；`.field-value` 字号 13px、颜色 `--c-text`、font-weight 400、自动换行。
3. **左侧保持无蓝色底**：仅恢复字段 grid 间距，不恢复蓝底背景。
4. **制品复选框仍从本行开始**：`.comm-product-checklist` 保持 `row wrap`。
5. **稿件进度 / 支付状态 tag 按钮长度保持原状**：grid 第二列按内容自适应，tag 不会拉伸。
6. **版本**：app.js v444→v445、sw.js v444→v445、preview-phone.html APP_VERSION=444→445、index.html `app.js?v=444→445`。五文件同步 webview assets，`node --check` 通过。
7. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 445）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v446（2026-08-24）

针对 v445 用户 1 图反馈修复左侧 tag 拉伸与字段标签（仅预览，未传 GitHub / 未打包 APK）：

1. **稿件进度 / 支付状态 tag 不再被拉伸**：`renderListPage` 中 tag 渲染由直接放在 `.field` 下改为包进 `.field-value` / `.field-tags`，并在 `.comm-dual .record-card-body .field .field-value` 加 `justify-self:start`，使 tag 按内容宽度显示。
2. **左侧「制品:」「完成:」冒号去掉**：`renderListPage` 中 design-commission 的「制品:」改为「制品」，「完成:」改为「完成进度」。
3. **左侧制品复选框文字间距加大**：`.comm-dual .record-card-body .comm-product-item` 的 gap 由 4px 改为 8px。
4. **版本**：app.js v445→v446、sw.js v445→v446、preview-phone.html APP_VERSION=445→446、index.html `app.js?v=445→446`。五文件同步 webview assets，`node --check` 通过。
5. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 446）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v447（2026-08-24）

六合一迭代（仅预览，未传 GitHub / 未打包 APK）：

1. **约稿单「您的平台昵称」自动同步到单主**：`setupFormInteractions` 对 `design-commission-detail*` 页面，给 `platformNick` 输入框加 `input` 监听，单主（clientInfo）为空时自动填入平台昵称。
2. **聊天记录导入提示内容替换**：`openCdImportChat` 的 textarea placeholder 改为指定字段清单（平台昵称/稿件用途/制品/尺寸/企划主题名称/姓名/昵称/英文名/生日/风格/颜色/元素三类/文案/备注/颜色格式/交付方式/是否可以展示）。
3. **生成约稿单导入合并为单页**：`openCdClientForm` 不再有「生成分享链接」按钮切换；选择分类后同一页内下方立即显示链接框 + 复制/直接填写按钮；`cdCatComboboxHTML` 增加 `onchange` 参数，`cdClientCatChange` 实时刷新链接。
4. **接稿详情表筛选月份跟随接稿排期表**：`renderCommissionDetailPage` 读取 `design-commission` 的 `dateFilter`，有值则把约稿单页 `cdMonth` 同步为其年月。
5. **左侧制品对齐 + 勾选框间距还原**：`.comm-products-field` 改为 `display:grid;grid-template-columns:61px 1fr`（「制品」标签与字段标签、复选框与字段值同列对齐）；撤销 v446 对 `.comm-product-item` 的 8px gap，恢复 4px。
6. **右侧轮播圆点上下间距对称**：`.cdc-dots` 的 `margin-top:6px` 改为 `0`，使圆点到上方轮播框间距与到下方「查看完整详情」按钮间距一致（均 8px）。
7. **版本**：app.js v446→v447、sw.js v446→v447、preview-phone.html APP_VERSION=446→447、index.html `app.js?v=446→447`。五文件同步 webview assets，`node --check` 通过。
8. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 447）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v448（2026-08-24）

八合一迭代（仅预览，未传 GitHub / 未打包 APK）：

1. **聊天/腾讯问卷导入「选择分类」上移**：`openCdImportChat` / `openCdImportJson` 将「选择分类」下拉从输入框下方移到 textarea 上方。
2. **生成约稿单导入删蓝底块**：`openCdClientForm` 删除自动生成的蓝底链接块（`cdClientLinkWrap` + `cdClientLinkInner` 内联调用），改为「生成填写链接」按钮调用 `cdShowClientLink`。
3. **接稿排期列表左侧模块字段扩充**：`design-commission.listFields` 在「截稿日期」前加「开稿日期」(`startTime`)、其后加「报价金额」(`quoteAmount`)；「制品」标签 12px 且与勾选框垂直居中。
4. **右侧轮播圆点固定底部**：`renderCommDetailCard` 把 `cdc-dots` 移出 `cdc-body`，置于卡片内底部、「查看完整详情」按钮之上；CSS `.cdc-dots{margin-top:auto}`。
5. **新增记录日期文案**：`design-commission` 三字段 接稿时间/开稿时间/截稿时间 → 接稿日期/开稿日期/截稿日期；关联块文案同步（开稿日期/截稿日期）。
6. **约稿单详情字段缺失 + 月份归属修复（关键 bug）**：`renderCdFullRecord` 新增 `type:'custom'` 处理，解析饭圈/二次模块的「重要信息（姓名/昵称/英文名/生日）、风格颜色（风格/颜色）、元素（必用/可选/避雷）」子字段并渲染非空项；`cdRecMonth` 在有关联接稿排期时改用排期日期（截稿优先，其次开稿/接稿）归月，使约稿条归入正确月份。
7. **九模块列表卡样式统一**：新增 `COMM_LIST_STYLE_MODULES`（开团/厂家/打样/灵感/接稿排期/授权/人物档案/故事小记/约稿记录），列表卡 `record-card-body` 挂 `comm-list-style`，字段改为 61px 两列、12px 标签 / 13px 值、无冒号，与接稿排期左侧模块一致；单列/双列保持原样。
8. **打样记录双列分页**：`groupbuy-samples` 加入 `TWO_COL_MODULES`，`PAGE_SIZES['groupbuy-samples']=10`（一页 5 排 10 个）。
9. **详情页小标题去粗对齐**：`.cd-rec-section` / `.form-section-title` / `.detail-label` / `.cd-rec-subtitle` 字号改 13px、取消加粗，与正文一致。
10. **版本**：app.js v447→v448、sw.js v447→v448、preview-phone.html APP_VERSION=447→448、index.html `app.js?v=447→448`。`node --check` 通过。
11. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v449（2026-08-24）

五微调（仅预览，未传 GitHub / 未打包 APK）：

1. **所有记录详情页表格标题取消加粗**：`.detail-table th` `font-weight:600→400`（约稿单详情制品表等表头去粗，与正文一致）。
2. **生成约稿单导入：删蓝底块 + 同一界面**：`openCdClientForm` 改为点击「生成填写链接」后在原弹窗内 `#cdClientLinkWrap` 就地注入链接框+复制/直接填写按钮（`cdShowClientLinkInline`），不再开新弹窗；`cdClientLinkInner` 与 `cdShowClientLink` 删除蓝底 `.cd-import-tip` 提示块。`cdCatComboboxHTML` 传 `onchange=cdClientCatChange()`，分类切换仅刷新已生成的链接（生成前不预生成）。聊天记录导入 / 腾讯问卷导入「选择分类」到填写框下边距本就一致（均 `margin-bottom:10px`），维持不变。
3. **接稿排期双栏对齐 + 圆点边距**：`.comm-dual .record-card-header{margin-bottom:4px}` + `.comm-dual .cdc-header{padding-top:16px}`，使左卡首字段「稿件进度」与右卡首行「姓名」顶部对齐；`.cdc-dots` 由 `margin-top:auto;padding-top:8px` 改为 `margin:8px 0;padding:0`，上下边距对称（去除顶部过多、底部全无）。
4. **接稿详情小标题加粗改回**：`.cd-rec-section` `font-weight:400→600`、`.form-section-title.cd-extra-subtitle` 加 `font-weight:600`（重要信息/风格颜色/元素等分组标题与追加制品标题恢复加粗；表格 th 仍按 v449-1 去粗，互不冲突）。
5. **九模块空字段仍显示标题**：`renderListPage` 对 `COMM_LIST_STYLE_MODULES`（开团/厂家/打样/灵感/接稿排期/授权/人物档案/故事小记/约稿记录）空值字段不再整行隐藏，改为仍渲染 `.field-label` 标题 + 空值。
6. **版本**：app.js v448→v449、sw.js v448→v449、preview-phone.html APP_VERSION=448→449、index.html `app.js?v=448→449`。`node --check` 通过。
7. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 449 + 关键改动已生效）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v450（2026-08-24）

五微调（依据用户文字描述 + 2 张截图，模型无法读图，间距/列宽取合理默认，待预览核查）：

1. **开团记录详情表格列宽（v450-1）**：`app.js` 详情渲染新增 `gbColStyle`（仅 `groupbuy-records` 生效）。单价/售卖数量/单号/售后数量/价格（价格尺寸）一致缩窄（52/62/92/62/52px）；是否流团缩窄（66px）；对应厂家加宽（104px）；其余列（制品名称/补偿方式）保持自适应。`.detail-table` 为 `table-layout:fixed`，故 th 加宽即生效。
2. **导入弹窗间距与链接展示（v450-2）**：
   ① 聊天记录导入 / 腾讯问卷导入「选择分类」下拉框：上边距 8px、下边距由 10px 改为 8px（与到上方蓝框上边距对称）。
   ② 约稿单导入回退 v447 式：移除蓝色说明块与「生成填写链接」按钮，链接（含复制/直接填写）初始界面直接显示（`openCdClientForm` 直接渲染 `cdClientLinkInner`，分类切换仍实时刷新）。
   ③ 聊天记录导入输入框提示词 `元素—必用/可选/避雷` → `元素·必用/可用/避雷`；解析逻辑同步支持「可用」（tagMap + aliasMap + 值拆分正则）。
3. **接稿排期双栏对齐 + 圆点边距（v450-3）**：发现右侧「姓名」位于 `cdc-slide` 首行而非 `cdc-header`，故 v449 的 `.cdc-header{padding-top:16px}` 推反方向；改为 `padding-top:10px` 使右「姓名」与左「稿件进度」顶对齐。圆点 `.comm-dual .cdc-dots{margin-top:5px}`（上边距减 3px）。
4. **接稿详情元素标题间隔号（v450-4）**：展示视图两处（双栏缩略 `cdRecRow` 与完整详情 `detail-row`）`元素-必用/可选/避雷` → `元素·必用/可用/避雷`（含「可选→可用」）。
5. **九模块展示标题分割线（v450-5）**：`openDetail` 内 `f.section` 分组标题由内联样式改为统一 `.form-section` 类（开团/厂家/打样/灵感/接稿排期/授权/人物档案/故事小记/约稿记录等详情页分组标题），`.form-section` 底部分割线由 `1px var(--c-border-light)` 加粗为 `1.5px var(--c-border)`，与接稿详情右侧 `.cdc-header` 分割线一致。
6. **版本**：app.js v449→v450、sw.js v449→v450、preview-phone.html APP_VERSION=449→450、index.html `app.js?v=449→450`。`node --check` 通过。
7. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（待 curl 验证）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v451（2026-08-24）

v450 六项微调交付后用户复查预览，提 6 处与预期不符的修正（模型无法读图，按文字描述推断实现）：

1. **接稿排期双栏对齐（v451-① 修正 v450-3）**：v450 把 `padding-top` 加在右侧 `.cdc-header`（推反方向），导致左「稿件进度」仍比右「姓名」高。改回**左侧下沉**——`.comm-dual .record-card-header{padding-top:16px}`（右侧 `.cdc-header` 恢复默认 `padding:12px` 不动），使左首字段与右 `cdc-slide` 首行「姓名」顶对齐。右侧轮播圆点 `.comm-dual .cdc-dots{margin-top:2px}`（再减 3px：5→2），且 `.cdc-dot` 尺寸 6px→4px（缩小）。
2. **开团记录详情表格（v451-② 修正 v450-1）**：`gbColStyle` 列宽统一——单价 / 售卖数量 / 单号 / 售后数量 / 价格 / 是否流团 全部 **62px**（原 price52/orderNo92/amount52/isDisbanded66 不一致）；对应厂家保持 104px。`afterSales` 字段新增「备注」`subkey:'note'`（置于价格 amount 之后），渲染随 `cols` 自动显示（该列表格 th 兼容 note/name 宽 90px）。
3. **约稿单导入弹窗（v451-③ 修正 v450-2②）**：恢复顶部蓝色说明模块 `.cd-import-tip`（v450 误将上下两蓝块都删，本次仅保留链接区、不显示「已生成」提示块）；链接仍初始界面直接显示。
4. **导入弹窗下拉框边距（v451-④ 修正 v450-2①）**：`.cd-import-tip` 下边距 12px→8px（与下方「选择分类」div 的下边距对称）；聊天记录 / 腾讯问卷导入「选择分类」div 上边距 8px→0（上留白由蓝块下边距 8px 提供），下边距保留 8px，使下拉框上下留白一致（上=8 下=8）。
5. **接稿详情元素标题前缀（v451-⑤ 修正 v450-4）**：v450 仅改了 `cdRecRow`/`detail-row` 两处裸文本，漏了**自定义 HTML 块**（饭圈/二次「元素」块按 `.style-color-col-label` 渲染，label 为裸「必用/可选/避雷」）。在 custom 块渲染逻辑对 `elementsRequired/Optional/Avoid` 强制覆盖前缀为「元素·必用 / 元素·可用 / 元素·避雷」。
6. **九模块列表卡片标题分割线（v451-⑥ 修正 v450-5）**：v450 改 `.form-section`（仅作用于详情页分组标题，对列表卡片无效）。新增 CSS `.record-list .record-card-header{border-bottom:1px solid var(--c-border-light);padding-bottom:8px}`（仅作用于普通 `.record-list` 卡片，排除双栏 `.comm-dual` 与首页 `.record-card-minimal`），列表视图卡片标题下出现分割线。
7. **版本**：app.js v450→v451、sw.js v450→v451、preview-phone.html APP_VERSION=450→451、index.html `app.js?v=450→451`。`node --check` 通过。
8. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 451）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v452（2026-08-24）

v451 交付后用户复查预览，提 6 大处修正（模型无法读图，按文字描述推断实现）：

1. **接稿排期双栏对齐（v452-① 修正 v451-①）**：v451 把 `padding-top:16px` 加在左侧 `.record-card-header`（标题下沉导致"左侧比右侧低"且标题位置被改动）。改为**标题不动**——左卡 `.comm-dual .record-card-header{padding:12px 14px 8px;border-bottom:1px solid var(--c-border-light);margin-bottom:0}`（与右侧 `.cdc-header` 同 padding 同下边线，两栏标题等高、首字段顶对齐），左卡 `.comm-dual .record-card{padding:0}`。右侧轮播圆点 `.comm-dual .cdc-dots{margin-top:-4px;margin-bottom:-3px}`（替代 v451 的 margin-top:2px），圆点尺寸保持 4px。
2. **九模块列表卡片分割线统一（v452-② 修正 v451-⑥）**：`.record-list .record-card-header` 分割线（1px solid var(--c-border-light)）与接稿排期列表视图右侧接稿详情模块 `.cdc-header` 分割线（同款 1px solid var(--c-border-light)）样式统一，注释标明两处声明一致。
3. **开团记录售后记录表格修复（v452-③ 修正 v451-②）**：v451 给 afterSales 加「备注」列 + 统一 62px 后，因 `type`（补偿方式）列未设宽度，`table-layout:fixed` 下被撑满剩余宽度导致表格错位。在 `gbColStyle` 给 `type` 显式设 `width:70px`（其余 orderNo/name/quantity/amount/note 等维持 62/90/62/90px，factory 104px），表格恢复正常不溢出。
4. **约稿单导入弹窗链接块高度（v452-④ 修正 v451-③）**：v451 恢复顶部蓝块 `.cd-import-tip` 后链接块视觉增高。收紧间距——`#cdClientLinkWrap` 上边距 8px→4px，`.cd-link-box{margin:12px 0→4px 0 0}`，链接块回到紧凑高度。
5. **导入弹窗下拉框边距（v452-⑤ 修正 v451-④）**：聊天记录导入 / 腾讯问卷导入「选择分类」div 下边距由 8px 改为 10px（再 +2px）。
6. **接稿详情颜色统一（v452-⑥）**：① 品类切换按钮（土味/封面/饭圈/二次）后数字 `.cd-cat-count` 由 `var(--c-primary)` 改为 `var(--c-text)`（与按钮内文字同色，激活态仍 #fff）；② 顶部三个导入渠道按钮 `.cd-import-btn-name` 由 `var(--c-primary-dark)` 改为 `var(--c-text)`（与四个品类按钮内文字色一致）；③ 约稿单卡片品类/单主展示块 `.cd-rec-cat` 由 `var(--c-text-muted)` 改为 `var(--c-text)`（同上统一）。
7. **版本**：app.js v451→v452、sw.js v451→v452、preview-phone.html APP_VERSION=451→452、index.html `app.js?v=451→452`。`node --check` 通过。
8. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（verified，curl 确认线上三处标记均 452）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v453（2026-08-24）

v452 交付后用户复查预览，提 5 处修正（附 6 张截图，模型无法读图，按文字描述推断实现，间距/列宽取合理默认，待预览核查）：

1. **约稿单顶部三按钮与单主颜色（v453-① 修正 v452-⑥）**：v452 把顶部三个导入渠道按钮 `.cd-import-btn-name` 由蓝（`var(--c-primary-dark)`）统一成 `var(--c-text)`，用户要求改回蓝色。改回 `color:var(--c-primary-dark)`。单主 / 品类展示块 `.cd-rec-cat` 保持与四个品类按钮（土味/封面/饭圈/二次，文字 `var(--c-text)`）同色 `var(--c-text)`，不再用 muted。
2. **约稿单链接块高度（v453-② 修正 v452-④）**：v452 把链接块收紧是错误的方向。先恢复——`#cdClientLinkWrap` 上边距 4px→8px、`.cd-link-box{margin:4px 0 0→12px 0 0}`（回到 v451 紧凑态之前的高度）；再应"生成的链接框高度加高"——`.cd-link-box .form-input{min-height:40px;padding:11px 10px;line-height:1.3}`（原仅 font-size:12px，框更矮）。
3. **接稿排期双栏对齐与字段样式回退（v453-③ 修正 v452-①）**：v452 把左卡 header 设成与右 `.cdc-header` 同 padding 但左卡 header 内容更高导致分隔线仍不一致、字段样式被改乱。v453 让左卡 `.comm-dual .record-card-header` 完全套用右 `.cdc-header` 样式规则（同 font-size13/weight600/flex 对齐/padding12 14 8/border-bottom），两栏标题等高、分隔线同一水平线；左栏字段 `.field-label` font-weight 500→400（与右侧 `.cdc-label` 一致），字段布局保持 `grid 61px 1fr baseline` 使"稿件进度"与右"姓名"顶对齐。
4. **开团记录售后记录表格等宽（v453-④ 修正 v452-③）**：v452 给 `type` 设 70px 后售后表（orderNo62+name90+quantity62+type70+amount62+note90=436）仍超出制品表（name90+price62+factory104+salesCount62+isDisbanded62=380）。v453 重算列宽使两表均 380px：制品表 name90/price62/factory104/salesCount62/isDisbanded62；售后表 orderNo50/name90/quantity50/type50/amount50/note90（制品表专属保持 62，售后专属 orderNo/quantity/amount 收 50，type 收 50，name/note 两表统一 90）。
5. **右侧轮播按钮下边距（v453-⑤ 修正 v452-① 圆点）**：v452 设 `.comm-dual .cdc-dots{margin-top:-4px;margin-bottom:-3px}` 导致底部"查看完整详情"按钮被截断看不到。v453 把 `margin-bottom:-3px→2px`（净增 5px），按钮可见。
6. **版本**：app.js v452→v453、sw.js v452→v453、preview-phone.html APP_VERSION=452→453、index.html `app.js?v=452→453`。需 `node --check` 验证。
7. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（待 curl 验证）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v454（2026-08-24）

v453 交付后用户继续复查预览，提 3 处修正（附 1 张截图，模型无法读图，按文字描述推断实现）：

1. **开团记录售后记录表格列宽重分（v454-①）**：售后记录表 `orderNo/quantity/amount`（单号/售后数量/价格）固定 62px；其余三列 `name/type/note`（制品名称/补偿方式/备注）不指定宽度，由 table-layout:fixed 自动均分剩余宽度。制品列表保持原宽（name90+price62+factory104+salesCount62+isDisbanded62=380），售后表总宽同样保持 380px。
2. **生成约稿单导入弹窗链接框换行加高（v454-②）**：把链接展示从 `<input type="text">` 改为 `<textarea class="form-input" readonly>`，新增 `.cd-link-box textarea.form-input` 样式：`white-space:pre-wrap;word-break:break-all;overflow-wrap:anywhere;resize:none;overflow:hidden;field-sizing:content;min-height:40px;height:auto;`，使超长链接自动换行且框高随内容自适应。
3. **接稿排期双栏左栏原封不动回退到 v449（v454-③）**：左卡 `.comm-dual .record-card` 去掉 `padding:0`，恢复默认卡片内边距；左 header 仅保留 `margin-bottom:4px` 并覆盖掉全局 `.record-list .record-card-header` 的底边框/下内边距；左字段 `.field-label` 字重改回 500、`.field-value` 去掉 `justify-self:start` 与额外字重，恢复 v449 字段样式。右侧轮播圆点下边距在 v453 `2px` 基础上再增加 2px 至 `4px`，使底部按钮更可见。
4. **版本**：app.js v453→v454、sw.js v453→v454、preview-phone.html APP_VERSION=453→454、index.html `app.js?v=453→454`。`node --check` 通过。
5. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 454）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v455（2026-08-24）

v454 交付后用户提 2 处修正（纯文字描述，模型无法读图，按描述推断实现）：

1. **开团记录详情两表与接稿详情制品表整表宽度一致（v455-①）**：开团记录详情（制品列表 / 售后记录）与接稿排期记录详情（制品列表）三张 `.detail-table` 统一固定 `width:380px`（`openDetail` 详情表渲染处对 `isCommProd||isGb` 加内联 `style="width:380px"`）。三表在 `table-layout:fixed` 下绝对宽度一致、列宽比例一致（均填满各自容器）：开团制品表 name90+price62+factory104+salesCount62+isDisbanded62=380 恰好；开团售后表 orderNo62+quantity62+amount62+type70+note90 + name 自适应补足 380；接稿制品表 _seq33+patternId33+price33+quantity33+size64 + 加急/完成各32 + name 自适应补足 380。
2. **接稿排期列表视图（双栏左栏）卡片头部调整（v455-②）**：左栏卡片头部——标题与编辑/删除按钮水平居中（`justify-content:center` + 标题 `flex:0 1 auto` 不撑满 + 按钮组 `justify-content:center`）、编辑/删除按钮上移与标题垂直对齐（`align-items:center`，即"上移"指按钮位置上移对齐而非新增上移按钮）、标题下方加与右侧 `.cdc-header` 同款 `1px solid var(--c-border-light)` 分割线；左栏字段整体下移 4px（`.comm-dual .record-card-body{padding-top:4px}`），其余字段样式保持不变。
3. **版本**：app.js v454→v455、sw.js v454→v455、preview-phone.html APP_VERSION=454→455、index.html `app.js?v=454→455`。`node --check` 通过。
4. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 455）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v456（2026-08-25）

v455 交付后用户提 4 处修正（附 4 张截图，模型可读取部分文字/布局）：

1. **开团记录详情表格宽度改回（v456-①）**：撤销 v455 对 `groupbuy-records` 详情表加的 `width:380px` 内联样式，恢复 `.detail-table` 默认 `width:100%`；靠弹窗内容区自身左右 padding 保持左右边距一致。
2. **接稿详情页月份不再跟随排期表 dateFilter（v456-②）**：删除 `renderCommissionDetailPage` 开头把 `ps.cdMonth` 同步为接稿排期页 `dateFilter` 年月的逻辑。约稿条按自身关联排期的开稿/截稿月份（`cdRecMonth`）独立归月，7 月记录回到 7 月列表，不再被拖到 8 月。
3. **单主标题字色与顶部品类按钮一致（v456-③）**：`.cd-rec-title` 颜色由 `var(--c-primary-dark)`（蓝）改为 `var(--c-text)`（深灰黑），与顶部品类按钮默认文字色一致（而非与蓝色导入按钮一致）。
4. **接稿排期列表视图左栏头部修正（v456-④）**：标题、编辑、删除按钮恢复为左右布局（标题左/按钮右），三者仍在一条水平线上（`align-items:center`）；标题下方保留与右侧 `.cdc-header` 同款分割线；整个头部区域（含标题、按钮、分割线）整体往上移 3px（`margin-top:-3px`）。
5. **右侧接稿详情模块分割线左右留空（v456-⑤）**：`.comm-detail-card .cdc-header` 加 `margin:0 16px; padding:12px 0 8px`，使底部分割线不再贯穿卡片，而是与左侧 `.record-card-header` 一样左右保留 16px 卡片内边距空白。
6. **版本**：app.js v455→v456、sw.js v455→v456、preview-phone.html APP_VERSION=455→456、index.html `app.js?v=455→456`。`node --check` 通过。
7. **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 456）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v457（2026-08-25）

本轮为用户 18 项需求集中实现（用户要求：先给预览、暂不传 GitHub、更新日志；严禁自作主张打包 APK）。模型不读取图片，按文字描述推断实现，待用户预览核查后迭代。

**统计与展示**
1. 底部统计按钮全部并排、一排最多 3 个（`.stats-grid` 改 `repeat(3,1fr)`）；标题行右侧增加「全部/年度」切换（新增 `setStatsScope` + `statsYearField`）。
2. 开团统计增加「累计制品」（同名制品计一次）与「累计购买人数」（purchaseCount 求和）。
3. 打样统计「打样总费用」→「总费用」。
4. 接稿统计删除「逾期预警」，顺序改为 本月收入/总接稿/总收入；日历大标题后增加「本月接稿 N」标签；上/本/下月间距缩短（`.cal-nav gap:4px`）。
5. 授权统计最前增加「本月授权数」。
6. 约稿记录新增「稿件评价」（非常满意/满意/一般/不满意，置于交易状态后，样式一致），展示与统计同步（本月约稿数/本月花费/约稿总数/已完成/满意度）；满意度=各评价分值(100/80/60/40)平均。
7. 各数据柱状表标题栏右侧增加「上年/下年」切换（`renderAnnualChart` 重写，新增 `getChartYear/setChartYear` + `pageKey`）；接稿柱状图改橙色 `#fa8c16` 且顶部圆角；开团柱状图顶部也加圆角。
8. 展示模块标题与填写标题一致（字段标签映射 `fieldLabelMap`，列表用表单 label）。
9. 厂家记录、打样记录展示模块改单列、每页 10 排（`TWO_COL_MODULES` 移除这两项；`PAGE_SIZES['groupbuy-factories']` 改 10）。
10. 每日打卡日视图底部「每日打卡/每周打卡」提示词小字字号加大（`.lgc-desc` 10→12 / 11→13px）。
11. 接稿排期列表视图左栏标题区上移 2px、字段下移 2px、逾期提示上移 2px。
12. 接稿详情月份按钮「2026年8月」改为可直接选年+月（真实日历月算术，修复 8 月下月显示 7 月；新增年份+月份选择面板）。
13. 开团详情制品列表/售后记录表格右侧外边距与左侧一致（`.detail-table margin:0` + `:has(.detail-table)` 去内边距）。
14. 每日打卡增加「年视图」（`renderLifeYearOverview` + `lifeYearStats`：每日显示累计打卡/最长连续/完成率/评分，每周显示累计打卡/连续完成/完成率/评分）；周视图日期标签改为可点开年+月面板跳转到对应周（无闪烁，复用 `.life-month-picker`）。
15. 每日记录睡眠/饮食记录按钮下方增加日期切换（前一天/后一天 + 「年+月+日」选择器，样式同接稿详情日期切换 `.cd-month-label/.cd-month-btn`）。
16. 搜索框占位统一为「搜索...」；输入即时响应去掉 200ms setTimeout 防卡顿。
17. 设置：约稿单名字按 土味/封面/饭圈/二次 显示并保留「美工·接稿详情」前缀（新增 PAGE_TITLES 四条）；版块设置下拉「选择模块...」→「选择模块」；新增「展示字段」自由选择开关（`toggleModuleField`）；自定义配色已保存方案显示方式与预设主题一致（改用 `.preset-theme` 色块 + 悬停删除）。

**版本**：app.js v456→v457、sw.js v456→v457、preview-phone.html APP_VERSION=456→457、index.html `app.js?v=456→457`。`node --check` 通过（多次校验）。
**交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 457）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

---

## v458（v457 修正/增强 14 项）

1. **聊天导入昵称修复**：`platformNick` 别名数组移除 `'昵称'`，避免「昵称：xxx」被误填进「您的平台昵称」字段。
2. **底部统计按钮 + 单位右移**：开团/打样/接稿/授权/约稿五个底部统计 `stats-scope-toggle` 按钮高度缩短（padding 1px 8px / 11px）；单位（人/个/种/亏损等）从标题/下方移到数量数字右侧（新增 `stat-unit` 内联 span）。
3. **每日打卡年视图/防闪/间距**：日期选择器改为 classList 切换 `show`（不再整页重建 innerHTML，消除点击闪烁）；年视图渲染包 try/catch 容错；周视图日期条间距 gap 6px→4px；周视图日期选择器复用 month picker 可跳对应周。
4. **每日记录日期按钮**：新增 `.lr-date-nav`（position:relative + 居中）把日期切换置于睡眠/饮食记录按钮下方居中，样式同打卡周视图；弹出下拉框改为步进式（选月后切换选日，`.lmp-back` 返回选月）。
5. **接稿日历月份计数**：「本月接稿数」去蓝色底色、左右加括号 `（本月接稿 N）`、字号加大加粗；上月/本月/下月导航按钮宽度与间距缩短。
6. **约稿统计（OC约稿）**：新增「总花费」；「已完成」前置到「约稿总数」前；展示模块「稿件评价」移到「交付时间」之后。
7. **灵感制品字段**：去除胶囊/tag 模式，改为与创建时间一致的纯文字显示。
8. **接稿排期展示**：支付状态移到报价金额之后；底部柱状图橙色改与日历开稿一致（`#ff9a3c`）；列表左分割线上移 2px、字段整体上移 4px；多制品标题不居中、与首个制品水平左对齐。
9. **接稿详情约稿单**：进度「已交付」的约稿单自动折叠，头部显示「已交付」绿色标记；`cdToggleRec` 尊重该默认态（点击即展开）。
10. **打样记录**：每页最多 5 排（PAGE_SIZES 10→5）。
11. **厂家记录增强**：合作记录新增「合作类型」下拉（打样/开团/售卖/无料/自印，置项目名称前）；新增「厂家评价」（非常满意/满意/一般/不满意，样式同合作状态）；展示模块「合作次数」前置（顺序：合作次数、主营品类、所在平台、联系方式、合作状态、厂家评价）；底部统计汇总（厂家总数、总合作次数、满意度）；与开团记录按厂家名联动（复用 detailExtra）。
12. **搜索修复**：首页/接稿详情搜索框占位统一「搜索...」；修复输入中断——渲染后恢复焦点与光标，输入不再被整页重建打断。
13. **设置模块下拉框统一**：版块设置「选择模块」原生 `<select>` 改为 `.combobox-wrapper` 选择器（与灵感记录制品名称下拉一致）；后续新增下拉框均沿用此样式。
14. **展示字段含隐藏 + 排序**：设置「展示字段」现包含未展示项（标注「已隐藏」），可勾选显示/隐藏，并用 ↑↓ 调整列表顺序（新增 `moduleFieldOrder` 与 `moveModuleField`；列表卡片渲染按序）。

**版本**：app.js v457→v458、sw.js v457→v458、preview-phone.html APP_VERSION=457→458、index.html `app.js?v=457→458`。`node --check` 通过。
**交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 458）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v459 — 验收修正（v458 九项回归 + 两处需求更正）

1. **统计单位样式**：底部统计 `stat-unit` 去粗（`font-weight:400`）、字号复原 11px、`.stat-value` 改 flex `justify-content:space-between` 使单位最右右对齐（开团/打样/接稿/授权/约稿五处统计统一生效）。
2. **年视图报错修复**：`lifeYearPickerY` 初始化为今年（原为 `null` 致「null年」/负数年、年视图无显示）；年视图渲染逻辑本身正常。周视图日期按钮已按 `7/27 - 8/2` 周段格式显示。
3. **每日记录日期按钮上移 + 消闪**：`lr-date-nav` 由睡眠/饮食卡片下方移至上方居中；日期选择器开合改为仅切换 `.show` class（新增 `lifeRecordDateNavHtml`/`lifeRecordDatePickerInner`/`lifeRecordRenderDatePicker`），不再整页 `renderLifeRecord` 重渲染，消除整屏闪。
4. **接稿排期日历**：本月接稿数恢复蓝色（`--c-primary`）+ 与日期标题水平居中、margin-left 收紧至 6px；月导航间距 `gap:4px→2px`、按钮 padding 收短；柱状图（接稿收入 series）补回每月金额标签（`annual-chart-bar-value`）。
5. **约稿统计顺序**：调整为 本月约稿数 / 本月花费 / 已完成 / 约稿总数 / 总花费 / 满意度（总花费置于约稿总数之后）。
6. **接稿列表位移**：`.comm-dual .record-card-header` `margin-top -7px→-9px`、`.record-card-body` `padding-top 14px→10px`（分割线上移2px、字段整体上移4px）。
7. **接稿详情已交付折叠**：`cdToggleRec` + 渲染初始 `collapsed = (undefined ? delivered : ...)` 逻辑确认正确——已交付约稿单默认折叠。
8. **厂家/约稿满意度兜底 + 联动**：满意度无数据时显 `0%`（原 `—`）并删除 sub 文字；厂家「关联开团」联动匹配放宽（`p.factory.includes(name) || name.includes(p.factory) || 标题/制品含厂家名`），消除空白。
9. **设置展示字段含未展示项**：候选池由 `mod.listFields` 扩为全部 `mod.fields`（排除 section/custom），非默认字段标「可添加」、可勾选展示；新增 `moduleFieldsShown` 存储用户额外勾选字段，`toggleModuleField`/`moveModuleField`/`visibleListFields` 同步支持；「选择模块」下拉框 `min-width:200px→150px` 且 `.module-selector` 居中对齐标签。

**版本**：app.js v458→v459、sw.js v458→v459、preview-phone.html APP_VERSION=458→459、index.html `app.js?v=458→459`。`node --check` 通过。
**交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 459）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v460 — 验收修正（v459 七项回归）

1. **每日打卡年/周视图**：年视图加单卡防御（`lifeYearStats` 包 try/catch 跳过坏卡、`allDefs` 空数组提示、year 空值兜底），不再整页「加载失败」；新增 `renderWeekChips(y,m,curOffset)` 周段选择器（`M/D-M/D` 按钮），选年、月后可直接选周段；`lifeCheckinJumpMonth` 周视图下选月后保持选择器展开并展示该月周段。
2. **每日记录日期按钮**：`lr-date-nav` 上边距 14px→8px（减 6px）、下边距 0→4px（加 4px）；选择日按钮 `.lmp-day` 改正方形（`aspect-ratio:1` + 居中）；新增 `lifeRecordMiniDateNavHtml()` 紧凑日期条，注入到睡眠记录卡与饮食记录卡（`.lr-card-head` 后、`lr-card-body` 前），两卡内日期按钮同步上移。
3. **接稿排期日历**：本月接稿数 `.cal-month-count` margin-left 6px→2px 且与日期标题水平居中（vertical-align:middle + nowrap）；新增 `parseNum`（兼容「12,345」千分位逗号）替换全部 `parseFloat(amount)`，修复柱状图与接稿统计金额「两位数变一位数」缩水 bug。
4. **接稿排期列表视图**：`.comm-dual .record-card-body .field` 标签右对齐（`text-align:right;padding-right:8px`），列宽 61px→64px、gap `0 0`→`2px 6px`，`.record-card-header` margin-top -9px→-12px、`.record-card-body` padding-top 10px→8px，左侧字段整体上移且贴近值。
5. **约稿单已交付折叠**：根因——`commissionDetails` 本身无 `progress` 字段（在关联的 `commissions` 上），故 `valIncludes(r.progress,'已交付')` 恒 false。改为查关联 `commissions`（`c.clientInfo===r.clientInfo && valIncludes(c.progress,'已交付')`）；渲染初始折叠 + `cdToggleRec` 同步。
6. **厂家合作记录联动**：`groupbuy-factories` 的 `detailExtra` 重写——除「关联开团」标签外，新增由关联开团汇总的「合作记录（来自开团）」区块，逐项列出制品/开团名与日期，关联完成后合作记录不再空白。
7. **设置崩溃修复**：`getSettings()` 回填缺省字段（`.theme`/`.navIcons`/`.navLabels`/`.fieldLabels`/`.fieldOptions`/`.moduleFields`/`.moduleFieldsShown`/`.moduleFieldOrder`），消除旧存储缺 `.theme` 致 `renderThemeSettings` 抛错整窗崩；四个 tab 渲染包 try/catch，失败显示提示而非崩窗。

**版本**：app.js v459→v460、sw.js v459→v460、preview-phone.html APP_VERSION=459→460、index.html `app.js?v=459→460`。`node --check` 通过。
**交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 460）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v461 — 热修：设置弹窗内容空白

1. **renderSettingsModal 健壮化**：校验 `_settingsTab` 合法性；tab 内容独立渲染并包 try/catch；若渲染结果为空则显「暂无设置内容」兜底，避免大片空白。
2. **renderFieldSettings 健壮化**：校验模块列表与 `_settingsModule`，非法时自动回退到第一个可配置模块；新增「当前模块：X · N 个字段」提示；无字段时显提示。
3. **getSettings 深度合并**：改用 JSON 深拷贝 + 递归 merge 填充缺省字段，确保旧存储缺 `theme` 子键、`navIcons` 等也能自动补齐。
4. **CSS 强制可见**：`.settings-section.active` 加 `display:block !important;min-height:120px`；`.modal-body` 加 `min-height:0`，防止 flex 子项坍缩导致内容不显示。

**版本**：app.js v460→v461、sw.js v460→v461、preview-phone.html APP_VERSION=460→461、index.html `app.js?v=460→461`。`node --check` 通过。
**交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 461）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v462 — 验收修正：年视图/每日记录日期条/接稿金额与列表/厂家合作记录

1. **年视图补每日打卡 & 标签规范**：`renderLifeYearOverview` 本就遍历全部 8 个打卡定义（5 日 + 3 周），每日卡片始终渲染；新增 `lyc-tag-day`(蓝)/`lyc-tag-week`(橙) 双色标签，`.lyc-tag` 去底色（`background:transparent`），周打卡小字改为橙色（非蓝、非底色）。
2. **年视图删除底部「添加打卡」**：`renderLifeCheckin` 仅在 `block !== 'year'` 时注入底部「添加打卡」模块，年视图不再显示。
3. **周段选择间距对齐**：`.lmp-weeks-title` 下边距 4→6px、`.lmp-weeks` 容器内边距底部 8→6px，使周段 chips 上下间距一致。
4. **每日记录主日期按钮**：`.lr-date-nav` 上边距 8→-4px（减 12）、下边距 4→6px（加 2）；标签由日期改为「选择日期」，与周视图日期按钮一致；日期选择器默认直接进入「日」网格（月/日同屏可切换），呼应「日选择和月选择放一起」。
5. **睡眠/饮食卡内日期按钮**：`lifeRecordMiniDateNavHtml` 标签由日期改为「选择日期」，位置保持在「新增记录」按钮之后、卡片内容之前（非底部），容器 `justify-content:center` 居中对齐，样式复用 `.cd-month-btn/.cd-month-label` 与周视图日期按钮一致。
6. **接稿排期日历对齐 & 金额修复**：`cal-title` 改 `inline-flex;align-items:baseline`、`.cal-month-count` `vertical-align:middle→baseline` 且 `margin-left` 2→-1px（左间距减 3px），「本月接稿 N」与标题基线对齐不再下沉；`MODULES['design-commission'].stats` 的 `totalRev`/`monthRev` 由 `parseNum(r.amount)` 改为 `parseNum(r.amount)||parseNum(r.quoteAmount)||0`，与年度柱状图口径一致，修复「金额依旧是错的」。
7. **接稿列表头部复位**：移除 `.comm-dual .record-card-header` 的 `margin-top:-12px`，标题/编辑/删除/分割线回到原位（只移字段、不动头部），字段位置如需微调由 `margin-bottom` 控制。
8. **厂家合作记录不再空白**：`groupbuy-factories.detailExtra` 放宽关联开团匹配（忽略大小写与空格，标题/厂家/制品名双向包含匹配），确保有开团关联的厂家自动汇总合作记录；手动填写的 `cooperationRecords` 仍由详情主循环渲染。

**版本**：app.js v461→v462、sw.js v461→v462、preview-phone.html APP_VERSION=461→462、index.html `app.js?v=461→462`。`node --check` 通过。
**交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 462）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v463 — 按验收 6 项逐条重改（修正 v462 改错方向）

> 说明：v462 把「每日记录日期标签」误改成「选择日期」、给年视图标签加了蓝/橙边框底色、并移除了接稿列表头部负边距——方向恰与验收相反。v463 逐条纠正。

1. **年视图标签灰字无边框 + 周模块橙色**：`.lyc-tag` 改为 `color:var(--c-text-light);border:none;background:transparent`（灰字、无边框无底色）；卡片加 `lyc-day`/`lyc-week` 类，`.life-year-card.lyc-week{border-left:3px solid var(--c-orange)}` 且 `.lyc-num` 橙色、`.lyc-day` 蓝色（深空蓝 `var(--c-primary)`），使「每周打卡」模块整体橙色、「每日打卡」蓝色，日/周清晰可辨。日期按钮间距由 gap:10px→6px 缩短。
2. **每日记录日期下拉框年月日同屏 + 显真实日期**：`lifeRecordDateNavHtml` 标签由「选择日期 ▾」改回真实日期 `YYYY年M月D日 ▾`（复用 `.life-monthbar-txt` 样式，与每日打卡周视图日期按钮一致）；`lifeRecordDatePickerInner` 由「月→日」两步改为**年+月+日同屏**（年栏 + 12 月网格 + 当月日网格一次渲染），`lifeRecordDatePickMonth` 不再切步。
3. **睡眠/饮食卡内日期按钮移到顶部居中**：从 `renderSleepRecordCard`/`renderDietRecordCard` 移除卡内 `lifeRecordMiniDateNavHtml`；在 `renderLifeRecord` home 视图中把 `lifeRecordDateNavHtml()` 移到 `renderLifeRecordTopCard`（周汇总卡）**之前**，`.lr-date-nav` 居中，实现「放在顶部周汇总卡片前面居中、样式同每日打卡周视图日期按钮」。
4. **接稿列表头部回 v459 + 日历计数上移 + 金额**：`.comm-dual .record-card-header` 恢复 `margin-top:-12px`（回 v459 样式）；`.cal-month-count` 加 `margin-top:-1px`（本月接稿数上移 1px）；金额口径维持 `parseNum(r.amount)||parseNum(r.quoteAmount)||0`（接稿统计与年度柱状图一致，v462 已修，v463 复核保留）。
5. **厂家合作记录填充（开团/制品名/开团时间）**：`groupbuy-factories.detailExtra` 重写——合作记录 = 已录入手动 `cooperationRecords` + 关联开团自动汇总，每行含 `coopType:'开团'`、`project`=制品名称、`date`=开团时间(`g.startTime`)；渲染加「合作类型」橙色标签（`.coop-type`），确保关联后列表不再空白。
6. **设置弹窗加固**：`.settings-section` 加 `max-height:62vh;overflow-y:auto`，防止内容过长溢出/重叠导致「乱」。

**版本**：app.js v462→v463、sw.js v462→v463、preview-phone.html APP_VERSION=462→463、index.html `app.js?v=462→v463`。`node --check` 通过。
**交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 463）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v464 — 按验收 6 项逐条重改（真改 + 自测）

1. **设置「版块设置」不再显示自媒体字段**：`renderFieldSettings` 模块列表过滤条件加 `&& k !== 'home'`（自媒体/首页模块含播放量/点赞/收藏/阅读量等），默认选中首个非 home 模块；已持久化在 home 的选择会自动回退，彻底修复「版块设置渲染了错误字段」。
2. **每日打卡年视图删除重写**：`renderLifeYearOverview` 重写为与日视图（`life-goal-card`）一致样式——保留标题（每日/每周打卡 + 年份）与统计（累计打卡、连续完成、完成率），**删除**热图、本月进度、今日待打卡、打卡按钮；新增「评分」按钮，点击展开 1–5 分评分面板，按 `typeKey+'_'+year` 持久化到 `lifeYearRatings`（localStorage），可清除重评。
3. **每日记录主页日期按钮复位 + 下拉加「选择日期」小字**：`renderLifeRecord` 把 `lifeRecordDateNavHtml()` 从「周汇总卡之前」移回「周汇总卡之后」（撤销 v463 #3 误移顶部）；`lifeRecordDatePickerInner` 在日选择区上方插入灰色小字「选择日期」（复用 `.lmp-weeks-title`，与周视图「选择周段」一致）。
4. **睡眠/饮食历史周导航移到顶部**：`renderLifeRecordHistory` 把「‹ 上一周 / 日期范围 / 下一周 ›」从底部移到底部周统计模块**之上**（睡眠/饮食记录按钮之下），日期范围 span 套用 `.lwm-range` 样式，容器 `.lr-week-nav` 改为卡片头风格（`.lwm-head` 风：边框/圆角/阴影 + 居中 + max-width）。
5. **接稿列表回 v460 样式 + 修复金额 40→4**：`.comm-dual .record-card-header` 去掉 `margin-top:-12px`（回 v460，撤销 v463 #4 回 v459）；`MODULES['design-commission'].stats`（`totalRev`/`monthRev`）与 `chart` 系列取值优先级由 `parseNum(r.amount)||parseNum(r.quoteAmount)||0` 改为 `parseNum(r.quoteAmount)||parseNum(r.amount)||0`——**报价金额优先**，修复「报价 40 却显示 ¥4」。
6. **开团制品回填厂家合作记录**：`saveForm` 在 `pageKey==='groupbuy-records'` 保存后调用新增 `syncGroupbuyToFactories(data)`：遍历制品列表，按厂家名匹配 `factories` 记录（`cooperationRecords` 双向包含匹配、忽略大小写与空格、`fac.id` 缺失则跳过），以 `coopType:'开团'`、`project`=制品名称、`date`=开团时间(`startTime`) 写入并去重（同 coopType+project+date 不重复），`note` 记「开团：<开团名称>」。

**版本**：app.js v463→v464、sw.js v463→v464、preview-phone.html APP_VERSION=463→464、index.html `app.js?v=463→v464`。`node --check` 通过。
**交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 464）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v465 — 验收再修 5 项

1. **设置「版块设置」回到 v458 样式并修复溢出**：`renderFieldSettings` 保持模块 combobox + 字段名称修改 + 选项管理 + 展示字段勾选/排序结构；修复 `.field-edit-row input` 未设 `min-width:0` 导致移动端输入框溢出「显示不全」的问题，并加 `@media(max-width:640px)` 下 label 宽度收窄为 90px；`home` 模块仍排除。
2. **每日打卡年视图修复每日打卡显示 + 评分改为统计按钮模块**：`renderLifeYearOverview` 对 `allDefs` 显式排序（每日在前、每周在后）确保每日打卡卡片渲染；移除卡片 `onclick` 跳转与 `›` 箭头（年视图不再点击展开详情）；把「评分」改为第 4 个统计按钮（`累计打卡 / 连续打卡 / 完成率 / 评分`），与前三者同样式，点击后展开 1–5 分面板。
3. **每日记录日期位置与历史视图导航样式**：`renderLifeRecord` 把 `lifeRecordDateNavHtml()` 从 toggle 行上方移到下方，且只在首页（非睡眠/饮食历史视图）展示；睡眠/饮食历史视图内部去掉「前一天/日期/后一天」按钮；`renderLifeRecordHistory` 的周导航改为与每日打卡周视图日期条一致——居中 ghost 按钮 + `.life-monthbar-txt` 显示 `M/D - M/D` 范围，去卡片底/阴影/「历史记录」标签。
4. **接稿排期列表视图 header 微调**：`.comm-dual .record-card-header` 加 `margin-top:-1px`（整体上移 1px），`margin-bottom` 由 6px 改为 2px（分隔线下边距 -4px）。
5. **开团回填厂家简化 + 厂家详情去关联区域**：`syncGroupbuyToFactories` 写入厂家 `cooperationRecords` 时不再带 `note`；`groupbuy-factories` 的 `detailExtra` 直接返回空字符串，详情页删除「关联开团」与「合作记录（含关联开团）」两个区块。

**版本**：app.js v464→v465、sw.js v464→v465、preview-phone.html APP_VERSION=464→465、index.html `app.js?v=464→v465`。`node --check` 通过。
**交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 465）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v466 — 强制刷新 SW 缓存（功能无改动）

- **背景**：用户反馈「版块设置」仍只显示个别字段、字段挤成一行、且「没变化」。经 Node 实测 `renderFieldSettings` 对开团记录能正确竖排渲染 4 字段 + 选项管理 + 展示字段，CSS 层 `.settings-section` 为 `display:block`、`.field-edit-row` 各自块级，结构上**不可能**产生横向一排；用户所见系浏览器 Service Worker 仍跑着旧缓存 `app.js`（v464 或更早版本「版块设置」确有横向错位/截断 bug）。v465 代码本身正确，但 SW 缓存未失效导致一直没拉到新代码。
- **处理**：仅把缓存版本号 +1（app.js?v=465→466、sw.js `xiao-workbench-v465`→`v466`、preview-phone.html `APP_VERSION=465→466`），不改动任何业务/样式逻辑；重新部署使新 SW 缓存桶生效，强制浏览器重新拉取 `app.js`。
- **核验**：`node --check` 通过；curl 确认线上三处标记均 466，版块设置结构符号（`settingsModuleCb`/字段名称修改/选项管理/展示字段/`k!=='home'`）全部在线。
- **未传 GitHub、未打包 APK**（用户规则：先预览 + 严禁自作主张打包）。

## v467 — 真正回到 v458「版块设置」结构

- **背景**：用户继续反馈 v466「还是一样没有变化」，并贴图。图中可见「展示字段（含未展示项，可勾选显示/隐藏，并用↑↓调整顺序）」区块存在且左侧标签被挤成竖排，证明此前 v465/v466 并未真正回到 v458。经核对 `/d/wbkey/webview-app/app/src/main/assets/app.js`（v458 时代 APK 资产）的 `renderFieldSettings`，**真实 v458 结构**为：原生 `<select>` 选择模块 + 字段名称修改 + 选项管理，**没有「展示字段」勾选/排序区块**，且 `home` 模块未被排除。
- **处理**：将 `renderFieldSettings` 完整改回 v458 结构：
  - 模块选择由自定义 combobox 改回原生 `<select onchange>`；
  - 模块过滤改回 `Object.keys(MODULES).filter(k => MODULES[k] && MODULES[k].fields)`（含 `home`）；
  - 删除「展示字段」整块 DOM 与相关处理逻辑；
  - 移除随之废弃的 `toggleModuleField`、`moveModuleField` 函数；
  - 保留 v465 对 `.field-edit-row input` 加的 `min-width:0` 与移动端 label 宽度修复，避免 v458 原有的溢出 bug。
- **版本**：app.js v466→v467、sw.js v466→v467、preview-phone.html APP_VERSION=466→467、index.html `app.js?v=466→v467`。`node --check` 通过；Node 实测 `renderFieldSettings` 输出含 `<select>`、无 `combobox-wrapper`、无「展示字段」、含 4 个 `field-edit-row`、含「选项管理」。
- **交付**：CloudStudio 沙盒 `036c0ec20b0f490ab25f63b72a3168ef` 重部署（curl 确认线上三处标记均 467；线上 `app.js` 中「展示字段」计数 0、`field-order-list` 计数 0、renderFieldSettings 内 `k !== 'home'` 计数 0）。**APK 未打包；GitHub push 仍挂起（用户暂不上传）**。

## v468 — 模块选择器改左对齐 + 下拉框用内容界面 combobox 样式

- **需求**：用户要求「版块设置」的「选择模块」标签与下拉框放在最左边，且下拉框样式与内容界面（制品名称/灵感记录等）的下拉框一致（即统一 combobox 规范）。
- **处理**：`renderFieldSettings` 的模块选择由 v467 的原生 `<select>` 改回内容界面同款 combobox 结构（`.combobox-wrapper` + `combobox-input` + `combobox-toggle` + `combobox-dropdown#settingsModuleCb`，选项 `onclick` 设 `_settingsModule` 后 `renderSettingsModal()` 重渲染）；模块列表仍按 v458 含 `home`；继续无「展示字段」区块。`.module-selector` CSS 由 `justify-content:center` 改为 `justify-content:flex-start`，使标签+下拉框左对齐。
- **版本**：app.js v467→v468、sw.js v467→v468、preview-phone.html APP_VERSION=467→468、index.html `app.js?v=467→v468`。`node --check` 通过；curl 确认线上三处标记均 468，`settingsModuleCb` combobox 与 `showComboboxDropdown('settingsModuleCb')` 上线、线上 CSS `module-selector{...justify-content:flex-start...}` 生效、仍无「展示字段」。
- **未传 GitHub、未打包 APK**（用户规则：先预览 + 严禁自作主张打包）。

## v469（2026-08-25）修复「版块设置」模块选择器布局
- **修复**：`.module-selector` 由 `display:flex;align-items:center` 横排改为 `display:flex;flex-direction:column;align-items:flex-start` 纵排，避免「选择模块：」标签被压成竖排、下拉框被挤窄；下拉框 `.combobox-wrapper` 设为 `width:100%;max-width:280px`。
- **版本**：app.js v468→v469、sw.js v468→v469、preview-phone.html APP_VERSION=468→469、index.html `app.js?v=468→v469`。`node --check` 通过；curl 确认线上三处标记均 469，线上 CSS `module-selector{...flex-direction:column...}` 生效。
- **未传 GitHub、未打包 APK**。

## v470（2026-08-25）字段/选项编辑行占满宽度
- **修复**：用户反馈「字段和选项模块没有占满」。去掉 `.field-edit-row` 的 `padding:8px;background:var(--c-primary-bg);border-radius:var(--radius-sm)` 内缩背景，让字段行贴边；label 宽度桌面 `120px→80px`、移动 `90px→70px`；`.option-edit-item` 加 `width:100%`，input 与字段 input 同加 `min-width:0` 确保 flex 占满剩余空间。同步移除已废弃的「展示字段」相关 CSS（`.field-visibility-list`/`.field-order-list`/`.field-order-item`/`.field-vis-item`/`.fo-*`）。
- **版本**：app.js v469→v470、sw.js v469→v470、preview-phone.html APP_VERSION=469→470、index.html `app.js?v=469→v470`。`node --check` 通过；curl 确认线上三处标记均 470，线上 CSS 字段/选项宽度规则生效。
- **未传 GitHub、未打包 APK**。

## v471（2026-08-25）恢复蓝色底+模块选择器横排+label 收窄
- **修复**：用户反馈 v470「蓝色底色怎么没了」「还是没占满」「选择模块的下拉框不要换行，改回去放选择模块标题后面」。
  - 恢复 `.field-edit-row` 的 `padding:8px;background:var(--c-primary-bg);border-radius:var(--radius-sm)` 蓝色底色；
  - label 宽度继续收窄：桌面 `80px→60px`、移动 `70px→55px`，让 input 更宽；
  - `.module-selector` 改回横排：`flex-direction:row;flex-wrap:nowrap`，标签 `white-space:nowrap`，下拉框 `flex:1 1 0;min-width:160px;max-width:280px`（放在标题后面，空间不足时缩放但不换行）。
- **版本**：app.js v470→v471、sw.js v470→v471、preview-phone.html APP_VERSION=470→471、index.html `app.js?v=470→v471`。`node --check` 通过；curl 确认线上三处标记均 471，线上 CSS 模块选择器横排、字段蓝底、label 收窄均生效。
- **未传 GitHub、未打包 APK**。

## v472（2026-08-25）修复「版块设置」模块选择器 DOM 未闭合导致布局错乱
- **修复**：v471 把模块选择器从原生 `<select>` 改成 combobox 后，combobox 多了一层 `.combobox-wrapper` + 内部 `.combobox-dropdown`，但 `renderFieldSettings` 中 `</div></div>` 只关闭了 combobox-dropdown 与 combobox-wrapper，**漏关了外层 `.module-selector`**。导致「字段名称修改」标题与字段行被包在 `.module-selector` 的 flex row 里，表现为标题被压成竖排、字段行挤在右侧、只占一半宽度。修复为 `</div></div></div>`，正确闭合 module-selector。
- **版本**：app.js v471→v472、sw.js v471→v472、preview-phone.html APP_VERSION=471→472、index.html `app.js?v=471→v472`。`node --check` 通过；curl 确认线上三处标记均 472，renderFieldSettings 内 `<div`/`</div>` 数量平衡。
- **未传 GitHub、未打包 APK**。

## v473（2026-08-25）「版块设置」模块选择器下拉框左移 4px
- **微调**：`.module-selector .combobox-wrapper` 加 `margin-left:-4px`，让下拉框整体相对标签左移 4px。
- **版本**：app.js 无改动、sw.js v472→v473、preview-phone.html APP_VERSION=472→473、index.html `app.js?v=472→v473`。`node --check` 通过；curl 确认线上三处标记均 473，线上 CSS `margin-left:-4px` 生效。
- **未传 GitHub、未打包 APK**。

## v474（2026-08-25）「版块设置」模块标签去冒号 + 拉近与下拉框间距
- **微调**：`renderFieldSettings` 中「选择模块:」改为「选择模块」（去掉冒号）；标签内联 `margin-right:8px→4px`；`.module-selector` 的 `gap:8px→4px`，使标题与下拉框更近（保留 v473 的 `combobox-wrapper margin-left:-4px`）。
- **版本**：app.js v473→v474、sw.js v473→v474、preview-phone.html APP_VERSION=473→474、index.html `app.js?v=473→v474`。`node --check` 通过；curl 确认线上三处标记均 474，标签无冒号、线上 CSS `gap:4px` 生效。
- **未传 GitHub、未打包 APK**。

## v475（2026-08-25）设置两区整块蓝底 + 选项去蓝底 + 标题字色统一 + 字段行上下对称
- **导航图标 / 版块设置 整块蓝底**：`.nav-icon-edit-list` 与「字段名称修改」区（`.field-area`）改为整块 `.field-area`/列表蓝底（`var(--c-primary-bg)`+圆角+内边距），行间不再各自独立蓝块；行内去掉独立蓝底与圆角。
- **选项管理去蓝底**：`renderFieldSettings` 中选项区移除 `.field-area` 包裹，「选项管理」字段恢复无底色（白底，仅 8px 间距），仅「字段名称修改」保留整块蓝底。
- **字段行上下对称**：`.field-edit-row` 去掉分隔线、改 `padding:11px 0` 均匀内边距，首行/末行上下留白一致，解决「最上/最下字段看着不水平居中」。
- **标题字色统一**：导航图标项名称 `.nav-icon-edit-name` 由 `var(--c-text-muted)` 改 `var(--c-primary)`；选项管理内每个字段小标题（`<div style=...font-weight:600>`）加 `color:var(--c-primary)`，与「配色设置」小标题主色一致（h4 标题本就用 `var(--c-primary)`）。
- **版本**：app.js、sw.js、preview-phone.html 均 v475（index.html `app.js?v=474→475` 修复上一轮未落盘）；`node --check` 通过；curl 确认线上标记 475、field-area 仅字段名区 1 处、选项标题主色生效。
- **未传 GitHub、未打包 APK**。

## v476（2026-08-25）标题色改用字体色 + 分割线加回 + 行高压缩
- **纠错**：上一轮（v475）误把「配色设置」里的「主色/浅色/深色」当成字体色，实际它们是主题强调色（--c-primary/--c-primary-light/--c-primary-dark）。本轮改用真正的字体色号：主字色 `--c-text`、浅字色 `--c-text-muted`。
- **版块设置小标题改主字色**：h4「字段名称修改」「选项管理」由 `var(--c-primary)` → `var(--c-text)`（主色字体色，深色），与配色设置小标题字体色一致。
- **字段/选项标签改回浅字色**：选项管理内字段小标题（平台/内容类型/发布状态）由 `var(--c-primary)` → `var(--c-text-muted)`；导航图标项名称 `.nav-icon-edit-name` 由 `var(--c-primary)` → `var(--c-text-muted)`（字段名称修改内的平台/发布时间/作品标题标签本就为 `--c-text-muted`，保持不变）。
- **分割线加回**：`.field-edit-row` 恢复 `border-bottom:1px solid rgba(0,0,0,.06)`（末行 `:last-child` 去线），字段行间重新有细分隔线；整块蓝底（`.field-area`）保留。
- **行高压缩**：`.field-edit-row` 内边距 `padding:11px 0 → 8px 0`，减少字段模块高度，使最上/最下字段上下留白对称、看着水平居中。
- **版本**：app.js、sw.js、preview-phone.html 均 v476（index.html `app.js?v=475→476`）；`node --check` 通过；curl 确认线上 476、h4 主字色、选项小标题浅字色、nav 名称浅字色、field-edit-row 分割线 + 8px padding 均生效。
- **未传 GitHub、未打包 APK**。

## v477（2026-08-25）色值再次校正 + 字段行 11px + 蓝底模块底部缩短
- **小标题深色**：`.nav-icon-edit-name` 由 `var(--c-text-muted)` → `var(--c-text)`（主字深色）；选项管理内字段小标题由 `var(--c-text-muted)` → `var(--c-text)`。
- **h4 标题改回蓝色**：「字段名称修改」「选项管理」h4 标题由 `var(--c-text)` → `var(--c-primary)`（主题主色蓝）。
- **字段行内边距 11px**：`.field-edit-row` `padding:8px 0 → 11px 0`。
- **字段名称修改蓝底模块底部缩短**：`.field-area` padding `10px 12px → 10px 12px 6px`，底部留白减少。
- **版本**：app.js、sw.js、preview-phone.html 均 v477（index.html `app.js?v=476→477`）；`node --check` 通过；curl 确认线上 477、各项色值与 padding 均生效。
- **未传 GitHub、未打包 APK**。

## v478（2026-08-25）字段标签深色 + 蓝底上下一致 + 选项管理标题行内联首个选项
- **字段标签深色**：`.field-edit-row label` 由 `var(--c-text-muted)` → `var(--c-text)`（主字深色），解决字段名称修改区标签仍显灰的问题。
- **蓝底模块上下边距一致**：`.field-area` padding `10px 12px 6px → 10px 12px`，上下均为 10px。
- **选项管理首个选项与标题同行**：`renderFieldSettings` 重构选项管理 DOM，每个字段的小标题（`.option-field-title`）与第一个选项输入框放在 `.option-field-title-line` 同一行；其余选项放入 `.option-edit-list.rest` 正常纵向排列；新增选项按钮仍在下方。
- **新增 CSS**：`.option-field-title-line`、`.option-field-title`、标题行内 `.option-edit-item.first-item` 行内样式，以及 `.option-edit-list.rest` 纵向布局规则。
- **版本**：app.js、sw.js、preview-phone.html 均 v478（index.html `app.js?v=477→478`）；`node --check` 通过；curl 确认线上 478、字段标签深色、蓝底 padding 10px 12px、选项标题行结构均生效。
- **未传 GitHub、未打包 APK**。

## v479（2026-08-26）蓝底上下 6px + 选项管理标题/首选项同行且其余左对齐
- **蓝底模块上下边距 6px**：`.field-area` padding `10px 12px → 6px 12px`，上下均为 6px。
- **选项管理布局重构**：`renderFieldSettings` 选项管理 DOM 改为 `.option-field-block`（标题列 + 输入列左右 flex）：`.option-field-title`（固定 60px）与 `.option-field-inputs`（flex:1）同行；首个选项与标题同一行，后续选项在输入列内纵向排列，与首选项左对齐。
- **CSS 调整**：删除 `.option-field-title-line`/`.first-item`/`.option-edit-list.rest` 旧规则，新增 `.option-field-block`、`.option-field-title{width:60px...}`、`.option-field-inputs{flex:1;min-width:0}`；选项输入框统一 flex:1 占满，不再固定 120px。
- **版本**：app.js、sw.js、preview-phone.html 均 v479（index.html `app.js?v=478→479`）；`node --check` 通过。CloudStudio 部署遇 504 暂未成功，待重试。
- **未传 GitHub、未打包 APK**。

> 补充：v479 于 2026-08-26 00:39 重新部署成功，CloudStudio 504 已恢复，curl 确认线上 v479、`.field-area` padding 6px 12px、`.option-field-block`/`.option-field-title`/`.option-field-inputs` 新结构均生效。

## v480（2026-08-26）蓝底 4px + 首选项靠近标题 + 加按钮右对齐 + 预览弹一下
- **蓝底模块上下边距 4px**：`.field-area` padding `6px 12px → 4px 12px`。
- **首选项与标题更近**：`.option-field-block` gap `8px→6px`；`.option-field-title` 去掉固定 `width:60px`（改为 `flex-shrink:0`），首选项输入框紧跟标题文字，不再因固定列宽留白而显得远；其余选项仍在输入列内与首选项左对齐。
- **增加选项按钮右对齐**：每个字段的「+ 添加选项」按钮外层包 `<div style="text-align:right;margin-top:4px">`，按钮靠右。
- **预览「弹一下」**：`preview-phone.html` 新增 `@keyframes phonePop`（scale .985→1 + 淡入）与 `.phone.pop` 动画；`frame.onload` 在应用每次加载完成时给手机壳加 `pop` 类触发该动画（先移除再 reflow 重触发），实现改完/刷新后右侧预览区轻轻弹一下。
- **版本**：app.js、sw.js、preview-phone.html 均 v480（index.html `app.js?v=479→480`）；`node --check` 通过；curl 确认线上 480、各项 CSS 与动画均生效；右侧面板已通过 present_files 弹出本地预览。
- **未传 GitHub、未打包 APK**。

## v481（2026-08-26）蓝底 2px + 增加选项按钮改蓝底长按钮置底左对齐
- **蓝底模块上下边距 2px**：`.field-area` padding `4px 12px → 2px 12px`。
- **增加选项按钮重构**：`renderFieldSettings` 选项管理 DOM 中，「+ 添加选项」按钮由 `.btn-outline btn-sm`（右对齐小按钮）改为 `.btn btn-primary add-opt-btn`（`width:100%` 长按钮），并移入 `.option-field-inputs` 列内作为末子元素，使其与选项左对齐、置于底部。
- **按钮配色与保存按钮一致**：`.btn-primary` 即 `background:var(--c-primary);color:#fff`（蓝底白字），与设置弹窗「保存」按钮同款。
- **addOptionItem 插入位置修正**：新增选项时 `container.insertBefore(item, addBtn)`，确保新选项插在「+ 添加选项」按钮之前，按钮始终留在最底部。
- **版本**：app.js、sw.js、preview-phone.html 均 v481（index.html `app.js?v=480→481`）；`node --check` 通过；curl 确认线上 481、`.field-area` padding 2px 12px、按钮 `btn btn-primary add-opt-btn`、insertBefore 逻辑均生效。
- **未传 GitHub、未打包 APK**。

## v482（2026-08-26）增加选项按钮锁死满宽 + 强制 SW 重拉
- **增加选项按钮满宽（修复不到删除按钮那边）**：新增 CSS `.add-opt-btn{display:flex;width:100%}`（覆盖 `.btn` 的 inline-flex，消除歧义），确保「+ 添加选项」按钮横跨整列、右边缘与各行删除按钮对齐。
- **选项保持左对齐现状**：`.option-field-title` 仍为自然宽度（`flex-shrink:0`，无固定列宽），各字段选项维持左对齐、首选项紧贴标题，未改回易致「距离太远」的固定列宽写法。
- **强制刷新**：升 v482（三处标记 + SW CACHE），使预览端 Service Worker 强制重新拉取最新 app.js，规避停留在 v480（当时按钮为右侧小按钮）的旧缓存。
- **版本**：app.js、sw.js、preview-phone.html 均 v482（index.html `app.js?v=481→482`）；`node --check` 通过；curl 确认线上 482、`.add-opt-btn` 满宽规则、按钮 `btn btn-primary add-opt-btn` 均生效。
- **未传 GitHub、未打包 APK**。

## v483（2026-08-26）选项字段标题固定宽度跨字段左对齐 + 添加按钮与输入框等宽
- **跨字段选项左对齐**：`.option-field-title` 由自然宽度改为固定 `width:60px`，「平台」「内容类型」「发布状态」三个字段的标题列等宽，各字段输入框左起点对齐。
- **添加选项按钮宽度与输入框一致**：「+ 添加选项」按钮改为 `flex:1`、不再 `width:100%`，并包在 `.option-edit-item` 行内，右侧放一个 `.option-delete-placeholder`（26px 占位，与删除按钮同宽但不可见），使按钮右边缘与输入框右边缘对齐，不再延伸到删除按钮列。
- **CSS 调整**：`.add-opt-btn` 改 `flex:1` + `justify-content:center` + `align-items:center`；新增 `.option-delete-placeholder{width:26px;height:1px;flex-shrink:0;visibility:hidden}`。
- **版本**：app.js、sw.js、preview-phone.html 均 v483（index.html `app.js?v=482→483`）；`node --check` 通过；curl 确认线上 483、标题固定 60px、按钮 flex:1、占位元素均生效。
- **未传 GitHub、未打包 APK**。

## v528（2026-08-27）接稿排期日历按钮化 + 接稿详情表单大改造
- **接稿排期日历视图**：本月接稿由文字计数改为蓝底白字按钮（`.cal-month-count` → pill 蓝按钮，`.cal-month-count:hover` 深蓝）；展示卡片头（标题/编辑/删除）在一条直线上水平左对齐（`.record-card-header` `align-items:flex-start`，操作区 `flex-wrap:nowrap` 不换行）。
- **接稿详情信息区左移 + 空状态 + 顶部底部提示词**：信息整体左移 3px（`.cd-form-shell{margin-left:-3px}`）；关联接稿排期项无信息时显示空白态（双栏预览与详情弹窗两处均改「未找到/未填写单主」→ 空行留白）；最上方加两行（请仔细填写 / 帮助我更好了解您的需求），最下方加两行（人物、参考图、色卡、模板等图片文件可直发/网盘/邮箱 / 请确认信息无误后再提交 尤其注意尺寸&颜色格式⚠️ 红字）。
- **平台昵称自动同步单主**：填完「您的平台昵称」后（input 事件 + save + JSON 导入 + 客户填单 + 聊天解析各路径）单主为空时自动填入平台昵称（`cdSyncPlatformNick`）。
- **饭圈/二次约稿单制品信息大框**：制品、尺寸合并为「制品信息」大框（制品/工艺/尺寸/出血，2×2 网格，与重要信息框一致），右侧提示词「特殊出血请备注 可直接给模板」。
- **颜色格式提示词**：颜色格式右侧提示词「部分小程序不支持CMYK格式」。
- **收件邮箱改名**：收件邮箱地址→「收件邮箱」并删除框内提示词；交付方式选「指定邮箱」时显示收件邮箱字段。
- **展示权限四项 + 解封日期联动**：是否可以展示下拉改（可以展示 / 不愿意展示 / 延期1-3个月展示 / 解封日期后可以展示）；新增「解封日期」字段置于表单最后，选「解封日期后可以展示」时显示。
- **固定接收邮箱改名**：固定接收邮箱→「接收邮箱」。
- **版本**：app.js、sw.js、preview-phone.html 均 v528（index.html `app.js?v=527→528`）；`node --check` 通过；commit `fd5bc37`+`a6e035f`，**已 push GitHub main**；CloudStudio 重部署 entry=index.html（v528 已验证）。未打包 APK。

## v529（2026-08-27）接稿排期日历视图微调 + 价目表默认出血 + 接稿详情表单验收修正
- **接稿排期日历视图（验收微调）**：①月份标题「YYYY年M月」整体下移 1px（`.calendar-header .cal-title{margin-top:1px}`）；②「本月接稿（N）」去掉字重，与普通文字一致（`.cal-month-count` 删 `font-weight:600`）；③日历视图展示卡片的标题/编辑/删除/分割线整体上移 2px（`.record-list.cal-view .record-card-header{margin-top:-2px}`，`cal-view` 类仅在 design-commission 日历视图的 record-list 上加）。
- **价目表默认出血字段**：价目表新增「默认出血」字段，置于「默认尺寸」之后；默认尺寸底下的提示词删除；主界面价目表菜单制品显示改为「默认尺寸+默认出血」，如 `75mm+3mm`（任一为空则只显示有值部分，`cdPriceListSizeText` 助手），菜单项与排序项均生效。
- **接稿详情表单（验收修正）**：
  - 信息区真正左移 3px：`cdOpenClientFormFromLink` / `cdRenderClientStandalone` 由 `buildCdClientForm(...)` 改为 `cdFormShell(buildCdClientForm(...))`，使顶部/底部提示词 + 左移 3px 在单主填写弹窗、独立页、详情弹窗三处入口均生效。
  - 关联排期无信息时显示空白而非「—」横线（`openCdDetail` 内相关 `cdRecRow` 值由 `'—'` 改为 `''`）。
  - 「+ 新增制品」按钮上移约 2px，改为蓝底白字、宽度与填写字段一致（`btn btn-primary btn-sm`，`width:100%`，`buildCdExtraProductsHTML`）。
  - 约稿单顶部（请仔细填写 / 帮助我更好了解您的需求）与底部（图片文件可直发…/请确认信息无误…）两行提示在所有入口均显示（补 `cdFormShell` 包裹）。
  - 企划/主题名称（theme）移到制品信息大框之前（饭圈/二次）。
  - 制品框/尺寸框/出血框仍支持从价目表导入：饭圈/二次制品 input 改为价目表下拉 combobox（`cdProductComboboxHTML` → `#cdProductBoxProd`），选制品后自动回填默认尺寸/出血（仅空值时，`setupFormInteractions` 监听）。修复编辑回填时已有 product 不显示的问题（combobox-input / combobox-value 写入 value）。
  - 尺寸标题右边提示词加回「特殊出血请备注 可直接给模板」（label 内 `form-label-hint`），原制品框的提示词移除。
  - 「您的平台昵称」输入框内提示词删除（ec 去 `placeholder`）。
  - 工艺框内提示词删除（制品信息大框工艺 input 去 `placeholder="工艺"`）。
  - 解封日期字段置于接收邮箱之前（交付规范区 `commDetailUnsealDateField()` 移到 `commDetailFixedEmailField()` 前）。
- **版本**：app.js、sw.js、preview-phone.html 均 v529（index.html `app.js?v=528→529`）；`node --check` 通过；**已 push GitHub main**；CloudStudio 重部署 entry=index.html。未打包 APK（v1.1.28 之后未再发包）。

## v530（2026-08-27）验收修正：详情全字段展示 + 提示词去省略号 + 日期手输/日历 + 约稿单多项

- **① 详情视图全部字段展示（空值留白，不隐藏）**：`openDetail`（通用记录详情）、`openCdDetail`（约稿单详情弹窗）、`renderCdFullRecord`（约稿单「查看完整详情」完整记录）三处均去除空字段跳过逻辑——image/multiselect 空值也渲染、dynamic-list 始终渲染表头（空列表显示空表）、readonly/简单字段空值也渲染为空白值（不显示横线/占位）、约稿单 custom 大框（制品信息/重要信息/风格颜色/元素）子字段空值也渲染。符合「所有模块展示信息都摆出来，没有信息就空着」。
- **② 全文提示词去省略号**：灵感记录「文字思路…」→「文字思路」（textarea placeholder），全文 `…` 提示词/占位符统一移除（仅此一处实际带省略号的占位 + 注释）。
- **③ 日期字段支持手输 + 日历点选**：`buildFormField` 对 `type:'date'` 由原生 `<input type="date">` 改为 `<input type="text">` + `onfocus` 内 `if(this.type!=='date'){this.type='date';this.showPicker()}`（参考首页日期筛选模式），既可手输也可点选日历；非 today 默认值且无值时 placeholder 提示「请选择日期」。
- **④ 接稿排期日历视图验收微调**：①月份标题「YYYY年M月」下移 1px（v529 已做，保留）；②「本月接稿 N」去括号 + 字重/字号与正文完全一致——`.cal-month-count` 由蓝 pill 改为纯文本内联（`display:inline;margin-left:6px;font-size:inherit;font-weight:inherit;color:var(--c-text);background:none;padding:0;border-radius:0`），app.js 模板去括号；③日历视图展示卡片头（标题/编辑/删除/分割线）上移 2px→**1px**（`.record-list.cal-view .record-card-header{margin-top:-1px}`）。
- **⑤ 约稿单（接稿详情）多项验收**：
  - 最上（`.cd-form-top`/`.cd-form-topline`/`.cd-form-tip`）与最下（`.cd-form-bottom`/`.cd-form-botline`/`.cd-form-warn`）提示词字体/字号/字重/颜色与「您的平台昵称」小标题一致（form-label: 13px/500/var(--c-text)），`.cd-form-top`/`.cd-form-bottom` 居中对齐；warn 行保留红字。
  - 弹窗标题改为「XX约稿需求」（XX=分类，如「饭圈约稿需求」）：`openCdDetail` 标题 `(mod.category || '约稿') + '约稿需求'`。
  - **修复「生成约稿单链接打不开」**：`cdRenderClientStandalone` 原仅 `document.body.classList.add('cd-standalone')`，但 CSS 选择器是 `.cd-standalone #app`/`.cd-standalone #sidebar`（匹配 html 而非 body），导致独立页 CSS 不生效、内容错乱/打不开；改为**同时**给 `document.documentElement` 加 `cd-standalone` 类，并加 `if (!body) return` 空守卫提升健壮性（index.html 内联脚本本就正确地加在 documentElement 上）。
  - 约稿单尺寸提示词合并为整句「初始为默认尺寸，特殊出血请备注 可直接给模板」：fq/ec 的 `sizeF.hint` 改为整句（`prepareFields`），特殊出血提示词置于 custom HTML 尺寸 label 内的 `form-label-hint`。
  - 尺寸/出血自动回填**覆盖追加制品**：新增 `cdBindProductAutoFill(container)` 遍历 `#cdProductBoxProd` 与所有 `.cd-ep-product-box`，监听 combobox 选制品后回填 size/bleed（仅空值，查 `data-key`/`data-ep`）；`setupFormInteractions` 与 `addCdExtraProduct` 均调用。新增 `cdProductComboboxHTMLForEp(id, value)`（追加制品命名空间 `data-ep`）。
  - 「+ 新增制品」按钮高度/字号与字段输入条一致：`btn btn-primary` + `margin-top:12px;width:100%;font-size:13px;padding:8px 12px`（去掉 `btn-sm`）。
  - 追加制品的制品/尺寸改为与初始一致的「制品信息大框」combobox：`cdExtraProductRowHTML` 改用 `cdProductComboboxHTMLForEp` + 2×2 网格（制品/工艺/尺寸/出血）+ 尺寸 hint，保证追加制品与初始制品信息格式始终一致。
- **版本**：app.js、sw.js、preview-phone.html 均 v530（index.html `app.js?v=529→530`）；`node --check` 通过；**已 push GitHub main**（commit `f36ee6a`）；CloudStudio 重部署 entry=index.html（已 curl 验证 `xiao-workbench-v530` / `app.js?v=530` / 日期 onfocus 代码在线）。未打包 APK（v1.1.28 之后未再发包）。

## v531（2026-08-27）验收修正：列表卡片空字段展示 + 约稿单提示词/标题/位置 + 日历样式回滚

- **① 列表卡片空字段也展示标签（值留白）**：v530 仅改了详情弹窗/约稿单大框的空字段跳过；本轮把 `renderRecordCard` 里的空值跳过逻辑去掉（`if (v == null || v === '') return;` → `v = ''`），所有模块的列表卡片字段无信息时也显示标签、值留空，与「所有模块展示信息都摆出来，没有信息就空着」保持一致；`clientInfo` 仍不在正文重复（标题已展示），`_firstProduct` 在接稿排期仍跳过（下面用复选框展示）。tag 字段空值时不渲染空 tag，显示空白值。
- **② 约稿单尺寸提示词拆分**：fq/ec 模块与追加制品的「制品信息」label 右侧加提示词「特殊出血请备注 可直接给模板」；尺寸 label 内 hint 仅保留「初始为默认尺寸」。即把 v530 的整句拆成 label 右侧提示 + 尺寸小字提示。
- **③ 约稿单弹窗标题统一为「XX约稿需求」**：`cdOpenClientFormFromLink`（从「生成约稿单」点「直接填写」打开的弹窗）标题由「单主填写：XX约稿单」改为 `(mod.category || '约稿') + '约稿需求'`；`openCdDetail` 已在 v530 改为该格式。
- **④ 约稿单表单整体再左移 2px**：`.cd-form{margin-left:-3px}` → `-5px`。
- **⑤ 新增制品按钮再上移 5px**：`margin-top:12px` → `7px`（保持宽度 100%、字号 13px、padding 8px 12px，与字段输入条一致）。
- **⑥ 接稿排期日历样式回滚/微调**：
  - 「本月接稿 N」恢复蓝底白字 pill 按钮样式（`.cal-month-count` 改回 `display:inline-block;font-size:12px;font-weight:500;color:#fff;background:var(--c-primary);padding:2px 8px;border-radius:12px`），仅去掉括号（app.js 模板去掉前导空格与括号）。
  - 月份标题「YYYY年M月」上移 1px：`.calendar-header .cal-title{margin-top:1px}` → `-1px`。
  - 日历视图卡片头（标题/编辑/删除/分割线）再上移 1px：`.record-list.cal-view .record-card-header{margin-top:-1px}` → `-2px`。
- **版本**：app.js、sw.js、preview-phone.html 均 v531（index.html `app.js?v=530→531`）；`node --check` 通过；**已 push GitHub main**；CloudStudio 重部署 entry=index.html。未打包 APK（v1.1.28 之后未再发包）。

## v532（2026-08-27）验收修正：日期手输+日历按钮、制品自动回填修复、新增制品按钮、日历样式

- **① 日期字段支持自由手输 + 点 📅 打开原生日历（修复「还是不能手动输入」）**：v530 的实现在 `onfocus` 时把 input 转 `type=date` 并立即 `showPicker()`，会抢占焦点、在手机端无法手输。本轮改为始终保持 `type="text"`（可自由键入 `YYYY-MM-DD`），在输入框右侧加 📅 按钮，点击才 `openDatePicker` 转 `type=date` 并 `showPicker()`；`openDatePicker(btn)` 先尝试把已有非 ISO 文本解析回 ISO 再转 date。新增 CSS `.date-field-wrap`（input 弹性 + 按钮固定 38×34）+ `.date-pick-btn`。
- **② 修复「约稿单尺寸/出血不跟随制品自动填入」**：根因 `cdBindProductAutoFill` 原以 `#cdProductBoxProd`（制品 combobox 内层 wrapper）为容器，但尺寸/出血 input 是其同级兄弟、不在该 wrapper 内，导致 `box.querySelector('[data-key="size"]')` 返回 null、永不回填。改为以**外层 `.cd-product-box:not(.cd-ep-product-box)`** 为容器（内含制品 combobox + 尺寸/出血），追加制品仍走 `.cd-ep-product-box`；选制品后正确查价目表回填默认尺寸/出血（仅空值）。`selectComboboxOption` 已派发 input/change 事件，联动正常。
- **③ 新增制品按钮再上移 2px**：`margin-top:7px` → `5px`（保持宽 100%、字号 13px、padding 8px 12px）。
- **④ 接稿排期日历样式微调**：
  - 「本月接稿 N」按钮与左侧「YYYY年M月」标题**垂直居中对齐在一条直线**：`.cal-title{align-items:baseline}` → `align-items:center`（pill 仍 `vertical-align:middle`）。
  - 本月接稿与数字距离拉近：`margin-left:6px` → `3px`。
  - 按钮内文字和数字加粗：`font-weight:500` → `700`。
  - 展示模块盒子（日历卡片）`padding-top` 减 1px：`.record-list.cal-view .record-card{padding-top:15px}`（基样式 16px）。
- **版本**：app.js、sw.js、preview-phone.html 均 v532（index.html `app.js?v=531→532`）；`node --check` 通过；**已 push GitHub main**；CloudStudio 重部署 entry=index.html。未打包 APK（v1.1.28 之后未再发包）。

## v533（2026-08-27）验收修正：日期格式统一、制品变更联动尺寸出血、新增制品按钮对齐、日历加粗/间距

- **① 所有日期输入/选择后统一格式为 YYYY-MM-DD**：v532 的 📅 方案在 `openDatePicker` 里把 input 转 `type=date`+`showPicker()` 但选完未转回，导致「选出来的日期」以浏览器本地格式显示（如 `2026/08/27`），与「手输的 YYYY-MM-DD」不一致。本轮 `openDatePicker` 选完/失焦（`change`/`blur`）后调用 `normalizeDateValue` 归一化为 `YYYY-MM-DD` 并转回 `type=text`；手输框加 `onchange="normalizeDateValue(this)"`，任意可解析日期（含 `2026/8/27`、`2026年8月27日` 等）失焦即规范为 `YYYY-MM-DD`。新增 `normalizeDateValue(inp)` 助手（复用 `fmtDate`）。
- **② 约稿单更换制品时尺寸/出血一起变**：`cdBindProductAutoFill` 的 `fill` 原仅「尺寸/出血为空才写」，故改选制品不会更新已有尺寸/出血。改为**始终以当前制品默认值为准覆盖写入**（`sizeInp.value = pl.defaultSize || ''`、`bleedInp.value = pl.defaultBleed || ''`），初始大框与追加制品大框均生效（容器 `.cd-product-box:not(.cd-ep-product-box)` + `.cd-ep-product-box`）。
- **③ 新增制品按钮再上移 2px，追加制品顶部对齐**：按钮 `margin-top:5px` → `3px`；追加制品 `cd-ep-field` 外层 `margin-top:8px` → `3px`，使点击后出现的制品大框顶部与按钮顶部处于同一间距基线。
- **④ 接稿排期日历样式**：
  - 「本月接稿」按钮整体右移 2px：`margin-left:3px` → `5px`。
  - 按钮内数字字重加粗：`font-weight:700` → `800`。
  - 日期标题「YYYY年M月」数字字重加粗：`.cal-title{font-weight:600}` → `800`（接稿排期与首页日历统一）。
  - 展示模块盒子（日历卡片）`padding-top` 再减 2px：`13px`（基样式 16px，v532 15px）。
- **版本**：app.js、sw.js、preview-phone.html 均 v533（index.html `app.js?v=532→533`）；`node --check` 通过；**已 push GitHub main**；CloudStudio 重部署 entry=index.html。未打包 APK（v1.1.28 之后未再发包）。

## v587（2026-08-28）五项 UI 修正（v534–v586 详见每日记录 .workbuddy/memory/2026-08-28.md）
- ① 首页平台视图工具栏：日期输入框(.date-field-wrap)与「+ 新增记录」按钮等宽（home-toolbar 类 + flex:0 0 160px；移动端重置为 flex:1 1 0）。
- ② 接稿排期列表「报价金额」加 ¥ 符号（prefix:'¥'，与授权记录授权费用一致）。
- ③ 接稿排期日历格子宽度恒等（.cal-grid 改 repeat(7,minmax(0,1fr)) + .commission-cal-grid .cal-day overflow:hidden）。
- ④ 接稿排期日历日期条排序：开截同天(开+截)置顶，其次纯开稿，最后纯截稿（renderCommCalCell 拆分 bothRecords/pureStartRecords/pureEndRecords）。
- ⑤ 故事小记列表「剧情标签」去胶囊，改平铺文字（listFields 去掉 tag:true）。
- 版本：app.js、sw.js、preview-phone.html 均 v587（index.html `app.js?v=586→587`）；node --check 通过；已 push GitHub main；CloudStudio 重部署 entry=index.html。未打包 APK。

## v588（2026-08-28）四项 UI 修正（#776–#779）
- ① #777 首页平台视图：日期输入框(.filter-select)与「+ 新增记录」按钮等宽、**不含中间日期选择按钮**——撤销 v587 把整个 .date-field-wrap 定宽 160px 的写法，改为 .date-field-wrap{flex:0 0 auto} + 内部 .filter-select{flex:0 0 160px;width:160px}（文本框 160px），日历按钮保持 .date-pick-btn 自身 38px 自然宽；移动端 media query 同步放开 .filter-select 为 flex:1 1 0 自适应。
- ② #778 接稿排期日历圆点/文字缩小+左右对称：.cal-day-tag-dot 6px→4px、.cal-day-tag-text/.cal-day-tag 10px→9px、内部 gap 3px→2px；新增 .commission-cal-grid .cal-day{padding:3px 0} 与 .commission-cal-grid .cal-date-row{gap:6px;padding:0 6px}，使「日期数字→圆点」间距 = 「文字→日期格右边框」间距 = 6px（对称留白）。
- ③ #779 接稿排期日历日期条置顶：给 .commission-cal-grid .cal-date-row 加 position:relative;z-index:2，压在工期条 .cal-period-bar(z-index:1 绝对定位) 之上，修复 v587「开截同天」日期条仍被前一天开稿工期条覆盖的问题。
- ④ #776 展示模块胶囊按字段分色（仅展示，不改新增表单）：新增 FIELD_TAG_MAP（按 pageKey+key+val）与 fieldDisplayTagClass(pageKey,key,val)，在 renderCard 数组型/单值型两处 tag 渲染的既有 if 链后插入 override（progress/paymentStatus 仍走 commTagClass，不受影响）。映射：开团进度(筹备中黄/进行中蓝/已截团橙/流团红/已结算绿)、厂家合作状态(长期合作橙/临时合作黄/暂停合作灰)、厂家评价(非常满意橙/满意绿/一般蓝/不满意红)、打样评价(合格绿/中等蓝/不合格红)、授权用途(自用蓝/无盈利绿/商用红/买断橙/企业黄)、故事状态(连载中蓝/已完结绿)、约稿交易状态(待接稿红/已接稿蓝/已完成绿/已取消灰)、约稿评价(非常满意橙/满意绿/一般蓝/不满意红)；注意 status 在 groupbuy-records 与 oc-commission 复用但映射不同，已按 pageKey 区分。
- 版本：app.js、sw.js、preview-phone.html 均 v588（index.html `app.js?v=587→588`）；node --check 通过；已 push GitHub main；CloudStudio 重部署 entry=index.html。未打包 APK。

## v589（2026-08-28）修复 v588 三处未生效（#780–#782）
- ① #780 首页日期输入框与新增记录按钮真正等宽（不含日历按钮）：把 `.toolbar.home-toolbar .date-field-wrap` 内的 `.date-pick-btn` 改为绝对定位（right:0/top:0/bottom:0），不占 flex 宽度；桌面端 `.date-field-wrap` 与 `.btn-primary` 均固定 160px，移动端 media query 二者均 `flex:1 1 0` 均分 toolbar，`.filter-select` 宽度 100% 并加 `padding-right:44px` 避免文字与日历按钮重叠。由此输入框可视宽度 = 按钮宽度。
- ② #781 接稿排期日历标签再缩小+防溢出：`.cal-day-tag-dot` 4px→3px，`.cal-day-tag`/`cal-day-tag-text` 9px→8px，dot-text 间隙 2px→1px；`.commission-cal-grid .cal-date-row` 间隙与左右内边距 6px→2px 以保持对称，日期数字字号缩至 11px，并允许标签行 `flex-wrap:wrap` 自然换行；对应 app.js `barAreaTop` 从 22 提高到 32，避免换行后的日期行与工期条重叠。
- ③ #782 接稿排期日历日期行彻底置顶：`.commission-cal-grid .cal-date-row` z-index 2→5；`renderCommCalCell` 将 `${bars}` 置于 `.cal-date-row` 之前，配合 z-index 与 DOM 后序绘制，确保前一日开稿延续过来的工期条不再覆盖当前格的日期/标签。
- 版本：app.js、sw.js、preview-phone.html 均 v589（index.html `app.js?v=588→589`）；node --check 通过；已 push GitHub main；CloudStudio 重部署 entry=index.html。未打包 APK。

## v590（2026-08-28）按 45:10:45 重做首页 toolbar，回退接稿日历日期条位置
- ① #783 首页平台视图改为「日期输入框 45% : 选择日期按钮 10% : 新增记录按钮 45%」三列并排：将 `.date-pick-btn` 从 `.date-field-wrap` 内部抽出作为独立 flex item（HTML 结构改为 `<div class="date-field-wrap home-date-wrap"><input...></div><button class="date-pick-btn home-date-pick">...</button><button class="btn btn-primary">+ 新增记录</button>`）；同步修改 `openDatePicker(btn)`，当按钮的前一个兄弟不是 input 时，从 `.home-toolbar` 中查找 `.home-date-wrap input`。桌面端与移动端 media query 中三者分别 `flex:0 0 45%` / `10%` / `45%`，`.toolbar.home-toolbar` 自身 `gap:0`，`.search-box` 独占一行。
- ② #784 接稿排期日历标签左右对称：保持 `.commission-cal-grid .cal-date-row` `gap:2px;padding:0 2px`，使圆点到左侧日期数字的距离与最右侧文字到日期格右边框的距离均为 2px。
- ③ #785 接稿排期日历日期条位置改回 22px：`renderCommCalCell` 中 `barAreaTop` 从 32 改回 22（v589 因标签换行才提高，v590 日期行已有 z-index:5 + DOM 后序置顶，无需再下移）；`cellMinHeight` 注释同步改回 `(22+3*13=61px)`。
- 版本：app.js、sw.js、preview-phone.html 均 v590（index.html `app.js?v=589→590`）；node --check 通过；已 push GitHub main；CloudStudio 重部署 entry=index.html。未打包 APK。

## v591（2026-08-29）推翻 v590「45:10:45  无间隔」，日历标签对称/工期条配色重定义/选项管理补字段（#786–#789）
- ① #786 首页平台视图 toolbar 三者加间隔，日历按钮对齐表单级 38px：撤销 v590「gap:0 + 45%/10%/45%」。改为 `.toolbar.home-toolbar{gap:8px}`，`.search-box` 独占一行（`flex:1 1 100%;margin-bottom:8px`）；日期输入框 `.home-date-wrap` 与「+ 新增记录」按钮 `.btn-primary` 均 `flex:1 1 0;min-width:0` 平分剩余宽度；日历按钮 `.date-pick-btn.home-date-pick` 固定 `width:38px;flex:0 0 38px;height:34px`（与开团记录开团时间选择日期按钮 `.date-pick-btn` 自然宽一致）。桌面端 + 移动端 media query 同步。
- ② #787 接稿排期日历标签左右对称：`.commission-cal-grid .cal-date-row` 由 `justify-content:flex-start;padding:0 2px` 改为 `justify-content:center;padding:0`（`gap:2px` 保留），使标签整体居中、左右留白相等（此前靠左排、右侧大片空白）。配套 `.commission-cal-grid .cal-day{padding:3px 0}`。
- ③ #788 接稿排期日历工期条配色重定义：原 `URGENT_MS=3天→红 / NORMAL_MS=7天→绿 / >7天→蓝` 改为「已交付=绿、截稿≤2天=红、3-5天=橙、>5天=蓝」。代码：`CAL_URGENCY_COLORS=['#e8857e','#fa8c16','#7ab5f5']`（红/橙/蓝），删除原 URGENT_MS/NORMAL_MS；`recordColorMap` 先判 `valIncludes(r.progress,'已交付')`→`#7ec678`，再按 `daysLeft<=2`→红、`<=5`→橙、否则→蓝。图例同步：紧急（截稿≤2天）红 / 正常（3-5天）橙 / 充裕（>5天）蓝 / 已交付 绿。
- ④ #789 设置「选项管理」补打样记录「打样品类」与灵感记录「制品」字段：二者真实选项来自独立 store（`customSampleCategories` / `customCategories`），原渲染仅读 `f.options`（空数组）导致无字段。在 `renderFieldSettings` 渲染循环针对 `groupbuy-samples.category` / `design-inspiration.category` 特判，分别从 `DB.get('customSampleCategories')` / `DB.get('customCategories')` 叠加 `SAMPLE_DEFAULTS` / `['封面','明信片','其他','镭射票','壁纸','海报']` 渲染可编辑选项；`saveSettingsAction` 保存时回写 `DB.set`（剔除默认项）。
- 版本：app.js、sw.js、preview-phone.html 均 v591（index.html `app.js?v=590→591`）；node --check 通过；已 push GitHub main（commit 70feb1a）；CloudStudio 重部署 entry=index.html。未打包 APK。

## v592（2026-08-29）首页 toolbar 居中/接稿标签右侧居中/图例去形容词+橙降饱和/预览按钮顶部居中（#790–#793）
- ① #790 首页平台视图 toolbar 真的居中且等宽：撤销 v591「日期输入框与新增按钮 flex:1 1 0 平分」仍偏左的问题——根因是 `.toolbar.home-toolbar` 仅 `gap:8px` 缺 `justify-content:center`（左对齐），且基类 `.toolbar .btn-primary{width:112px}` 与 `.date-field-wrap .filter-select{flex:1}` 未被 home 专属规则覆盖。修复：`.toolbar.home-toolbar{display:flex;justify-content:center;align-items:center;gap:8px;flex-wrap:wrap}`，给 `.home-date-wrap` 与 `.btn-primary` 各补 `width:auto` 抵消基类固定宽/基准宽，使整组居中、日期输入框与新增记录按钮真正等宽、日历按钮固定 38px 居中。桌面端 + 移动端 media query 同步。
- ② #791 接稿排期日历标签改为「日期数字居左 + 圆点+开稿/截稿/开+截 在右侧剩余空间内居中」：撤销 v591 `justify-content:center` 把「日期数字+标签」整体居中的错误。改为 `.commission-cal-grid .cal-date-row{justify-content:flex-start;flex-wrap:nowrap;gap:2px;padding:0}`（日期数字 `.cal-date` 自然左对齐），并把 `.cal-day-tags` 包一层 `.comm-tags-center{flex:1 1 auto;min-width:0;justify-content:center}`，使标签组占据日期数字右侧剩余空间并自身居中——圆点到左日期数字的距离 = 最右文字到右框的距离（左右对称）。`renderCommCalCell` 的 cell 渲染把 `${commTagStr}` 包入 `.cal-day-tags.comm-tags-center`。首页日历同样结构不动。
- ③ #792 接稿排期日历工期条图例去形容词 + 橙色降饱和：图例「紧急/正常/充裕」形容词删除，仅留区间——「截稿≤2天 / 3-5天 / >5天 / 已交付」。`CAL_URGENCY_COLORS` 橙色 `#fa8c16`→`#f6ad5c`（降饱和，与红 #e8857e / 绿 #7ec678 / 蓝 #7ab5f5 一致的柔和低饱和）。
- ④ #793 预览页「显示手机框」浮动按钮（#exitFrame，仅全屏模式可见）从右上角 `top:8px;right:8px` 改顶部居中 `position:fixed;top:8px;left:50%;transform:translateX(-50%);right:auto;z-index:200`，避让右上角设置按钮。全屏模式 `.header` 隐藏（`body.content-only .header{display:none}`），不与 #exitFrame 重叠。
- 版本：app.js、sw.js、preview-phone.html 均 v592（index.html `app.js?v=591→592`）；node --check 通过；已 push GitHub main（commit fa5a773）；CloudStudio 重部署 entry=index.html，curl 验证线上三处版本号均为 v592、#f6ad5c 与 comm-tags-center 已落地。未打包 APK。

## v593（2026-08-29）首页 toolbar 改用 grid 三列，彻底居中（#795）
- ① #795 首页平台视图 toolbar 仍「整体偏左」：v592 用 flex `flex:1 1 0` 理论上可让日期输入框与新增按钮等宽、整体居中，但实际视觉仍偏移。改为 CSS Grid 三列布局 `.toolbar.home-toolbar{display:grid;grid-template-columns:1fr 38px 1fr;gap:8px;align-items:center}`，让布局不再依赖 flex 基准/增长计算：日期输入框占第 1 列（`1fr`）、日历按钮固定 38px 占第 2 列并自然居中、新增记录按钮占第 3 列（`1fr`），三者严格等间距且输入框与按钮宽度严格相等。搜索框 `.search-box{grid-column:1/-1;margin-bottom:8px}` 独占第一行，并补 `.toolbar.home-toolbar .search-box input{width:100%}` 保证搜索输入框在桌面端也撑满。
- ② 同步移动端 media query：`.toolbar.home-toolbar{gap:8px;justify-items:stretch}`，其余 grid 项宽度/按钮 padding 与桌面一致。
- 版本：app.js、sw.js、preview-phone.html 均 v593（index.html `app.js?v=592→593`）；node --check 通过；已 push GitHub main（commit 8997927）；CloudStudio 重部署 entry=index.html，curl 验证线上 index.html/sw.js/preview-phone.html 三处版本号均为 v593、`grid-template-columns:1fr 38px 1fr` 已落地。未打包 APK。


## v594（2026-08-29）接稿排期日历节点条渲染 + 日期格弹窗（#796/#797）
- **重构（废除跨日延伸）**：`renderCommissionCalendar` 不再把「开稿~截稿」渲染成连续工期条，改为仅渲染「开稿/截稿节点日」自身任务（27 开稿不延伸到 28）。
  - **节点标记（日期数字右侧）**：开稿=橙 `#ff9a3c`、截稿=黄 `#FFDE75`、开+截同天=紫 `#712258`（沿用 `commTagStr` 标签，满足「节点标记与工期条互不影响」）。
  - **工期条（日期格中间，上限 3 条）**：颜色由 `commissionBarColor(r,dateStr,today)` 决定——已交付=绿 `#7ec678` > 开+截=紫 > 截稿按紧迫度（≤2天红 `#e8857e` / 3-5天橙 `#f6ad5c` / >5天蓝 `#7ab5f5`）> 开稿=橙。
  - **截断规则**：单格最多 3 条；正常任务（未交付）>3 直接截断；正常≤3 时用「已交付」补足到 3 条（已交付变绿显示），超出则优先隐藏已交付。
  - 删除旧 `periodRecords` / `recordColorMap` / `tracks` / `recordTrack` 跨日追踪，`renderCommCalCell` 改为基于 `nodeRecs`（start/deadline == 当日）并应用本地拖拽排序。
- **日期格弹窗（#797）**：`commissionDateClick` 由「切换筛选」改为打开浮层（`date-picker-modal` 遮罩 + `.cal-date-popup`）。
  - 顶部统计：今日开稿总数、今日截稿总数。
  - 明细列表：各单主 `clientInfo` + 制品数量（`calcProductQty`），标注 开稿/截稿/开+截/已交付（对应色）。
  - 拖拽排序：HTML5 `draggable`，`calDragStart/calDragOver/calDrop` 改顺序，`localStorage` 键 `xiao_cal_sort_<dateStr>` 持久化；仅改视图不碰底层数据，排序同时反映到日历格内工期条顺序。
  - 保留「按此日筛选」（`commissionFilterDate`）+ 关闭按钮。
  - 新增 CSS：`.cal-date-popup/.cal-date-stats/.cal-date-list/.cal-date-item/.ci-name/.ci-qty/.ci-tag`（次级文字用 `--c-text-muted`，适配深浅主题）。
- 版本：app.js、sw.js、preview-phone.html 均 v594（index.html `app.js?v=593→594`）；node --check 通过；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK（待用户确认）。


## v595（2026-08-29）接稿排期日历恢复跨度 + 节点置顶 + 条色仅红橙蓝绿 + 弹窗统计卡去蓝底（纠正 v594）
- **纠正 v594 的误删**：v594 把「开稿~截稿」连续跨度工期条整个删掉是**错误**的。用户要的是**跨度保留**（`periodRecords` + 贪心 `recordTrack` 轨道分配，同一任务跨天保持同一行），只是「当日开稿/截稿的节点任务」在格内**置顶**显示。
- `renderCommCalCell` 排序链：节点任务（`isStart||isEnd`）置顶 → 其余按 `recordTrack` 保持跨天同行 → 同组内应用本地拖拽排序（`xiao_cal_sort_<date>`）。
- **上限 3 条不再显示 `+N`**：移除 `cal-period-overflow` 溢出提示；超出时优先隐藏「已交付」（从尾部往前删），仍超则截断。
- **工期条颜色只出红橙蓝绿**：`commissionBarColor(r, todayTime)` 简化为「已交付绿 > ≤2天红 > 3-5天橙 > >5天蓝」，**删掉**紫（开+截）与开稿橙 `#ff9a3c` 两种条色，颜色不再依赖日期。节点标记（日期数字右侧小圆点+文字）仍为 开稿橙/截稿黄/开+截紫，与工期条互不干扰。
- **弹窗统计卡去蓝底**：`.cal-date-stat` 去掉 `background:var(--c-primary-bg)`（用户反馈「今日开稿/截稿 不要蓝色底色」）。
- 版本：app.js、sw.js、preview-phone.html 均 v595（index.html `app.js?v=594→595`）；node --check 通过；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK。


## v596（2026-08-29）接稿排期日历同一轨道重叠覆盖（纠正 v595 理解偏差）
- **用户纠正**：v595 把「新的一天放最上面」理解成「把节点任务放在上方轨道」，实际意思是「同一轨道内 28 号节点条用 z-index 盖住 27 号延伸过来的同轨道段」——是图层叠加，不是垂直分居。
- **实现**：任务按开始日期分配优先级（越晚开始优先级越高）；3 条轨道允许重叠；每条轨道内当日最高优先级的条显示，低优先级同轨道条被盖住；每格最多 3 条可见（无 +N）。
  - `periodRecords` 按开始日期升序，`recordPriority` 记录优先级索引。
  - 分配轨道：按优先级降序处理，优先把任务放进与已存在任务有重叠的轨道（让高优先级任务盖住低优先级任务）；无重叠则放入任务最少的轨道。
  - 渲染：每轨道取当日跨度内优先级最高的任务，用 `z-index` 体现层级；条色仍只走 `commissionBarColor(r, todayTime)` 红橙蓝绿。
- 节点标记（日期数字右侧 开稿橙/截稿黄/开+截紫）和弹窗统计卡无蓝底保持 v595。
- 拖拽排序：目前仅作用于弹窗内明细列表顺序，不再直接影响日历格内轨道覆盖。
- 版本：app.js、sw.js、preview-phone.html 均 v596（index.html `app.js?v=595→596`）；node --check 通过；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK。


## v597（2026-08-29）接稿排期日历工期条顺序与底部列表一致（修正 v596）
- **用户纠正**：v596 按「开始日期晚的覆盖早的」排序导致日历格内顺序与底部列表模块不一致。用户要求日历格内显示顺序和列表完全一致：`MM → 逆逆 → 好好 → 看看看 → 等等`。
- **实现**：`renderCommissionCalendar` 中 `periodRecords` 直接使用 `commissionRecordSort` 排序（与列表模块相同规则：制作中>修改中>已接稿>待接稿>已交付，同优先级按 acceptTime→startTime→deadline 升序），每个格子取前 3 条渲染。
- 移除了 v596 的 `recordPriority`、重叠轨道、`z-index` 覆盖逻辑。
- 保持：工期条只走红橙蓝绿、无 `+N`、弹窗统计卡无蓝底、节点标记颜色不变。
- 版本：app.js、sw.js、preview-phone.html 均 v597（index.html `app.js?v=596→597`）；node --check 通过；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK。


## v598（2026-08-29）接稿排期日历恢复「稳定轨道」（修正 v597 排列乱跳）
- **用户纠正**：v597 改成「每格按列表顺序取前 3 条」后，①28 号开稿的任务又被覆盖看不见；②30 号~5 号能看到的那几条排列很乱、每天都在换行。要求「一条就在一条线上不要乱变，参考 v596 样式」。
- **实现**：重新引入**稳定轨道分配**，同时保留 v597 的「顺序与底部列表一致」。
  - `periodRecords` 仍按 `commissionRecordSort` 排序（与列表模块完全一致），并用 `listIdx[id]` 记录列表序号。
  - **轨道分配（`tracks` / `recordTrack`）**：按列表顺序依次入轨——优先放入**无任何重叠**的轨道（同任务跨天固定占用同一行，不再逐日跳动）；三条轨道全都冲突时，才归入「重叠任务中开稿日最早」的那条轨道，由后开稿者覆盖。
  - **同轨覆盖规则**：`renderCommCalCell` 每条轨道当日只渲染一条，取**开稿日最晚**的那条（同开稿日并列时取列表序号更小的）。这样保证「新开稿任务在自己开稿日一定可见」，且后续日期不会被更早开稿的同轨任务抢回（不会闪跳）。
  - 渲染位置固定为 `top = 22 + 轨道号 × (11+2)px`，格子内最多 3 条，无 `+N`。
- 保持：工期条颜色只走红橙蓝绿（`commissionBarColor`）、节点标记（日期数字右侧 开稿橙/截稿黄/开+截紫）与工期条互不影响、日期格弹窗统计卡无蓝底、拖拽排序本地持久化。
- **冒烟验证**：独立脚本模拟 5 条并发任务（8-25~9-05），轨道分配 `[["A","D","E"],["B"],["C"]]`，逐日渲染行号稳定，「稳定轨道校验 PASS」。
- 版本：app.js、sw.js、preview-phone.html 均 v598（index.html `app.js?v=597→598`）；node --check 通过；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK。


## v599（2026-08-29）接稿排期日历：当日新增开稿优先 + 弹窗排序联动日历与排期模块
- **新渲染规则（用户明确给定，取代 v598 的稳定轨道方案）**：
  1. **当日新增开稿任务条层级高于跨日期延伸的工期条**；当日多条新增稿件**全部叠在延伸条上层**，并按排期顺序排列。
  2. **弹窗手动调整单主排序后，日历格与底部排期展示模块顺序同步联动**。
  3. **单格最多展示 3 条，优先保留当日新增稿件**。
- **日历渲染改动**（`renderCommissionCalendar` / `renderCommCalCell`）：
  - 删除 v598 的稳定轨道分配（`tracks` / `recordTrack` / `recOverlap` / `listIdx` / `byId`）。
  - `periodRecords` = `applyCommOrder(筛选后.sort(commissionRecordSort))`——先按列表规则排，再叠加弹窗拖拽产生的全局自定义顺序。
  - 每格可见集：`dayAll`（覆盖当日）拆成 `newOnes`（`recStart(r) === dateStr`，当日新增开稿）与 `carryOn`（跨日延伸）；`list = newOnes.slice(0,3).concat(carryOn.slice(0, 3 - min(newOnes.length,3)))`。
  - 渲染位置改为 `top = 22 + i × 13px`（`i` 为可见集下标），新增稿件自然落在第 0、1… 行。
- **全局自定义顺序（新增）**：`xiao_comm_order`（localStorage，id 数组）。
  - `loadCommOrder()` 带内存缓存 `commOrderCache`；`applyCommOrder(list)` 用稳定排序叠加（未登记的记录保持原相对顺序排在其后）。
  - `syncCommOrder(dateIds, allIds)`：拖拽后以 `allIds` 补全顺序表，再把 `dateIds` 按新顺序**原地重排回它们原来的位置区间**（不会把某条顶到整个列表最前面）。
  - `renderListPage` 的 `design-commission` 分支改为 `records = applyCommOrder(records.slice().sort(commissionRecordSort))`。
  - `calDrop` 在写 `xiao_cal_sort_<date>` 后追加 `syncCommOrder(calModalOrder.slice(), DB.list('commissions').map(r => r.id))`。
- **场景验证**（独立脚本，27号 4 个开稿 / 28号 2 个开稿）：
  - 27号：按展示模块顺序显示前 3 条 `MM / 逆逆 / 好好`（第 4 条受 3 条上限截断）。
  - 28号：`等等(新增) / 吽吽(新增) / MM(延伸)` —— 两条新增各占一行，不再互相覆盖（修复「第二个稿件没覆盖在逆逆上面」）。
  - 在 28 号弹窗把「吽吽」拖到「等等」前 → 全局顺序表 `[1,2,3,4,6,5]`，底部模块变 `MM→逆逆→好好→看看看→吽吽→等等`，28号格同步变 `吽吽 / 等等 / MM`。6 项断言全 PASS。
- 版本：app.js、sw.js、preview-phone.html 均 v599（index.html `app.js?v=598→599`）；node --check 通过；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK。


## v600（2026-08-29）接稿排期日历：恢复「稳定轨道」，但避免同一天新增稿件互相覆盖
- **用户反馈**：v599 改成「当日新增优先」后，截图显示 29 号~5 号的条每天都在换行，要求「一条就在一条线上不要乱变，参考 v596 样式」。
- **根因**：v599 每格独立取「新增优先 + 延伸补满」，丢弃了轨道概念，导致同一任务跨天时行号变化。
- **实现（v600）**：
  - 重新引入稳定 3 条轨道 `tracks/recordTrack`，同一任务跨天固定同一行。
  - **关键修复（v598 没有这条，导致 28 号第二个新增被第一个盖住）**：轨道分配时**避免同一天开稿的任务进入同一轨道**。
    - 分配优先级：①无重叠且无同开始日期的轨道；②无重叠轨道；③有重叠但无同开始日期的轨道；④不得已才与「开稿日最早」的任务同轨。
  - 渲染：每条轨道当日取「开稿日最晚」的任务；同日开稿并列时取列表序号小的。
  - 保留 v599 的全局自定义顺序 `xiao_comm_order` 及其与弹窗/列表模块的联动。
- **截图场景验证**（27号 MM/逆逆/好好 + 28号 等等/声声）：
  - 轨道分配：`T0=[MM(27), 看看看(27), 等等(28)]`、`T1=[逆逆(27), 声声(28)]`、`T2=[好好(27)]`。
  - 27号：`MM / 逆逆 / 好好`。
  - 28号：`等等（覆盖MM）/ 声声（覆盖逆逆）/ 好好（延伸）`。
  - 29号：`等等 / 声声 / 好好`（好好截稿日）。
  - 30号：`等等 / 声声`（逆逆截稿）。
  - 31号：`等等 / 声声`（MM截稿）。
  - 9-04/05：`看看看 / 声声`（MM截稿后 看看看 露出）。
  - 稳定轨道校验：PASS。
- 版本：app.js、sw.js、preview-phone.html 均 v600（index.html `app.js?v=599→600`）；node --check 通过；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK。


## v601（2026-08-29）接稿排期/首页日历图例文字调整
- 用户反馈：底部示意条文字含义不清，要求改为「当日有开稿 / 当日有截稿 / 当日既有开稿又有截稿」。
- 改动：
  - `app.js` L1273-1275（首页日历底部图例）：`开稿`→`当日有开稿`、`截稿`→`当日有截稿`、`当天同时存在开稿和截稿项目`→`当日既有开稿又有截稿`。
  - `app.js` L2786-2788（接稿排期日历底部图例）：`开稿`→`当日有开稿`、`截稿`→`当日有截稿`、`开+截同天`→`当日既有开稿又有截稿`。
- 日期格内小圆点文字仍保持原样（`开稿`/`截稿`/`开+截`）。
- 版本：app.js、sw.js、preview-phone.html 均 v601（index.html `app.js?v=600→601`）；node --check 通过；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK。


## v602（2026-08-29）接稿排期日历节点标签：当天既有开稿又有截稿显示「开+截」
- 用户反馈：接稿排期日历中，9 月 4 号当天既有开稿又有截稿，但日期格右上角显示的是分开的「开稿」「截稿」两个标签，没有合并成紫色「开+截」；首页日历已经是「开+截」。
- 根因：`renderCommissionCalendar` 里只有当**同一条记录**同时开稿截稿时才显示「开+截」，没有处理「当天既有开稿记录又有截稿记录」的情况。
- 改动（`renderCommissionCalendar` / `renderCommCalCell`）：
  - 拆分 `startRecords` → `startOnlyRecords`、`deadlineRecords` → `endOnlyRecords`。
  - 新增 `hasStart` / `hasEnd` 两个布尔标记。
  - 标签逻辑改为：`(bothRecords.length > 0 || (hasStart && hasEnd))` 显示紫色「开+截」；否则只开稿显示橙色「开稿」；否则只截稿显示黄色「截稿」。
  - 与首页日历逻辑（L1237-1243）保持一致。
- 日期格内工期条颜色、弹窗内单条记录的「开稿/截稿/开+截」标签均不受影响。
- 版本：app.js、sw.js、preview-phone.html 均 v602（index.html `app.js?v=601→602`）；node --check 通过；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK。


## v603（2026-08-29）接稿排期日历日期格内边距对齐首页日历
- 用户反馈：接稿排期日历日期格中的日期数字过于靠近左边，没有边距；首页日历有正常边距。
- 根因：`.commission-cal-grid .cal-day{padding:3px 0}` 把默认的左右 3px 内边距覆盖了。
- 改动：`index.html` L422 `.commission-cal-grid .cal-day{padding:3px 0}` → `padding:3px`（与首页日历 `.cal-day{padding:3px}` 一致）。日期数字仍靠左，`align-items:flex-start` 保持不变。
- 版本：index.html `app.js?v=602→603`、sw.js `v603`、preview-phone.html `APP_VERSION=603`；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK。


## v604（2026-08-29）首页日历日期格标签圆点/文字大小对齐底部图例
- 用户反馈：首页日历日期格内的「开稿/截稿/开+截/进入临空」圆点和文字太小，要与底部图例一致。
- 改动：`index.html` 在 `.cal-day-tag-more` 后新增首页限定样式：
  - `.home-comm-tags .cal-day-tag, .home-deep-tags .cal-day-tag { font-size:12px; gap:4px }`
  - `.home-comm-tags .cal-day-tag-dot, .home-deep-tags .cal-day-tag-dot { width:6px; height:6px }`
  - `.home-comm-tags .cal-day-tag-text, .home-deep-tags .cal-day-tag-text { font-size:12px; font-weight:500 }`
- 接稿排期日历（无 `.home-comm-tags/.home-deep-tags` 包裹）保持原有 8px 字号不变。
- 版本：index.html `app.js?v=603→604`、sw.js `v604`、preview-phone.html `APP_VERSION=604`；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK。

## v605（2026-08-29）首页日历日期格标签文字改为 9px
- 用户反馈：v604 把首页日历标签文字设为 12px 偏大，要求改为 9px（圆点 6×6px 及 .cal-day-tag 间距不变）。
- 改动：`index.html` L410 `.home-comm-tags/.home-deep-tags .cal-day-tag-text` 的 `font-size:12px` → `font-size:9px`。
- 版本：index.html `app.js?v=604→605`、sw.js `v605`、preview-phone.html `APP_VERSION=605`；四文件已同步 `D:/wbkey/webview-app/app/src/main/assets/`。未打包 APK。
