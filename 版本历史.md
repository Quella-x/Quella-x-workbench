# 小筱工作台 版本历史

> **维护约定（重要）**：本文件是项目的持续版本账本。**每次功能改动 / 发版后，由阿布把对应条目追加到「四、后续变更记录」段**，不要覆盖历史、不要新建重复文件。
>
> 两条独立编号线：
> - **功能版本**：v1–v27（网页本体的业务 / 样式迭代，止于 2026-08-03 的 v27）
> - **APK 打包版本**：v1.0.0–v1.1.16（WebView 壳发布序号，每次发一个包 +1）
>
> 详细开发日志在 `.workbuddy/memory/2026-08-0X.md`，本文件只记里程碑与决策。

---

## 编号对照（先读这个）

- **v1.1.16 的功能基础 = v27**。业务功能在 2026-08-03 定稿于 v27（含 PWA 化、Supabase 云端同步）。2026-08-04 至 08-05 的所有改动都是「WebView 壳显示适配」（顶部对齐 / 状态栏 / overlay / 打包脚本），没有新增业务模块，因此功能版本号仍停在 v27。
- 即：**v1.1.16 = v27 功能代码 + WebView 壳显示适配 + 打包发布**。
- 功能版本线：v1（基础）→ … → v27（定稿）
- APK 版本线：v1.0.0 → … → v1.1.16（共 27 个命名发布包）
- 当前最新（2026-08-05）：功能 **v27** / APK **v1.1.16**（versionCode 27，144444 字节）
- **2026-08-06 起进入功能 v29 迭代**（注：v28 为 WebView 壳 SW 缓存轮次，未涨功能号；本轮业务改动以 v29 标记）。v29 业务代码已在 `workstation/` 工作树，但**尚未打包发布**；下次发版建议 APK 升到 **v1.1.17**（versionCode 28）。

---

## 一、功能版本演进（网页本体）

### 2026-08-01 · 创造起点 + v2
- **v1 基础版**：解析桌面《个人工作台完整设计需求文档.docx》→ 纯 HTML/CSS/JS 单页应用，部署 CloudStudio。4 大模块 / 13 子页：首页·自媒体发布记录（四平台）、开团记录 / 厂家 / 打样 / 价目计算器、灵感 / 接稿排期 / 授权 / 价目表、OC 人物档案 / 关系图谱 / 故事小记 / 约稿。localStorage 持久化、CRUD、图片预览、统计卡片、响应式（电脑 + 手机）。
- **v2 大规模改造（13 项）**：浅蓝主题、select→combobox 可手输、多制品、人物档案全新字段、关系图谱思维导图等。

### 2026-08-02 · v14
- v14 改造：恢复内联「+ 新增记录」按钮、价目计算改实时报价小票风格、接稿排期同模字段下沉到制品级（含数据迁移）。

### 2026-08-03 · v15–v27（密集迭代 + 三件大事）
- v15（9 项）：OC 时间轨迹板块、思维导图圆形节点 + 自由缩放、报价计算器改版、列表两列布局、人物档案手动排序…
- v16（7 项）· v17（4 项，节点自适应）· v18（7 项，拖拽平移 / 甘特图贪心算法重写）· v19（7 项，自定义关系类型）· v20（5 项，冰川蓝主题 / 删毛玻璃）· v21（3 项 + 3 补丁，排序按钮布局）…
- **v22 大更新**：报价计算器柄图分组 + 同模新公式 + 自动 SET 折扣（SET 与折扣互斥、同模单品层可叠加）。
- v23 · v24（柄图控制同模）· v25（3 项 + 补丁，combobox 化）· v26（3 项 + 补丁）· v27（日历格子缩小 / 颜色绑定修正 / 降饱和度）…
- **v27 补丁 11**：PWA 化（离线可装、html2canvas 本地化、manifest + service worker）。
- **v27 补丁 12**：Supabase 云端同步（17 个业务 store 双向、last-write-wins、防抖推送 + 定时拉取）。
- **v27 补丁 13**：PWA 合规化准备打包（manifest.json + PNG 图标）。
- **关键决策（8/3 深夜）**：TWA 路线在 OPPO/ColorOS 16 被判走不通（系统强制第三方链接走浏览器）→ 放弃 TWA，改原生 WebView APK（手动 aapt2 构建）。

---

## 二、APK 打包版本（WebView 壳）

### 2026-08-04 · 打包长征 v1.0.0–v1.1.2（13 个包）
手动 aapt2 构建（包名 `io.github.quella_x.webview`，versionCode 1→13）。连踩 5 大坑：
1. 缺 `<uses-sdk>` → ColorOS 弹「古早 App」兼容性警告（修：minSdk 22 / targetSdk 34）。
2. 点开闪退 → `NoClassDefFoundError`（匿名内部类 `MainActivity$1` 没喂进 d8）。
3. ColorOS 16 拦截 WebView 外网 → `loadUrl` 5 秒超时（改本地 assets）。
4. `file://` 协议层被禁用 → `loadDataWithBaseURL` + `shouldInterceptRequest` 内联方案。
5. ColorOS 16 asset 沙箱路径黑名单 → 资产根级平铺才读得到。

### 2026-08-05 · 发布微调 v1.1.3–v1.1.16（14 个包）
- v1.1.3–v1.1.11：**顶部对齐连调 9 版**才对（核心教训：`align-items:flex-start` + `min-height`，不用 `align-items:center` 让内容下沉）。
- v1.1.12：撤掉「白白让出 32px 空间」（header padding-top 32→8）。
- v1.1.13：撤 main-header 白色横条 + overlay `inset:32px`（阴影不染状态栏）。
- v1.1.14：恢复装饰线条 + 主区浅蓝。
- v1.1.15：建立 git 基线（首次提交 v1.1.15）+ 上线 GitHub Pages；确立「电脑浏览器 = 最终效果」预览流程（除顶部 32dp 状态栏差异外布局一致）。
- **v1.1.16（13:40 发布成功）**：144444 字节，versionCode 27，sw.js 缓存 v38，用 `build_v1.1.16.sh`。OPPO 先卸载旧版再装。

---

## 三、关键坑 / 经验（跨版本通用）

- **OPPO/ColorOS 16 系统性限制**：TWA 走不通、`file://` 被禁用、WebView 外网被拦、asset 沙箱限路径、versionCode 跨版需先卸载再装。
- **顶部对齐黄金规则**：`align-items:flex-start` + `min-height`；header 是占位条就透明，装饰条才留 background/border；header 内部不要再重复让出系统状态栏已让出的 32dp。
- **构建链路**：aapt2 → javac → d8 → `jar uf` → zipalign → apksigner；脚本见 `D:\wbkey\webview-app\build_v1.1.16.sh`；Bash 环境禁用 `rm`，靠独立工作目录避免清理。
- **版本双轨**：功能版本（vN）与打包版本（vX.Y.Z）独立计数，发版只涨打包号，功能无新增时不涨功能号。

---

## 四、后续变更记录（在此持续追加）

<!-- 追加格式示例：
### YYYY-MM-DD · 功能 vN / APK vX.Y.Z
- 改动摘要（一句话说清做了什么、为什么）
- 交付 / 影响（如 APK 路径、versionCode）
-->

### 2026-08-06 · 功能 v29（WebView 壳内未发版，待打包 v1.1.17）

- **报价计算（design-calc）**：手机端「制品列表 / 加价项目」改为卡片式录入（对齐接稿排期同款），解决制品名称显示不全。
- **接稿排期（design-commission）**：字段「客户信息 → 单主」；列表展示块增加 单主 / 进度 / 支付 / 用途 / 制品名称 / 截稿 / 报价；日历内开稿、截稿统一用实心圆点 + 单主名显示。
- **首页日历（home）**：日期格顶部固定平台发布圆点、底部固定开稿 / 接稿记录；小红书 / 抖音 / 视频号 / 公众号按钮高度缩小；日历下方新增「灵感速记」模块（写入 `inspirations`，进入「美工·灵感记录」，首页不展示）。
- **价目表（design-pricelist）**：每个条目新增上移 / 下移排序按钮（同类目内交换 `sort` 字段），保留原有编辑 / 删除。
- **全局弹窗**：新增记录 / 查看记录支持手机端横向右滑手势返回（等效点 ✕ 关闭）；仅移动端启用，输入 / 按钮区域不触发。
- 代码注释统一以 `v29` 标记本轮改动；`sw.js` 缓存轮次此前已到 v30。
- 交付状态：源码改动在 `workstation/`（未提交）；APK 仍为 v1.1.16，需重建 v1.1.17 才能上机。

### 2026-08-06（当晚修正）· 功能 v29 调优

- **报价计算（design-calc）**：手机端制品 / 加价项目从卡片式改回横向字段行（对齐接稿排期制品列表），字段顺序：制品名称 → 柄图标识 → 单价 → 数量 → 同模 → 删除。
- **接稿排期（design-commission）**：列表卡片标题已展示单主，正文展示块去掉重复的单主；「制品名称」标签改为「制品」，多个制品用「、」隔开；日历开稿恢复为仅实心圆点，截稿显示圆点 +「截稿」文字。
- **首页日历（home）**：平台发布圆点保持在日期格顶部原位置，下一行显示开稿 / 接稿 / 截稿记录（实心点 + 文字）；灵感速记输入框改为 combobox 下拉选择器，占位词改为「灵感主题」「制品名称」，文字思路 textarea 增加上间距。
- **价目表（design-pricelist）**：移除条目后的 inline 上移 / 下移按钮，改为工具栏「↕️ 调整排序」按钮，点击弹出独立排序面板，在面板内完成同类目 reorder，完成后刷新价目表。
- 源码已提交 `a008180`；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-06（二次修正）· 功能 v29 继续调优

- **报价计算（design-calc）**：手机端「柄图标识」占位词改为「柄图」，避免横向行布局下显示不全；并微调 mobile 列宽让输入框更宽松。
- **接稿排期（design-commission）**：日历开稿 / 截稿统一恢复为 v27 显示样式——日期数字同行显示实心圆点 +「开稿 / 截稿」文字。
- **首页日历（home）**：平台发布记录圆点保持在日期格顶部原位置；下一行显示发布记录数量；再下一行显示开稿 / 接稿 / 截稿标签（实心点 + 文字）。灵感速记的「灵感主题」从 combobox 改回普通输入框，「制品名称」保持下拉选择器。
- **人物档案（oc-profiles）**：取消每张卡片右上角的 inline 上移 / 下移按钮，改为工具栏「↕️ 调整排序」按钮，点击打开独立排序弹窗，在弹窗内 reorder 后统一保存。
- 源码已提交 `7e54d23`；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-06（三次修正）· 功能 v29 继续调优

- **报价计算（design-calc）**：制品列表 header「制品名称 → 制品」；柄图标识占位恢复「柄图标识」四字并加宽手机端列宽（1fr/76/50/44/44/28）确保显示全；加价项目 header「项目名称 → 加价项目」。
- **接稿排期（design-commission）**：制品列表字段标题「制品名称 → 制品」；加价项目字段标题「项目名称 → 加价项目」；日历开稿橙（`#ff9a3c`）/截稿黄（`#f5c518`）实心圆点 + 文字、大小一致，截稿用 `margin-left:auto` 固定右侧，对齐 v27。
- **首页日历（home）**：平台发布圆点移到日期数字下方独立行；开稿橙 / 接稿蓝 / 截稿黄实心圆点 + 文字，截稿固定右侧，对齐接稿排期日历样式。
- **人物档案（oc-profiles）**：「↕️ 调整排序」按钮改用 `btn btn-outline toolbar-eq`，与 OC 时间线「📥 导入故事小记」按钮显示样式一致。
- **开团记录（groupbuy-records）/ 售后记录**：制品名称去掉动态 options，渲染为纯输入框（不再显示为 combobox 下拉）。
- **打样记录（groupbuy-samples）**：评价「不合格」标签颜色由灰改为红（`.tag-danger`）；打样品类 `text` 改为 `combobox` 下拉框，固定选项（吧唧、明信片、镭射票、大肠发圈、发带、小卡、封口贴、贴纸、手提袋、色纸）+ 手动输入记录（`customSampleCategories`，A-Z 排序），UI 与灵感记录制品名称下拉一致。
- **灵感记录（design-inspiration）**：制品名称标签「制品名称 → 制品」；下拉框固定选项改为（封面、明信片、其他、镭射票、壁纸、海报）+ 手动输入记录，A-Z 排序。
- **价目表（design-pricelist）**：制品名称字段标签「制品名称 → 制品」；展示块「价目表」标题去字间距、字号 18→22px、padding 收紧。
- 源码已提交 `3633e74`；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-06（四次修正）· 功能 v29 继续调优

- **接稿排期日历（design-commission）**：开稿与截稿落在同一天时，合并为单一「开+截」实心圆点 + 文字，色号 `#712258`（此前为开稿橙 / 截稿黄两个独立标签）；底部图例改回 v27 样式——分别用实际色展示「开稿(橙 #ff9a3c) / 截稿(黄 #f5c518) / 开+截同天(#712258)」，再附紧急 / 正常 / 充裕进度色。
- **首页日历（home）**：① 删除「接稿」标签（仅保留开稿 / 截稿）；② 平台发布圆点 `.cal-dot` 由 5px 改为 6px，与开稿 / 截稿圆点（`.cal-day-tag-dot` 6px）等大；③ 日历底部新增「开稿(橙) / 截稿(黄)」示意图例。
- **打样记录（groupbuy-samples）**：评价「不合格」标签在列表卡片 multiselect 数组分支中由灰（`.tag-gray`）改为红（`.tag-danger`），与单行分支一致（此前数组分支漏改，导致仍显示灰）。
- **开团记录（groupbuy-records）**：① 字段「截止时间 → 截团时间」（数据键 `endTime` 不变）；② 售后记录子表新增「单号」列，置于「制品名称」前；③ 列表展示模块仅保留 状态 / 制品数 / 开团时间 / 截团时间 / 购买人数（制品总价 / 邮费 / 成本不再列于列表）；④ 开团营收 / 成本 / 利润柱状图顶部数字去除（仅保留柱形，月份标签与图例保留）。
- **开团三项金额计算说明（告知项，非代码改动）**：制品总价 / 邮费总价 / 总成本均为**手动填写的数字字段**（type:number），系统不会自动汇总；`制品列表` 里的「单价 × 售卖数量」不会自动累加到「制品总价」。统计区「总营收 = Σ(制品总价 + 邮费总价)」「总成本 = Σ(成本)」「总利润 = 总营收 − 总成本」、以及柱状图，均直接读取这三个手填值。如需改为按制品列表自动计算，可后续再加。
- 源码已提交 `4c9cd94`（并已推送到 GitHub origin/main；此前 `d836a25`→`3633e74` 五笔提交亦一并推送成功）；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-06（五次修正）· 功能 v29 继续调优

- **首页空白紧急修复**：`renderCalendar` 中 `START_COLOR` / `END_COLOR` 被错误地定义在 `for` 循环内部（块级作用域），而底部图例在循环外部引用它们，导致运行时 `ReferenceError`，`renderHome` 中断、首页主体空白。已将这两个颜色常量上提到函数顶部，首页恢复正常。
- **接稿排期日历（design-commission）**：① 开稿 / 截稿 / 同天标签所在 `.cal-date-row` 改为 `justify-content:center`，在日期数字后整体居中；② 开稿与截稿同一天时文案由「开+截」改为「同天」（色号仍 `#712258`）；③ 底部图例实心点与文字间距调近（`.cal-legend .status-dot` 去 margin-right，保留 `legend-item` 的 4px gap）。
- **开团记录柱状图**：多 series 柱形最后一根（利润）原先被设为 `border-radius:3px 3px 0 0` 呈半圆顶，现统一改为 `border-radius:0`，与营收 / 成本柱一样平头。
- **记录详情弹窗**：左侧标题 `.detail-label` 宽度由 100px 缩至 80px，`.detail-row` 的 gap 由 12px 缩至 8px，标题与内容更紧凑。
- 源码已提交 `63ee272` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（六次修正）· 功能 v29 继续调优

- **首页日历**：① 日期格拆分为 `.cal-day-top`（平台发布圆点 + 条数）与底部 `home-comm-tags`（开稿 / 截稿 / 开+截），上部永远留出空间给平台发布；② 平台发布实心点与日期数字间距和接稿排期时间条一致；③ 同一天开稿+截稿合并为「开+截」；④ 底部图例改为平台（小红书/抖音/视频号/公众号）+ 开稿 / 截稿 / 开+截，按顺序排在公众号后面。
- **接稿排期日历**：① 日期数字回到左侧（`.commission-cal-grid .cal-date-row` 改回 `justify-content:flex-start`）；② 截稿标签不再 `margin-left:auto` 固定右侧，与开稿顺序排列；③ 同一天开稿+截稿文案由「同天」改回「开+截」。
- **开团记录柱状图**：恢复柱顶数字显示（v29-fix5 误理解为要去掉数字，实际用户希望显示）。
- **价目表**：新增「单位」字段（`priceUnit`），支持选择「元」或「元/p」，列表展示相应显示 `¥price` 或 `¥price/p`。
- **统一列宽与标题对齐**：将所有 dynamic-list 中名称列（制品名称/制品/项目名称/加价项目）统一为 `flex:1.5 1 0; min-width:90px`；规范 `.dynamic-list-header span` 的 `box-sizing:border-box;padding-left:8px` 与透明边框占位，确保标题与下方输入框内容起点一致。
### 五次修正（2026-08-07 01:36，用户反馈 F 轮 5 项）

- **首页日历**：① 移除 `.cal-day-top` 与 `justify-content:space-between` 结构，改为日期数字 → 平台圆点 → 条数 → 开稿/截稿/同天顺序流式排列；② 平台发布圆点到日期数字的上边距由 4px 缩为 1px，与接稿排期时间条到日期数字间距一致；③ 条数不再居中，紧跟平台圆点；④ 开稿/截稿/同天标签整体上移；⑤ 同一天开稿+截稿合并为「开+截」；⑥ 底部图例平台（小红书/抖音/视频号/公众号）+ 开稿 / 截稿 / 同天。
- **接稿排期日历**：① 日期格中继续显示「开+截」；② 底部图例由「开+截」改为「同天」，与「开稿」「截稿」并列。
- **开团记录柱状图**：当记录未填写开团时间（`startTime`）时，原柱图区域空白且无提示；现增加空态提示「请为记录填写开团时间，柱图即可显示」，并告知用户三项金额（制品总价/邮费总价/总成本）为手动填写，柱图按 `startTime` 年月聚合。
- **价目表**：① 制品分类下拉框增加 `noSort: true`，保持固定顺序「纸片类 / 其他材质类 / 线上&应援类 / 加价项目」，不再被按拼音排序；② 「单位」字段由 `<select>` 改为 `multiselect` + `single: true`，复用多选 checkbox 样式（可打勾），但限制单选；保存后按字符串处理，列表展示 `¥price` 或 `¥price/p`。
- **标题栏与内容框对齐**：① 修复 `.dynamic-list-header` 因 `margin:-8px -8px 6px;padding:4px 0` 导致标题文字起点比输入框内容起点少 8px 的问题，改为 `padding:4px 8px 4px 8px`，使标题 span 文字起点与 `.form-input` 内容起点严格对齐；② 给售后记录「单号」列补全 header/input 宽度规则（`flex:0 0 70px`）；③ 开团记录制品列表、售后记录、灵感速记、报价计算的「制品名称」统一改为「制品」。
- 源码已提交 `3125d5f` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（G 轮修正）· 功能 v29 继续调优

- **首页日历（home）**：① 平台发布圆点区域固定高度（`.cal-dots` `min-height:12px`、`margin-top:4px`），无论当天有无发布记录都留出相同区域；② 开稿 / 截稿 / 同天标签通过 `margin-top:auto` 固定于日期格底部，不再随平台点有无上下浮动；③ 同一天开稿+截稿继续显示「开+截」；④ 底部图例「同天」文案改为「开稿+截稿同天」；⑤ 灵感速记「制品」占位词改为「请选择或输入制品」；⑥ 平台发布新增记录的「内容类型」改为单选（`multiselect` + `single: true`），保持 checkbox 样式，无默认勾选。
- **接稿排期日历（design-commission）**：底部图例「同天」文案改为「开稿+截稿同天」；日期格中同一天继续显示「开+截」。
- **开团记录（groupbuy-records）**：① 移除柱图空态提示；② 制品列表与售后记录子表中的「制品」列标签改为「制品名称」；③「进度状态」改为单选（`single: true`），默认勾选「进行中」。
- **厂家记录（groupbuy-factories）**：「合作状态」改为单选（`single: true`），默认勾选「临时合作」。
- **打样记录（groupbuy-samples）**：「样品评价」改为单选（`single: true`），保持 checkbox 样式，无默认勾选。
- **灵感记录（design-inspiration）**：「制品」占位词改为「请选择或输入制品」。
- **接稿排期（design-commission）**：①「稿件用途」改为单选，默认勾选「自用」；②「同柄图」改为单选，无默认勾选；③「修改次数」字段标签改为「修改类型、次数、价格」。
- **授权记录（design-auth）**：「授权类型」改为单选，默认勾选「商用」。
- **价目表（design-pricelist）**：①「制品分类」增加选项「修改类型」；②「单位」增加选项「元/次」。
- **任务档案（oc-profiles）**：「性别」改为单选，保持 checkbox 样式，无默认勾选。
- **人物关系（oc-relations）**：①「关系状态」改为单选，默认勾选「稳定」；②「关系类型」选项按 A-Z（拼音）排序；③ 自定义关系输入框占位词改为「请输入自定义关系类型」。
- **故事小记（oc-stories）**：①「剧情标签」改为单选，默认勾选「灵感」；②「剧情状态」改为单选，默认勾选「连载中」。
- **时间线（oc-timeline）**：「重要性」改为单选，保持 checkbox 样式，无默认勾选。
- **约稿记录（oc-commission）**：①「稿件用途」改为单选，默认勾选「自用」；②「约稿平台」改为单选，无默认勾选；③「交易状态」改为单选，无默认勾选，并新增选项「待接稿」；④ 筛选器同步增加「待接稿」。
- **电脑端标题对齐**：将 `.dynamic-list-header span` 的 `padding-left` 由 `8px` 调整为 `9px`，补偿透明边框缺失的 1px 视觉边界，使标题文字与下方输入框内容在电脑端严格左对齐。

### 2026-08-07（H 轮修正）· 功能 v29 继续调优

- **首页日历（home）**：① 平台发布记录条数（`.cal-day-count`）上边距由 `2px` 缩为 `0`，更贴近平台发布圆点；② 开稿 / 截稿 / 同天标签（`.home-comm-tags`）由 `margin-top:auto` 改为 `margin-top:2px`，不再被推到日期格底部，而是紧跟在平台圆点 / 条数区域下方。
- **开团记录（groupbuy-records）**：年度营收 / 成本 / 利润柱状图顶部堆叠数字去除（保留柱形、月份标签与图例）。
- **接稿排期（design-commission）**：①「稿件用途」「支付状态」「稿件进度」改为单选（`multiselect` + `single: true`），分别默认勾选「自用」「定金」「已接稿」；② 原「修改类型、次数、价格」单字段拆分为三个独立字段：「修改类型」（text）、「修改次数」（number）、「修改价格」（number，提示词「元/次」）。
- **人物关系（oc-relations）**：① 自定义关系类型在渲染时按 A-Z（拼音）排序；② 给自定义关系类型选项增加删除按钮（`✕`），点击可从 DB 与当前 checkbox 组中移除该自定义选项。
- **电脑端标题对齐**：将 `.dynamic-list-header span` 的 `padding-left` 由 `9px` 调回 `8px`（保留透明边框占位），进一步修正标题文字与下方输入框内容的左对齐。
- 源码已提交 `a55a27e` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（I 轮修正）· 功能 v29 继续调优

- **首页日历（home）**：① 平台发布圆点区域固定高度（`.cal-dots` 由 `min-height:12px` 改为 `height:12px`），无论当天有无发布记录都占 12px；② 发布条数 `.cal-day-count` 始终渲染，无记录时以 `&nbsp;` 占位并设 `min-height:12px`，保证高度固定；③ 开稿 / 截稿 / 同天标签（`.home-comm-tags`）通过固定上方的平台区域获得稳定位置，不再因平台记录有无上下浮动。
- **接稿排期（design-commission）**：将「修改类型」「修改次数」「修改价格」三个独立字段合并为 `modifications` 动态列表，一行展示三列（修改类型 / 次数 / 价格（元/次）），支持添加条数，最多保留 2 条（通过 `maxRows: 2` 限制）。
- **人物关系（oc-relations）**：自定义关系类型选项后的删除按钮由红色实心 `✕` 改为更轻量的 `.custom-opt-del` 灰色小圆角按钮，hover 变红，视觉上不再突兀。
- **电脑端标题对齐**：修正 `.dynamic-list-header span` 的 `padding-left` 计算逻辑。因 `.dynamic-list-header` 用 `margin:-8px` 延伸至容器边缘，标题 span 的内容起点比输入框内容起点少 8px，故将 `padding-left` 由 `8px` 调整为 `16px`，使电脑端所有 dynamic-list 标题与下方输入框严格左对齐。
- 源码已提交 `6ff21df` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（J 轮修正）· 功能 v29 继续调优

- **接稿排期（design-commission）**：① 默认视图由「列表」改为「日历」；② 在日历视图下，记录列表与日历位置互换，列表在上、日历在下；③ 「修改项目」动态列表中「价格（元/次）」列宽由 `90px` 加宽到 `110px`，提示词可完整显示。
- **人物关系（oc-relations）**：① 自定义关系类型的删除按钮由常驻 `×` 改为 hover 时才显示，避免视觉杂乱；② 思维导图节点圆形由 `80px` 缩小到 `60px`，内部头像 / 文字圆由 `56px` 缩小到 `44px`，连线端点半径同步从 `35` 调整为 `25`。
- **电脑端标题对齐**：区分普通列与 combobox/datalist 列的标题偏移。`.dynamic-list-header span` 默认 `padding-left:8px` 与普通输入框对齐；给 combobox/datalist 列的标题 span 额外加上 `.combobox-col` 类并设 `padding-left:16px`，解决 I 轮统一 16px 后普通列反而偏右的问题。
- 源码已提交 `9664379` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（K 轮修正）· 功能 v29 继续调优

- **接稿排期（design-commission）**：① 顶部「列表 / 日历」切换按钮位置互换，改为「日历 / 列表」顺序；② 日历视图下内容区域恢复「日历在上、列表在下」；③「修改项目」动态列表增加「备注」列，并将「修改类型」改为下拉选择器（`combobox`），选项从价目表「修改类型」分类自动加载，UI 与灵感记录制品名称下拉框一致。
- **人物关系（oc-relations）**：① 自定义关系类型的删除按钮不再放在表单字段的 checkbox 项内，改为在关系页顶部单独列出所有自定义关系类型，每个类型旁提供删除按钮；② 思维导图节点圆形由 `60px` 进一步缩小到 `48px`，内部文字 / 头像圆由 `44px` 缩小到 `36px`，连线端点半径由 `25` 调整为 `20`。
- **电脑端标题对齐**：去掉 combobox/datalist 列标题的 `.combobox-col` 特殊 `padding-left:16px`，所有 `.dynamic-list-header span` 统一使用 `padding-left:8px` + 透明边框占位，标题文字与输入框内容起点一致，解决接稿加价项目、开团制品名称 / 单价 / 是否流团、售后制品名称 / 补偿方式等列标题偏左偏右问题。
- 源码已提交 `1e9cded`；因本地代理 `127.0.0.1:7897` TLS 握手失败，暂未能推送到 GitHub origin/main，需网络/代理恢复后手动重推；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（L 轮修正）· 功能 v29 对齐治根

- **人物关系（oc-relations）**：① 自定义关系类型管理做成独立卡片，始终显示在人物关系页顶部，空状态时提示“暂无自定义关系类型”；卡片内可直接输入新增，每个自定义类型带常驻 `✕` 删除按钮；② 思维导图节点圆形回退到 `60px`（内部文字 / 头像圆 `44px`，连线端点半径 `25`），保持 J 轮确定的尺寸。
- **电脑端 dynamic-list 标题对齐（治根）**：发现历次反复偏左偏右的根因是 `.dynamic-list-header` 用 `margin:-8px` 伸出容器边缘，导致 header 内容区起点与 row 内容区起点相差 8px。L 轮去掉负 margin，header 与 row 共享 container 内容区，再使用 `::before` 伪元素把标题背景延伸至容器边缘。所有 `.dynamic-list-header span` 统一 `padding-left:8px + 透明边框`，与输入框内容起点严格一致，彻底解决接稿修改项目、开团制品列表、售后记录等列标题偏左偏右问题。
- 源码已提交 `dc0920e` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（M 轮修正）· 功能 v29 对齐补刀

- **电脑端 dynamic-list 标题底色恢复**：L 轮用 `::before` 伪元素承载淡蓝背景，在某些渲染路径下标题底色丢失。M 轮改为直接给 `.dynamic-list-header` 设置 `background:var(--c-primary-bg)`，同时用 `margin:0 -8px` 把背景延伸至容器边缘，标题文字仍靠 `span{padding-left:8px + 透明边框}` 与输入框内容对齐。这样标题既有淡蓝底色，又保持像素级左对齐。
- **人物关系（oc-relations）**：自定义关系类型管理从关系页顶部独立卡片中移除，放回「关系类型」字段内。在新增 / 编辑关系表单中，每个自定义关系类型选项标签右侧显示常驻 `✕` 删除按钮（内置选项无删除按钮），点击即可从 DB 和当前选项中移除该自定义类型，与管理其他 allowCustom 字段体验一致。
- 源码已提交 `2144fc2` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（N 轮修正）· 功能 v29 回退 K 轮 + 关系删除归位

- **电脑端 dynamic-list 标题对齐**：M 轮改坏标题底色与左对齐，用户要求按 K 轮 `1e9cded` 回退。N 轮将 `.dynamic-list-header` 恢复为 K 轮样式：`margin:-8px -8px 6px;padding:4px 8px 4px 8px`，背景色 `var(--c-primary-bg)` 延伸至容器四边；标题 span 统一 `padding-left:8px + 透明边框`，与输入框内容起点一致。不再做 combobox-col 特殊补偿或 `::before` 伪元素，只按 K 轮给出的样式精修。
- **首页日历（home）**：平台发布记录条数 `.cal-day-count` 从独立行挪到平台圆点后面（与圆点同一行）；开稿 / 截稿 / 同天标签整体上调并固定位置，不再被推到日期格底部。
- **人物关系（oc-relations）**：删除按钮不再放在每个自定义关系类型选项后面，改为放在「自定义关系类型」输入框 +「添加」按钮旁边：输入要删除的自定义关系类型后点「删除」即可从 DB 和当前 checkbox 组中移除。
- 源码已提交 `01f4e2d` 并推送到 GitHub origin/main；APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（O 轮修正）· 功能 v29 七项精修

- **电脑端标题对齐（只修标出的两处）**：接稿排期「修改项目」列表的「次数 / 价格 / 备注」标题此前没有定义 header 宽度导致挤成一坨，现补上对应 flex 宽度；后将 header 的 `padding-left` 减到 `0px` 以修正视觉偏右。开团记录「制品列表」的「单价 / 对应厂家」标题偏左，将 header span 的 `padding-left` 加大到 `22px`，使其与输入内容视觉左对齐。同时发现接稿排期与开团记录的「制品列表」都使用 `data-key="products"`，导致两者的 header 样式互相影响；现改用 `data-module="schedule"` / `data-module="groupbuy-records"` 分别控制，避免改开团时把接稿排期的「价格」header 也推偏。其余列一律不动。
- **人物关系（oc-relations）关系类型删除改为红色勾选删除**：自定义类型选项标记 `data-custom="true"`，红色「删除」按钮与「添加」按钮并排，无额外提示文案；勾选要删的自定义类型后点红色「删除」即从 DB 与当前表单所有关系类型 checkbox 组中移除。
- **首页平台发布（home）**：24 小时数据（点赞 / 收藏 / 播放量 / 阅读量）与 7 天数据各用 `row` 分组并排成一行四个。
- **开团记录（groupbuy-records）字段重组**：①「制品总价 / 邮费总价」合并一行；② 新增「拼团人气」放在「购买人数」后并排；③「总成本」改名「制品总成本」，新增「邮费总成本」放在其后并排；④ 数据柱状图与盈亏统计的成本统一改为「制品总成本 + 邮费总成本」。
- **打样记录（groupbuy-samples）**：「样品费用 / 样品数量」用 `row` 分组并排。
- **时间线（oc-timeline）重要性按钮**：按参考图改为「一个长椭圆容器 + 内部四段」的分段胶囊样式。未选中时每段显示彩色圆点 + 文字（重要 / 较重要 / 一般 / 次要）；选中段整体填充对应颜色，圆点和文字变白。表单内字段与列表顶部过滤按钮统一此样式，并同步更新选项 label 为不带「红/橙/绿/蓝」前缀的纯文字。后将时间线 toolbar 增加 `timeline-toolbar` 类，把 toolbar 与过滤条间距统一为 `8px`；过滤条宽度改为 `width:100%; max-width:200px` 并水平居中，使移动端与搜索框同宽、桌面端保持 200px。
- **设置页**：①「导航栏图标自定义」改为图标 + 名称都可编辑，图标输入框与名称输入框并排且等宽（新增 `navLabels` 设置项与 `getNavLabel()`，侧栏与设置页同步）；②「备份数据」按钮文案改为「导出数据」。
- 源码已提交 `de3eb50` 并推送到 GitHub origin/main；Service Worker 缓存已升级到 v44。若本地预览仍看不到变化，请 **Ctrl+F5 硬刷新** 或浏览器「设置 → 隐私与安全 → 清除浏览数据」清掉 localhost:8123 / 127.0.0.1:51063 的缓存。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（O 轮收尾微调）· 功能 v29

- **开团记录「制品列表」标题继续右移**：用户截图反馈「单价 / 对应厂家」标题仍偏左，将 `data-module="groupbuy-records"` 下这两个 header span 的 `padding-left` 从 `22px` 进一步加大到 `34px`，使其与输入框内容视觉左对齐。
- **时间线重要性过滤条宽度明确对齐搜索框**：将 `.timeline-filter-bar` 从 `width:100%; max-width:200px` 改为桌面端固定 `width:200px`（与 `.search-box input` 完全一致），并在 `@media (max-width:768px)` 下改为 `width:100%`，避免旧写法在某些容器下被压缩得比搜索框窄。
- 源码已提交 `de3eb50`；Service Worker 缓存已升级到 v44。由于网络 SSL/TLS 握手失败，最后一次推送未成功，待网络恢复后手动重推。

### 2026-08-07（O 轮再微调）· 功能 v29

- **接稿排期「修改项目」价格列缩短**：将 `modifyPrice` 的 label 从「价格（元/次）」改为「价格（元）」，输入框与标题宽度从 `110px` 减到 `80px`，整体更紧凑自然。
- **时间线重要性按钮改为内容自适应**：取消固定 `200px` 和等分 `flex:1`，改为 `.timeline-filter-bar{width:auto}`，每个 `.timeline-color-filter{flex:0 0 auto;padding:6px 14px}`；选中后的胶囊只包裹文字，不会撑开变长。手机端 padding 进一步减到 `6px 10px`，更窄。
- 源码已提交 `33b8a37` 并推送到 GitHub origin/main；Service Worker 缓存已升级到 v45。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（P 轮修正）· 功能 v29

- **接稿排期「修改项目」标题继续左移**：用户反馈「次数 / 价格 / 备注」标题仍偏右，在 `padding-left:0` 基础上再加 `margin-left:-6px`，使标题文字进一步向左对齐输入内容。
- **开团记录「制品列表」标题继续微调**：
  - 「单价 / 对应厂家」标题 `padding-left` 从 `34px` 改为 `30px`；
  - 「售卖数量 / 是否流团」标题在默认 `8px` 基础上减 `2px`，设为 `padding-left:6px`。
- **时间线重要性按钮与搜索框同高、选中段缩窄**：将 `.timeline-color-filter` 垂直内边距从 `6px` 提到 `8px`，使胶囊总高度与 `.search-box input` 一致；仅对 `.timeline-color-filter.active` 减少水平内边距到 `10px`（手机端 `8px`），让选中后有底色的段更窄。保留未选中按钮原样，避免整体过滤条被撑得过宽。
- Service Worker 缓存升级到 v46；源码已提交 `52d8973` 并推送到 GitHub origin/main。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（Q 轮修正）· 功能 v29

- **时间线重要性按钮回退到 v44 并缩短选中段**：整体恢复 v44 的长椭圆容器 + 四段等分样式（`.timeline-filter-bar{width:200px}`，手机端 `width:100%`；`.timeline-color-filter{flex:1;padding:6px 0}`）。仅对 `.timeline-color-filter.active` 改为 `flex:0 0 auto;padding:6px 10px`，让选中后有底色的段缩短、只包裹文字。
- **接稿排期「修改项目」标题回退到 v45**：去掉 P 轮新增的 `margin-left:-6px`，恢复为 `padding-left:0`。
- **开团记录等其余表格保持 P 轮状态不动**：单价/厂家 `padding-left:30px`，售卖数量/是否流团 `padding-left:6px`。
- Service Worker 缓存升级到 v47；源码已提交 `df40651` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（R 轮修正）· 功能 v29

- **时间线重要性按钮完全回到 v44 并加宽**：移除 Q 轮的选中缩短效果，恢复 v44 的 `.timeline-color-filter{flex:1;padding:6px 0}` 与 `.timeline-color-filter.active{border-radius:999px;color:#fff}`；电脑端 `.timeline-filter-bar` 宽度从 `200px` 加大到 `240px`（手机端仍为 `100%`）。
- **接稿排期「修改项目」标题再左移 4px**：在 v45 `padding-left:0` 基础上加 `margin-left:-4px`。
- Service Worker 缓存升级到 v48；源码已提交 `e47a45b` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（S 轮修正）· 功能 v29

- **时间线重要性按钮恢复两侧半圆并加宽**：电脑端 `.timeline-filter-bar` 宽度从 `240px` 加大到 `300px`；选中项 `.timeline-color-filter.active` 改为 `flex:0 0 auto;padding:6px 14px;border-radius:999px`，让有底色段只包裹文字且左右保持半圆；未选中项仍 `flex:1` 等分剩余空间。
- **接稿排期「修改项目」标题继续左移**：「次数 / 价格 / 备注」标题 `margin-left` 从 `-4px` 调到 `-8px`，与输入内容更严格对齐。
- Service Worker 缓存升级到 v49；源码已提交 `6e732f3`，但推送 GitHub origin/main 失败（HTTP 401 Unauthorized，鉴权失败，待恢复后重试）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（T 轮修正）· 功能 v29

- **时间线重要性按钮位置固定、电脑端继续加宽**：回到四段等分布局（`.timeline-color-filter{flex:1;padding:6px 0}`），选中项只改变颜色/背景，不改变宽度或位置；电脑端 `.timeline-filter-bar` 宽度从 `300px` 加大到 `360px`（手机端仍为 `100
### 2026-08-07��T ���������� ���� v29

- **ʱ������Ҫ�԰�ťλ�ù̶������Զ˼����ӿ�**���ص��Ķεȷֲ��֣�.timeline-color-filter{flex:1;padding:6px 0}����ѡ����ֻ�ı���ɫ/���������ı���Ȼ�λ�ã����Զ� .timeline-filter-bar ���ȴ� 300px �Ӵ� 360px���ֻ�����Ϊ 100%����
- **�Ӹ����ڡ��޸���Ŀ���������΢��**�����������������ƣ�margin-left:-12px�������۸񡹱��� margin-left:-8px������ע������ margin-left:-4px���ָ�֮ǰ����Ч�������ټ������ƣ���
- Service Worker ���������� v50��Դ�����ύ 416ea72 �����͵� GitHub origin/main��ˢ��ǰ�� **Ctrl+F5** Ӳˢ�¡�APK ��δ��������ؽ� v1.1.17 �ϻ���

### 2026-08-07��U ���������� ���� v29

- **ʱ������Ҫ�԰�ťλ�ù̶� + ѡ�б�������ֻ��������**��ÿ��ѡ��� lex:1 �ȷ֣����ʱ�������/λ�ò��䣻��ѡ������ڲ� .timeline-color-label ��ʾ��ɫ���ұ��������׵���֣�������ֻ�������֣����ͼ1һ�¡����Զ� .timeline-filter-bar ���ȱ��� 360px���ֻ��� 100%���������򡢵���/������ť�� toolbar ��ʽδ�Ķ���
- **�Ӹ����ڡ��޸���Ŀ���������΢��**����������margin-left �� -12px �Ӵ� -16px�����۸񡹱��� -8px������ע���� -4px ����  �����Ҷ����������ݡ�
- Service Worker ���������� v51��Դ�����ύ 2f2e0c7 �����͵� GitHub origin/main��ˢ��ǰ�� **Ctrl+F5** Ӳˢ�¡�APK ��δ��������ؽ� v1.1.17 �ϻ���

### 2026-08-07（V 轮修正）· 功能 v29

- **时间线重要性按钮选中背景再放大、电脑端容器继续加宽**：电脑端 .timeline-filter-bar 宽度从 360px 加大到 440px；选中项内部 .timeline-color-label 的内边距从 4px 12px 加大到 6px 18px，让底色胶囊更宽。
- **接稿排期「修改项目」标题继续微调**：「次数」margin-left 从 -16px 加大到 -20px；「价格」从 -8px 调到  ；「备注」从   调到 6px，整体更对齐输入内容。
- Service Worker 缓存升级到 v52；源码已提交 5f4fbdd，但 git push origin main 失败（SSL/TLS 握手失败，走本地代理 127.0.0.1:7897），待网络恢复后重试。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（W 轮修正）· 功能 v29

- **时间线重要性按钮选中背景再放大**：外框 .timeline-filter-bar 保持 440px 不变；选中项内部 .timeline-color-label 内边距从 6px 18px 加大到 8px 24px，让底色胶囊更宽。
- **接稿排期「修改项目」标题再微调**：「次数」从 -20px 回调到 -12px（往右）；「价格」从   调到 +4px（往右）；「备注」保持 +6px。
- **搜索框 / 导入 / 新增按钮 toolbar**：HTML/CSS 与 v44 一致，未改动。
- Service Worker 缓存升级到 v53；源码已提交 e2cbf9c，但 git push origin main 失败（HTTP 401 Unauthorized，凭据失效），待恢复后重试。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（X 轮修正）· 功能 v29

- **时间线重要性按钮完全回退到 v1.1.16 独立胶囊样式**：取消长椭圆外框，恢复为 .timeline-filter-bar{display:flex;flex-wrap:wrap;gap:8px} 容器 + .timeline-color-filter 独立胶囊按钮；未选中项使用 .inactive 白底灰字带边框，选中项直接填充原色（#e74c3c/#fa8c16/#27ae60/#3498db）并显示白色圆点。按钮尺寸比 v1.1.16 稍大（padding:8px 18px）。
- **接稿排期「修改项目」标题最终对齐**：「次数」从 -12px 回调到 -6px（往右）；「价格」保持 +4px；「备注」从 +6px 回调到  （往左）。
- Service Worker 缓存升级到 v54；源码已提交 eddd374 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（Y 轮修正）· 功能 v29

- **时间线 toolbar 恢复 v1.1.16 样式**：移除 `timeline-toolbar` 类及其 `margin-bottom:8px` 覆盖，恢复为 `<div class="toolbar">`，使用默认 `margin-bottom:16px`。
- **时间线重要性按钮改回 W 轮样式**：恢复固定长椭圆外框（`.timeline-filter-bar{width:440px}`），内部 `.timeline-color-label` 选中时显示彩色胶囊，外框尺寸点击不变。
- **接稿排期「修改项目」标题继续微调**：「次数」保持 `-6px`；「价格」从 `+4px` 调到 `-2px`（往左）；「备注」从 `0` 调到 `-4px`（往左）。
- Service Worker 缓存升级到 v55；源码已提交 `72d37d4` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（Z 轮修正）· 功能 v29

- **接稿排期「修改项目」标题「次数」继续往左**：margin-left 从 -6px 调到 -10px。
- Service Worker 缓存升级到 v56；源码已提交 21b5c1b 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AA 轮修正）· 功能 v29

- **接稿排期「修改项目」标题「次数」继续往左**：margin-left 从 -10px 调到 -16px。
- **接稿排期「修改项目」「修改类型」下拉框缩短**：列宽由 `flex:1 1 0; min-width:80px` 改为固定 `70px`，使下拉框更紧凑。
- Service Worker 缓存升级到 v57；源码已提交 `9a15bcf` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AB 轮修正）· 功能 v29

- **搜索框 / 筛选 / 新增记录确认 = F 轮样式**：拉取 F 轮（3125d5f）`index.html` 逐行核对 `.toolbar`、`.search-box`、`.filter-combobox`、`.btn-primary` 及 `@media(max-width:480px)` 窄屏布局，与当前完全一致（自 v1.1.16 起未变动），无需改动。
- **接稿排期「修改项目」「修改类型」下拉框还原**：AA 轮缩到的 `70px` 过短，还原回 F 轮原始 `flex:1 1 0; min-width:80px`。
- **时间线重要性按钮确认 = 固定外框**：`.timeline-filter-bar{width:440px}` 固定，点击只改内部 `.timeline-color-label` 胶囊，外框尺寸不变，符合需求。
- Service Worker 缓存升级到 v58；源码已提交 `8410ddb` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AC 轮修正）· 功能 v29

- **时间线重要性按钮外框尺寸严格固定**：给 `.timeline-filter-bar` 加固定 `width:440px;height:46px`，4 个 `.timeline-color-filter` 改为 `flex:0 0 25%;height:40px;min-width:0;overflow:hidden`，并限制内部 `.timeline-color-label` 最大宽度，确保点击选中后只变内部胶囊颜色/白点，外框宽高完全不变。
- **接稿排期「修改项目」「修改类型」下拉框重新缩短到 75px**：AB 轮回退到 80px 后用户仍嫌宽，本次改为固定 `75px`（比 F 轮 80px 略短，又不像 AA 轮 70px 那么挤），并通过更具体的选择器 `.dynamic-list-row .combobox-wrapper[data-subkey="modifyType"]` 覆盖通用 `flex:1.5` 规则，实际生效。
- Service Worker 缓存升级到 v59；源码已提交 `36421c4` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AD 轮修正）· 功能 v29

- **时间线重要性按钮：宽度与搜索框一致、居中、高度自适应**：`.timeline-filter-bar` 宽度从 440px 改为 `200px`（等于搜索框 `.search-box input{width:200px}`），保持 `margin:0 auto` 居中，去掉 AC 轮加的固定 `height:46px` 与 segment 固定高，恢复自适应高度（与 Y 轮/W 轮一致，不加宽加高）；点击选中仍只变内部胶囊，外框尺寸不变。
- **接稿排期「修改项目」「修改类型」下拉框参考上方制品列表「制品名称」列宽**：制品名称列规则为 `flex:1.5 1 0; min-width:90px`，本次将修改类型下拉框定为固定 `90px`（参考其 min-width:90px），比 AC 轮 75px 略宽，与制品名称视觉协调。
- **间距说明**：toolbar 内 `.toolbar{gap:8px}`，但 `spacer` 把「导入故事小记」+「+新增事件」推到最右，故搜索框与导入故事小记按钮实际水平间距并非 8px 而是被撑开；时间线筛选栏居中于页面，与上方 toolbar 的垂直间距为 `.toolbar{margin-bottom:16px}` + 自身 `margin:0 auto 16px`（上16px）。若用户要的是"重要性按钮水平对齐到导入故事小记按钮"，需改为非居中方案，待确认。
- Service Worker 缓存升级到 v60；源码已提交 `e03de11` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AE 轮修正）· 功能 v29

- **时间线重要性按钮选中胶囊改小**：`.timeline-color-filter.active .timeline-color-label` 的 `padding` 从 `0 14px` 改为 `0 8px`，与截图中胶囊大小一致。
- **时间线重要性按钮与上方 toolbar 间距改为 8px**：`.timeline-filter-bar` 的 `margin` 改为 `8px auto 16px`，并用 `.toolbar:has(+ .timeline-filter-bar){margin-bottom:8px}` 让时间线页面 toolbar 下边距也变成 8px，与 toolbar 内部按钮 `gap:8px` 一致。
- **接稿排期「修改项目」「修改类型」下拉框改为 130px**：由 90px 改为固定 130px，header 同步 130px。
- **接稿排期「修改项目」标题左对齐底下内容框**：去掉「次数/价格/备注」标题的负 margin，统一恢复 `padding-left:8px;margin-left:0`，让标题 span 的左边缘与下方 input 内容框左边缘对齐。
- Service Worker 缓存升级到 v61；源码已提交 `3deb31e` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AF 轮修正）· 功能 v29

- **时间线重要性按钮选中胶囊放大**：`.timeline-color-filter.active .timeline-color-label` 的 `padding` 从 `0 8px`（AE 轮）放大回 `0 16px`，让彩色胶囊更宽。
- **所有搜索框到下方按钮间距改为 10px**：全局 `.toolbar{margin-bottom}` 从 `16px` 改为 `10px`；时间线页面 override 同步 `8px → 10px`，并把 `.timeline-filter-bar` 顶部 margin 从 `8px` 归 `0`，使"搜索框工具栏 → 重要性按钮"垂直间距精确为 10px。
- **接稿排期「修改项目」「次数」输入框加宽**：`form-input[data-subkey="modifyCount"]` 与对应 header span 从 `flex:0 0 55px` 改为 `70px`。
- Service Worker 缓存升级到 v62；源码已提交 `f4633ca` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AG 轮修正）· 功能 v29

- **时间线重要性按钮选中胶囊再放大**：`.timeline-color-filter.active .timeline-color-label` 的 `padding` 从 `0 16px`（AF 轮）改为 `0 22px`。⚠️ 注意：时间线栏宽锁定为搜索框宽度 `200px`，每个重要性分段仅约 48px，胶囊在 `padding≈14px` 时已填满整段并被 `overflow:hidden` 裁切，故继续加大水平 padding 在视觉上不会再变宽；若需胶囊明显更大，只能加宽整条时间线栏（如 200→260px）或增加胶囊高度（会撑高外框）。待用户确认方向。
- **时间线间距回退 8px**：`.toolbar:has(+ .timeline-filter-bar){margin-bottom}` 与 `.timeline-filter-bar` 顶部 margin 均由 AF 轮的 10px/0 回退为 `8px`（恢复"搜索框工具栏 → 重要性按钮"垂直间距 16px，与 toolbar 内部按钮 gap 一致）。全局 `.toolbar{margin-bottom:10px}` 保持不变（仅时间线页被 override 回 8px）。
- Service Worker 缓存升级到 v63；源码已提交 `f52372a` 并推送到 GitHub origin/main（首次推送遇 schannel SSL 握手失败，重试成功）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AH 轮修正）· 功能 v29

- **全局 `.toolbar{margin-bottom}` 回退 16px**：AF 轮改的 `10px` 撤销，恢复初始 `16px`（与 v1.1.16 一致）。时间线页 override 仍保持 `8px`（AG 轮定）。
- **时间线选中胶囊高度加高**：`.timeline-color-filter.active .timeline-color-label` 的 `padding` 从 `0 22px` 改为 `5px 22px`（加垂直 5px，胶囊明显变高）；同时为保持外框高度稳定不跳动，未选中态 `.timeline-color-label` 同步加 `padding:5px 4px`，使整条时间线栏高度一致、仅选中段显彩色。
- **外框内边距取消**：`.timeline-filter-bar` 的 `padding:3px` 改为 `padding:0`，让胶囊（分段）紧贴外框边框，不再被 3px 内边距挤在中间。
- Service Worker 缓存升级到 v64；源码已提交 `7bc961a` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AI 轮修正）· 功能 v29

- **故事小记 / 时间线 / 约稿记录 顶部「角色筛选排」改为只显示一排**：`.relation-person-grid` 由 `flex-wrap:wrap` 改为 `flex-wrap:nowrap; overflow-x:auto`（单行横向滚动，角色再多也不换行）；`.relation-person-btn` 加 `flex:0 0 auto` 防止被压缩。三个模块顶部这排按钮（📋全部 + 各角色名）现在始终只占一行。
- Service Worker 缓存升级到 v65；源码已提交 `9647aff` 并推送到 GitHub origin/main。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AJ 轮修正）· 功能 v29

- **角色筛选排改为「折叠」方式（替代横向滚动）**：用户要求只显示一排且用折叠而非滚动。`.relation-person-grid` 恢复 `flex-wrap:wrap`，新增 `.collapsed{max-height:42px;overflow:hidden}` 折叠态 + `.relation-toggle-btn`（展开 ▾ / 收起 ▴）按钮；故事小记、约稿记录（通用 render）与时间线（独立 render）的角色筛选 grid 默认带 `collapsed` 类，并在其后插入「展开」按钮，点击由新函数 `togglePersonGrid(btn)` 切换折叠/展开。人物关系的「快捷查看」角色排未改（用户只指定三个模块）。
- **人物关系底部关系列表分页改为每页 5 条**：`renderRelations` 内 `pageSize` 由 `20` 改为 `5`，同步 `PAGE_SIZES['oc-relations']` 由 `20` 改为 `5`。
- Service Worker 缓存升级到 v66；源码已提交 `dd4c76e`（本地，推送因 GCM 凭据管理器无头环境卡住失败，待恢复后 `git push origin main`）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AK 轮修正）· 功能 v29

- **「展开/收起」折叠按钮并入同一排**：用户要求折叠按钮与角色筛选按钮放同一行（而非单独一行显示在下方）。新增 `.relation-person-row{flex:nowrap}` 容器包裹「角色 grid + 展开按钮」，角色 grid 改 `flex:1; min-width:0`，按钮 `flex:0 0 auto` 固定在右侧同一行；`togglePersonGrid` 仍用 `previousElementSibling` 取 grid（结构未变）。故事小记、约稿记录、时间线三模块统一生效。
- **人物关系底部关系列表分页改为每页 10 个**：`renderRelations` 内 `pageSize` 由 `5` 改为 `10`，同步 `PAGE_SIZES['oc-relations']` 由 `5` 改为 `10`（用户明确「一页最多显示10个」）。
- Service Worker 缓存升级到 v67；源码已提交 `fc373b0`（本地，GitHub 连接被重置/无法连接，待网络恢复后 `git push origin main`）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AL 轮修正）· 功能 v29

- **人物关系「快捷查看」角色排也改为折叠 + 一头一尾固定 + 中间均匀分布**：之前快捷查看排无折叠、无「全部/展开」端点。现与三模块统一结构——新增 `.relation-person-row` 包裹「角色 grid + 展开按钮」；grid 加 `distribute` 类用 `justify-content:space-between`，使「📋 全部」固定头部、「展开 ▾」固定在行外右侧尾端、中间角色名均匀铺开；grid 默认 `collapsed`（只显一排，超出隐藏）。新增 `clearPersonRelations()` 作为「全部」点击处理（取消选中并收起内联详情）。
- **角色筛选排「全部」与「展开」分别固定在行首/行尾，中间角色按钮均匀分布**：之前「全部」在 grid 内部，grid 用 `justify-content:space-between` 导致角色少时中间空白过大。现把「全部」拆到 row 最左作为固定项，grid 仅含角色按钮并改为 `justify-content:space-evenly`，「展开」固定在 row 最右。故事小记、时间线、约稿记录、人物关系快捷查看四模块统一。
- **实时报价 4 项改造（AN 轮）**：
  1. **制品序号**：制品列表新增「序号」列（只读，显示在制品名称左侧），序号 = 行序 01、02、03…；删除行后随渲染自动重排；仅作展示引用编号，不参与价格运算。
  2. **加价项目绑定制品序号**：加价项目行新增「绑定」下拉，选项为制品序号 + 「无（不绑定）」。绑定后该加价项在实时报价中缩进（`.dc-rr.bound`，左侧竖线）嵌套展示在对应制品下方；不绑定则独立「加价项目（不绑定）」块展示；无论是否绑定，金额全部计入加价合计/总价。
  3. **同模行内化**：移除全局「同模阶梯价倍率」卡片；改为每行制品勾选【同模】后行内出现「同模类型」下拉（改色+字×0.6 / 改人+字色×0.8 / 改人×0.5）+ 可手改倍率输入框。柄图标识 >1 自动勾选本行同模；同柄图标识多行自动继承同模类型（仅初始赋值，每行独立可单独取消/改倍率）。
  4. **加急功能**：稿件用途倍率下方新增「加急」卡片（整单复选框 + 倍率输入框，默认 2，可改）。计算顺序严格：制品→SET→加价→用途倍率→（折扣/同担同推）→**最后整体 × 加急倍率 = 最终报价**；不勾选则 ×1。
- Service Worker 缓存升级到 v70；源码已提交 `71fda0e`（本地，GitHub 当前连接失败待重推）。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AO 轮修正）· 功能 v29

- **制品列表提示词精简**：提示由「序号自动生成，柄图自动分组，行内勾选同模启用阶梯计价」改为「柄图自动分组，勾选同模启用阶梯计价」（移除"序号自动生成"，因序号本就自动）。
- **序号去框**：制品列表「序号」列去掉背景方框（`.dc-prod-seq` 改为纯数字、字号字重与制品文字统一、无背景无圆角）；收据序号 `.dc-r-no` 保持加粗（序号加粗早已生效）。
- **同模类型下拉框显示格式**：选项由 `改色+字 ×0.6 / 改人+字色 ×0.8 / 改人 ×0.5` 改为 `改色+字*0.6`、`改人+字/色*0.8`、`改人*0.5`（× 改为 *，与用户公式一致）。
- **加价项目表头/行重排**：提示由「不参与同模计价、SET折扣；可绑定制品序号缩进展示」改为「不参与同模计价、SET折扣」；表头与每行重排为【制品绑定】(置最前、列宽扩大，下拉选项"无") →【名称】(占位由"项目名称"缩短为"名称") → 数量 → 单价 → 删除。
- **加急改为制品单独勾选**：移除整单加急复选框，每个制品行新增【加急】复选框；保留「加急倍率」输入（默认 2）。计算规则：加急仅作用于被勾选制品——最终价 = 用途/优惠/同担后的价格 × (1 + (加急倍率-1) × 勾选制品基价占比)；无制品勾选则不生效。导入/保存按制品存储 `urgent` 字段（兼容旧版整单 `urgentEnabled`）。
- **实时报价加价项目树形展示**：绑定的加价项改为缩进树形（`├─` / `└─`），去掉左侧蓝色竖条（移除 `.dc-rr.bound` 的 `border-left`），字体颜色与制品相同（`var(--c-text)`），序号（01/02…）保持加粗；不再显示"（绑定XX）"冗余标注。
- **加急在收据的展示与「倍率计算」同款**：加急板块移到「优惠」之后、「最终报价」之前，显示「加急倍率 ×rate」与「加急后价格」，格式与倍率计算（稿件用途 ×rate / 倍率后价格）一致。
- Service Worker 缓存升级到 v71；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AP 轮修正）· 功能 v29

- **修复同模类型下拉显示为 `undefined*0.6`**：DC_MODEL 中除"无同模"外未设 `label`，AO 轮误用 `m.label` 导致 undefined。改回 `${m.value}*${m.rate}`，显示为 `改色+字*0.6`、`改人+字/色*0.8`、`改人*0.5`。
- **mobile 制品列表改为两行布局**：之前 8 列单行在手机上拥挤。现 768px 以下 `.dc-product-row` 改为 grid-template-areas 两行：第一行「序号/制品/柄图标识/单价/数量/删除」，第二行「同模勾选/加急勾选」，与接稿排期制品列表的 mobile 换行风格一致；同模展开行仍占满整行。
- **加价项目树行与制品首字左对齐**：`.dc-rr.bound` 左内边距从 28px 改为 16px，使 `├─/└─` 后的文字与制品序号后第一个字处于同一 x 坐标（40px）。
- **加急仅精确应用于勾选项目**：移除 AO 轮的"勾选基价占比整体相乘"估算方案。新算法：
  1. 先计算每个制品及其绑定加价在 SET 组折扣后的金额；
  2. 按制品 `urgent` 勾选状态拆分为 `urgentBase` / `nonUrgentBase`；
  3. 用途倍率：勾选部分 = urgentBase × uRate × urgentRate，未勾选部分 = nonUrgentBase × uRate；
  4. 折扣/同担同推优惠再作用于加急后的金额；
  5. 最终报价 = 上述结果 - 同担减免。
  这样只有勾选制品及其绑定加价享受加急倍率，未勾选项目完全不受影响。
- Service Worker 缓存升级到 v72；源码待提交。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AQ 轮修正）· 功能 v29

- **制品行布局精调**：
  - 序号列由 44px 收窄到 28px（mobile 24px），行列间距由 6px 收紧到 4px（mobile 3px），让制品下拉框更贴近序号；
  - 加急/同模位置互换：加急在左、同模在右，并且共占同一行、间距收紧；
  - 同模改为「勾选 + 类型下拉框」inline 形式，勾选后直接在 checkbox 右侧出现类型下拉与倍率输入，去掉原来的独立「同模类型」展开行。
- **新增同模规则示意表**：在「优惠体系」卡片上方新增「同模规则」卡片，列出 `改色+字 ×0.6 / 改人+字/色 ×0.8 / 改人 ×0.5` 示意。
- **同模明细改回显示类型标签**：收据中同模明细由通用「同模 ¥… ×…」改回「改人+字/色 ¥… ×…」等具体类型标签；多件时显示为「改人+字/色 首件 ¥… + 余N件 ×¥…」。
- **加急收据展示优化（明确仅勾选生效）**：加急板块改为显示「加急基数（仅已勾选制品）/ 加急倍率 / 加急后价格」，与倍率计算格式一致，避免视觉上误以为整单加急；计算逻辑保持仅勾选制品及其绑定加价乘以加急倍率，未勾选项目不受影响。
- Service Worker 缓存升级到 v73；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AR 轮修正）· 功能 v29

- **mobile 制品列表对齐与显示精修**：
  - 序号左边缘与「+ 添加制品」按钮文字左边缘对齐（mobile `.dc-prod-seq` 增加 `padding-left:10px`）；
  - 柄图标识列加宽（64px → 72px），placeholder 完整显示；
  - 第二行 grid-template-areas 改为 `. urgent same same same same`，使「加急」checkbox 与上方「制品」输入框左对齐，并解决文字截断；
  - 同模选择区去掉 select/number 原生箭头与小圆（`-webkit-appearance:none`、隐藏 spinner）。
- **同模规则卡片改为 radio 行样式**：移除 AQ 轮的静态表格，改为与「稿件用途倍率」同款的 `calc-rate-row`：左侧 radio + 名称，右侧数字显示倍率；包含「无同模」示例行，标题改为「同模阶梯价倍率（选择同模更改项）」。
- **加价项目缩进树与制品名对齐**：`.dc-rr.bound` 左内边距由 16px 改为 4px，配合 `.dc-r-tree` 22px 宽度，使 `├─/└─` 后文字与制品序号后第一个字严格同 x 坐标。
- Service Worker 缓存升级到 v74；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AS 轮修正）· 功能 v29

- **mobile 制品列表布局精调**：数量从第一行移到第二行；第二行布局改为 `. urgent same same qty`，加急与同模相邻且靠近；制品名跨两列，柄图标识保持 60px 展示完整；序号左侧 padding 保持与「+ 添加制品」按钮左对齐。
- **同模类型下拉改为 combobox 下拉选择器**：与制品名称同款结构（input + ▼ toggle + 下拉选项），选项显示 `改色+字*0.6 / 改人+字/色*0.8 / 改人*0.5`；去掉原生 select 的箭头/小圆。
- **加价项目缩进树与制品明细文字左对齐**：`.dc-rr.bound` 左内边距由 4px 改为 16px，使 `├─/└─` 后文字与制品明细文字左边缘对齐。
- **导入接稿增加修改加价模块**：从接稿记录 `modifications` 字段导入修改类型/次数/价格/备注；实时报价新增「修改加价」编辑区，支持新增/删除/下拉选择修改类型；次数×价格计入修改加价总额。
- **实时报价收据新增修改加价与最终总价**：定金尾款后追加：修改加价、需付尾款（尾款+修改加价）、最终总价（最终报价+修改加价）。
- **修复导入接稿时加急覆盖全部制品的问题**：导入时不再使用旧版整单 `urgentEnabled` 覆盖所有制品的加急状态，改为仅读取 per-product `urgent` 字段。
- Service Worker 缓存升级到 v75；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AT 轮修正）· 功能 v29

- **制品列表列序修正**：header 与 row 的列序统一为「序号 / 制品 / 柄图标识 / 价格 / 数量 / 加急 / 同模 / 删除」，解决 mobile 端 price 区域缺失导致布局错乱的问题。
- **mobile 制品列表两行布局重排**：第一行「序号 / 制品 / 柄图标识 / 价格 / 数量 / 删除」；第二行「加急 / 同模（含类型下拉与倍率）」，让加急与同模相邻，数量放到第一排。
- **移除同模下拉框后的小圆**：同模类型 combobox 去掉 ▼ toggle 按钮，保留 input focus/click 打开下拉，避免 mobile 上显示多余小圆。
- **实时报价加价项目缩进树对齐**：`.dc-rr.bound` 左内边距调整为 18px，`.dc-r-tree` 固定宽度 22px，使 `├─/└─` 后文字与制品字段严格左对齐。
- **修改加价收据间距与标题**：在定金尾款后增加 `margin-top:12px`，新增「修改项目」小标题；最终总价放到需付尾款前。
- **总结性价格数字统一加粗**：`.dc-rr.total > span:last-child` 统一 `font-size:13px;font-weight:700`，确保倍率后价格、折后价格、加急后价格、需付尾款、最终总价等数字与正文同字号且加粗。
- Service Worker 缓存升级到 v76；源码已提交 `946f1b1` 并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AU 轮修正）· 功能 v29

- **删除同模区域的第二个小圆（倍率数字框）**：AT/AU 前同模区域存在两个圆——「同模」勾选框（功能开关，保留）+ 倍率手填数字框（空着时缩成小白圆，多余）。因类型下拉已自带倍率（如 `改色+字*0.6`，选择即自动套用），AU 轮在 `dcRenderProducts` 中删去 `.dc-prod-model-rate` 输入框，并清理桌面端 / mobile 两处死 CSS。现在同模区域只剩「同模勾选框 + 类型下拉框」，不再有第二个白圆。
- Service Worker 缓存升级到 v77；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AV 轮修正）· 功能 v29

- **加价树 `├─/└─` 与制品左对齐**：收紧 `.dc-rr.bound` 左内边距由 18px → 12px（与 `.dc-rr` 制品行同左边缘），`.dc-r-tree` 宽度由 22px → 28px，使 `├─/└─` 符号起点与制品序号左边缘对齐，符号后文字仍落在制品文字起点（40px），形成严格嵌套视觉。
- Service Worker 缓存升级到 v78；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。

### 2026-08-07（AW 轮修正）· 功能 v29

- **加价树 `├─/└─` 改为与制品名称（文字）左对齐**：用户纠正 AV 轮——应是制品名称文字起点（40px）而非序号（12px）。`.dc-rr.bound` 左内边距 `12px → 40px`，`.dc-r-tree` 保持 28px，使 `├─/└─` 符号起点落在制品名称左边缘，符号后文字再缩进一层。
- Service Worker 缓存升级到 v79；源码已提交并推送到 GitHub。刷新前请 **Ctrl+F5** 硬刷新。APK 仍未打包，等重建 v1.1.17 上机。
